# Acceptance matrix

## Current interpretation (2026-09-08)

The full-resolution G1–G8 ledger below is the completion measure: **23/65
verified**, not a development percentage. The A1–A18 table and its dated
57-test run are historical baseline evidence, not current production claims.
Later work can supersede that behavior without completing the stricter gates.

- G1.14 closes on local production build `xB2bE6fs2ZV80Ri_IVt9S` at
  `60c122e`: three cold-cache runs each on `/`, authenticated `/overview` and
  an actual exact-source report yield performance medians 96/98/97 and
  accessibility 100 throughout. One real synthetic Storage upload, exact
  document/status/content checks, no skips/retries. The independent review
  confirms the complete gate; see `docs/local-upload-browser-verification.md`.
  G1.16 and broader upload/rights release conditions remain open. Production
  is PR81 merge `5e642a678cdeb8e3f17343146181d53acf81899f`, deployment
  `dpl_BgPP2ibjzYQiHQ7EZ9E9uY4tXUvF`, READY at 16:19:20.081 UTC on
  2026-09-08 across all six aliases. Canonical upload admission is enabled;
  legacy issuance remains paused. See `docs/hosted-own-upload-readiness.md`.
- PR81 distinguishes acknowledged preparation from selected-report failure.
  Exact head `36093080` passed 3,346 units, 2,120 SQL assertions, 30 locks and
  232 browser cases with 57 actual Storage uploads and zero skips/retries.
  One unchanged full-job rerun passed after a sign-out request socket
  failure before response headers; the first failure is retained, with no cause asserted.
  Authenticated empty-account production pages and the deployed recovery asset
  were verified at 16:20:27 UTC. Desktop/mobile component inspection passed;
  no hosted report-failure injection or broad gate completion is claimed.
- Local compact-engine commit `ba35b15` passed a real filesystem proof on an
  82 MB decoded, two-million-record synthetic source: four million exact events,
  staged merges, complete readback and zero generated-data residuals. The original
  remained unchanged. This is a closed local processing prerequisite, not hosted
  upload/report/job/lifecycle integration or a completed acceptance gate.
- PR80 CI `34240644001` passed: 3,310 units (204 files), 85 fresh migrations,
  2,120 SQL assertions (56 files), 30 independent lock checks and 232 browser
  cases in 16.0 minutes, 57 actual Storage uploads, zero skips/retries. Both
  compatible hosted migrations are installed. Existing upload caps are unchanged.
- Hosted dense VCF preparation produced 500,000 variants and observations in
  220.357201 seconds. Its later report attempt failed, then one explicit report
  retry returned 200 and exact-source A/C with one provenance source. Original
  download hashes match; app DELETE returned 204. Independent 15:27:20 checks
  found all nine selected counts and both Storage keys zero, with the other four
  sources' metadata/counts unchanged. Browser download event was observed; byte
  verification used the authenticated app endpoint. Native deletion confirmation
  remains unverified. This is preparation plus recovery, not an uninterrupted
  pass. PR81 corrects the misleading preparation-error copy; no broad gate closes. Details: `docs/hosted-own-upload-readiness.md` and parent task
  `work/wgs-release-pr80/`.
- Actual abandoned-upload cleanup completed at 12:58:20.741655 UTC, **50.367195
  seconds after its real lease expiry**, with the original 14:27:30.374461 UTC
  deadline unchanged, manifest complete and Storage/session/staging residuals
  zero. This extends the earlier 21.430137-second scheduled derivative retry
  proof; neither proves every retention scope or deadline.
- Fresh native public upload prepared five variants from 425 bytes with zero
  initial analyses. Explicitly enabling only traits produced the bitter-taste
  `rs1726866` A/A report with honest 1-of-2 coverage (`rs713598` absent).
  Authenticated application download matched the original bytes/hash; real
  application DELETE returned 204 at 13:17:27 UTC. Independent 13:18:46 checks
  found file/variant/observation/analysis/normalization/Storage-ledger counts
  zero and preserved the account. Native JavaScript confirmation is not proved.
- WGS streaming source `75a1a05`, build `1D0A_d2kzmT7TFNSpjf3L`, passed one
  small native Storage/report/download/deletion case: 17.6 seconds, 20.1 seconds
  total, three fresh Storage POSTs and zero skips/retries. A had 1,999 variants /
  2,002 observations (including reference/no-call); B's five variants remained
  intact; later-batch C returned 415 with no public/private working rows. Native
  deletion removed six synthetic files across attempts. Independent 13:51:50 UTC
  checks found source/derived/run/index/batch rows and 12 exact Storage keys zero;
  the other 33 files' fingerprint was unchanged. The original wrapper exit 1
  was a scratch report-path mismatch; its exact unmodified report contract
  passed separately without a browser rerun. Earlier selector failures remain
  preserved. See parent task `work/wgs-browser-verification-v3/` and the hosted
  readiness ledger for receipts.
- Earlier local review found previously
  accepted long alleles could exceed the larger registration envelope. The
  corrected helper targets one-MB batches and preserves the four-MB genetic
  stage bound, with only 1,024 bytes of registration metadata headroom. All 80
  focused checks across three files and 71 actual SQL assertions pass; the SQL
  verification rolled back with exact state preservation. Evidence is in parent
  task `work/wgs-position-registration-verification/byte-bound/byte-fix-receipt.json`.
- Dense local v3 source `138bb6f`, application `d722225`, build
  `wi0PVIbQtedwR6Zm8lKOp`, prepared **500,000 variants / 500,000 observations**
  in **103.304 seconds** (complete 14:37:36.179692 UTC; direct SQL 37.929564
  seconds). Report generation then failed in 8.482 seconds on an unindexed
  observed-point scan. Migration `20260908143927` adds the exact
  `(file_id, chrom, pos, source_line)` index; local lookup probe: 0.515 ms.
  A separate continuation on the same source passed in 10.4 seconds / 13.4
  seconds total, zero uploads/skips/retries: report POST 200 in 1.159 seconds,
  exact-source A/C and one provenance source, matching raw/decoded download
  hashes, native DELETE 204 in 2.580 seconds. At 14:45:06.892 UTC all 11 selected
  counts, including both Storage keys, were zero; the other 33 files' fingerprints
  and counts were unchanged. Exit 0, listeners closed, resource flags false.
  Evidence: parent task `work/wgs-dense-continuation/` and dense v3 preparation
  receipts. This is preparation plus a separate continuation, not an uninterrupted
  pass. Sampled preparation app/DB peaks: 529,740,596 / 404,121,191 bytes;
  continuation DB peak: 512,753,664 bytes. Cluster-wide WAL growth of approximately
  1.007 GB / 277.5 MB is not isolated per-file capacity evidence.
- Earlier dense v1/v2 failures remain preserved; all their synthetic originals
  and working rows are cleaned, following exact-claim expiry/recovery at
  14:34:15 UTC after an initial native 409. The earlier small-browser proof is
  unchanged. PR80 subsequently passed full CI and hosted preparation with report
  recovery, as recorded above. Production is PR80;
  acceptance remains **23/65**. No full-size WGS, 100-genomes/month or one-month
  original-retention capability is inferred; no additional spending is authorized.
- Protected hosted own-file evidence now includes actual chosen finding, ready
  notice delivered to Zoho and followed, exact-grant withdrawal, byte-identical
  retained download and complete source deletion. This contributes to the own
  G2.6 slice; other required journeys and broader capacity remain open. No new
  gate is promoted. See `docs/hosted-own-upload-readiness.md`.
- PR65 closes G4.6 after complete CI and hosted verification. Exact merge
  `16c01b71fdda862d9a1fd23153a6f618e2a255a7` is live on both public domains;
  the additive metadata migration preserves all files and historical unknowns.
  `docs/hosted-rollout-2026-09-06.md` records the test, migration, privilege,
  deployment and authenticated UI receipts. No other G gate is promoted.
- PRs 51–55 passed full CI and merged. PR 55's exact head was
  `1eb4a945e01f9fe2e8a549b5568073448f06ef67`, CI `33990743833`;
  production was verified READY at merge `a2823a6478b8e76c072054f88a3c62a6c2e95329`.
- Hosted schema and reviewed report-copy parity are recorded in
  `docs/hosted-rollout-2026-09-05.md`. All 151 published templates match the
  reviewed repository content; five have source-read dates and deeper study
  context. This is not scientific revalidation of the whole catalog.
- PR 52 deliberately replaced unvalidated personal score quantities with
  coverage-only output in chat, export and authenticated table reads. A6's
  former percentile claim is no longer a current product capability.
- ADR 0021 superseded the historical Medicines withholding decision with
  eleven per-position, letters-only reports. Following PR 64's source
  correction, all eleven were published with matching changelog entries at
  `2026-09-05T23:26:58Z`. The existing 151 estimates were unchanged. This
  does not complete the catalog-wide scientific citation gate.
- Embryo session/transport/unwind foundations do not complete source acceptance,
  physical cleanup, final graph purge or upload steps 3–5. No G gate is
  promoted by these primitives. The capability remains disabled.
- PR72 released four reviewed everyday-report updates and three additional
  personal-preview traits. Exact-head CI passed all 216 browser cases;
  merge `2450606` is production READY on both domains, with exact hosted
  content and unchanged other reports/files verified. Canonical coverage is
  now 71 statements in eight reports with 19 sources, not the entire catalog.
  G1.11/G4.7 remain NO; the ledger's count is 23/65.

## Historical baseline acceptance (A1–A18)

**Required baseline:** `864736979c92a08ba77e8580d61946eba6864918`
(`8647369`; 48 E2E tests on 2026-08-28).

G1.2 non-test-code baseline/current counts over the same population:
`@ts-expect-error` 0/0; `@ts-ignore` 0/0; `as any` 1/1;
`eslint-disable` 0/0.

**Historical withheld capabilities: 1**, none current (§8, alternative complete outcome 1). Pharmacogenomics was withheld on 2026-09-03 and left that status the same day under ADR 0021; `docs/capability-register.md` carries it as `shipped-degraded` and records **Withheld capabilities: 0**. Re-checked against that register on 2026-09-10, because this line and the G7.4 row both still read as if the withholding stood.
Pharmacogenomics — the Medicines category — is withheld on an obstacle
classified safety (primary) and scientific (supporting); its dossier is
`docs/withheld/pharmacogenomics.md` and its ADR is
`docs/adr/0018-pharmacogenomics-withheld.md`. No other gate is degraded,
blocked or excused by it.

Evidence for each Section-2 acceptance item. "E2E" refers to a Playwright
spec run against a production build backed by the local Supabase stack (real
PostgREST/Storage/Auth/Mailpit); "unit" to a vitest test; "live" to the
provisioned hosted project or deployment.

**Full-suite result: 57/57 E2E tests passed in a single clean run**
(2026-09-01, `pnpm e2e`, production build + local stack). The run
surfaced one real app bug — igv.js resolved to its UMD `browser`-field
build, whose AMD-or-global dispatch left the bundled module namespace
empty, so the genome browser never rendered in production — fixed by
importing `igv/dist/igv.esm.js` directly.

| # | Item | Status | Evidence |
| --- | --- | --- | --- |
| A1 | Clean clone → self-hosting guide → local app runs; deviations fixed and guide re-run clean | ✅ | Clean-room audit followed `docs/self-hosting.md`, found the `worker/.env.example` + `CRON_SECRET` deviations; both fixed and re-verified (`docs/audit-report.md`). Every documented `pnpm` script exists; every required env var is in `.env.example`/`worker/.env.example`. |
| A2 | Sign-up → Resend verification email → verified session; password reset works | ✅ | `e2e/auth.spec.ts`: sign-up shows "Check your email", the Mailpit-captured verification link yields a signed-in session; reset link flow changes the password and the new password signs in. `enable_confirmations=true`. |
| A3 | ≥12 real providers, each with source URL + last-verified date this run; US→NY flags NY exclusions; Buy links resolve | ✅ | 16 providers with canonical source URLs and `last_verified 2026-09-01`. The live checker resolved 14/16 buy/privacy pairs directly; the two automation-blocked pages were confirmed through current official pages indexed within the prior month. `e2e/providers.spec.ts` covers rendering, NY exclusion, and external buy links. |
| A4 | Synthetic 23andMe sample renders ≥100 reports with genotype-specific results + honest not-covered states | ✅ | `data/samples/synthetic_23andme.txt` covers all 135 template variants (GRCh37, liftover-exercising). `e2e/upload-vcf.spec.ts` + report resolution over 162 templates (151 estimates and, since ADR 0021, 11 Medicines `variant_call` positions, none of which the sample covers); reports page shows covered vs "your file does not cover this variant". |
| A5 | GIAB HG001 VCF (chr20–22): parse → annotate → report; genome browser at a locus; rsID + gene search | ✅ | `e2e/upload-vcf.spec.ts` uploads the real 187k-variant GIAB subset through the UI, processes to `annotated`, renders the igv.js browser and rsID/gene/locus search, reading the genotype from the observed `genotype` figure inside the results claim block. `e2e/genome-data.spec.ts` (tiny GRCh38 fixture) pins the expert path on the figure contract: one attributed claim block per results table, the four contract attributes on every genotype, the four-level breadcrumb, the heading and first-viewport interactive caps with the track in view, the region range as text, the search states, the data page's coverage figures with no percent text and the three "Data and methods" entry points. `src/lib/genome/pipeline-integration.test.ts` (unit) parses the full GIAB file and resolves every template. |
| A6 | ≥1 PRS with percentile + ancestry-portability caveat + numeric coverage | ✅ | 3 real PGS Catalog scores seeded; `src/lib/genome/prs.ts` (unit-tested) computes raw/z/percentile/coverage; report page renders percentile bar, mandatory `ancestry_note`, and numeric coverage fraction. |
| A7 | Research job → review-queue draft → publish updates changelog + triggers opt-in digest | ✅ | `e2e/research.spec.ts`: fixtured release drafts a `review` template (idempotent), publish flips to `published`, changelog shows the entry, mock-Resend captures the digest to the opted-in user; unauthorized refresh → 401. |
| A8 | mtDNA (and Y where supported) + admixture with "what your file supports" labels | ✅ | Process route computes admixture (EM over the 168-marker panel), mtDNA and Y haplogroups and stamps the panel id, version, coverage and result state on the admixture row. `/genome/[subject]/ancestry` renders the regions through one claim block of `ancestry-share` figures with a map, the well-supported switch, the two chips, the always-visible table and the G4.4 sentences; below the reliability threshold the exact §4.6 grey-state sentence with the measured counts. `e2e/upload-vcf.spec.ts` (GIAB chr20–22) pins the grey state, the lineage empty states and the XX gloss; `e2e/ancestry.spec.ts` pins both states on two users: the figure-unit contract, the sum rule in both toggle states, tab order, the panel focus contract, the label denylist, the gradient feather, the first-viewport budget, first-party origins and axe in both themes. |
| A9 | Copilot: no-key local instructions; consent dialog names provider + data classes; tool call retrieves a genotype; streamed answer cites its report; grant revocable | ✅ | `e2e/copilot.spec.ts`: local-mode instructions with no provider (local-first ordering); cloud provider → consent dialog naming host + data classes before any genome data leaves; `get_genotype` tool call; the answer cites the report + genotype; revoke in Settings re-requires consent. `e2e/copilot-refusal.spec.ts`: one prompt per treatment rule, what-to-eat, which-embryo, cross-subject, diagnosis and prognosis prompts each get the fixed refusal with zero provider requests on any path; a later allowed prompt reaches the provider without the refused turns in its history; a fabricated number is replaced. |
| A10 | Tier-2 BAM/CRAM: resumable direct-to-Storage upload (interrupted+resumed in E2E), hashed, listed, re-downloadable | ✅ | `e2e/tier2-upload.spec.ts`: 14 MB BAM, 2nd TUS chunk aborted then resumed, stored, sha256 shown, "Stored (Tier 2)" listed, re-downloaded byte-for-byte identical. |
| A11 | Worker skeleton consumes a queued annotation job and writes results back; documented | ✅ | `worker/` self-contained queue consumer (`FOR UPDATE SKIP LOCKED`) with a real VCF-annotation stage joining `ref_variants`; `worker/src/annotate.test.ts` (unit, runs in root CI); Dockerfile + compose + README; alignment/calling documented as the extension path. |
| A12 | RLS proof: user A denied direct PostgREST/storage reads of user B's data; anon denied everywhere private | ✅ | `e2e/rls.spec.ts` (6 tests) attacks real PostgREST + Storage: cross-user reads return 0 rows, forged inserts rejected, cross-user storage denied, anon denied on every private table, `llm_keys` hard-denied even to authenticated. |
| A13 | Export ZIP has originals + variants + reports; account deletion uses the cancellable seven-day notice and joined-object purge lifecycle; no fee path | 🟡 | `e2e/deletion-export.spec.ts` verifies the free export and cancellable notice period. `e2e/account-deletion-purge.spec.ts` proves exact joined Storage deletion, relational zero-residual checks, Auth-user-last deletion, and pseudonymized terminal control rows. Database tests additionally prove complete deletion of wholly owned, unshared adult subjects plus their family-pair and Portrait outputs. Cross-account adults and embryo/claim/invitation/reviewer graphs deliberately fail closed until their independent notice, transfer, restriction, or preservation dispositions are implemented, so this remains 🟡 rather than overstating full-contract coverage. No billing/fee code path exists. |
| A14 | Network audit: on landing/dashboard/report, third-party origins == allowlist; `window.fbq` undefined; no beacon/pixel hosts | ✅ | `e2e/network-audit.spec.ts` over real rendered pages: only first-party origins observed (fonts self-hosted via `next/font`), `window.fbq`/`window.gtag` undefined, no tracker-host fragments. |
| A15 | Legal pages complete; placeholder gate passes; disclaimers on report surfaces by E2E | ✅ | `e2e/legal.spec.ts`: all 8 legal requirements + dual-surface Plus Bio disclosure present and on-topic; report-surface disclaimer visible; `pnpm gate:legal` passes. Legal audit returned clean. |
| A16 | Design tokens implemented; wordmark + attribution; light+dark; axe/a11y pass; Lighthouse ≥90 | ✅ | `e2e/a11y.spec.ts`: axe WCAG2 AA on landing/providers/privacy/sign-in/dashboard/settings/uploads in both themes; design-token assertions (Fraunces, pill radius, attribution, numbered steps, theme toggle). UX audit confirmed AA contrast in both themes. Lighthouse against the served production build: landing performance 95 / accessibility 100, providers performance 92 / accessibility 100 (`scripts/lighthouse-check.ts`). |
| A17 | CI green on main: typecheck (strict), lint, unit, E2E; no secrets in repo history; `.env.example` complete | ✅ | GitHub Actions `checks` job green (typecheck+lint+unit+gate). `pnpm gate:secrets` scans the tracked tree and every added line in non-merge commits after the required baseline; only exact local fixtures covered by ADR 0006 are accepted. The hosted Supabase publishable key formerly present in deployment documentation was removed from the current tree. `.env.example` is complete and E2E runs against the local stack. |
| A18 | LICENSE, README, architecture doc, ADR directory (incl. Gating Decision), dataset-license audit | ✅ | `LICENSE` (AGPL-3.0), `README.md`, `docs/architecture.md`, `docs/adr/` (0001 Gating Decision + 0002–0006), `docs/dataset-licenses.md` (ClinVar/dbSNP/gnomAD/GWAS/PGS/1000G verified; SNPedia excluded as NC). |

## Notes

- **Lighthouse (A16):** axe accessibility is enforced per-rule in CI; the
  Lighthouse performance/accessibility ≥90 gate
  (`node --experimental-strip-types scripts/lighthouse-check.ts` against the
  served production build) passed with landing 95/100 and providers 92/100
  (performance/accessibility). It needs a Chrome run, so it is a documented
  local/CI-optional check rather than part of the default E2E gate.
- **Hosted deployment:** Supabase provisioned + migrated + provider/reference
  data seeded; Vercel linked and deploying green; the remaining secret env
  vars are a one-time owner step (`docs/deployment.md`).

## Full-resolution gates (G1–G8)

Current audited state: **23/65 YES**. `NO` means the exact gate is not yet
proved; partial implementations are intentionally not rounded up. The adult
subject invitation work adds a safe TEST-LOCAL reservation and acceptance
boundary, but it does not claim the class-(b) upload, quarantine, purpose,
revocation, notification, or ownership-transfer contract is complete.

| ID | Statement | YES/NO | Evidence |
| --- | --- | --- | --- |
| G1.1 | Production build exits cleanly without source warnings. | YES | `pnpm build` exits 0; `package.json`; verified 2026-09-01. |
| G1.2 | Typecheck passes and suppression counts do not exceed baseline. | YES | `pnpm typecheck`; baseline/current counts are recorded above and reproducible with `git grep` over non-test `*.ts,*.tsx,*.js,*.jsx`. |
| G1.3 | Lint treats warnings as failures and passes. | YES | `pnpm lint`; `package.json` uses `eslint --max-warnings=0`. |
| G1.4 | Unit suite and required pure-module coverage pass. | YES | `pnpm test` (290 tests across 36 files as of `31e1d7c`); unit specs under `src/lib/**/*.test.ts`, `src/copy/**/*.test.ts`, `src/components/**/*.test.ts` and `scripts/**/*.test.ts`. |
| G1.5 | E2E has zero failures, skips, retries, or quarantine. | YES | `pnpm e2e` (61 tests); `playwright.config.ts` has `retries: 0`; `scripts/run-e2e.ts` validates `test-results/results.json`; `docs/test-diff-register.md`. **One transport failure on CI, and what ruled it out, 2026-09-11.** Run 34538866398 attempt 1 on `4ea41af` failed with `e2e/portrait.spec.ts:539` dying on `apiRequestContext.get: socket hang up` — a death in transport rather than an assertion; 305 passed, 1 failed, 4 skipped by serial mode. Three independent things establish it was not the change under test. The diff was four documents, two embryo-spec titles and one integer constant, reaching no source, no server and nothing portrait or family. The two commits underneath it, `8732f61` and `5cc8e64`, passed CI in full as runs 372 and 373. And the spec was reproduced locally twice: alone (`10 passed (1.4m)`, no skips, no retries), and then **under load in CI's own ordering** — `a11y.spec.ts` first, `portrait.spec.ts` immediately after — which returned `88 passed (31.3m)` with line 539 green in **5.7 s**, on a container simultaneously running four agents and a unit suite. That second run is the one that settles it: `a11y.spec.ts` took **29.0 minutes** locally against CI's 17.1, so the local attempt applied *more* pressure than CI did and still did not reproduce the failure. The honest conclusion is a transient condition on the hosted runner, not a defect in the suite — recorded rather than waved away as a flake, because "flake" is not a root cause. The single re-run allowance was spent once, deliberately, and the row stays YES on the evidence that the suite is green on identical code. |
| G1.6 | Extended RLS attack suite covers every new table and revocation. | NO | `e2e/rls.spec.ts` does not yet cover all tables in `supabase/migrations/20260831*.sql`; adult invitation database invariants are narrower in `supabase/tests/adult_subject_invitation.sql`. Measured 2026-09-10 **against the live database**, which is the authority here: `public` holds **155** tables with row-level security enabled, **131** of them with no policy at all — the strictest posture, denying every role that does not bypass it, with access through security-definer functions. **24** carry policies and **11** carry a policy that reads the caller's identity (`auth.uid()` or `auth.jwt()`); all 11 are named by a pgTAP test or `e2e/rls.spec.ts`. **No table with a caller-scoped policy is untested**, so the coverage still owed is over deny-all and public-read reference tables, not an open path from one person's data to another. `anon`, `authenticated` and `inherit_upload_only` do not bypass row-level security; `service_role` does, by design, so a route using the admin client reaches these tables regardless — which is why, for the deny-all tables, the protection is not the database's but that every read goes through a security-definer function that checks authority first. Three enforcements now hold this: `supabase/tests/rls_deny_all_tables.sql` pins that 12 named closed tables still have RLS on and no policy, and that the browser roles still cannot bypass it — adding any policy to one of them fails it; and `scripts/rls-policy-coverage.test.ts` requires a test for every caller-scoped policy, verified by planting one, and separately requires that no file under `src/` reads one of those closed tables through the admin client directly, reading the table list out of the pgTAP file so the two cannot drift — verified by planting a `from("generated_exports")` read, which the gate named by file and table. Two reference tables that hold no one's data are exempt from that second check. **An earlier revision of this row reported 85/66/20/14/6 from a static scan of the migrations and was wrong**: the pattern required a quoted policy name and silently skipped every `create policy unquoted_name`, and a static scan reads migration history rather than current state. The database disagreed the first time the deny-all test ran, which is why these figures come from it. What remains for this row: naming is presence of coverage, not proof that the naming test attacks the policy. **The sentence above is now closed, and closing it found a defect, 2026-09-10.** Four pgTAP files attack rather than name: `rls_cross_account_attack.sql` (127 assertions) issues real statements against all **11** caller-scoped tables as three identities — anonymous, a foreign signed-in account, and the legitimate owner — for read, insert, update and delete, with the owner as an anti-vacuity control on every table so a test cannot pass by denying everyone; `rls_closed_table_attack.sql` (12) attacks all **131** closed tables with the list read from `pg_class`/`pg_policy` at run time, so table 132 is covered the day it lands, and plants a readable table to prove the harness can detect a leak; `rls_revocation_immediate.sql` (24) covers revocation end to end; `rls_truncate_not_bypassed.sql` (9) is the finding below. **Before this, no pgTAP file performed a cross-account read attack on any of the 11**: that coverage lived only in `e2e/rls.spec.ts` through PostgREST, which reached 9 of 11 — `upload_sessions` and `report_observed_calls` appeared nowhere in it. Every foreign and anonymous read returned zero rows or 42501; no account can read another's data. **One assumption the whole deny-all posture rests on was false.** PostgreSQL row-level security governs SELECT, INSERT, UPDATE and DELETE only; TRUNCATE is authorised by the table privilege alone and no policy is ever consulted. `anon` and `authenticated` each held TRUNCATE on **15** `public` tables — `user_variants`, `user_prs`, `consent_grants`, `profiles`, `genome_files`, `ancestry_results`, `llm_settings` among them — and `research_releases`, which `rls_deny_all_tables.sql` names as closed, is one of them. Reproduced in a rolled-back transaction as `anon`: 6,545 rows in `user_variants` visible as **zero**, `delete` refused with 42501, **`truncate` accepted**, count zero. Stated without drama: all three browser roles are NOLOGIN and are reached only by `authenticator` doing SET ROLE for a PostgREST request, and PostgREST never emits TRUNCATE, so this was not a one-request exploit against the deployed surface — it was blast radius, turning any injection into a statement running as a browser role, or any security-invoker function interpolating input, into the irreversible loss of every user's variant calls and consent records. Nothing in the repository issues TRUNCATE as a browser role, so the privilege bought nothing. The origin is the schema default: `pg_default_acl` grants `arwdDxtm` on new `public` tables to both roles, and `D` is TRUNCATE; later migrations revoked the four DML privileges where they were unwanted and left TRUNCATE behind, which is why the fifteen are spread across unrelated migrations. `20260910170000_revoke_truncate_from_browser_roles.sql` revokes it from every existing table and from the schema default, leaves `service_role` untouched because the purge executor runs as it, and reports rather than assumes the `supabase_admin` half, which a hosted project's `postgres` may not own. Verified after applying: no browser role holds TRUNCATE on any `public` table, `service_role` still does, and the same `anon` statement now answers `permission denied`. **The row stays NO pending CI**, which runs pgTAP on a fresh database; two clauses also remain — the storage-prefix half is `storage.objects` behaviour that only `e2e/rls.spec.ts` can exercise, and `chats`/`chat_messages` carry `*_select_own` policies no browser role can reach (neither table has any grant), so those policies are dead code and the tests assert the real behaviour rather than pretending otherwise. |
| G1.7 | Network audit covers every registered route/state/theme/auth mode. | NO | Measured 2026-09-10. `e2e/network-audit.spec.ts` is **6 tests over roughly 8 routes**: the landing page, the providers page, the dashboard, a report page and the browse page with the embedded genome browser (the last three signed in), plus `/privacy`, `/terms` and `/about`. Against **113** built routes that is the subset this row already described, and the shortfall is wider than route count: neither theme is exercised, and no route is visited both signed in and signed out — each test picks one. **One gap was not a subset problem and is now closed.** The brief names three globals — `window.fbq`, `window.gtag`, `window.dataLayer` — and the shared assertion checked only the first two. `dataLayer` is how Tag Manager arrives and the one that appears without a visible script tag, pushed by an inlined snippet. It is now asserted alongside the other two, so every existing test in the file gained the check. **The static side is clean and was checked rather than assumed**: no `<script>` tag or `next/script` in any layout, no third-party script `src` anywhere under `src/`, and the external hosts that do appear are not browser origins — `rest.ensembl.org` occurs only in the server-side `api/jobs/annotation-refresh` route, while `pubmed.ncbi.nlm.nih.gov`, `doi.org` and `pgscatalog.org` are `<a href>` citation targets carrying `rel="noopener noreferrer"`, which a page does not contact unless a reader clicks away. The remaining hosts in source are synthetic (`example.test`, `synthetic.invalid`, `example.invalid`). **What keeps this NO** is the coverage the brief asks for, not a known leak: every registered route, in both themes, signed in and signed out. That is roughly 113 routes × 2 themes × 2 auth states against a browser suite already running about 15 minutes, so extending it is a runtime-budget decision rather than a line of code, and the `dataLayer` assertion is not a substitute for it. **The cost reasoning in the sentence above is wrong, re-measured 2026-09-10.** Two things: the population is not 113 routes — 88 of the register's 160 entries are endpoints, which render no page and contact no origin, so what the brief asks for is the **62 kept pages**; and `e2e/a11y.spec.ts` already performs a `page.goto` on every one of them in both themes, under a coverage assertion that fails when a registered page is missed. Attaching a request listener to a navigation that is already happening is not a runtime-budget decision. The work is to lift `ALLOWED_ORIGINS`, the tracker fragments and the three-global check out of `e2e/network-audit.spec.ts` into `e2e/helpers.ts` and assert them after each load in that sweep, which takes origin coverage from 8 surfaces to 62 in both themes and inherits the completeness assertion; the six existing state-specific tests stay as the deep cases. The auth dimension is the one real gap: each existing test picks signed in or signed out, and the sweep audits public pages signed out and authenticated pages signed in, so no route is checked in both. **Extended from 8 surfaces to 62, 2026-09-10.** `ALLOWED_ORIGINS`, the tracker fragments and the three-global check now live in `e2e/helpers.ts` as `assertNoThirdParty`, and the register-derived accessibility sweep calls it after every navigation it was already making — the public pages, the six document routes and the 22 authenticated pages, in both themes. So the promise is proven on **every kept page the register carries**, not on the landing page and four others, and it inherits the coverage assertion that fails when a registered page is missed. The authenticated sweep attaches one listener and clears it per route rather than one listener per route, which would leave 44 on the same page. Verified: `77 passed, no skips, no retries`, with no unexpected origin, no tracker-like host and none of `window.fbq`, `window.gtag` or `window.dataLayer` defined on any of them. The six state-specific tests in `e2e/network-audit.spec.ts` stay as the deep cases, including the one that renders the real igv browser and asserts it never reaches igv.org. **The row stays NO on the two dimensions it shares with G1.13a**: auth mode, because the public sweep runs signed out and the authenticated sweep signed in so no route is checked in both; and state, because the sweep visits each route in the one state its `VISIT` URL produces rather than in each state the register declares. Cost, stated because it is real: the a11y spec is 16.3 minutes locally with both audits in it, against 7.0 minutes in CI at the previous scope. **The auth dimension is closed too, 2026-09-10.** Every public route is now swept a second time with a session, at the full tag, viewport and theme matrix and with the origin audit, on one reused page; and every authenticated route is visited without a session and asserted to land on sign-in, which is what signed-out means for those routes and is a stronger statement than skipping them - a page that rendered its own content to a signed-out reader fails here, which is a disclosure bug before it is an accessibility one. `1 passed, no skips, no retries`, 11.0 minutes. **What is left is state alone**: each route is visited in the one state its `VISIT` URL produces, not in each state the register declares, and that is the same 288-pair matrix `UNPROVEN_ROUTE_STATE_PAIRS` measures at 5 proven in G1.12. **CI run 353 on `b828c58` confirmed the auth dimension green, 2026-09-10**, which is what flipped G1.13a; the origin audit rides the same navigations, so it is confirmed with it. **This row stays NO on state alone** — each route is visited in the one state its `VISIT` URL produces, not in each state the register declares, which is the same 288-pair matrix `UNPROVEN_ROUTE_STATE_PAIRS` measures at 5 proven in G1.12. That is now the single thing between this row and YES. |
| G1.8 | Legal gate runs rendered-page mode over all required routes. | YES | The G5.7 and G5.8 checks this clause requires are now in `scripts/legal-placeholder-gate.ts` and run in both modes (see those rows). **The route list was the other half of the problem, and it was worse than "incomplete".** G1.8 asks for every legal, consent and disclosure route "including new ones", and the list was hand-kept and had drifted to **4 of the 14** legal routes that exist — `/legal`, `/legal/appeals`, `/legal/consents`, `/legal/future-person`, `/legal/gdpr`, `/legal/incident-response`, `/legal/insurance-and-discrimination`, `/legal/self-hosting`, `/legal/state-genetic-privacy` and `/legal/where-inherit-works` were never fetched. A hand-kept list cannot honour "including new ones" at all, so it is now derived from the filesystem: **17 routes** (14 legal plus `/about`, `/privacy`, `/terms`), and a new legal page is covered the day it lands. A count guard fails the gate if discovery returns fewer than 12, so a broken walker cannot quietly shrink coverage back. Dynamic segments are deliberately skipped — `/legal/[artifact]` and `/legal/consent/[key]` have no fetchable URL without valid parameter values, and fabricating one would test a 404 rather than a document; their prose is still checked in source mode, which walks the whole directory. **One of the two prerequisites is now done.** The fetch loop had no error handling, so an unreachable server exited with a Node stack trace and no report — which reads as a broken gate rather than a broken page, and rendered mode is precisely where that happens because it depends on a server someone else started. It now records `<route>: unreachable — <reason>` and continues, verified by pointing `SERVER_URL` at a dead port before and after: the old code threw `TypeError: fetch failed` with no findings, the new code reports all 17 routes and exits 1. **Rendered mode now runs in CI.** It previously did not run at all: `gate:legal` was invoked once at step 11, before Supabase and before the production build, so `SERVER_URL` was unset and every CI run to date took the source path. A second invocation now runs after the existing production build — `next start` on port 3199, a readiness poll, then `SERVER_URL=http://127.0.0.1:3199 pnpm gate:legal` — reusing that build rather than adding one, and placed before the browser suite so a legal failure fails fast instead of after seventeen minutes. If the server never answers the step exits 1 with an explicit annotation, so a gate that checked nothing cannot report success. **Running it locally is what stopped it landing broken.** Against a real build all seventeen routes failed at once, on the gate's own G5.7 call-to-action pattern matching the site nav's "Get started" — one finding about the persistent chrome, repeated once per page, saying nothing about the legal text. Rendered checks are now scoped to the `<main>` landmark, which X6.1 already excludes persistent navigation from; a page without the landmark fails rather than falling back to the whole document, because that fallback would quietly restore header-matching on exactly the page that lost it. Verified against a synthetic server on all three branches: violations confined to the chrome pass, violations inside `<main>` still produce all five findings on all 17 routes, and a document with no landmark fails 17 times. The step's `run:` block was then extracted verbatim from the workflow YAML and executed under `bash -e`: the gate passed, the server was cleaned up by its trap, and the not-ready branch exits 1. **CI run 315 executed it**: the job log records `checked 17 rendered routes` followed by `legal placeholder gate passed` at step 23, on commit `cbf2a51`, and the run concluded success. Both prerequisites this row named are now done and rendered mode runs on every CI run, so the row closes. |
| G1.9 | Name gate passes. | YES | `pnpm gate:names`; `scripts/name-gate.ts`; the private denylist is supplied through `NAME_DENYLIST_FILE` and is never committed. |
| G1.10 | Readability and vocabulary gate passes with self-test. | YES | `pnpm gate:readability` runs the pinned ten-case self-test and, as of 2026-09-03, extracts static TSX, the `src/copy/**/*.ts` registry (constants, objects, arrays, `as const` tuples, function bodies, template slots as the placeholder word, role from key or export name), mail templates under `src/emails/`, template literals in JSX, copy props (`heading`, `label`, `description`, `summary`, `note`, `text`, `children`), chart axis-label props, report templates, provider fields, and consent artifacts; it scans 1,621 blocks (956 long, 434 short-role, 266 sentence-capped, 152 copy-registry) using `data/plain-vocabulary.json` and `data/jargon.json`, reads contractions as their full words, exits 0 on the whole tree, and is a required step in `.github/workflows/ci.yml`; regression tests in `scripts/readability-gate.test.ts`; see `docs/readability-audit.md`. |
| G1.11 | Claims/provenance gate passes. | NO | **`pnpm gate:claims` exists as of 2026-09-10** — `scripts/claims-gate.ts`, wired into the static half of CI after `gate:routes`, built to the shape of `scripts/route-gate.ts`. Ten checks, each traced to a brief line: the citation register's schema and its snapshot rule for non-permanent sources; every claim resolving to evidence with a matching access date; every surface a claim names being a real registered route, export contract or mail module; every `FigureProvenance` built in `src/` resolving to a real citation, a table a migration creates, or a module with a unit test beside it; every element carrying a claim marker also carrying provenance; the chrome-kind exemption never co-occurring with a claim; every report-body prose block being a registered canonical claim matching verbatim; every source a template cites existing in the register; and each of the seven designated surfaces reaching the shared claim component. Read at source rather than in the DOM on purpose: three components emit these attributes and every value is an expression, so an attribute-presence scan proves the attribute is present, not that `computed:embryos/carrier` names real code. `scripts/claims-gate.test.ts` plants a defect per check plus the empty-scan case: 36 tests. **Six checks find nothing — the registers are internally sound**: 19 cited sources with all 7 snapshots present, 71 claims on 94 pieces of evidence, every citation and access date consistent. What fails is coverage, and `docs/claims-divergence.json` now measures it in 42 entries checked in both directions: **675 of 746** report-body prose blocks are not registered claims, **201** template citations (**177** distinct sources) are absent from the register, and **six of the seven** designated surfaces never reach the shared component — report bodies are the one that does. The pass message carries those ratios, so a green run states the distance rather than implying the work is done. **The gate found four live defects and they are fixed**: three provenance attributes named modules nobody had written (`computed:genome/browser`, `computed:embryos/carrier`, `computed:embryos/qc`), and a fourth could emit `citation:` with an empty id. The browser one was not a rename — the region search read `user_variants.genotype` inline in the page, so no module produced the value until the search was extracted into `src/lib/genome/browser.ts`. `FigureProvenance`'s computed variant is now a union of eleven real modules, which makes that class of defect a compile error. One divergence is genuinely accepted and recorded as such: a within-family figure's citation is the publication its stored numbers were measured under, so the id comes from the row and can never be a literal; the correctness the check was reaching for is carried by the type instead. **The row stays NO on the backlog, and two parts of it are owner decisions, not engineering**: `data/citations.json` holds no `statute`, `registry` or `regulator` entry and `data-legal-verbatim` exists nowhere, while a quoted statute would exceed the 25-word quote cap — so how much of 22 legal pages should ever be a bound claim is unanswerable today; and the glossary the brief specifies in three places does not exist at all, so it is built or recorded as withheld. The rest is 675 source reads and 177 publications, which cannot be automated without inventing evidence. |
| G1.12 | Route/state register gate and titled tests pass. | NO | **`pnpm gate:routes` now exists, 2026-09-10.** `scripts/route-gate.ts` runs in the static half of CI after `gate:secrets` and enforces five things the register asserted and nothing checked: declared methods against exported methods (37 endpoint pairs), registered redirect status against the status the implementation emits (10 redirects), registered kind against built kind at every concrete path the register pins (101 paths), declared storage buckets against the buckets migrations create (4 declared, 3 created), and the (route, state) matrix as a ratchet (288 required, 5 proven, 283 unproven, held by `UNPROVEN_ROUTE_STATE_PAIRS`). The first four compare against `docs/route-divergence.json` in both directions, so a fix that leaves a stale entry fails as loudly as a new divergence. `scripts/route-gate.test.ts` plants a defect for each check on a repository that is real except for the one thing under test — the app tree, migrations and browser tests are symlinked so the floor guards see their true size — and asserts the empty-scan case fails rather than reading as a clean product: 14 tests. **The state matrix is now measured rather than asserted.** 288 (route, state) pairs are required by the register's 11 `stateProfiles` across 160 routes; 5 are proven by a browser test title today — `/embryos empty`, `/embryos/compare consent-required`, `/family/invite complete`, `/genome/[subject]/reports empty` and `/genome/[subject]/reports partial-coverage`. A title proves a pair when it names the registered path and the state id as separate, non-overlapping, whole tokens, so `empty-history` does not prove `empty` and a state id occurring only inside the path proves nothing. The remainder is a ratchet failing in both directions: proving a pair means lowering the number in the same change, and a rise means a proof was lost. The row stays NO on its second half: 283 of 288 required route/state pairs have no titled test, so the gate that measures the coverage exists and the coverage does not. **The 283 was checked for scanner blindness before anyone spends a session on it, 2026-09-10, and it is overwhelmingly real.** A pair counts as proven when a browser test *title* names the route and the state as separate anchored spans, and `browserTestTitles` reads titles out of the source statically — so a title built by interpolation resolves at run time to something that reader never holds. `legal page ${route} is complete, on-topic, and placeholder-free` is **one** source title that Playwright turns into twelve, each naming a real route the suite really drives. Measured: **9 of 161** titles carry an interpolation, and of those only the legal one holds both a path and a state word — `axe: ${route} (${theme})` names no state, and the withdraw and report-preview ones interpolate a mode or a slug rather than a state. So at most about **12** of the 283 are genuinely driven today and merely invisible to this reader; the other ~271 are a real product and test gap. The gate now says so in its own pass line rather than letting 283 read as a pure product measure, and it reports the unresolved count instead of guessing at the resolved text — a gate that inferred what runs would be worse than one that admits what it cannot see. **This also names the trap in closing it**: the mechanism is a title convention, so 283 pairs could be "proven" by renaming tests without driving one extra state. That would be manufacturing a green ratchet, and the number is only worth what the tests behind it actually do. **Three pairs closed honestly, 283 to 280, 2026-09-10 — and how they were chosen is the point.** Nothing was retitled to make a number move. Each of the three was read first to confirm the test already drives that exact state, and only then was the title made say so. `/genome/[subject]/reports/[slug] not-covered` navigates to the APOE report and asserts "Your file does not cover this variant." together with "A missing result is not a negative result."; `/genome/[subject]/reports/[slug] complete` uploads a real fixture and asserts the covered report's six headings and its one attributed genotype figure; and `/embryos/[embryoId] consent-required` was already asserting `[data-slot="blocking-state"][data-state="consent-required"]` and merely wrote its path as `/embryos/{id}`, which is not the notation the register uses. Verified `2 passed (33.5s)`, no skips, no retries, on the two retitled report tests. That is the whole honest yield available from renaming: every other candidate either names no state or drives none, so the remaining 280 need tests that do the driving. **Six more, 280 to 274, and the seam is now exhausted — recorded so nobody re-mines it (2026-09-10).** Five came from a lead in `docs/capability-register.md`, which says the refused branch of every Embryo route is proven with the jurisdiction flag unset: `e2e/embryos.nojurisdiction.spec.ts` asserts `[data-slot="jurisdiction-unavailable"]` with `data-jurisdiction-source="unset"` on `/embryos/upload`, `/embryos/request-data`, `/embryos/compare` and `/embryos/{uuid}`, and its sibling does the same for `/embryos` through the blocked tiles. The sixth is `/embryos/compare empty`, which asserted `[data-slot="blocking-state"][data-state="empty"]` while its title said only "the zero-cohort blocking state". Every one was read before the title was touched; nothing any of those tests *does* changed. Verified `3 passed (24.2s)` on the jurisdiction-off project and `1 passed (28.7s)` on the sixth, no skips, no retries. **The search's basis, stated exactly, because the first version of this sentence overstated it.** The mechanical sweep covered three selector families - `data-state=`, `[data-slot="blocking-state"]` and `[data-slot="jurisdiction-unavailable"]` - and found no further test driving a declared state without naming it. Calling that "systematic" was too strong: at least eight other state-ish markers exist (`empty-state`, `analysis-state`, `person-state`, `nothing-read`, `grey-state`, `portrait-empty`, `carrier-empty`, `data-coverage-status`). Each was then examined by hand, and the conclusion survives for a reason worth keeping: **they mark component-level states, not the route-level state the register declares** - a carrier panel with nothing bound, a portrait with no classified position, an ancestry figure reading nothing. A page whose one panel is empty is not thereby in the register's `empty` state. Where such a marker does coincide with a route state, the pair is already proven (`/embryos empty`). So the yield is nil, but by examination rather than by a sweep that covered everything. So the running total is **14 of 288 proven**, and the remaining 274 cannot be reached by reading titles at all - they need tests that do the driving. **Why this tranche existed while the embryo result states do not:** the register applies a state profile wholesale, so all eight states land on every `product-result` route, but `docs/capability-register.md` records that no ingest path creates a cohort or a quality row, so `complete`, `partial-coverage` and `not-covered` have no reader who can reach them there. `jurisdiction-unavailable` and `empty` are states of refusal and absence rather than of data, which is exactly why they were drivable today. **That conclusion was wrong about `complete`, corrected 2026-09-10 by reading the suite rather than the ingest path.** The premise holds — no ingest path builds a cohort — but the suite does not go through ingest: `seedCohort` in `e2e/embryos.spec.ts` inserts `embryo_cohorts` at line 200 **and `embryo_qc` at line 256** with the service-role client, which is the same way every embryo state already under test is driven. So a cohort and a quality row do exist in the suite, `/embryos complete` is already asserted, and compare and detail `complete` need new fixture setup rather than unshipped product. I inferred unreachability from a missing ingest path without checking whether the tests needed one, which is the error to avoid repeating: the question is what a fixture can reach, not what a user journey builds. **`partial-coverage` and `not-covered` do stay blocked, and for a better reason than I gave.** `data/embryo/allowed_conditions.json` ships `status: "withheld_until_calibrated_models_are_registered"` with zero conditions, and `src/lib/embryos/allowed-conditions.ts` states that emptiness is "a deliberate, valid state" and that "nothing here may populate it". Those two states are withheld by design until calibrated models are registered, so they are not a gap to be closed by testing at all. **Eleven document `complete` pairs proven 2026-09-11, and the ratchet moves 274 to 263.** This is the one honest use of the title convention and it is worth separating from the abuse the row warns about. `e2e/legal.spec.ts` drove all eleven states already — it navigated to each document route and asserted that route's own required content, placeholder-freedom and a length floor — but it did so from a single interpolated loop, `` test(`legal page ${route} …`) ``. `scripts/route-gate.ts` reads titles **statically out of the source**, so `${route}` reached it as five literal characters and proved nothing; the gate says as much itself, counting these among the titles "built by interpolation, which this static reader cannot resolve and does not guess at". Splitting the loop into eleven statically titled tests changed no assertion and added no navigation the suite was not already making. The distinction that matters: renaming a test that drives nothing would manufacture a green ratchet, which is why this row calls the seam exhausted; resolving an interpolation the reader cannot evaluate reveals a proof that was already earned. All eleven verified in a browser — `13 passed (41.2s)`, no skips, no retries — and each pair is recorded in `docs/route-divergence.json`, which the gate compares in both directions, so a lost proof fails as loudly as an unrecorded one. The remaining nine interpolated titles are the axe sweeps, which prove no pair and are not candidates. |
| G1.13a | Full axe tag matrix passes on every registered route. | YES | **Three of this row's four dimensions closed 2026-09-10, verified green on CI run 343.** `e2e/helpers.ts` ran `["wcag2a", "wcag2aa"]` at whatever viewport Playwright happened to pick. `docs/inherit-v2-brief.md` line 1061 pins six tag sets — `wcag2a`, `wcag2aa`, `wcag21a`, `wcag21aa`, `wcag22aa`, `best-practice` — across **320x568, 390x844 and 1280x800**, in both themes, plus reduced-motion emulation and every gated result in both states. All of that now runs, on one page load per route because navigation dominates, over every registered kept page under the coverage assertion that already fails when one is missed. **Widening it found two live defects, both fixed.** The four auth pages — sign-in, sign-up, forgot-password, reset-password, which every account passes through — rendered a wordmark, a card and a footer as plain `<div>`s: `landmark-one-main`, and `region` with four nodes outside any landmark, so a screen-reader user had no way to reach the form but to read from the top. And at phone widths only, `scrollable-region-focusable` on three surfaces: a table wider than the screen scrolling inside a container nothing can focus, so its right-hand content was unreachable without a pointer — 16 nodes on `/providers`, one on `/legal/gdpr`, one on the variant browser. Each is now a focusable region named by the heading above it rather than by retyped text. The gated half lives in `e2e/report-gate.spec.ts`, where both states are already driven: one report from each of the three gated categories, in both forms. Thirty templates are gated and they render two shapes between them, so one per category covers every gate a person can meet. **What keeps this NO is the fourth dimension, auth.** The public sweep visits 36 pages signed **out** and the authenticated sweep 25 signed **in**; no route is audited in both, and the header and navigation differ between them. Cost, stated because it is real: the spec went from 4.8 to 7.0 minutes on CI, and the audit of `/family/health-picture` — 660 figures, the widest table in the product — needed its own 600-second budget after run 341 timed out inside axe. **The auth dimension is closed, 2026-09-10, and with it the fourth of four.** Every public route is swept again with a session at the full matrix, and every authenticated route is visited without one and asserted to redirect to sign-in rather than answer. `1 passed, no skips, no retries`. Every clause of the brief's paragraph now runs: six tag sets, every registered kept page, both themes, three viewports, reduced motion, both gated states, both auth modes. **Pending its first green CI run**, which is what this row will record; the local evidence is stated above and the cost is real - the accessibility spec is about 27 minutes locally with every audit in it. **CI run 353 is that green run, on `b828c58`, 2026-09-10** — the commit carrying the auth dimension — so every clause of the brief's paragraph has now run in CI rather than only locally: six tag sets (`wcag2a`, `wcag2aa`, `wcag21a`, `wcag21aa`, `wcag22aa`, `best-practice`), every registered kept page under the coverage assertion that fails when one is missed, both themes, all three viewports, reduced-motion emulation, both gated report states, and both auth modes. **This row turns YES.** What it does not carry, and should not be read as carrying, is G1.13b: reflow, target size, keyboard order and text alternatives are that row's four measurements and axe makes none of them. **The CI cost is measured now, not just the local one, 2026-09-10.** Both rows quoted a local figure and left the number every future run actually pays unstated. The full browser job is **53.4 minutes** (307 passed) on CI, up from roughly 37, and `e2e/a11y.spec.ts` alone is **17.1 minutes** against about 7 before this session. That is the real price of six tag sets across three viewports in both themes and both auth modes, plus the keyboard-escape check and the origin audit riding the same navigations. It is worth it and it is not free; a row that states only the local cost understates what it asked for. **And that cost cannot be bought back without buying back coverage, examined 2026-09-10.** The obvious suspicion — that six tag sets means six axe runs — is wrong: `analyze()` in `e2e/helpers.ts:378` batches all six into a single `new AxeBuilder({ page }).withTags([...AXE_TAGS]).analyze()`, so the six cost one pass. What the sweep actually spends per route is two navigations — one fresh load per theme, because the theme provider animates the chrome on a live flip — and eight axe runs: three pinned viewports plus one reduced-motion pass, per theme. `axeViolations` already resizes rather than reloads between viewports precisely because navigation dominates, and reduced motion already runs once, at desktop only, because it changes which animations run rather than how the page lays out. So every remaining reduction removes a viewport, a theme, an auth mode or the reduced-motion pass — a clause of the brief's paragraph, not an inefficiency. 53.4 minutes is the price of the matrix as specified, and anyone asked to make this job faster should be told plainly that the only lever left is coverage. **One registered page this sweep had never seen, added 2026-09-11.** `/withdraw/request` is a registered page literal of `/withdraw/[token]`, but it is built as a `route.ts` returning a hand-written HTML document with its own nonce, Content-Security-Policy and Set-Cookie, because it mints a rights-activation candidate before any markup — which a React page cannot do in the same response. `docs/route-divergence.json` records that as a deliberate `kindDivergence`. What it also records is the cost: being an endpoint put the surface outside the app layout, outside `pageAuthContract` and outside this sweep, which enumerates **pages**. So the headings, landmarks, contrast and focus order of a page that every withdrawal and invitation mail links a person straight to were checked by nothing at all. It is now swept at the same bar as every other public page, through a small `ENDPOINT_RENDERED_PAGES` map that says in the file why it cannot come from the register walk — and it **passes**: `axe: /withdraw/request (light)` and `(dark)` both clean, in `13 passed (41.2s)`. Its `default-src 'none'` policy does not obstruct the audit, because Playwright evaluates through the debugger protocol rather than injecting a script tag the policy would refuse. This does not close the divergence — that needs the mint moved into middleware or an action so the interstitial can be a page again — but it closes the hole the divergence opened, which is the part that was costing a reader something. |
| G1.13b | Reflow, target size, keyboard order, and alternatives pass. | NO | **All four measurements exist as named Playwright tests as of 2026-09-10**, sweeping 56 routes from the same register-derived lists the axe sweep uses. `4 passed, no skips, no retries`. Each is the brief's own rule, not a proxy for it. **Text alternatives passes outright**: the ancestry map's region table is on the page at zero activations and the grey state's numbers are one keyboard-operable `summary` away, and the test opens a `details` by focusing its summary and pressing Enter rather than assuming it is reachable. **Reflow found 33 routes scrolling sideways at 320 CSS px and 32 are fixed**, not recorded: 31 were the marketing header laying the wordmark, the theme toggle and two auth controls in one non-wrapping row needing 344px, and one was the `/files` action row at 370px. Both wrap now. The remainder is `/genome/[subject]/data/browser`, down from 485 to 446px after its igv container became a horizontal scroller, recorded by exact width with its cause honestly unidentified — every candidate element the sweep can name has a scrolling ancestor. **Target size is the finding that keeps this row NO and it is product-wide**: 990 undersized (page, control) pairs across 22 components, measured at 390x844 against the brief's own 44px, which line 553 pins as `--size-control` for "every button, input, switch, tap target" and line 706 enforces by deleting the 36px size outright. `src/components/ui/button.tsx` has no 44px step at all — default 36, sm 32, lg 40, icon 36, icon-xs 24 — and the theme toggle renders at 32x32 in both chromes, so no swept route is free of one. Recorded per component rather than per page: one decision produced all 990, and 990 rows would be a worse document than 22 that say so. Nothing is weakened to accommodate it — the bar is still 44, every control is still measured, and `UNDERSIZED_CONTROL_OCCURRENCES` holds the 990 as an exact-equality ratchet so the collapse cannot hide growth. **Keyboard traversal recorded two, and the first draft of this row recorded six.** The four that went were stop shortfalls — eight on the report library at both widths, one on `/copilot/[scope]` — and none of them was real: the sweep was counting controls inside a closed `details`, which Chromium still gives client rects, so it expected Tab to reach them. Tab is right not to; the summary that opens the disclosure is itself a tab stop and is counted and reached, and the report library's eight are its category jump links behind the collapsed filter strip the first-viewport interactive budget requires. Making the failure **name** the elements rather than count them is what exposed it — "eight stops missing" reads like eight unreachable controls, and "never reached: a 'Everyday traits', a 'Cancer', …" reads like a disclosure. The counter excludes closed-disclosure descendants now and no shortfall survives. What remains is real and is one surface: the genome browser breaks tab order, and **traps focus** — Tab past its last control loops back inside, a WCAG 2.1.2 Level A failure, inside igv.js 3.8.5. Its violation count is evidence rather than the compared key, because it moved 3 to 1 when a scroll container was added to that page. **Two things need an owner, not an engineer**: raising the control scale changes the rendered height of every surface, which collides with G2.7's frozen identity and the G2.5/X6.x first-viewport budgets; and brief line 2575 asks for the text alternative "reachable as an equivalent list-and-text **route**", where the shipped answer is in-page and arguably stronger — read literally the clause fails everywhere until such a route is registered. **The trap is escapable, 2026-09-10, and SC 2.1.2 is met rather than mitigated.** The criterion does not require Tab to be the key: it requires that focus can be moved away using only the keyboard, and that where the key is not an unmodified arrow or Tab, the reader is told which key it is. Both halves ship. The container binds Escape in the capture phase on our own element, so it runs before anything igv binds inside and needs no reach into igv's DOM, and moves focus to the next thing a Tab would have reached after the region so the traversal continues forwards rather than restarting; `BROWSER_KEYBOARD_ESCAPE` is rendered above the region and referenced by it through `aria-describedby`, so a reader arriving by Tab is told on entry rather than only if they looked. **The sweep enforces it rather than trusting it**: a traversal ending trapped is additionally required to escape, and that check is a plain failure rather than a ledger entry, because an unescapable trap has no honest interim. It reads the key from the component's own accessible description rather than assuming Escape — a page advertising a different key is checked against what it advertises, and a page advertising nothing fails on that alone — and requires focus to land outside the region *and* forwards of it, since moving backwards would satisfy the letter and strand the reader before the widget they just left. A floor asserts the check ran at least once, so it cannot quietly stop running and leave the ledger as the only thing saying the trap exists. Verified `1 passed (2.8m)`, and planting the defect — the page still advertising Escape with nothing answering it — fails with `div "Interactive genome browser" advertises Escape as its escape and pressing it left focus inside the trap`. The ledger entry stays, reframed: the tab-order defect is still igv's and still real, and both order violations are unchanged. **What keeps this row NO is unchanged and is the target-size decision**, which needs an owner rather than an engineer. **The target-size finding was not an owner decision, and 14 of its 22 components are closed, 2026-09-10.** This row and the ledger both recorded the 990 as blocked on an owner, because raising the control scale "collides with G2.7's frozen visual identity". Checked rather than repeated: it does not. G2.7's `src/lib/design/tokens.test.ts` pins `--card`, `--paper`, the eight subject colours and the Fraunces/Inter pairing — **no size, height or spacing token at all** — and `--size-control` did not exist in `src/app/globals.css`, so the brief's control scale had never been implemented rather than implemented and frozen. The brief had also already decided the value: line 553 pins `--size-control: 44px` for "every button, input, switch, tap target", and line 706 says of the `Action` component "One size: 44px. The 36px size is deleted rather than carved out; a second size that violates the touch minimum is not worth its complexity." The corroboration is in the codebase itself: **24 files already hand-patch `min-h-11` onto individual call sites, 50 occurrences**, and `e2e/overview.spec.ts` already asserts its primary target is at least 44px tall — the workaround the missing token forced. The scale is declared now and `button.tsx`, `input.tsx` and `select.tsx` render every size at it, so the variants differ in padding, type scale and icon size and never in whether a finger can hit them. Measured, not assumed: **990 undersized (page, control) pairs fell to 807**, and `UNDERSIZED_CONTROL_OCCURRENCES` moved with them in the same change, in both directions. Thirteen button components and the text input left the ledger. **Eight entries remain and they are one real question plus two judgement calls**: seven are text links, where the brief genuinely does not say whether a breadcrumb or a nav item is a "tap target" and the answer sets the line height of running text across the product — that one is an owner decision and stays recorded as one; the others are the checkbox glyph and the disclosure `summary`, where a literal 44px box would be poor design and the honest fix is to measure the label that actually receives the pointer event rather than the glyph. **Cost, measured rather than predicted**: the full accessibility sweep is `77 passed, 1 failed (28.5m)` after the change, and the one failure was not the control scale — it was `aria-prohibited-attr` from an `aria-label` this session put on a bare `div` while building the keyboard escape, a WCAG violation introduced while fixing one and caught by the sweep that exists for it. The first-viewport budgets are unaffected in the direction that matters: they are maxima on how many interactive elements fit above the fold, and taller controls make that number smaller. The `docs/density-baseline.json` white-space snapshot is a different matter and is now stale with respect to HEAD — it is a per-commit capture pinned to `8647369`, is not wired into CI, and its reproduce script is macOS-specific, so it was not re-captured here and this row does not claim it passes. **The CI cost is measured now, not just the local one, 2026-09-10.** Both rows quoted a local figure and left the number every future run actually pays unstated. The full browser job is **53.4 minutes** (307 passed) on CI, up from roughly 37, and `e2e/a11y.spec.ts` alone is **17.1 minutes** against about 7 before this session. That is the real price of six tag sets across three viewports in both themes and both auth modes, plus the keyboard-escape check and the origin audit riding the same navigations. It is worth it and it is not free; a row that states only the local cost understates what it asked for. |
| G1.14 | Lighthouse passes exact route and threshold contract. | YES | `scripts/lighthouse-check.ts` runs Node-native Lighthouse with real session cookies, per-category thresholds, exact request/final URLs, Document 200 and same-page authenticated content. At `60c122e`, fresh local production build `xB2bE6fs2ZV80Ri_IVt9S`: nine cold-cache audits, performance medians landing 96 / Overview 98 / exact-source caffeine report 97, accessibility 100 in every run, one actual provider upload, zero skips/retries. Independent receipt review passes; see `docs/local-upload-browser-verification.md`. This is local evidence; required integration CI belongs to G1.16. |
| G1.15 | Template integrity gate passes without baseline loss. | YES | `pnpm gate:templates`; `scripts/validate-templates.ts` validates 162 templates (151 estimates, 11 Medicines variant calls) and genotype/citation structure, the Medicines-only rows of ADR 0021 (phenotype, function, dose, drug-choice and should-take language over every prose field; a 25-word sentence cap; `source` provenance with access dates) and, as of 2026-09-03, the two prose rules in `src/lib/genome/template-prose.ts`: the first-glance title (≤12 words, no term from `data/jargon.json`, no bare figure) and the naked relative figure (§4 §2.4 adjacency plus any numeric multiplier) over every summary and interpretation; unit tests in `src/lib/genome/template-prose.test.ts`. |
| G1.16 | Pull-request CI runs every mandated gate and E2E; integration CI is green. | NO | `.github/workflows/ci.yml` runs typecheck, lint, build, unit, legal, names, templates, readability, secrets, local Supabase, pgTAP (`supabase test db`), seed and the full browser suite, and every run on pull request #41 is green; `gate:routes` (G1.12) was added on 2026-09-10 and runs in the static half after `gate:secrets`; the row stays NO because `gate:claims` (G1.11) still does not exist, and the required integration-branch Lighthouse job/green execution are not established by the local G1.14 receipt. |
| G1.17 | Repository/history secret gate passes with explicit fixture allowlist. | YES | `pnpm gate:secrets`; `scripts/secret-gate.ts`; exact local-only values and paths in `scripts/secret-allowlist.json`; accepted ADR 0006; the current tree, all authored non-merge commits after the baseline, `.env.production` paths, and thirteen tracked genome fixtures are checked. CI uses a full-history checkout. |
| G2.1 | Route register exactly represents the required product hierarchy. | NO | `docs/route-register.json` exists and `/overview`, the five-item navigation and the My Genome routes now match it (`e2e/overview.spec.ts`, `e2e/report-skeleton.spec.ts`); several Family and Embryo routes still render `src/components/capability-unavailable.tsx`. |
| G2.2 | Every route declares and tests every required state. | NO | `e2e/overview.spec.ts` covers Overview states A and C and `e2e/report-skeleton.spec.ts` the covered, not-covered and no-file report states; `docs/route-register.json` is not yet backed by one substantive E2E per required route/state pair. **Sized honestly, 2026-09-10.** This row's requirement is the same 288-pair matrix `UNPROVEN_ROUTE_STATE_PAIRS` holds at 5 proven, and the shape is now measured rather than described: 62 kept pages across 8 state profiles, with `product-result` carrying **136** of the 288 (17 routes x 8 states), `static-document` 44, `account-management` 30, `restricted-flow` 24, `versioned-document` 18, `auth-flow` 16, `public-rights-flow` 12 and `public-embryo-analysis` 8. About 12 of the unproven pairs are driven today and invisible to a static title reader (see G1.12); the rest is real work, and it is large — it means driving every one of 62 pages through up to eight declared states in a browser, which is what this row asks for and is a multi-session body of work rather than a gap someone closes in an afternoon. Stating the size is the point: the honest way to move this row is state by state with a test that drives each one, never by retitling. **Sized a second way, by state rather than by profile, because that is what says how to attack it (2026-09-10).** Of the 288: **complete 62**, **error 62**, **empty 40**, **processing 34**, **consent-required 27**, **jurisdiction-unavailable 27**, **partial-coverage 18**, **not-covered 18**. Complete and error are 62 each because every kept page declares both. Two consequences worth knowing before anyone plans this. First, **`complete` is the largest single tranche and the most meaningful** — it asks whether each page actually renders its populated state — and it is *not* already covered by the accessibility sweep despite that sweep visiting all 62 pages in both themes: the sweep asserts axe-cleanliness and route coverage, not that a page reached `complete`, and an empty account's Overview is `empty` rather than `complete`, so a visit proves a state only where the fixture actually populates it. Second, **`error` at 62 is the expensive one**: it needs a failure induced per page rather than content prepared per page, which is a different kind of test infrastructure from everything the suite does today. Neither number shrinks by retitling — see G1.12 for why that route is exhausted and why using it further would manufacture a green ratchet. **`error` is not expensive test work; it is a product gap, established 2026-09-10.** The whole of `src/` contains **zero** error boundaries: `find src -name error.tsx -o -name global-error.tsx -o -name not-found.tsx` returns nothing across 137 App Router files. So no route implements a route-level `error` state at all — an induced failure renders Next.js's own built-in page, byte-identical for every route, which is precisely why no test can distinguish 62 of them. Nor is there a fault flag to induce one with: exactly three environment flags are read at runtime (`INHERIT_TEST_JURISDICTION`, `INHERIT_CANONICAL_UPLOADS_PAUSED`, `INHERIT_PREPARED_WGS_ENABLED`) and none induces a route error. This reverses the row's own earlier framing: `error` was called "the expensive one" as though it needed a different kind of test infrastructure, when what it actually needs first is the product to have the state. Writing tests before that would be writing 62 assertions about a page nobody authored. A second thing an owner has to settle before any of it: **the register never defines `error` at route level.** The only definition anywhere is `public-embryo-analysis.stateProjection.error`, which no route implements — so whether a form's inline `role="alert"` counts as its route's `error` state is undecided, and the answer moves roughly 11 pairs between reachable and blocked. **The rest of the 274 is now classified rather than estimated**: 107 reachable with existing fixtures, 50 needing new test-side setup only, 66 blocked on unshipped capability, and 51 where the state is not meaningful for the route because the register applies a profile wholesale — that last 51 being a register-authority problem no test can fix. 157 of 274 are pure test work; the floor reachable by testing alone is 117. The earlier estimate that "about 12" unproven pairs were driven-but-invisible is superseded by that classification. |
| G2.3 | Every pre-existing route has a verified kept/redirect/gone disposition. | NO | Required `gate:routes` exists as of 2026-09-10 and is described at G1.12; the disposition half of this row is the part it does not yet decide. The path correspondence it builds on: `scripts/route-register-correspondence.test.ts` walks `src/app` for the files that create URLs and compares them with the register. **Re-measured 2026-09-10, correcting the figures this row published on 2026-09-09.** The earlier scan matched register paths literally and so missed `parameterContract`, which pins a dynamic segment to fixed literals. Three of the seven surfaces it called unregistered are in fact the registered form: `rights.withdraw` is `/withdraw/[token]` with token in {`request`, `session`}, and `api.withdraw` is `/api/withdraw/[token]` with token fixed to `session`, so `/withdraw/request`, `/withdraw/session` and `/api/withdraw/session` are exactly what the register asks for. **The conclusion drawn from that error inverts.** This row said the shipped session-based rights flow does not match its register; it does match it. The real divergence is the opposite one, and it is defect D-081: the pre-migration `/api/withdraw` and the `/withdraw/[token]` directory, which implements as an open bearer segment what the register pins to two literals and marks `raw-bearer: forbidden`. Corrected counts: **4** built surfaces have no register entry (`/api/uploads`, `/api/uploads/[id]/complete`, `/api/files/[id]`, `/api/withdraw`) and **53** registered routes have no handler (51 endpoints and the two unbuilt legacy redirects; re-counted 2026-09-10, correcting 56), of which `api.third-party-subject-export` resolves to `/api/withdraw/session/export`. All redirect entries still reconcile. The first three unregistered surfaces are Priority 1: subject-upload issue and complete, and the file DELETE verb. This row previously held that no gate could be written until each unbuilt entry was decided a commitment or a plan. That is true only of the unbuilt direction, which the gate deliberately does not fail; built-but-unregistered and pinned-segment-implemented-openly are gated today. Limits unchanged: static file existence only, no `src/middleware.ts`, and a surface served by a server action is not detected. **`pnpm gate:routes` now exists, 2026-09-10.** `scripts/route-gate.ts` runs in the static half of CI after `gate:secrets` and enforces five things the register asserted and nothing checked: declared methods against exported methods (37 endpoint pairs), registered redirect status against the status the implementation emits (10 redirects), registered kind against built kind at every concrete path the register pins (101 paths), declared storage buckets against the buckets migrations create (4 declared, 3 created), and the (route, state) matrix as a ratchet (288 required, 5 proven, 283 unproven, held by `UNPROVEN_ROUTE_STATE_PAIRS`). The first four compare against `docs/route-divergence.json` in both directions, so a fix that leaves a stale entry fails as loudly as a new divergence. `scripts/route-gate.test.ts` plants a defect for each check on a repository that is real except for the one thing under test — the app tree, migrations and browser tests are symlinked so the floor guards see their true size — and asserts the empty-scan case fails rather than reading as a clean product: 14 tests. Redirect dispositions in particular are no longer taken on trust: all ten registered redirects are now checked against the status they actually emit, and two were wrong. |
| G2.4 | Reachability and instrumented task-depth limits pass. | NO | Measured 2026-09-10. The row named only the missing suite; the reachability half is worth stating because it is half-true in a way that matters. **Structure holds and is already enforced.** `src/copy/overview.ts` defines exactly **9** entry boxes, **3** per domain across the three sections (My Genome, Family, Embryos), and `src/copy/overview.test.ts` already pins the count, the per-domain split, the exact labels and the three headings. `EntryBox` renders a single `<Link href>`, so each box is one navigation. **But four of the nine do not arrive where the box says.** `resolveBoxHref` sends `family.portrait` to the Family landing unconditionally — its own comment states "No eligible-pair resolution exists yet: the domain landing is the blocking state" — and `family.copilot` and `embryos.copilot` to their domain landings because `COPILOT_GROUP_SCOPES_AVAILABLE` is `false`, while `family.individual-risks` falls back to the Family landing for an account with no adult. That is deliberate and defensible, since a blocking state beats a dead route, but G2.4 asks for the nine boxes to be *reachable* in one navigation and for four of them the navigation currently lands on a domain index instead of the destination named on the box. **One latent hazard, unenforced.** `resolveBoxHref`'s `default` returns the Overview route itself, a self-link. It is unreachable today because every box either carries a static `href` or has a `switch` case, but nothing checks that: a tenth box added without a case would link Overview to itself and no test would notice. **Task depth is genuinely absent**, as the row said. No E2E instruments `click` and `submit` counts, nothing enumerates T1–T9 against their ceilings (≤3 for T1/T2/T3/T6/T7, ≤6 for T4/T9), and the T8 floor of 3 actions including one typed confirmation — the rule that a three-click path to irreversible destruction is itself a defect — is unverified, as is the register's enumeration of `confirmation` steps excluded from ceilings. |
| G2.5 | Numeric white-space and density budgets pass against baseline. | NO | Measured 2026-09-10, and the baseline half is in better shape than this row implied. **The baseline is complete for its own commit.** `docs/density-baseline.json` records `routeCount` 22 and `captureCount` 44 at `8647369`, and `git ls-tree` at that commit finds exactly **22** `page.tsx` files — so it covers every route that existed then, which is what X6.2 requires. It also satisfies X6.2's harder condition that the baseline be measured at the baseline commit *before any UI file is edited*: the file carries a `sourceIdentityVerification` stating every tracked file in the archived capture checkout matched its baseline Git blob. The toolchain is present too — `scripts/density-baseline/` holds `capture.mjs`, `compute.mjs`, `verify.mjs` and `reproduce.sh`. **Its thresholds are faithful to the brief, and that is worth recording so nobody 'corrects' them.** The G2.5 bullet gives flat figures (white space ≥ 0.60, ≤ 7 interactive elements); **X6.1 supersedes them explicitly** — "Every budget in this specification is restated on that basis" — because a flat cap of 7 counting navigation "makes the sketched Overview unbuildable". The file matches X6 on all seven: white space 0.62 hub/standard and 0.45 wide-data, successor ink ≤ 0.6 of predecessor, interactive 7/12/24 by surface class, visible characters 480/700, decorated elements 40/60, prose 68ch with a 45ch floor, and CIEDE2000 ΔE ≥ 8 against the light ground. Lowering 0.62 to 0.60 to match the G2.5 line would be a regression, not a fix. **What is missing is the enforcement, and the two populations are now counted exactly.** Nothing enforces this: there is no density gate in `package.json`, none in the CI workflow, and `e2e/density.spec.ts` — named by an earlier version of this row — **does not exist**. `scripts/density-baseline/verify.mjs` exists but nothing references it. **The earlier figures here were wrong in both directions and are corrected.** Counting `page.tsx` files gives 71; the register, which is the authority, carries **62 page routes** (plus 10 redirects and 88 endpoints). And the relative rule's population is not "the baseline's 22": a single baseline route names several successors — `/settings` alone names five (`settings.index`, `.consents`, `.copilot`, `.data`, `.people`) — so the 22 baseline routes name **29 distinct successor route ids, every one of which exists in the register as a page**. So the relative rule applies to **29** routes and **33** have no predecessor, not 49. **The surface classes enforcement needs already exist.** The register carries `surface` on every page route — hub 5, standard 15, reading 27, flow 12, wide-data 2, marketing 1 — which is what keys X6.1's interactive caps (7 / 12 / 24) and X6.2's white-space floors. X6.2 names only two white-space classes, so everything that is not one of the two wide-data surfaces takes 0.62; reading against the looser 0.45 would be raising a ceiling, which X6.2 forbids. **Why this is a rebuild rather than wiring.** `capture.mjs` takes its route list from `contract.routes` — the baseline document's own routes — and reaches authenticated pages by signing in against `supabase-fixture.mjs`, a stub shaped for the baseline app. Pointing it at the current build needs a different route source (the register), an auth path for the current app, fixture data for 25 authenticated routes, and real ids for dynamic segments. The E2E suite already solves auth and data with a real stack, so the enforcement belongs there, importing `compute.mjs` for the CIEDE2000 ink measurement rather than resurrecting the standalone pipeline. That harness is the remaining work. |
| G2.6 | All four upload-subject paths work end to end under TEST-LOCAL. | NO | `e2e/adult-subject-invitation.spec.ts` covers reservation/acceptance only; `src/app/api/uploads/route.ts` still accepts only the self subject. `/embryos/upload` renders the embryo paths' first two steps (the three questions, whose embryos, the two class attestations, who can sign) and then states that this deployment cannot take an embryo file yet (`e2e/embryos.spec.ts`); no embryo file path exists until the E0 routes, sanitiser and worker land (ADR 0020). The cohort runtime now exists behind the flow (`supabase/tests/embryo_cohort_runtime.sql`: draft, signatures, co-parent acceptance, finalization, cards, disposition, restriction, expiry); the file path itself waits for the ingest slice. |
| G2.7 | Frozen visual identity and all-new-route theme checks pass. | YES | `src/lib/design/tokens.test.ts` pins every baseline token value and the Fraunces/Inter pairing in `src/app/globals.css` and the 3:1 floor of the eight added `--subject-N` tokens in both themes, unchanged. **Every one of the register's 62 kept pages is now audited in both themes**, measured 2026-09-10: 29 public static pages in the derived sweep, 6 legal document pages, 21 authenticated pages visited by a signed-in account, and 6 more audited by the specs that can build their state — `/family`, `/family/[person]`, `/family/[person]/permissions` and `/withdraw/[token]` in `e2e/family.spec.ts`, `/family/portrait/[pairId]` in `e2e/portrait.spec.ts`, `/embryos/[embryoId]` in `e2e/embryos.spec.ts`. Three assertions keep the lists honest against the register: nothing registered goes unaudited, nothing is named that the register does not carry, and every authenticated page is accounted for. **The authenticated half was three URLs**, `/dashboard`, `/settings` and `/uploads`, against the register's 25 authenticated pages — and two of those three are registered *redirects* rather than pages, so the hand-kept list measured their destinations by accident. It is derived now, and it audits content rather than empty states: the account uploads `tiny-grch38.vcf` and chooses its reports first, `slug` is a report that file covers, and `/genome/me/data/browser` is visited with `?q=rs762551` because the browser answers nothing without a search. **The document pages were audited by nothing at all.** `/legal/[artifact]`, its `versions/[version]` and `diff/[from]/[to]`, and the same three under `/legal/consent/[key]` carry dynamic segments, so the public sweep skipped them and the authenticated sweep never saw them: seventeen legal documents were checked by the rendered legal gate for placeholder text and by no one for accessibility. Their parameters are not invented — `supabase/migrations/20260906135854_own_report_layer_language.sql` ships `consent.own-polygenic` at version 2 with version 1 superseded, so all six URLs resolve to a real document in any database the migrations built, the diff routes included, and the spec asserts a 200 first because an empty not-found page has no violations. **The bar itself was not the same everywhere, and nothing said so.** Five specs each carried their own copy of the audit and four filtered it to `serious` and `critical`. That is not a smaller version of the same check — a `moderate` violation is still a WCAG failure — so the Family, Portrait, Embryo, Ancestry and Copilot surfaces were held to a lower bar than every marketing page, not by decision but because the bar lived in each spec rather than in one place. `axeViolations` and `expectAxeClean` in `e2e/helpers.ts` are the one definition now, at zero WCAG 2.1 A/AA violations of any impact, and every spec calls them; no filtered assertion remains in `e2e/`. **Widening it found a real defect, in a control a person uses.** The variant browser was the one page that failed, in both themes: `Form elements must have labels`, on igv.js's zoom slider, `<input type="range" min="0" max="22">`. axe reported it at the shadow-DOM target `[".min-h-64", "input[min=\"0\"]"]`, which is the diagnosis: igv 3.8.5 renders its navbar into an open shadow root, `querySelector` does not cross that boundary, and `labelIgvControls` searched only the container it was handed — so the whole labelling pass, written for exactly this problem, had been silently matching nothing. It walks the container and every shadow root under it now, and re-running the audit on the same page returns no violations. **Evidence**: `e2e/a11y.spec.ts` 73 passed, `e2e/family.spec.ts` 6 passed, `e2e/ancestry.spec.ts` 2 passed, and `e2e/embryos.spec.ts`, `e2e/portrait.spec.ts` and `e2e/family-health-picture.spec.ts` pass at the new bar, all locally against a real build, browser and database. **CI run 334 proves the one assertion that could not run locally.** `e2e/copilot-refusal.spec.ts` also lost its filter, and it needs the isolated CI browser runtime, which asserts an unprivileged uid — so it cannot run in a root container at all. The run is green end to end: `✓ 71 [chromium] › e2e/a11y.spec.ts:188:5 › axe: every registered authenticated page, both themes (1.2m)`, `✓ 160 [chromium] › e2e/copilot-refusal.spec.ts:116:5 › each gated prompt gets its exact refusal as the whole turn and the provider receives nothing; an allowed prompt still reaches it`, inside `301 passed (16.2m)` and `E2E contract passed: 301 result(s), no skips, no retries.` **The row closes.** Every registered kept page is audited in both themes at the one bar, the frozen tokens are unchanged and still pinned, and the one defect this work exposed is fixed rather than registered. |
| G3.1 | Thirty-persona comprehension harness exists and records raw runs. | NO | Required `scripts/comprehension/` and `docs/comprehension-runs/` are absent. |
| G3.2 | Ten comprehension tasks are bound to exact fixtures/surfaces. | NO | Required `docs/comprehension-protocol.md` is absent. |
| G3.3 | Scored comprehension thresholds pass twice. | NO | No runnable comprehension command or committed run exists under `scripts/comprehension/`. |
| G3.4 | A runnable human-comprehension protocol includes recruitment, the ten bound tasks, script, shared rubric, consent and thresholds; human results are recorded where run. Where not run, the packaged protocol plus green G3.3 satisfies code completion, with the human round explicitly launch-blocking. | NO | Brief G3.4 (`docs/inherit-v2-brief.md`, line 2621) distinguishes code completion from launch. `docs/comprehension-protocol.md`, its runnable script/rubric and `docs/release-checklist.md` are absent, and G3.3 is not green. No human result may be invented. When a real round exists, its per-task results belong in `docs/comprehension-results-<date>.md`; a human task below 10/12 raises that simulated task's threshold to 29/30. The earlier row incorrectly treated the absent human round as the sole code-completion blocker. |
| G3.5 | E2E proves the first heading on every result page is a plain-language sentence of at most 12 words, contains no term in `data/jargon.json`, and includes any numeric figure's unit and comparison baseline in the same visual block. | YES | Measured 2026-09-10, then enforced. **The product already satisfied it; nothing did.** Existing E2E pinned selected heading text and heading-count budgets but never the sentence, jargon and numeric-context contract. Across **163** result headings — 162 report templates plus the ancestry surface's `H1` — **0** exceed 12 words and **0** carry a term or alias from `data/jargon.json`. **The heading is not the title, and checking the title would have checked the wrong string.** `reportNameOf` cuts the gene suffix at " · ", so `Caffeine metabolism · CYP1A2` renders as `Caffeine metabolism`; it was private to the report page, so it moved to `src/lib/genome/reports.ts` rather than being copied, because two copies drift silently. **The numeric rule is where a naive gate would have been useless.** Any-digit matching flags nineteen headings, seventeen of them distinct, — `Type 1 diabetes`, `TP53 codon 72 (Pro72Arg)`, `Omega-3/6 fatty acid conversion`, `Clopidogrel, the *2 position` — every one a name or allele designation and not one a figure. A figure is therefore a number carrying magnitude: a decimal, a percent, per-mille or multiplier sign, a natural-frequency `N in M`, or a unit. A heading carrying one fails outright rather than being waved through, because the brief wants its unit and comparison baseline in the same visual block and a string gate cannot see a block; none exists today, so a new one stops and gets decided. **Two halves, one rule.** `pnpm gate:first-glance` runs all 163 in CI; `e2e/first-glance.spec.ts` asserts the rendered `h1` is exactly the string the gate checked and then runs the same `headingFailures` against it, so a page that built its heading another way is caught rather than passing on good data. Verified by planting each rule: a 13-word heading, `polygenic` in a heading, `2.4×`, and `3 in 100` all fail with their own message; the twelve name-numbers still pass; and rendering the full title instead of the name fails the browser half on `Expected "Caffeine metabolism" / Received "Caffeine metabolism · CYP1A2"`. The jargon floor is asserted too — ADR 0012 records 203 terms and aliases against the brief's 200, and the gate refuses to run below it. **The report catalogue was not "every result page", and checking caught it before this row claimed otherwise.** An `<h1>` sweep across `src/app/(app)` found six result surfaces naming their heading in copy rather than a template — ancestry, Portrait, Family health picture, Compare embryos, Data and methods, Genome browser — so the gate now reads all six from source and covers **168** headings. They are declared rather than inferred, because Settings, My files and the domain landings carry an `h1` and are not result pages. Planting `penetrance` into `HEALTH_PICTURE_H1` fails it by name, and renaming the constant away fails with `no longer exports HEALTH_PICTURE_H1 for the first-glance gate to check`. **CI run 320 ran both halves**: step 12 logs `checked 163 result headings against 203 jargon terms and aliases` then `first-glance gate passed` (168 after the widening), and the browser half passes as `✓ 117 … first-glance.spec.ts … (5.5s)` inside `E2E contract passed: 236 result(s), no skips, no retries`. The row closes. One deliberate strictness is recorded rather than hidden: a heading carrying a figure fails outright instead of having its block inspected, which is stricter than the brief and cannot produce a false pass. |
| G4.1 | Any element with `data-figure-kind="relative"` has, inside the same `data-claim-block`, two `absolute` elements (before and after, `data-abs-before` / `data-abs-after`), one `difference-pp` and one `natural-frequency`; prominence is numeric, read via `getComputedStyle` (each absolute's font-size, font-weight and contrast ≥ the relative's); natural frequency is exactly "N in 100", or "N in 1,000" below 1 per cent; the test fails if any of the four is absent. | NO | `<RelativeFigure>` (`src/components/figures/relative-figure.tsx`) is the only way a relative reaches a page (`src/lib/genome/template-prose.ts`): it emits the absolute before, the absolute after, the difference in points and the natural frequency, and the relative text last; `src/components/figures/figures.test.ts` pins that order and `src/lib/figures/natural-frequency.ts` the ladder, under X4.1's one-denominator-per-block rule (which climbs above 100 only when values collide or round below 1, and overrides the per-figure definition by X0.1). Still NO: no seeded template carries a relative figure (0 of 151), so no surface renders one and no E2E reads the computed font-size, font-weight or contrast of an absolute against a relative; `e2e/report-skeleton.spec.ts` asserts only that no percentile renders without an absolute (X4.2). |
| G4.2 | Every risk figure carries MODELLED or OBSERVED in the data layer and renders that distinction visibly; every `data-claim-block` containing at least one modelled figure carries the exact string "This is a model, not an observed outcome." once per block, not once per figure; no surface presents a modelled figure as measured. | NO | Every figure carries `data-figure-basis` (`observed`, `modelled`, or `exact` for Mendelian arithmetic per W9 §3.1) from `src/lib/figures/contract.ts`; `claimBlock()` flags a block with any modelled figure and `<ClaimBlock>` renders `MODELLED_MARKER` exactly once per block (`src/components/figures/figures.test.ts`); `e2e/ancestry.spec.ts` asserts every ancestry share is `modelled`. **The per-block rule is now counted on a rendered surface**, measured 2026-09-10. `e2e/ancestry.spec.ts` walks every `[data-claim-block]` on `/genome/me/ancestry` and requires exactly one `[data-modelled-marker]`, with the contract's exact string, in each block holding at least one `[data-figure-basis="modelled"]` and none in a block holding none; that the marker never appears outside a block; and that every `[data-figure-kind]` on the page declares a basis from `FIGURE_BASES`. It imports the constants from `src/lib/figures/contract.ts` rather than retyping them, so the page and the assertion cannot drift. This is the clause the row previously recorded as untested: the marker was pinned only in `src/components/figures/figures.test.ts`, over HTML that test rendered itself, so a surface could drop it and stay green. **Verified by planting**: suppressing the marker's render fails with `block 0 holds 7 modelled figures / Expected: 1 / Received: 0`. **Still NO**, for two reasons that are not this one. "Carries MODELLED or OBSERVED **in the data layer**" has no persisted basis behind it: no risk figure is stored with one, and the only place a basis is persisted is the captured own-upload ancestry artifact (`src/lib/uploads/own-ancestry-content.ts`). And the other modelled surface, the embryo compare cell, is unreachable — embryo ingest is `not shipped`, so no cohort renders an `absolute_risk` cell for the assertion to read. A third thing the row should say plainly: the product ships a **third** basis, `exact`, with its own marker for Mendelian arithmetic (W9 §3.1), so a gate written to the brief's literal two values would fail every `src/components/family/**` block for a reason the brief did not intend. |
| G4.3 | Read through overriding X4/X5.1: report-layer counts are never summed across layers, never share a mixed headline or count token; every report count uses a component with one mandatory class emitting `data-figure-class` (`variant-call` or `estimate`); every count-pattern element is classified, no heading contains counts of different classes, and the exact layer definition is visible or one click away. | YES | Independently reviewed at `d1e3aafa894ca98eca0ba362de68d172dae224ca`; complete CI `33995911775` passed after main PR59 integration, including the production-browser count, Overview, search/filter/expansion and Medicines contracts. `docs/report-count-contract.md` inventories every count surface; runtime guards, homogeneous/mixed starter tests and six detected DOM mutations prove classification/definition failures are caught. Both real seeded layers are exercised. Earlier 13 targeted browser tests also passed without skips/retries. The overriding vocabulary is verified, not waived. Combined CI `33997076266` then passed all 139 browser tests at `edd37da`; PR63 merged as `34fddb4` and deployed READY on both public domains. Authenticated production filtering preserved 26 report links and classified counts; see `docs/hosted-rollout-2026-09-05.md`. |
| G4.4 | Every estimated quantity derived from a reference panel renders the panel and its version, the informative markers used of those required, an interval or an explicit statement that none is available, and the resolution limit in plain words; polygenic results add the coverage fraction and the ancestry-portability statement; an E2E fails if any ancestry percentage renders without a sibling interval or unavailability statement. | NO | Admixture proportions satisfy it: `src/copy/ancestry.ts` renders the panel line (`PANEL` id and version), the markers line with a `coverage` figure, `no range yet` as every share's unit with the once-per-block unavailability sentence, and the resolution limit; `e2e/ancestry.spec.ts` fails if any `ancestry-share` lacks a `figure-unit` matching a range or `no range yet`, and if any other text node carries a percent sign. Haplogroup calls render matched-of-tested markers but not the tree version or an unavailability statement (D-019); polygenic estimates have no result surface yet (`user_prs` is computed but rendered only as coverage on `/genome/[subject]/data`), so the polygenic half of the gate has nothing to assert against. **Two of the blockers above no longer hold, re-read 2026-09-10.** D-019 is closed: `src/components/results/ancestry/lineage-card.tsx` renders `treeLine(LINEAGE_TREES[parent])`, `LINEAGE_NO_RANGE` and `LINEAGE_RESOLUTION_LIMIT`, `src/components/results/ancestry/ancestry.test.ts` pins all three, and `docs/capability-register.md` records it closed. And the polygenic surface exists: `src/app/(app)/genome/[subject]/data/page.tsx` renders, per score, a coverage figure inside a `ClaimBlock` beside the panel name, the PGS id, the trait and the seeded ancestry-portability note — so the brief's two polygenic extras are shipped. What that surface still lacks is the base requirement: no panel **version**, no interval-or-unavailability statement and no resolution limit, where the lineage card carries all three. The other gap is proof rather than product: `e2e/ancestry.spec.ts` and `e2e/upload-vcf.spec.ts` pin only the **empty** lineage state, so the rendered tree, version, no-range and resolution-limit path is unit-tested and never seen in a browser. One clause of this gate is vacuous rather than satisfied and should not be counted either way: the brief also names archaic-ancestry fractions, and `docs/capability-register.md` carries Neanderthal ancestry as not shipped. **The polygenic surface's three missing base requirements are shipped, 2026-09-10.** It now renders, per score, the panel it was read against, an explicit statement that no interval is available, and the resolution limit in plain words — in the shape `src/copy/ancestry.ts` established for the lineage cards: `panelVersionLine` mirrors `treeLine`, `RESOLUTION_LIMIT` mirrors `LINEAGE_RESOLUTION_LIMIT`, and the absent interval re-exports the report surface's own `NO_RANGE_YET` rather than respelling it, so the two surfaces cannot drift. **The version is the part worth stating carefully, and the honest answer is that there is not one.** `public.prs_scores` has no version column — `pgs_id, name, trait, n_variants, citation, source_url, ancestry_note, percentile_ref, updated_at` — and none of the three shipped seeds in `data/prs/*.json` carries one, so the surface says so in words rather than implying a build nobody recorded. It is never filled from `updated_at`, which records when the catalogue row was written rather than which build of the score it holds, nor from the `GRCh38` in `source_url`, which is a genome build and not a panel version; `scorePanel` (`src/lib/genome/prs-panel.ts`) passes a version through if a record ever carries one, so the sentence changes by itself when the data does. Verified by planting five defects including two fabrications: inventing a version in `scorePanel` fails four assertions across two files, and spelling one into the copy is caught by a digit scan on the rendered sentence. **Still NO, and the reason is now narrower.** These three sentences have unit tests only — the rendered polygenic path has never been seen in a browser, which is the same gap this row already records for the lineage card — and the archaic-ancestry clause stays vacuous while `docs/capability-register.md` carries Neanderthal ancestry as not shipped. **The polygenic half is proven in a browser now, 2026-09-11.** `e2e/genome-data.spec.ts` reads all three sentences off `/genome/[subject]/data` for every listed score: the panel-and-version line, the explicit statement that no interval is available, and the resolution limit — asserting each is *visible*, not merely un-hidden. The strings are imported from `src/copy/genome/polygenic.ts` rather than retyped, so page and assertion cannot drift, and the panel sentence is rebuilt from the name and id the page itself displays through two `data-slot` hooks, so a version invented anywhere between the record and the screen fails the comparison. `5 passed (53.5s)`. **The lineage half turns out not to be a test gap at all, and that is the finding.** This row has twice called the populated lineage card "unit-tested and never seen in a browser", implying a missing test. A synthetic mtDNA/Y fixture was built to close it — 98 defining markers copied verbatim from `data/ref/haplogroups/*`, byte-stable, calling mtDNA K1 at 17/17 and Y I2 at 4/4 under the real `classify` (`src/lib/genome/lineage-fixture.test.ts`, 8 tests) — and building it established that **no file the product creates today can reach that card**. `classify`'s only app caller is the legacy tier-1 dispatcher in `src/app/api/files/[id]/process/route.ts:313,327`, behind an early return at line 63 for any file with `single_logical_sample_verified_at` set, which the finalization RPC writes unconditionally on every file it creates. The canonical path computes no lineage: its content schema pins `state: "unavailable"` with no field a haplogroup could occupy. So the empty-state sentence the browser tests pin is the **only** state a current upload can reach, and no test could ever have shown otherwise. Recorded against both lineage rows in `docs/capability-register.md`, which described what the card renders when a call exists without saying that current files produce none. **The row stays NO**, and the reason has moved from proof to product: closing it needs lineage computed on the canonical path, which is an owner's decision about where that work belongs, not a test someone forgot to write. |
| G4.5 | Every comparison across genomes or embryos renders the joint-selection constraint and, where more than one outcome is shown, a per-subject trade-off view; no composite ranking, no "best" subject; the exact string "Inherit does not rank embryos and does not recommend one." is present; E2E asserts the absence of any ranking control. | NO | `/family/health-picture` now renders the joint-selection sentence, the per-person trade-off panel and the exact string (`src/components/family/trade-off-panel.tsx`), and `e2e/family-health-picture.spec.ts` asserts the absence of any ranking control; that spec has not yet executed in CI, and `src/app/(app)/embryos/compare/page.tsx` is still a placeholder (Embryo part E1). Turns YES when both surfaces render and the two specs pass in a recorded run. (The former row text, "Ancestry outputs satisfy geographic and comparability bounds", was a paraphrase; the ancestry gate is G4.4.) **Both stated blockers are false, verified 2026-09-10 by reading the files.** `src/app/(app)/embryos/compare/page.tsx` is not a placeholder and contains no `CapabilityUnavailable`: it renders `StandingStatement`, `ContextStrip`, `TradeOffPanel`, one `CompareTable` per layer, `QcTable`, and a how-sure section carrying `NO_RANGE_YET` and `NOT_DIAGNOSTIC`. And the family spec does execute in CI: `playwright.config.ts` ignores only `*.nojurisdiction.spec.ts` and `.github/workflows/ci.yml` runs `pnpm e2e`, so `e2e/family-health-picture.spec.ts` — which asserts the trade-off panel, `NOTHING_PICKS`, `NO_RANKING`, `AVAILABILITY`, that the panel is never inside a `<details>`, and that nothing sorts, ranks or sums — has been running all along. **What actually keeps this NO** is one thing, and `e2e/embryos.spec.ts` already names it: no `embryo.analysis` grant can be seeded, because the grant tables demand a paired write the client cannot make, so `/embryos/compare` has no browser proof in its complete state. Closing it needs a security-definer seed path that cannot double as a production demo path, which would collide with G8.2. |
| G4.6 | Every input-quality property that materially affects a result appears in the provenance block on the result surface, in plain words: call rate, coverage fraction, liftover status, whether a variant was directly genotyped or absent, and, for any file Inherit did not generate, that Inherit did not generate it and cannot verify its provenance. | YES | PR65, reviewed head `76bb28d`, full CI `34001194730`: 1,754 units, 812 database assertions and 139 browser tests, no skips or retries. `docs/result-input-provenance.md` records all eight surfaces, exact checked/contributing files, completion-bound call/no-call rates (including unnamed VCF SNPs), conversion facts and explicit historical/upstream unknowns. Actual embryo QC detail/comparison renderer and closed DTO/loader tests prove their source-facts contract, not the unfinished embryo workflow. Merge `16c01b7` is READY on both public domains; hosted migration `20260906003053` is verified, with file/catalog fingerprints unchanged and client mutation denied. Authenticated library filtering and a covered detail retain usable results and visible source facts without invented historical rates. Complete evidence and verification boundaries: `docs/hosted-rollout-2026-09-06.md`. No real file was reprocessed or deleted. |
| G4.7 | Every scientific claim carries a citation resolvable to a PMID, DOI, statute reference, registry accession or regulator publication, with an access date, per Part C. | NO | Every seeded template carries at least one citation with a PMID or DOI and the provenance section links it. Medicines and reviewed estimate templates carry source-read dates. Released canonical registration binds eight summaries, 51 study-context paragraphs and 12 genotype interpretations with 19 dated sources after PR72; see `docs/sources/everyday/review.md` for testing and release status. **Counted 2026-09-10, and the structure is sound while the dating is not.** Across 162 templates there are **221** citations and every one is well formed: 0 malformed PMIDs, 0 malformed DOIs, 0 carrying neither, 0 missing a label, 0 with a malformed or future access date, and 0 templates without a citation. `pnpm gate:templates` already enforced all of that, plus ADR 0021's rule that every Medicines citation carries an access date. **The gap is coverage of the access date: 32 of 221 carry one, so 189 do not.** That cannot be closed from here. An access date records when a person actually read the source, and inventing 189 of them is precisely the fabrication the brief forbids — so the honest move is to stop the gap growing rather than to fill it. `UNDATED_CITATION_BACKLOG` in `scripts/validate-templates.ts` pins 189 and the gate fails in both directions: a new undated citation reports `190 citations carry no accessedOn, up from 189; a new citation must record the date its source was read`, and dating one without lowering the record reports `188 … down from 189; lower UNDATED_CITATION_BACKLOG to 188 so the ratchet holds`. Both were verified by planting. So a new citation must arrive dated, and the backlog can only shrink. **What the row still owes is unchanged and is human work, not engineering**: dated claim-level coverage for the remaining catalog, all genotype interpretations, other pages, emails and exports, and retiring the legacy citation column. |
| G4.8 | Copilot passes the adversarial output contract. | NO | PR70 repairs prohibited model assertions and exact streamed-token reconstruction. All 64 new completion cases run through the shipped self-scope chat route, preserving useful explanations and replacing prohibited responses whole. Full CI `34009298500` passed all 207 browser cases; merge `f8c43bb` is production READY. Still incomplete: A.9's exact 80-case `src/lib/copilot/evals/cases.jsonl`, family/cohort tools and inherited scope flows, and the live release evaluation. The 64 self-scope regressions are not substituted for those requirements; see `docs/copilot-output-contract.md` and the PR70 hosted receipt. |
| G5.1 | Jurisdiction is server-enforced for every restricted capability. | NO | `data/jurisdictions.json` is fail-closed, but no complete resolver/gate protects every data-returning route. Measured 2026-09-10: the fail-closed claim holds and is currently absolute rather than theoretical — with `realJurisdictions` empty and every profile's `jurisdiction_code` null, all 12 restricted capabilities resolve to `unreviewed` for every account, and call sites such as `family/page.tsx`, `embryos/access.ts` and `embryos/guards.ts` pass `null` explicitly and render the unavailable copy. `adult_self_analysis` is the sole unrestricted capability, which is why the Priority 1 journey is unaffected. **Enforcement measured, and it is real but plural.** All **7** API routes under family, embryo, cohort, portrait and carrier areas enforce a capability: four through `@/lib/embryos/guards`, one through `personCapability` in `@/lib/family/access`, and one — `embryo-cohorts/[id]/withdraw` — as a bare re-export inheriting `cohorts/[id]/restrict`'s guard. `family/acknowledge` carries none by design: it records a Tier-2 cookie and a one-time portrait acknowledgement stamp, returns no genetic result and opens no capability. That plurality is the actual problem this row owes, and it caught me first: an initial scan matched two of the three mechanisms and reported three of seven routes as unguarded — **all three false positives**, one an alias, one importing the family module, one the acknowledgement route. There is no chokepoint, so a new route using none of the three is caught by nothing. `scripts/jurisdiction-enforcement.test.ts` now closes that: every route in scope must reach a known mechanism or be listed with a reason, alias re-exports are followed to their target, and a listed exception that gains a check fails as stale. Verified by planting three — a new unguarded family route, a check added to the exception, and the guard removed from an alias's target, which correctly failed both the target and the alias. The row stays NO: this proves the routes under those areas, not "every data-returning route", and a page or server action outside `src/app/api` is outside its reach. See G5.1a for the no-inference gate and G5.5 for why no jurisdiction is reviewed. |
| G5.1a | User-declared jurisdiction cannot be inferred or race-changed. | NO | Measured 2026-09-10, and the gap is larger than this row's earlier line about missing E2E. **The no-inference half now has the gate the brief asks for**: `scripts/jurisdiction-inference.test.ts` scans all production source — not route handlers alone, since a helper in `src/lib` reading `x-forwarded-for` infers just as well as a route doing it — and finds **no** IP, edge-geo, `Accept-Language`, `navigator.language`, `resolvedOptions()` or `request.geo` read anywhere. Verified by planting all four classes. A fixed `timeZone: "UTC"` is deliberately not matched: pinning a formatting zone is the opposite of inferring a person's, and the two files that do it are the only prior hits. **The declaration half is not merely untested — it is unbuilt.** `profiles.jurisdiction_code` exists, is nullable with no default, and is read by `src/lib/legal/jurisdictions.ts`. Nothing writes it: no database function updates it, no TypeScript sets it, no settings page collects it and no sign-in step collects it. All **62** profiles in the local database read `<null>`. So the brief's collection contract — a required selection at first sign-in with no pre-selected default, an attestation string, changeable in settings, an append-only audit row per change and re-evaluation of every active grant — has no implementation to test. **The consequence is fail-safe, and worth stating for Priorities 4 and 5.** `data/jurisdictions.json` lists `adult_self_analysis` as the sole unrestricted capability, which is why the Priority 1 own-genome journey works with every profile unset. The other **12** — third-party adult analysis, family heritability, all six portrait capabilities, embryo analysis, embryo single-locus, embryo statistical estimate and carrier match — are jurisdiction-restricted, so with no declaration possible they resolve to `unreviewed` and are closed to every account. Several call sites pass `null` explicitly and render the unavailable copy. Family and embryo flows are therefore exercisable only through the separate test-jurisdiction path, which is what G5.4 means by real-jurisdiction acceptance remaining disabled. Nothing here is a security hole — the failure closes rather than opens — but no real user can reach a jurisdiction-restricted capability at all, and the row should not read as though only E2E were missing. The three E2E cases the brief names (declared-prohibited, declared-unreviewed, and a declaration changed between page load and submit) cannot be written until a declaration can be made. **Build order matters here, and the declaration is second, not first.** G5.5 records that `realJurisdictions` holds zero entries against a catalog of 249 selectable codes, so every declaration would resolve to `unreviewed` anyway. Shipping the collection path on its own would add a control that changes no outcome for any country; the signed jurisdiction review has to come first. |
| G5.1b | Actor and every subject jurisdiction must all permit access. | NO | Family surfaces enforce it: `familyCapability` (`src/lib/family/access.ts` over `src/lib/legal/jurisdictions.ts`) resolves the acting account and every contributor against `data/jurisdictions.json` and returns the strictest decision, unset codes read as `unreviewed`; `src/lib/legal/jurisdictions.test.ts` and `src/lib/family/access.test.ts` prove a permitted actor is blocked by an unreviewed or prohibited contributor. Still NO: the browser suite runs under `INHERIT_TEST_JURISDICTION=1`, which resolves every account to the `TEST-LOCAL` row, so no E2E exercises a refused contributor, and the My Genome and Embryo routes do not yet apply the same resolver. ADR 0014 §Decision 4. Embryo routes enforce it the same way (`embryoCapability`, `cohortCapability` over every required upload principal, `src/lib/embryos/access.ts`), and the refused branch of all four Embryo routes is proven with the TEST-LOCAL flag unset in `e2e/embryos.nojurisdiction.spec.ts` (Playwright project `jurisdiction-off`). |
| G5.2 | Versioned consent documents and re-consent contract pass. | YES | Measured 2026-09-10 against the live database and the renderer. **This row was wrong about three of the four things it named.** Routes, summaries and re-consent are implemented; only E2E is genuinely absent. **Data**: `consent_artifacts` carries `version`, `effective_on` (NOT NULL), `summary_markdown` and `summary_of_changes`; `consent_artifacts_current_idx` is a partial unique index keeping exactly one live version per key; and a check constraint already requires a change summary on every version after the first. Across **19** artifacts every summary is inside the brief's 150-word budget (largest 88, `consent.upload-embryo`), and the two superseding versions — `consent.own-monogenic` v2 and `consent.own-polygenic` v2 — carry change summaries of 14 and 15 words against a 60-word budget. Two keys have therefore exercised a real supersede, so this is not a hypothetical contract. **Rendering**: `src/components/legal/artifact-document.tsx` renders "Version N · effective &lt;date&gt;" in a `<time>` element, a `data-legal-summary` block, a "Changes:" line when one exists, the body, the body sha256, and a "Permanent link to this version" pointing at `/legal/consent/[key]/v/[version]` — the stable permalink the brief asks for. **Forced re-consent is enforced, and more strictly than the brief requires.** `private.current_own_report_grant_v1` joins `consent_artifacts` on both `ca.version = pg.artifact_version` and `ca.body_sha256 = pg.artifact_body_sha256`, then requires that same row to have `superseded_at is null`, and independently re-hashes `body_markdown` and compares it. A grant signed under v1 therefore stops resolving the moment v2 supersedes it, and editing a published document invalidates every grant under it. **New enforcement**: the two word budgets were the only part of the data contract the schema did not hold. `supabase/tests/consent_artifact_word_budgets.sql` pins both, plus that the set is populated and that at least one superseding version exists so the change budget is not vacuous. Verified by planting a 151-word summary and a 61-word change summary; both assertions fired. **The placement question is answered, and the answer was no.** The brief places the change summary "in the same block as the accept control". The accept control is `own-report-choices.tsx`, and it showed the label, the description, a collapsed body and the checkbox — never the change summary. The data layer did not even read the column: `prepare-own-report-choices.ts` selected `body_markdown` and not `summary_of_changes`. So in the one case the clause exists for — a grant signed under v1 while v2 is live, which is the state of both `consent.own-monogenic` and `consent.own-polygenic` — someone who had already agreed met a plain "Enable", with no sign they had agreed before and no way to see what had changed. The block above the checkbox now names the version signed, carries a change summary per version since, and links that version in full; the permalink resolves because `getArtifactVersion` does not filter on `superseded_at`. **E2E now drives a real supersede.** `e2e/consent-versions.spec.ts` uploads, grants, publishes a superseding version, and proves all six things the brief lists: the grant stores the signed version; the capability goes off while the grant stays unrevoked, which is forced re-consent rather than revocation; the change summary and the permalink render in the block holding the checkbox, asserted positionally with `compareDocumentPosition` because a summary elsewhere on the page satisfies the words and not the requirement; the permalinked version renders its version, a `<time>` element and a non-empty summary; and re-consenting restores the capability and records the new version. Two things had to be learned by hitting them. A published document is immutable — `consent_artifacts_immutable` rejects every update with SQLSTATE 55000, service role included — so the spec supersedes the way the shipped migration does, under the table lock with the guard dropped and restored. And the restore order is load-bearing: `consent_signatures` and `purpose_grants` both reference `(artifact_key, version)`, while `purpose_grants.signature_id` references `consent_signatures`, so the grant must go before the signature. Cleanup runs in one transaction and is verified, not assumed; it ran correctly even on a deliberately failed run. Verified locally against a real build, browser and database, with a planted regression in the prior-signature lookup failing it on the missing notice. **CI run 317 then ran it**: the job log records `✓ 32 [chromium] › e2e/consent-versions.spec.ts:100:5 › a superseded document forces re-consent and says what changed beside the accept control (9.1s)`, and the suite closes with `E2E contract passed: 233 result(s), no skips, no retries` — so it executed rather than being skipped. All 233 passed with this spec superseding a shipped consent document mid-suite and restoring it, which is the evidence that its cleanup holds in CI and not only locally. The row closes. |
| G5.3 | Third-party upload stays quarantined until subject confirmation. | NO | `e2e/adult-subject-invitation.spec.ts` proves no file exists at invitation acceptance; it does not implement or prove quarantined upload/purge. Embryo subjects are created `quarantined` at finalization and nothing reads them until the ingest worker publishes a source set (pgTAP asserts the lifecycle); restriction deletes derived rows inline and revokes every key and grant, while source-object purge belongs to the withdrawal slice. |
| G5.3a | Revocation is immediately inaccessible and fully purged in seven days. | NO | Measured 2026-09-10 against the live local database, replacing this row's earlier one-line assertion. **The inaccessible half holds; the delete half does not, for one purpose.** `docs/retention.md` registers `purpose.derived-60s` as "Delete within 60 seconds … derived rows, cached artifacts and messages attributable to the exact revoked subject-and-purpose tuple", while explicitly sparing source bytes and raw variants shared with other live purposes — which is why `public.user_variants` is correctly untouched. The live revocation trigger `private.clear_revoked_own_report_outputs_v1` deletes exactly two things: `public.user_prs` for that grant's `reports.polygenic` runs, and every `private.own_analysis_runs` row for the grant. `ancestry` is a grantable and revocable purpose (the `purpose_grants_purpose_check` constraint lists all ten), and legacy ancestry rows live in `public.ancestry_results`, written by `POST /api/files/[id]/process` rather than by the purpose-gated generator. Reads are correctly gated: `loadAncestryResultSnapshot` filters canonical and legacy rows alike through `filterOwnAnalysisFiles(subject, "ancestry", …)` and re-confirms after every result and source read, so revoking `ancestry` makes those rows immediately unreadable. **They are never deleted.** Across every function in the live database only two mention `ancestry_results` — a schema-shape assertion and `purge_account_deletion_database_v1` — and the only other removal is the reprocess path in the same route. So a revoked ancestry grant leaves derived ancestry rows in place indefinitely, against a registered 60-second deadline; account deletion or re-uploading the file is currently the only thing that clears them. `public.report_observed_calls` was checked and is **not** a defect: `own_copilot_chat_v1`, `own_report_generation_v1` and `own_subject_export_content_v1` all read it, so it is shared across live purposes and the registered rule forbids deleting it on one revocation. The fix for ancestry must delete only rows for the revoked subject, and only when no other live unexpired `ancestry` grant still covers them, so it cannot be the existing polygenic one-liner: legacy files have `single_logical_sample_verified_at` null and need not have an analysis run to scope by. **Decided 2026-09-10, and the registered rule was the thing that was wrong.** `POST /api/export` reads `ancestry_results` with no ancestry-purpose check, because export runs on `raw.export`/`export.share-link` rather than on the analysis grant. Subject scoping there is correct and was checked separately: the query selects by `user_id` and the result is narrowed to the account's own legacy files, so only the purpose check is absent. Since storage, analysis, sharing and AI permissions are separate, revoking an analysis purpose does not withdraw already-derived data from the subject's own export. So the behaviour is correct and `docs/retention.md` now records the exception under `purpose.derived-60s`; D-096 is closed by decision rather than by deleting genetic data. **The access half now has its browser proof, and building it corrected this row's model of how revocation works.** `e2e/ancestry-revocation.spec.ts` uploads the synthetic 168-marker fixture, makes the explicit ancestry choice, and asserts the result genuinely readable — map `data-mode="shown"`, region shares printed, and one `private.own_analysis_runs` row — before revoking through the same control a person uses. On the very next load, with no retry and no polling, the map is not shown, `data-slot="nothing-read"` renders, no share survives, and the journal row is gone. The presence assertions are the point: the earlier attempt was abandoned because an absence assertion passes on a fixture that never produced a result. **Three plants passed before one bit, and that is the finding.** Removing `filterOwnAnalysisFiles` entirely — both call sites — changed nothing, and neither did disabling the `clear_revoked_own_report_outputs` trigger. The two mechanisms that actually do the work are `revoke_directional_purpose_v1` → `execute_own_report_purge_v1`, which deletes the journal rows, and `own_ancestry_content_v1`, which re-checks the grant inside the database; the TypeScript filter is a third layer over both. Suppressing only the purge separates them cleanly: the journal assertion fails because the row survives, while every surface assertion still passes because the database read check hides a row that is still there. So the access half and the delete half are independently enforced, and this spec asserts each with its own observable rather than letting one stand for the other. **CI run 320 ran it**: `✓ 14 [chromium] › e2e/ancestry-revocation.spec.ts … revoking ancestry makes the derived result unreadable on the very next load (5.8s)`, inside `E2E contract passed: 236 result(s), no skips, no retries`. **The row still does not close**, and the reason is unchanged: this covers the canonical path, while legacy `public.ancestry_results` rows are gated on read and never deleted against a registered 60-second deadline, and D-097 — which half of the export is right — is an owner decision. **The decision is not uniformly implemented, found after it was taken and recorded as D-097.** The canonical half of the same `ancestry.json` is grant-gated — `own_subject_export_content_v1` filters `purpose='ancestry'` against `purpose_grants`, and `own-subject-content.ts` reads twice around its authority check and refuses if the reads differ, to avoid releasing a buffered result after withdrawal. So after revocation an export holds the person's legacy ancestry and not their canonical ancestry. Both halves cannot be right; reconciling them is an owner call and the two directions are not symmetric. **One positive result this row should credit**: isolated deletion is proven at the database level. `supabase/tests/canonical_own_ancestry_generation.sql` drives two files in one account and, on deleting the first, asserts that only its own report and ancestry journals cascade, that the second file's metadata and exact sha256 survive, that its independent completed ancestry content is unchanged, and that its Storage object remains. That is the goal's "prove isolated deletion" commitment, which has no dedicated gate of its own. Storage-side *revocation* residue is a different question and is still unmeasured, and the seven-day source clause is untested separately. **A reader's warning about this row's own shape, added 2026-09-10 after it misled one.** The row opens with "the delete half does not [hold], for one purpose" and only much later records that the owner settled it the other way. Both statements are in here, the second supersedes the first, and reading the first as open implementation work leads straight to writing a migration that deletes a person's derived ancestry against an explicit decision to keep it. **That is not open work.** `docs/retention.md` — the sole authority for purge targets — now carries the exception inside `purpose.derived-60s` itself: "Nor derived rows that a separate live permission independently consumes — decided 2026-09-10", with legacy `public.ancestry_results` as its worked case, because `POST /api/export` reads them under `raw.export`/`export.share-link` rather than under the analysis grant. So no correctly-scoped deletion exists: the guard "only when no other live permission still covers these rows" can never be satisfied for a live account, which makes such a migration either a no-op or a violation of the register. What is genuinely open here is **D-097 and nothing else** — whether the legacy half or the canonical half of the same `ancestry.json` is right — and it is an owner decision whose two directions are not symmetric: matching legacy to canonical withholds data a person can retrieve today, matching canonical to legacy relaxes a withdrawal protection someone wrote on purpose. The sentence above about a 60-second deadline applying to these rows is stale on its own terms: since that decision the register does not impose one on them. |
| G5.4 | Accountless subject/future-person rights routes and retention work. | NO | PR71 connects the fragment entry, browser-bound activation, correct-account review/acceptance and accountless refusal to canonical notices and physical draft cleanup, including shared-evidence protection. Exact-head full CI `34018132773` passed all 213 browser cases and 30 independent-session lock checks; the six focused invitation browser cases include an unrelated deleting account and retries after cleanup. Six hosted migrations and all 45 changed function definitions/privileges are verified; merge `e9778f8` is production READY on both domains. Main integration run `34019023140` completed successfully. Real-jurisdiction acceptance remains disabled. Active-key rotation, quotas, the adult URL-token migration (D-081), future-person routes and the broader rights-purpose matrix are unfinished. See `docs/rights-invitation-flow.md` and the hosted release receipt; no full-gate acceptance credit is added. |
| G5.5 | Structural human-review gate defaults all unreviewed capabilities off. | NO | `data/jurisdictions.json` defaults real jurisdictions to unreviewed and `next.config.ts` rejects TEST-LOCAL on Vercel production, but the required structural review gate is absent. **Measured 2026-09-10, and the default is not a default — it is the whole population.** The catalog offers **249** selectable country codes and `realJurisdictions` holds **0** entries, so no country has a reviewed capability record. Every declared code falls through `resolveCapability` to `defaultRealJurisdiction`, where all **12** restricted capabilities read `unreviewed`. The only jurisdiction that permits anything is `TEST-LOCAL`, which grants all 12 and is flag-gated away from production. **This, not the missing declaration UI, is what closes Priorities 4 and 5.** G5.1a records that nothing writes `profiles.jurisdiction_code`; this row records that building that path would unlock nothing, because all 249 selectable codes resolve to `unreviewed` regardless. Someone could ship the first-sign-in selection in full and no family, portrait, embryo or carrier-match capability would open for any account. The unblocking step is a signed review under `signedReviewContract` for at least one real jurisdiction — a human, legal act this repository cannot perform or simulate, and the correct order is review first, declaration second. |
| G5.6 | Free exports are complete and correctly subject-scoped. | NO | Measured 2026-09-10 by reading `src/app/api/export/route.ts` against the live schema, replacing this row's earlier one-line assertion. **Complete is close; subject-scoped is absent.** The archive writes `manifest.json`, `consents.json`, `ancestry.json`, `reports.json`, `reports.txt`, `prs.json`, `chats.json`, and per file `originals/<id>`, `canonical/<id>.jsonl`, `variants/<id>.csv` and `observed/<id>.json`. Retired originals are handled honestly rather than silently: each omitted one gets its own warning saying the retention period ended and that prepared records and saved reports remain, which is the visible half of the original-retirement commitment. Of **34** account-scoped tables (those carrying `user_id`, `account_id`, `owner_user_id` or `subject_account_id`), the two export paths read **9** — `ancestry_results`, `chat_messages`, `chats`, `consent_grants`, `genome_files`, `user_variants` on the legacy side, and `own_analysis_runs`, `purpose_grants`, `report_observed_calls`, `user_prs` through `own_subject_export_content_v1`. Most of the other 25 are **correctly** excluded and their absence is not a gap: every nonce and session table, the worker and preparation internals, and above all `llm_keys` and `copilot_context_tokens`, which are credentials — exporting those would be a security defect, not completeness. **The finding was attribution, and it is now fixed.** The route surfaced no subject anywhere: `manifest.json`'s `files[]` carried id, name, type, tier, size, sha256, status, build, created_at, variant_count and row_count but no `subject_id`, there was no subjects member, and the string "subject" occurred in the route only in an import — while `own_subject_export_content_v1` resolved `subject_id` internally all along, so the data was present and simply never written out. For an account holding only its own subject that cost nothing; for one holding other-adult or family subjects the archive could not say whose data each file was. `files[]` now carries `subject_id`, and `route.test.ts` asserts it for both files, verified by removing the field and watching the assertion fail. **What remains open is a rights decision, not a bug**: whether `public.subjects`, `subject_consents`, `subject_account_bindings`, `subject_principals` and `provider_recipient_grants` belong in the archive at all. **The attribution is now proven by an executed export.** `e2e/export-subject-scope.spec.ts` uploads through the real journey, calls `/api/export`, opens the archive and checks `manifest.files[]` against the `subject_id` the database resolved for that file — then checks every file in the archive is attributed, and to a subject the account actually holds, since an archive naming someone else's subject would be a disclosure rather than a missing field. This is the part `route.test.ts` cannot reach: a unit assertion over a mocked query proves the mock was read, not that the value is the one the database resolved. Verified by removing `subject_id` from the manifest, which fails it against a real subject uuid rather than a constant, and **CI run 320 ran it**: `✓ 114 [chromium] › e2e/export-subject-scope.spec.ts … every exported file names the subject the database resolved for it (2.8s)`. **Still open**: whether `public.subjects`, `subject_consents`, `subject_account_bindings`, `subject_principals` and `provider_recipient_grants` belong in the archive is a rights decision, not a bug; archive completeness stays with `e2e/deletion-export.spec.ts`; and no per-subject export route is built — the register's `/api/subjects/[id]/export` is among the 53 unbuilt entries. **Correspondence was measured beyond paths for the first time on 2026-09-10, and it found a live privacy defect.** Nothing in the repository checks HTTP methods, route kinds or storage prefixes against the register — `scripts/route-register-correspondence.test.ts` covers paths only, and `grep -rn storagePrefixes scripts/ src/ e2e/ supabase/` returns nothing. Comparing them by hand found four things. **`/api/browse/region` was a GET, and is registered as a POST for a stated reason.** Its `policy.transport` reads "subject-region-cursor-and-nonce-are-body-only-and-forbidden-in-the-URL-query-path-logs-analytics-traces-errors-referrers-and-audit-detail", and a GET cannot carry a body — so a person's file identifier and the exact stretch of their genome they were reading travelled in a query string, which reaches server logs, referrers and traces by default. Two registered bounds were also ten times wider in code than on paper: the span cap was 10 Mb against a registered 1 Mb, and the page 5,000 rows against a registered 500. **All three are fixed**: the route is a POST with a closed body, enforces the registered `exact-same-origin` requirement, reads the registered bounds, and carries the registered no-store, same-origin-referrer headers. `src/app/api/browse/region/route.test.ts` pins each — including that the module exports no GET, so no parameter can travel in a URL again — and `e2e/upload-vcf.spec.ts` now asserts in a browser that the request is a POST whose URL carries **no** query string and whose body holds the four values. Verified green: `9 passed`, `E2E contract passed: 9 result(s), no skips, no retries.` The two registered requirements this does **not** build are named in the route's own comment: the `X-Inherit-CSRF` token bound to the session and the one-time browse nonce, both of which need a minting presentation and a signing key. **Those hand-read findings are now enforced, 2026-09-10.** **`pnpm gate:routes` now exists, 2026-09-10.** `scripts/route-gate.ts` runs in the static half of CI after `gate:secrets` and enforces five things the register asserted and nothing checked: declared methods against exported methods (37 endpoint pairs), registered redirect status against the status the implementation emits (10 redirects), registered kind against built kind at every concrete path the register pins (101 paths), declared storage buckets against the buckets migrations create (4 declared, 3 created), and the (route, state) matrix as a ratchet (288 required, 5 proven, 283 unproven, held by `UNPROVEN_ROUTE_STATE_PAIRS`). The first four compare against `docs/route-divergence.json` in both directions, so a fix that leaves a stale entry fails as loudly as a new divergence. `scripts/route-gate.test.ts` plants a defect for each check on a repository that is real except for the one thing under test — the app tree, migrations and browser tests are symlinked so the floor guards see their true size — and asserts the empty-scan case fails rather than reading as a clean product: 14 tests. **It found one live defect and fixed it.** `/signup` and `/login` were `next.config.ts` redirects with `permanent: false`, which emits 307 where the register records `expectedStatus` 308 for all ten legacy aliases; a 307 asks the browser back to the dead path every time, so the alias never retires and the old URL stays indexed. Both now read `permanent: true`, matching the eight aliases already built as pages calling `permanentRedirect`. The other eight divergences are recorded, not fixed, each with why it exists and what closing it needs: two method divergences (`api.account-delete` exports an undeclared `GET` that mints the operation nonce its own registered `POST` body requires and the brief never describes issuing — an authority blocker, not an oversight; `api.export` declares the asynchronous large-export contract's `POST` and polling `GET` while the code ships only the older synchronous streaming `GET`, so the verb count understates the gap), one kind divergence (`/withdraw/request` is a registered page literal served by a hand-written `route.ts`, which puts it outside the app layout, `pageAuthContract` and the accessibility sweep), and five storage divergences (`genomes-staging` holds real uploaded bytes under no declared prefix, filename contract, retention id or disposition, which is the sharpest of them; `generated-artifacts` is created but written by nothing, and the same literal doubles as a retention target class over database rows; `exports`, `legal-evidence` and `future-person-identity` are declared for surfaces that are unbuilt). So the three method and kind divergences this row recorded by hand, and the storage gaps it called unchecked by anything, are each held by an assertion that fails in both directions; `legal-evidence` was a fourth storage divergence the hand pass missed. **The pin this row leans on does not verify, found 2026-09-10.** `docs/route-register.json` records `briefSha256` `2914f42bba3ccdb34816f07c23b4cffdee14f3328b4fa5f2a0f231133be9abbe`; the actual SHA-256 of `docs/inherit-v2-brief.md` is `2d6bc5194bc5dc936930d4439c34dae34c0409ee478c339b73d46b262bc72162`, and **nothing in code reads `briefSha256` at all** — it appears only in the register and in prose, and the `gate:canon` its own `canonicalValidation` block names does not exist as a script. So "derived from and pinned to the brief" is an assertion this repository does not check. The pin is deliberately **not** updated here: rewriting it would assert a derivation nobody has performed, and the register's authority over the divergence ledger rests on that derivation being real. Establishing which of the two documents moved, and writing the gate that keeps them together, is the work. |
| G5.7 | No fee path exists and the legal gate proves it. | NO | `pnpm gate:legal` now carries the G5.7 checks that G1.8 requires it to run, so the row's earlier claim that no payment code is present is enforced rather than asserted. **Fee-path half**: no payment-processor package appears in `package.json` (0 of 54 dependencies) and no payment-processor origin appears in any file under `src/`. Measured 2026-09-10; the only prior matches were false positives worth naming, since they are why the patterns are shaped as they are — an unbounded `adyen` matches "Re**adyEn**velope", and `checkout`/`billing` match a git checkout in `capture-emails.ts` and a comment in the export route saying there is deliberately no billing. The gate therefore matches payment-processor **origins and package names**, not those words. **Copy half**: legal and disclosure surfaces are checked for a price, a recurring charge, a call to action and a superlative. `data/providers/providers.json` is never scanned — those are other companies' quoted prices and explicitly not a fee path. Verified by planting all six: a `js.stripe.com` origin, an `@stripe/stripe-js` dependency, a price, a call to action, a superlative, and a reworded exception. **The price plant initially did not fire**, because `exec` returns only the first match and the recorded exception masked every later match of that pattern in the same file; the gate now checks every match, and the plant fails as it should. One recorded exception, in `scripts/legal-fee-exceptions.json` and checked in both directions: the terms page caps damages at "the greater of one hundred US dollars (US$100) or the amount you paid us for the service". That charges nobody and gates nothing, so the fee-path half is unaffected, but it is a price on a legal page. Its second branch also presupposes a payment relationship the product promises will never exist, which reads as boilerplate from a paid-service template. **Rewording a limitation-of-liability clause is a legal decision and is not done here.** The row stays NO on two counts the gate cannot reach. The brief requires that no payment-processor origin appear in **any response**, and rendered mode fetches 17 legal, consent and disclosure routes — since 2026-09-10 on every CI run, not merely on request — which is every such route but not every route the application serves, so the wider claim still rests on static source. And "a marketing claim" is not detected in general — only prices, recurring charges, calls to action and superlatives are, and the deliberately narrow superlative list omits "best" and "leading" because legal prose uses both innocently. |
| G5.8 | Protective legal anchors exist and are tested by id. | NO | Measured 2026-09-10 against the terms page, replacing this row's earlier one-line assertion, and now checked by `pnpm gate:legal` — by anchor id rather than by prose, as the brief specifies. **Three of the eight required statements are wholly absent, and the brief names this exact failure**: "a beautifully versioned, marketing-free terms page that omits the uploader's warranty passes every other gate". Present and anchored: the uploader warranty of being 18 and holding documented permission (`eligibility`), no medical advice and no diagnosis (`not-medical`), and the limitation of liability (`liability`). Present but incomplete: `no-warranty` disclaims accuracy and completeness but not the brief's qualifier that it covers **data Inherit did not generate**, which is every uploaded file; and `governing-law` states the law with no forum, venue or exclusive-jurisdiction wording. **Absent**: an uploader indemnity for claims arising from third-party and embryo uploads — no indemnity wording anywhere on the page; a no-reliance statement for reproductive decisions — no reproductive, IVF, family-planning or conception wording, though Embryo Analysis ships; and a statement that Inherit sells nothing and takes no payment for sequencing. On that last one the page promises not to sell **the user's data**, which is a different promise; G5.7 now proves no fee path exists in code, but the terms never say so to the reader. `scripts/legal-anchor-requirements.json` records all eight with the anchor id each should carry, and the gate checks both directions: a statement recorded as present whose anchor disappears fails, and one recorded as absent whose anchor appears fails so the record cannot go stale. Verified by planting both — renaming `liability` and adding `indemnity`. **The missing clauses are not drafted here.** Writing an indemnity, a reproductive no-reliance statement or a forum selection is legal work. Two limits keep this NO beyond the three absences: the brief requires these statements on **each consent document shown at the activation point** as well as the terms, and that surface is unmeasured; and the brief requires each to be asserted by a **named E2E assertion**, which `e2e/legal.spec.ts` does not yet do by id. |
| G5.9 | Future-child preview satisfies all bounded-harm assertions. | NO | PR 57 CI `33993182906` passed `e2e/portrait.spec.ts` within its 131-test browser run, including unavailable/refusal states, no image, canvas or `svg[role=img]`, the closed trait allow-list and ≥ 8 refusal cards. The page-proper tests assert zero output figures and claim blocks for legacy-label fixtures. They do not yet demonstrate the positive-result figure/claim-block contract or the complete bounded-harm proof. A green refusal-state run alone does not satisfy this gate and adds no acceptance credit. |
| G6.1 | Whole-tree and new-commit name scan passes. | YES | `pnpm gate:names` scans tracked and untracked non-ignored files plus every reachable commit message with a committer timestamp after the baseline. `scripts/name-gate-fixtures.json` proves lowercase-domain and camelCase detection. |
| G6.2 | External-name allowlist has only the permitted categories. | YES | `data/allowed-external-names.json`; the gate rejects unknown categories, orphaned provider entries, non-exact provider reasons, missing evidence paths, and incomplete provider source/date fields. |
| G6.3 | Out-of-tree denylist and provider carve-out are enforced in CI. | YES | Accepted ADR 0007; `.github/workflows/ci.yml` fails when the encrypted `NAME_DENYLIST` secret is absent, writes it only under `RUNNER_TEMP`, and exports `NAME_DENYLIST_FILE`; unit tests prove carve-out and override behavior. |
| G6.4 | End-state scan is clean and all 16 providers remain sourced. | YES | `pnpm gate:names` returns zero findings; all 16 rows retain their source arrays and expose matching canonical `source_url` and `last_verified` fields refreshed on 2026-09-01. Fourteen live link pairs resolved directly and two automation-blocked pages were confirmed from current official indexed pages. |
| G6.5 | Provider names have no evaluative proximity. | YES | `scripts/evaluative-tokens.json`; the gate scans all `docs/`, `src/`, and comments for a provider name within 200 characters of a registered evaluative token; boundary behavior is unit-tested. |
| G7.1 | Required gating ADRs exist from 0006 onward. | NO | `docs/adr/` holds 0006–0021 (0015 future-child preview scope, 0017 the health picture, 0008 readability contract, 0009 statistical presentation contract, 0010 Overview information architecture, 0011 report taxonomy, 0013 offline map rendering, 0014 third-party adult consent and revocation model, 0018 pharmacogenomics withheld, superseded by 0021; 0019 the embryo-comparison presentation model, accepted over the E1 surfaces; 0020 embryo ingest and cohort lifecycle, proposed until the E0 routes and worker exist); the minimum set in the brief still lacks the jurisdiction-gating mechanism and the density contract, each waiting on its workstream. |
| G7.2 | Core docs/env are current and a clean-clone run is recorded. | NO | Measured 2026-09-10 by comparing every `process.env.X` read under `src/` against `.env.example`: **17** template keys against **20** variables read. Four of the five undeclared ones are correctly absent — `NODE_ENV` and `VERCEL` are set by the runtime, `INHERIT_TEST_JURISDICTION` is a test flag `next.config.ts` rejects on production, and `ALLOW_PRIVATE_LLM_ENDPOINTS` is in fact documented as a commented-out opt-in (my first scan missed it by skipping comment lines). The two template keys not read under `src/` are not stale either: `scripts/secret-gate.ts` reads both. **The fifth is a real defect, now D-098.** `NEXT_PUBLIC_APP_URL` appeared in neither the template nor the self-hosting guide, and falls back to `https://www.inherit.bio`. It builds every link inside outbound mail — invitations, rights and withdrawal links, and the cancel/export links in an account-deletion notice — so a self-hoster who set the documented `NEXT_PUBLIC_SITE_URL` and missed this one would send their users' mail links, and the rights tokens those links carry, to a site they do not run. The two variables sit beside each other and fall back in opposite directions: `NEXT_PUBLIC_SITE_URL` falls back to localhost, which fails visibly. Both files now document it; the fallback is deliberately unchanged, since the hosted deployment may depend on it and that is not readable from here. **The row stays NO on the half I did not attempt**: the brief requires a clean clone followed only by `docs/self-hosting.md` to reach a running app with the new surfaces, and that run to be recorded. No such run is recorded, and this measurement is a document comparison, not a clone. **Both numbers above are wrong, re-measured 2026-09-10, and the method is why.** Grepping `process.env.X` misses 14 of the 34 real reads: five modules take `env: Environment = process.env` as a defaulted parameter and read `env.KEY` inside, and `src/lib/limits.ts` reads `process.env[name]` with the name passed in. The template declared **19**, not 17 — this row's own correction about the commented-out `ALLOW_PRIVATE_LLM_ENDPOINTS` was never folded back into its count. Measured properly: **34** variables read under `src/`, **19** declared, **15** read and undocumented, and **none** documented and unread. That last figure contradicts this row a second time: it says `DATABASE_URL` and `INHERIT_NORMALIZATION_DIRECT_DATABASE` are not read under `src/` and are kept for `scripts/secret-gate.ts`. Both are read, in `src/lib/uploads/normalization-database.ts`, through the `env` parameter the grep could not see. **`pnpm gate:env` now exists** (`scripts/env-gate.ts`, wired into the static half of CI after `gate:claims`) and reads all four access shapes, resolving same-file `const NAME = "KEY"` indirection; an unresolvable computed read fails unless recorded. Two ledgers, both compared in both directions: `RUNTIME_INJECTED` holds the seven the platform or the harness sets rather than the operator, each with its reason, and fails if the template ever starts declaring one; `DYNAMIC_ENV_READS` holds the one dynamic site and the keys it promises, and fails if the site stops naming one. Seven floor guards, so an empty scan cannot read as a clean product. Eight keys were added to `.env.example` as commented-out placeholders with descriptions derived from the code that reads them; no value was copied from anywhere and no real `.env` was opened. Verified: `env gate passed: 34 environment variables read across 36 of 757 modules under src/ (20 read directly, 15 through 5 process.env bindings, 1 recorded dynamic read site), 27 declared in .env.example, 7 recorded as runtime-injected`, 21 gate tests, twelve planted defects caught across all four access shapes, both ledger directions and the floors. **The row still stays NO on the same half**: the clean-clone run has not been attempted, and `docs/self-hosting.md` does not yet carry the eight newly documented variables, so the guide can still drift from the template the gate compares against. |
| G7.3 | Acceptance matrix covers every G gate with concrete evidence. | YES | This G1.1–G8.6 table in `docs/acceptance-matrix.md`; gate ids are complete and NO rows name the missing command, test, or path. |
| G7.4 | Capability and all named evidence registers exist and are current. | NO | `docs/capability-register.md` exists (created 2026-09-03: 22 capability rows and 3 declared gaps; the Pharmacogenomics row reached a terminal status on 2026-09-03 and is `shipped-degraded` — this row said `withheld`, which the register itself contradicts at `Withheld capabilities: 0`; the dossier `docs/withheld/pharmacogenomics.md` remains as the record of why nothing more ships — the nine elements, three designs actually built with their fixtures and the verbatim gate output under `docs/withheld/pharmacogenomics/designs/`, and ADR 0018; 9 rows still carry `not shipped`, which is not a terminal status), `docs/test-diff-register.md`, `docs/retention.md`, `docs/density-baseline.json` and, as of 2026-09-09, `docs/fixture-paths.md` exist. **Re-measured 2026-09-10 against the register itself**, because this row had drifted from it: `docs/figures-register.json` now exists — written for G8.3 and extended for G8.6 — so five of the six named files are present and `docs/release-checklist.md` is the only one absent. The table carries **26** rows, `shipped` 7 · `shipped-degraded` 12 · `withheld` 0 · `not shipped` 7, counted from the table; this row said 9 not-shipped, which was stale. **The remaining seven are not a documentation problem.** Sequence ingest (BAM, CRAM), carrier status, Neanderthal ancestry, carrier-pair arithmetic, embryo ingest, embryo quality and embryo comparison each name a real unbuilt workstream and an obstacle inside the operator's control, so none of them may honestly take `withheld` — `docs/withheld/` holds no dossier for any of them and nothing in the product says they are impossible. G7.4 flips when those capabilities ship, degrade honestly or are shown impossible, and when the release checklist exists; writing the checklist alone would not close it. |
| G8.1 | All pre-existing E2E tests pass without weakening. | YES | `pnpm e2e` passes all 57 tests; `docs/test-diff-register.md` records the runner change and confirms no pre-existing E2E test changed. |
| G8.2 | Production routes cannot render demo/fixture data as user data. | NO | Rules (b) and (c) are now built and enforced. `docs/fixture-paths.md` records the one production path that accepts fixture input — `src/app/api/jobs/research-refresh/route.ts` POST, gated on `Bearer JOBS_SECRET` or `CRON_SECRET` before the body is read — plus the protective `auto-e2e-` export exclusion, and the fixture body now takes `JOBS_SECRET` specifically, as G8.2(b) says, with `CRON_SECRET` left to drive the live path — a narrowing, pinned by `src/app/api/jobs/research-refresh/route.test.ts`, whose first case fails without it. `scripts/mock-token-allowlist.json` justifies all 17 production files carrying a demonstration, mock, fixture or dummy token, and `scripts/mock-token-allowlist.test.ts` fails in both directions — an unlisted hit and a stale entry — verified by planting each. `scripts/no-demo-result-path.test.ts` separately proves no route segment offers a demonstration and no page or component reaches a fixture module; the chat page's "hosted demo" string named in the brief is already gone. **The stated blocker is stale as of 2026-09-10 and is restated here.** It read that rule (a) needs the two-seed evidence of G8.3, "which needs `docs/figures-register.json`" — that file now exists, and `e2e/figures-two-seed.spec.ts` and `e2e/figures-cross-surface.spec.ts` difference five surfaces and enforce cross-surface agreement. Rule (a) still waits on G8.3, but on its two remaining Family surfaces rather than on a missing register. |
| G8.3 | Every registered figure differs correctly across two seeds. | NO | Both halves existed only as a single line when this row was first measured, on 2026-09-10 — no `docs/figures-register.json`, and no spec running a surface against two seeds — so here is what was found on the way to the five surfaces that are differenced now. **This is the brief's own detection for its first anti-pattern**, "a beautiful surface over an unimplemented pipeline", which is why one pinned value per surface explicitly does not satisfy it. **The figure system is already centralised, which makes the register derivable rather than hand-kept.** `src/lib/figures/contract.ts` declares **11** kinds (`absolute`, `relative`, `difference-pp`, `natural-frequency`, `percentile`, `coverage`, `interval`, `ancestry-share`, `genotype`, `carrier-status`, `measure`), 4 classes and 3 bases, and exactly **two** places emit the attribute: `src/components/figures/figure.tsx:46` from `spec.kind`, and `src/components/figures/relative-figure.tsx:85` hardcoding `relative`. So no surface can invent a kind outside the contract. **Seeds A and B largely exist already**: `e2e/fixtures/` holds seven synthetic GRCh38 VCFs with different values — `tiny`, `aims-mixed`, `medicines`, `carrier-pair`, `behavior-scope`, `observed-reference` and `personal-previews` — and the suite already drives several of them to rendered results, so the missing piece is a spec that collects figures under two of them and differences the values, not new fixtures. `src/lib/claims/collect-dom.ts` is the nearest precedent for the browser-realm collector but reads claims and citations, not figures, so a figure collector is still to be written. **The mechanism now exists and one surface is proven.** `docs/figures-register.json` is written, `e2e/figure-collector.ts` collects every `data-figure-kind` element with its class, basis, provenance and identifying ancestor, and `e2e/figures-two-seed.spec.ts` renders `/genome/me/ancestry` from two seeds and differences it. Seed B is the shipped generator at a second seed **and** different mixing weights (`SEED_B`, `WEIGHTS_B`), because the seed alone redraws the same mixture and lands near the same proportions — two seeds agreeing to the rendered decimal would make the gate look green while proving nothing. Seed A is byte-identical to before the generator was parameterised, checked by sha256. **Three things were found by running it rather than reasoning about it.** Pairing figures by their position among figures of the same shape was wrong: ancestry shares render in descending order, so the first run matched seed A's SAS against seed B's EUR and reported two regions as unchanged when it had compared different regions. The key now includes the nearest `data-region` or `data-claim-id`. Both coverage figures then read `168 of 168` under either seed, because the generator writes a row at every panel position — so seed B now drops every twelfth position (`DROP_EVERY_B`) and the coverage figures move, rather than being registered as invariant to accommodate a fixture. A first attempt at that kept only every fourth position and left 19 usable markers against a floor of 42, which renders the grey state: the seeds would have differed by rendering different surfaces instead of different values. **The register held exactly one entry at that point**, the unassignable chip: `estimateAdmixture` returns five proportions summing to 1 and `present.ts` computes the chip as `max(0, 1 - assigned)`, so it is 0.0% for every genome — registered rather than dropped from collection, because a non-zero value would mean the estimator had started leaving mass unattributed. Verified by planting the anti-pattern the gate exists for: forcing every share to a constant 0.25 fails it with all five regions named by their region code plus the hidden chip. **Five surfaces are differenced now**: `/genome/me/ancestry` under the two AIMs seeds, and `/genome/me/reports/caffeine-metabolism-cyp1a2-rs762551`, `/genome/me/reports`, `/genome/me/data` and `/genome/me/data/browser?q=rs762551` under `tiny-grch38.vcf` and `tiny-b-grch38.vcf`. Seed B carries the same four positions as seed A with different calls — rs762551 is 0/1 under A and 1/1 under B — plus thirteen positions seed A does not have, and **every one of those thirteen was added because a specific figure would not otherwise move**, found by running the comparison rather than by reading the code. `read 1 of the 1 positions this needs` genuinely cannot move — a one-position report is covered while it renders, and an uncovered position yields the not-covered state rather than a lower number — so both occurrences are registered with that reason. `calls in 4 of 4 listed, supported records` is the opposite case: it counts the uploaded file's own records, and two people's files legitimately differ, so registering it would have recorded a limitation of these fixtures as a property of the product. `read 1 of the 2 positions this needs` on `/genome/me/reports` is the lactase report, which reads rs4988235 and rs182549; the extra row seed B needed is rs182549 itself rather than filler, so one row moves the record count and that coverage figure together, 1 of 2 against 2 of 2. The three shipped PGS panels on `/genome/me/data` all read `read 0 of the N positions this needs` under both genomes, because neither tiny fixture carried a panel position — seed B now carries twelve drawn from the committed panels, and `user_prs.matched` records 3, 4 and 5 against seed A's zeros, so the figures read 3 of 50, 4 of 223 and 5 of 424. `/genome/me/data/browser` answers nothing without a search: the vacuity guard failed it until the surface was given `?q=rs762551`, which is why that guard is in the comparison. Its one registered figure is the search's own coverage — `searchRsid` builds a one-element `hits`, so `needed` is 1 for every genome, and the row it builds comes from a `user_variants` record, so `read` is 1 as well. **A claim in the previous revision of this row was wrong, and the correction belongs here rather than quietly dropped.** It said rs671 stays 0/0 because the parser drops homozygous-reference rows, leaving the position uncovered under either seed. The parser does drop them from `user_variants`, but the report surfaces read the canonical prepared source, which keeps reference calls: `public.report_observed_calls` holds `G/G` at rs671. The position is covered under both seeds, it renders the alcohol-flush personal preview, and that preview's 1-of-1 coverage is one of the two registered figures above. rs671 still stays 0/0 under both, because calling it under one seed only would change that preview's prose and move no figure. **Two mechanical defects in the gate itself surfaced the same way.** The surfaces were asserted in a loop of hard assertions, so a run stopped at the first failing surface and the rest were never read — a pass over four of them was described as three greens and one failure when only two had been compared. The assertions are soft now, and one run names every surface. And a figure only one seed renders would have crashed the value comparison on `undefined`; it is left to the key-set assertion that exists for it. **Verified by planting**, on each half: forcing every ancestry share to a constant 0.25 fails the ancestry surface while the report surfaces pass, and forcing the variant browser's genotype to a constant fails with `genotype|variant-call|observed|computed:genome/browser|-|1 = "A/C" under both seeds` while the ancestry test and the other three My Genome surfaces pass. **The remaining surfaces were measured rather than assumed, and there are two of them, not four.** `/embryos/compare` and `/embryos/[embryoId]` **cannot** be differenced at all: `docs/capability-register.md` carries embryo ingest as `not shipped`, no ingest path creates a cohort or a quality row, so neither surface has a reachable result state to render a figure in. They become differenceable when that workstream lands, and until then their absence is not a gap in this gate. **`/family/health-picture` renders 660 figures, and counting them changed the shape of the work.** Instrumenting the surface with the G8.6 collector, under the real two-account setup, found exactly three shapes: **324** report-coverage figures (`computed:genome/reports`), **324** input-provenance figures (`computed:genome/input-provenance`, all reading `calls in 58 of 58 listed, supported records`), and **12** genotype figures. Two things follow. First, **not one of the 660 carries an identifying ancestor** — no `data-region`, `data-chip` or `data-claim-id` — so pairing them across two seeds would be by position among figures of identical shape, which is precisely the failure that made the first ancestry run compare seed A's SAS against seed B's EUR. Each cell is a claim about one report for one person and should say so before it is differenced. Second, and more consequential: **298 of the 324 report-coverage figures read `read 0 of the 1 positions this needs`**, and a report neither pair's files cover reads 0 of its N positions under any genome. Making them move would mean fixtures that cover disjoint report sets, which changes *which* genotype figures exist rather than what they say — the structural difference this gate explicitly is not about. So coverage on this surface is seed-invariant by shape, and honouring the brief's "or be explicitly registered as seed-invariant" needs a shape-level entry in `docs/figures-register.json` rather than 298 per-figure ones. That is a schema addition, and it is the next step here — together with cell identity, and a seed-B carrier pair, for which `e2e/fixtures/carrier-pair-fixture.ts` is already a plain data module with a `verify()` that runs the real parser and the real runs measure. **The row stays NO** because those two Family surfaces are not differenced, and G8.2 rule (a) needs the full reachable set. **`/family/health-picture` is differenced now, 2026-09-10, and the two things this row named as the next step are both built.** Cell identity: `HealthPictureCell` emits `data-cell`, composed by `healthPictureCellId(layer, reportKey, column, viewerAccountId)` into `estimate/alcohol-flush-aldh2-rs671/self:You`, so every one of the 660 figures names the report and the person it is a claim about before it is paired. The person is named the way a reader tells the columns apart — kind chip plus display label — never by `dataSubjectId`, which is a fresh uuid per account and says nothing when the same journey is walked twice. `data-claim-id` was deliberately not reused: in this repository that marks a citation-backed claim collected by `collect-dom.ts` and held to `gate:claims`, and a family cell is a reading of one person's file rather than a cited statement, so `e2e/figure-collector.ts` learned `data-cell` as a fourth identifying ancestor. Shape-level invariance: `seedInvariantShapeValues` holds **one** entry — the report-coverage shape on that surface, 324 figures, with the exact histogram of what they read under both seeds (298 + 10 + 14 + 2). The justification is structural rather than convenient: a cell renders one `genotype` figure per position read as a genotype, and this gate already requires both seeds to render the same figures, so `read` can only differ through the unrecognised term — a file whose letters at a report position that report cannot interpret. Writing 298 of those into a fixture would be inventing a broken file to make a gate green, and the other route changes *which* cells render letters. Seed B is a pair rather than a parameter, because that surface renders two adults' files: both adults of pair B read a new hand-written synthetic VCF whose four report positions are all called differently and still inside the letters those reports interpret, with a different ROH shape so the 324 input-provenance figures move too. Verified `5 passed (2.4m)`, no skips, no retries, through the real journey per pair — two uploads, a real invitation through the mail worker, acceptance in the invitee's own session, six signed permissions and the Tier-2 gate. **Two plants, both bitten, both directions at once**: forcing a genotype constant while swapping one histogram value fails naming the unregistered constant by report and person and the stale register value beside it; removing cell identity fails separately, naming the two cells that collapsed onto one id. **One soft spot stated rather than buried**: that entry excuses the whole shape, 324 of 324. It is held to an exact histogram and the escape hatch is written into the entry, so the figure is immovable between two files that describe people rather than immovable absolutely; if a reviewer reads that as too broad, the honest alternative is to leave it unregistered and let the gate fail. **The row stays NO** for the two reasons it already gave: the embryo surfaces cannot be differenced while ingest is not shipped, and G8.2 rule (a) needs the full reachable set. |
| G8.4 | Entire deterministic suite passes twice from clean state. | NO | One clean 57-test run is recorded; the required two consecutive full gate/build/database cycles have not run. **The count above is stale, 2026-09-10.** The browser suite is 302 cases, not 57 — CI run 341 reports `296 passed`, one failure and five that did not run. The row is unchanged in substance: two consecutive full gate/build/database cycles have still not run, and this container cannot perform them, because `scripts/ci-browser-runtime.ts` refuses to start the isolated runtime as root and this session's uid is 0. That is an execution-environment limit, not a missing capability. |
| G8.5 | Every live surface/endpoint/prefix matches its register. | NO | `scripts/route-register-correspondence.test.ts` now enforces the route half. It fails when a built route has no register entry, and when a segment the register pins to literals is implemented as an open dynamic segment — the shape that puts a raw bearer token in a URL path. `docs/route-divergence.json` records the 5 divergences that exist, each with its file, why it is there and what closing it needs, and is checked in both directions: an unlisted divergence fails, and a listed one that no longer exists, or whose file moved, fails too, so a fix cannot leave a stale entry behind. Verified by planting all four: a new unregistered endpoint, a dropped ledger entry, a ledger entry pointing at the wrong file, and an open `[token]` segment where the register fixes the literal `session`. Each failed the assertion meant to catch it. Measured 2026-09-10: 113 built routes, 160 register entries, **4** built surfaces unregistered and **53** registered routes unbuilt — figures that correct this row's earlier 7 and 52, for the reason G2.3 records. The row stays NO. Three of the four unregistered surfaces are Priority 1 and still have no declared contract: `POST /api/uploads` and `POST /api/uploads/[id]/complete` (the latter carrying `maxDuration = 300`, the finalization ceiling the brief asks to solve) and `DELETE /api/files/[id]`, which is bound by registered deletion deadlines. Registered-but-unbuilt is not failed, since the register describes routes the product has not reached. **Why the entries are missing turns out to matter, and it is an authority blocker rather than an oversight.** The brief, which the register is derived from and claims to pin by `briefSha256`, says at line 2194 that `POST /api/uploads` does not exist: "no upload transits a function and a `POST /api/uploads` rejection is unreachable — there is no such route". It does exist. Its safety argument survives and was checked, not assumed: `issueSubjectUpload` reads at most 4096 bytes of JSON and never the file or a filename, so bytes still go browser to Storage and byte-level refusal genuinely cannot live there. Only the existence claim is false. A specification that denies a route exists cannot be transcribed into an entry for it, so these contracts cannot be written without an owner correcting the brief; deriving them from the handlers would put implementation-shaped authority into the file meant to constrain the implementation. Recorded in `docs/protocol/decisions.md` and `docs/route-divergence.json`. **Storage prefixes are no longer unverified, 2026-09-10.** **`pnpm gate:routes` now exists, 2026-09-10.** `scripts/route-gate.ts` runs in the static half of CI after `gate:secrets` and enforces five things the register asserted and nothing checked: declared methods against exported methods (37 endpoint pairs), registered redirect status against the status the implementation emits (10 redirects), registered kind against built kind at every concrete path the register pins (101 paths), declared storage buckets against the buckets migrations create (4 declared, 3 created), and the (route, state) matrix as a ratchet (288 required, 5 proven, 283 unproven, held by `UNPROVEN_ROUTE_STATE_PAIRS`). The first four compare against `docs/route-divergence.json` in both directions, so a fix that leaves a stale entry fails as loudly as a new divergence. `scripts/route-gate.test.ts` plants a defect for each check on a repository that is real except for the one thing under test — the app tree, migrations and browser tests are symlinked so the floor guards see their true size — and asserts the empty-scan case fails rather than reading as a clean product: 14 tests. **It found one live defect and fixed it.** `/signup` and `/login` were `next.config.ts` redirects with `permanent: false`, which emits 307 where the register records `expectedStatus` 308 for all ten legacy aliases; a 307 asks the browser back to the dead path every time, so the alias never retires and the old URL stays indexed. Both now read `permanent: true`, matching the eight aliases already built as pages calling `permanentRedirect`. The other eight divergences are recorded, not fixed, each with why it exists and what closing it needs: two method divergences (`api.account-delete` exports an undeclared `GET` that mints the operation nonce its own registered `POST` body requires and the brief never describes issuing — an authority blocker, not an oversight; `api.export` declares the asynchronous large-export contract's `POST` and polling `GET` while the code ships only the older synchronous streaming `GET`, so the verb count understates the gap), one kind divergence (`/withdraw/request` is a registered page literal served by a hand-written `route.ts`, which puts it outside the app layout, `pageAuthContract` and the accessibility sweep), and five storage divergences (`genomes-staging` holds real uploaded bytes under no declared prefix, filename contract, retention id or disposition, which is the sharpest of them; `generated-artifacts` is created but written by nothing, and the same literal doubles as a retention target class over database rows; `exports`, `legal-evidence` and `future-person-identity` are declared for surfaces that are unbuilt). The row still stays NO: the gate proves the register and the code describe the same surface, which is not the same as every live surface matching its register — four built surfaces still have no entry at all, the two method divergences and the kind divergence are recorded rather than closed, and five buckets disagree. Forms remain unverified, and a server-action surface is still invisible to a static walk. **`webhooks.resend` is not the transcription gap this row's ledger calls it, re-read 2026-09-10.** `docs/inherit-v2-brief.md` mentions resend only as a dependency (line 63) and its API key in the secret allowlist (line 2579). It describes **no webhook endpoint and no contract for one**. So writing register entries from the handler would document an endpoint the brief never authorised, which is the opposite of a register "derived from and pinned to the brief" — it would make the register agree with the code by abandoning the thing that gives it authority. The handler itself is sound and was read rather than assumed: it verifies svix signatures through `Resend.webhooks.verify` and returns 503 when `RESEND_WEBHOOK_SECRET` is unset, and that key is documented in `.env.example` (the env gate passes at 34 read against 27 declared). So this is **not closable by transcription**: it needs either the brief to describe the endpoint, which is an author's decision, or the register entry reconsidered. It compounds this row's other finding that `briefSha256` does not verify and nothing in code reads it — the register-to-brief derivation is itself unchecked, and this is what that looks like from the other end. |
| G8.6 | Repeated figures have one source of truth and cannot diverge. | NO | Measured 2026-09-10, replacing this row's one-line assertion. **Both halves now exist for the My Genome domain, and building them found a live divergence.** The brief asks that `docs/figures-register.json` list every figure appearing on more than one surface with its single source of truth, and that a gate fail when the same figure key renders two different values in one run. `docs/figures-register.json` has a `crossSurface` section and `e2e/figures-cross-surface.spec.ts` is that gate. It is the mirror of the G8.3 spec: that one renders one surface from two genomes and requires every figure to move, this one renders one genome across every surface and requires every repeated figure to hold still. A figure's key here carries no ordinal — kind, class, basis, provenance and the nearest identifying ancestor — because the question is whether the same figure agrees with itself in two places; provenance is part of the key, so two numbers with different provenance are two claims and may differ. **Discovery, not assumption, produced the register.** A first run collected every figure on the six registered own-subject routes for one account and printed what repeated. Two figures do. `coverage|quality|observed|computed:genome/input-provenance|-` — the source file's own call count — appears on five surfaces and, on the variant browser, twice on one page (the search's inputs and the region track). All of them read it through one component from one snapshot. `coverage|quality|observed|computed:genome/reports|-` — a report's position coverage — appears on the report list and the report page, **and those two computed it separately, by different rules**. The page counted `summarizeReportCalls(...).interpreted`, which gives a position whose files disagree the state `conflicting`; the list counted every outcome whose status is `genotyped`, which counts that position as read. So an account whose two files disagree at a report position saw the list claim a position the report itself would not show, each surface individually defensible. Both now call one exported `reportCoverage` in `src/lib/genome/report-evidence.ts`, and `interpreted` is the rule that survived: a position two files disagree about has not been read, it has been contested. **Proved rather than argued, in both directions.** `src/lib/genome/report-evidence.test.ts` pins the semantics — for a report whose every outcome is `genotyped` but two of whose positions are in the conflict set, `reportCoverage` reads 3 of 5 — and restoring the old expression fails it with `expected { read: 5, needed: 5 } to deeply equal { read: 3, needed: 5 }`. The E2E gate was planted by forcing the list's `read` to 0, which fails with `a report's position coverage: one figure, one value — [["/genome/me/reports",["read 0 of the 1 positions this needs"]],["/genome/me/reports/alcohol-flush-aldh2-rs671",["read 1 of the 1 positions this needs"]]]`. **One thing recorded and one claim corrected.** Every surface's figure count is registered, so a surface that gains a figure has to be looked at rather than silently joining the comparison. The first write-up of this row called `/genome/me` rendering **no figure at all** a product gap; it is not. Brief §4.0 specifies `/genome/[subject]` as the three-tile router — Reports, Ancestry, Ask Copilot — and `e2e/report-skeleton.spec.ts` pins that shape, so rendering no figure is its design. It is registered at zero rather than dropped from the surface list so that a figure appearing there is noticed and asked the cross-surface question. The surface the brief means by findings first is `/overview`, and that one does render them: metric lines under §3.1 and the deterministic starter set under §7.2, both shipped. **The row stays NO** because the register covers the six own-subject routes only. Family health picture, Portrait, Compare embryos and the public surfaces are not collected yet, so "every figure that appears on more than one surface" is not yet a whole-product claim — the same remaining set that keeps G8.3 open. |
