# Capability register

Status: binding (brief X15, X16.6, G7.4). Authority for the ship/withhold
status of every capability in Inherit's service category. One row per
capability; silence about a capability is never a withholding.

A row carries one of three terminal statuses:

- `shipped` — a person can reach it end to end on the hosted service.
- `shipped-degraded` — it ships in a narrower form and the row names the
  honest UI state that tells the person so.
- `withheld` — withheld on evidenced impossibility; the row references the
  dossier under `docs/withheld/<capability>.md`.

A row that has not reached a terminal status carries `not shipped` and names
the surface a person meets today and the workstream that owns it. `not
shipped` is not a withholding: no dossier exists for it and nothing in the
product says it is impossible. G7.4 stays NO while any row carries it.

Counts on this revision: shipped 9 · shipped-degraded 4 · withheld 0 ·
not shipped 9. Withheld capabilities: 0 (pharmacogenomics left `withheld`
on 2026-09-03 under ADR 0021; its dossier `docs/withheld/pharmacogenomics.md`
remains as the record of why nothing more ships).

## Capabilities

| Capability | Status | Honest UI state today | Evidence | Open work |
| --- | --- | --- | --- | --- |
| Array ingest (consumer raw-text files) | shipped | Upload accepts the four consumer array formats; a position an array did not test renders the exact not-covered sentence ("Your file does not cover this position. Array files test a fixed set of positions, and this one isn't on it. Inherit never guesses a genotype it hasn't seen."). | `src/lib/genome/parsers/array.ts`, `src/lib/genome/parsers/sniff.ts`, `src/app/api/files/[id]/process/route.ts`, `src/copy/reports/strings.ts` (`NOT_COVERED_ARRAY`), `e2e/upload-vcf.spec.ts` | — |
| Sequence ingest (VCF, gVCF) | shipped | VCF and gVCF are parsed, lifted over and annotated; a variant a VCF omits renders the exact §2 §4.5 not-covered sentence. | `src/lib/genome/parsers/vcf.ts`, `src/lib/genome/liftover.ts`, `src/app/api/files/[id]/process/route.ts`, `e2e/upload-vcf.spec.ts`, `e2e/report-skeleton.spec.ts` | — |
| Sequence ingest (BAM, CRAM) | shipped-degraded | Stored, hashed and downloadable, never analysed on the hosted service. The uploader says so: "BAM/CRAM up to 5.0 GB are stored, hashed and downloadable; analysis needs the self-host worker." and the file list labels the row "Stored (Tier 2)". | `src/components/uploads/uploader.tsx`, `src/app/(app)/files/page.tsx`, `src/app/api/files/[id]/process/route.ts` (refuses Tier 2), ADR 0016 | — |
| Single-variant reports | shipped | 162 seeded templates in nine categories (151 statistical estimates and, since ADR 0021, 11 specific-variant reports in the Medicines category); each report renders the six-heading skeleton with one attributed genotype figure, the exact not-covered sentences and the not-diagnostic line; the list renders one group per layer with the layer tabs, and counts are never summed. | `src/app/(app)/genome/[subject]/reports/[slug]/page.tsx`, `src/components/reports/report-skeleton.tsx`, `data/templates/*.json`, `e2e/report-skeleton.spec.ts`, `e2e/report-gate.spec.ts` | The "Specific variants" layer now holds the Medicines templates only; every other seeded template is `layer='estimate'` (D-014 narrowed, not closed). |
| Polygenic reports | shipped-degraded | Scores are computed and stored; no percentile or risk number renders anywhere because no shipped score is checked in people like the reader. The report says "We can't put a range on this yet, so we don't show a single number." and the list says "{k} of these reports cannot give you a number yet." with a "Why?" link to the science page's "Why a report may show no number yet". The expert page shows panel coverage only. | `src/lib/genome/prs.ts`, `src/app/api/files/[id]/process/route.ts`, `src/app/(app)/genome/[subject]/data/page.tsx`, `src/app/(marketing)/science/page.tsx#polygenic`, `src/copy/reports/strings.ts` (`NO_RANGE_YET`, `cannotNumberSentence`) | Portability check per score (G4.4) before any number renders. |
| Carrier status | not shipped | No template, page or category names carrier status. The only carrier sentence is the support panel's partner-testing note on reproductive reports. | `src/components/reports/support-panel.tsx`, `src/lib/figures/spec.ts` (`carrier-status` figure kind, uninstantiated) | Reports workstream: carrier templates on the `carrier-status` figure, or a dossier. |
| Pharmacogenomics | shipped-degraded | The "Medicines" category renders eleven per-position reports in the Specific variants group of the reports list (a tab; the list opens on the general library), like any other category (ADR 0021, operator approval of 2026-09-03 recorded in `docs/protocol/decisions.md`). Each report says only which letters the file shows at one GRCh38 position a CPIC guideline names (VKORC1 rs9923231; CYP2C19 rs4244285 and rs12248560; CYP2C9 rs1799853 and rs1057910; SLCO1B1 rs4149056; TPMT rs1800462 and rs1800460; NUDT15 rs116855232; DPYD rs3918290; CYP3A5 rs776746), which CPIC-named forms carry that letter, and what the position cannot tell the reader — that one position does not show the pair of gene copies and says nothing about how a medicine works in the reader, a dose or a choice of medicine. The limits on the surface: per-position only; no metabolizer phenotype, no response, no dose, no drug choice, no frequency, no effect size; the DPYD report leads with "This is one of the positions guidelines list for DPYD. C on both copies here says nothing about the other positions, which this report does not read."; the category description reads "The letters your file shows at single DNA positions that prescribing guidelines name."; "What you can do" reads "Inherit does not say what any doctor should do with this result. You can show it to any doctor you choose." Excluded, with the research note’s reasons in ADR 0021: CYP2D6, HLA-B\*57:01 (rs2395029), IFNL3, UGT1A1\*28 (rs3064744), TPMT \*3C (rs1142345), G6PD. Currency: every template carries its guideline PMID and `accessedOn: "2026-09-03"`; CPIC content is subject to updates and Inherit has no automatic detection of a superseded guideline yet. The approval is the operator’s; no competent reviewer’s claim entry exists for the category’s net impression, and the register says so until one does. | ADR: `docs/adr/0021-pharmacogenomics-per-position-reports.md` (supersedes 0018). Decision: `docs/protocol/decisions.md` (2026-09-03, "Medicines: the operator lifts the withholding for the honest subset"). Templates: `data/templates/medicines.json` (11, `layer: "variant_call"`, `evidence: "emerging"`, CPIC and dbSNP `source` entries and `accessedOn` on every citation). Facts: `docs/design/pharmacogenomics-research-2026-09-03.md`. Code: `src/lib/genome/taxonomy.ts` (`pharmacogenomics` → `medicines`), `src/copy/reports/strings.ts` (`WHAT_YOU_CAN_DO_MEDICINES`, `whatYouCanDo`, `CATEGORY_DESCRIPTIONS.medicines`), `src/app/(app)/genome/[subject]/reports/[slug]/page.tsx`, `src/app/(app)/genome/[subject]/reports/page.tsx`, `scripts/validate-templates.ts` (`MEDICINES_BANNED_PATTERNS`, `sourceFindings`, the §6.4 rows). Tests: `src/lib/genome/pharmacogenomics-withheld.test.ts`, `src/lib/genome/taxonomy.test.ts`, `src/copy/reports/strings.test.ts`, `scripts/validate-templates.test.ts`, `scripts/readability-gate.test.ts`, `e2e/report-skeleton.spec.ts` (with `e2e/fixtures/medicines-grch38.vcf`). Licence: `docs/dataset-licenses.md` (CPIC row, accessed 2026-09-03). Record of why nothing more ships: `docs/withheld/pharmacogenomics.md` and its three built designs. | Degraded until: (a) a competent reviewer’s claim entry on the category’s net impression (dossier condition A item 4; `data/claims.json`); (b) the Copilot intent guard of brief line 2262 (sibling branch); (c) an automatic check of each template’s guideline PMID against CPIC’s publication and pair tables, so a superseded or retired guideline is a defect rather than a discovery; (d) for any phenotype, dossier condition B — a multi-allelic schema, an X-chromosome haploid key path, and a diplotype assignable without inferring phase. Two legal questions stay live and unresolved, and both sources stay unused: the companion knowledge base’s research-use term against its CC BY-SA 4.0 grant, and the allele registry’s unreadable terms (UNVERIFIED). |
| Traits and wellness | shipped | "Everyday traits" (33 templates) and "Food, drink and metabolism" (27) render through the report skeleton. | `data/templates/{basic-traits,aesthetic-cosmetic,environmental-sensitivity,lifestyle-wellness,metabolic-obesity,gastrointestinal}.json`, `e2e/report-skeleton.spec.ts` | — |
| Continental ancestry | shipped-degraded | Five place-named regions from the 168-marker panel, each share a single number with the unit "no range yet" and the block sentence "Inherit can’t put a range on these shares yet, so each one is a single number. When a range can be computed it will appear beside every share."; below 42 usable markers the page renders the grey map and "Your file covers only {k} of 168 ancestry markers — too few to draw a map. This is a limit of the file, not a result about you." | `src/app/(app)/genome/[subject]/ancestry/page.tsx`, `src/components/results/ancestry/*`, `src/copy/ancestry.ts`, `src/lib/ancestry/*`, `public/geo/regions.topo.json`, `e2e/ancestry.spec.ts`, ADR 0013 | Degraded until an interval exists: a marker-subsampling interval and the panel threshold belong to the science dimension (X16.6, D-017). |
| Sub-continental ancestry | shipped-degraded | The page states, in the regions block: "The 168-marker panel your file was compared with can only tell five broad regions apart. The map shows those and nothing smaller." and "Five broad regions is the finest this panel can tell apart. It cannot place DNA within a region." No sub-continental control renders because no region in that tier qualifies (`tierQualifies`). | `src/copy/ancestry.ts` (`subContinental`, `RESOLUTION_LIMIT`), `src/lib/ancestry/regions.ts`, `data/ref/regions/regions.json` (one tier), `e2e/ancestry.spec.ts` | A licence-clean sub-continental panel with its provenance is the science dimension's work (X16.6); the region set is versioned (`ancestry-regions-v1`) so a finer tier is a new release. |
| Maternal line (mtDNA) | shipped-degraded | The haplogroup as text with its inline definition, matched-of-tested markers as an observed `coverage` figure, the stored support note and the §4 §7.5 sentence "This traces one single line — your mother’s mother’s mother, and so on. It says nothing about the rest of your ancestry. Ten generations back you have about 1,024 ancestors; this line is one of them."; with no mitochondrial positions the card keeps the stored note. No dated, cited place mapping exists, so the line is text, not a route on the map, and the page says: "The map shows broad regions only. Your mother’s and father’s lines are below as text, because Inherit has no dated, sourced route for them yet." | `src/components/results/ancestry/lineage-card.tsx`, `src/lib/genome/haplogroups.ts`, `data/ref/haplogroups/*`, `e2e/upload-vcf.spec.ts`, `e2e/ancestry.spec.ts` | Degraded until a dated, cited lineage mapping exists (design §4.3). The card now names the tree and its version, states that no range is put on the name and gives the tree's resolution limit (D-019 closed). The tree's own licence position is unresolved (D-022). |
| Paternal line (Y) | shipped-degraded | As the maternal line; with no Y data the card leads with "Your file has no Y-chromosome data, so no father’s line can be read from it. This says nothing about who your father was." and keeps the stored note and the gloss "In plain terms: this is expected when the file comes from someone without a Y chromosome, e.g. most women." | as above | as above |
| Neanderthal ancestry | not shipped | The `#neanderthal` card renders the heading "How much of your DNA came from Neanderthals" and the true reason: "We can’t show this yet. Inherit has not yet built and licence-checked the marker list this needs, and we will not guess. This page will say so until that changes." No `archaic-hominin` wording exists on any surface (`e2e/ancestry.spec.ts`). | `src/components/results/ancestry/neanderthal-card.tsx`, `src/copy/ancestry.ts` | The marker list has not been built or licence-checked; the obstacle is within the operator's control, so the card may say "yet". Until the list exists the row is `not shipped`; the terminal status is `shipped` once it does, or `withheld` with a dossier if a licence-clean list cannot be assembled. |
| Another adult’s results, with their consent | shipped-degraded | `/family` lists the adults this account may see or share with, ordered by name only; `/family/[person]` renders that adult’s shared report layers and, if granted, their ancestry, read from their own record and only behind one Tier-2 gate per session ("I understand this can tell me something I can’t un-know."); a layer that is not shared is absent and the page says so; `/family/[person]/permissions` carries the two independent columns, all rows default off, pause, resume and the confirmed stop with its tombstone. A revoked, paused or stopped record answers 404 before any gate. Portrait cannot be turned on until the invitee has signed in on their own after accepting. | `src/lib/family/{graph,access,tier2,subject-route,grant-token,independent-login}.ts`, `src/app/(family-hub)/family/page.tsx`, `src/app/(app)/family/[person]/page.tsx`, `src/app/(app)/family/[person]/permissions/page.tsx`, `src/app/api/family/acknowledge/route.ts`, `src/app/api/family/[person]/sharing/route.ts`, `supabase/migrations/20260903120000_family_sharing_runtime.sql`, `supabase/tests/family_sharing.sql`, ADR 0014, `e2e/family.spec.ts` | Degraded: Path B (upload with written permission) is not built and its link is not shown; the person-scoped Copilot answers 404, so the hub tile states its blocking reason; the counsellor directory under the gate is absent (X16.2). |
| Family risk comparison (side by side) | shipped-degraded | `/family/health-picture` renders one table per layer with the observed genotype as each cell's figure, the exact banner, the non-dismissible trade-off panel and G4.5's gate string; every column footer states that no baseline is known, because no `risk_models` row or demographics exist, so no absolute risk, interval or difference is shown. Columns appear only for people whose `family.heritability` grant is live in both directions from their own sessions (the sixth permission row). | `src/app/(app)/family/health-picture/page.tsx`, `src/components/family/health-picture-*.tsx`, `src/components/family/trade-off-panel.tsx`, `e2e/family-health-picture.spec.ts` | ADR 0017. Absolute risk waits on a cited `risk_models` row and a demographics writer. |
| Carrier-pair arithmetic | shipped-degraded | `src/lib/family/carrier-pair.ts` evaluates the closed rule per gene: each person's own heterozygous pathogenic or likely pathogenic variant in the same gene, autosomal recessive, and runs of homozygosity (measured at ingest and stored per file) below threshold in both files renders the exact 25-in-100 sentence as one `exact` natural-frequency figure and names both variants and classifications; every other case renders the exact refusal sentence with one reason from the closed table of eight (`sex-unknown` for X-linked patterns because no chromosomal sex is recorded, D-031; `two-copies` for a file that shows two changed copies). No classified reference position exists in production today (every `ref_variants.clinvar_significance` is null), so the shipped state is the sentence that says so. | `src/lib/family/carrier-pair.ts`, `src/lib/family/roh.ts`, `src/components/family/carrier-*.tsx`, `e2e/family-health-picture.spec.ts` | ADR 0017. The X-linked hundred-pregnancy split waits on a sourced writer for chromosomal sex; the registry's classifications wait on the science dimension. |
| Future-child preview (portrait) | shipped-degraded | `/family/portrait/[pairId]` renders, for a pair whose two people each hold their own account, an independent login, an own-session `family.portrait` grant toward the other and their own acknowledgement, and only behind the domain's one Tier-2 gate: the persistent banner pair, the header sentence, the distinguishing principle, one pair-attributed claim block per gene both files show a change in — the recessive match as the derivation "1 in 4 (25%) affected · 2 in 4 (50%) carriers · 1 in 4 (25%) neither" with three `exact` natural-frequency figures at 100 inside the 100-dot renderer (DOM spans, a stacked bar, a legend, a table fallback), the exactness label once, the covered-against-known count, the segregation sentence, a non-collapsible "How sure we are" and "This is a chance, not a prediction about a particular child."; every other match, an X-linked pattern included, the side-by-side page's refusal sentence with its named reason and never a fraction (D-031); the one-sided sentences of brief line 2238 without a distribution — the five trait cards, each "Inherit has not registered a sourced table for {trait} yet, so this card shows nothing.", the eleven server-rendered refusal cards under "What Portrait will not tell you, and why", and one destructive "Delete Portrait" that revokes the viewer's own grant. Any unmet step renders the blocking screen naming each person's missing steps with the viewer's own acknowledgement checkbox; no image, no polygenic output, no sex prediction, no `0%`. Degraded because every trait entry is `unregistered` (no `data/citations.json`), every real jurisdiction is `unreviewed`, and no classified reference position exists in production, so the shipped outputs section reads "Inherit has no classified positions to check yet, so it cannot look for a change you both carry." and the recessive block is proved only on synthetic fixture rows. | `src/app/(app)/family/portrait/[pairId]/page.tsx`, `src/lib/family/{portrait,mendel,distribution,traits}.ts`, `src/components/family/portrait/*`, `src/copy/family/portrait.ts`, `data/family-trait-allowlist.json`, `e2e/portrait.spec.ts`, ADR 0015 | A registered trait table with its citations (`data/citations.json`); a sourced writer for chromosomal sex before an X-linked split renders (D-031); `/science/limits` for the refusals link; the `compute_portrait` worker job if outputs are ever persisted. |
| Embryo ingest | not shipped | `/embryos/upload` renders the five-step flow's first two steps screen by screen, none with more than five controls of its own — the brief's three questions (the second revealed by the first), the four illustrated options and the secondary link as actions, the two class attestations (never pre-ticked), and who can sign as four actions each leading to its named screen and sentence — with "No" and "A PDF report only" ending the flow on the brief's sentences; every other path ends on "Inherit cannot take embryo files on this site yet." with the letter to the laboratory, and the same sentence stands above step 1. Nothing is persisted and no request leaves the page. `/embryos/request-data` renders the letter. The A.6 refusals have one home, `sniffV2` names multi-sample VCFs, laboratory tables and PDFs, and the table header rule and mapping plan exist as pure functions no route consumes yet. | `src/app/(app)/embryos/upload/page.tsx`, `src/components/embryo/upload/upload-flow.tsx`, `src/lib/embryos/upload-flow.ts`, `src/copy/embryos/upload.ts`, `src/copy/upload/errors.ts`, `src/lib/genome/parsers/{sniff,pgt-table}.ts`, `data/ref/lab-tables/column-synonyms.json`, `e2e/embryos.spec.ts`, `docs/adr/0020-embryo-ingest-and-cohort-lifecycle.md` | Platform E0 (the eight RPCs, the nine `api.embryo-*` routes, the browser sanitiser, the `split_cohort_vcf` worker), then E2 steps 3–5 and ADR 0020 to Accepted (A.10, ADR 0016 transport). |
| Embryo quality | not shipped | `/embryos/[embryoId]` renders every state of design w10 §1.4 (jurisdiction, empty, processing, consent-required, the one Tier-2 gate, complete, shape-blocked and read-failed error states) over the closed policy shapes, with the quality block reading the mean depth as a figure and printing only registered source labels; no ingest path (E2) creates a row yet, so no reader can reach a result. The Overview's State E counts are wired to `embryo_qc`. | `src/app/(app)/embryos/[embryoId]/page.tsx`, `src/app/(app)/overview/page.tsx`, `src/copy/overview.ts` (`STATE_E`) | Embryos workstream. |
| Embryo comparison | not shipped | `/embryos/compare` renders every state of design w10 §1.4 with the standing statement, the trade-off panel and G4.5's exact string, one attributed block per cell, the closed cell words, and the QC table as a compare surface; no ingest path (E2) creates a cohort yet. The refused branch of every Embryo route is proven with the jurisdiction flag unset (`e2e/embryos.nojurisdiction.spec.ts`). The Overview still offers "Compare your embryos". | `src/app/(app)/embryos/compare/page.tsx`, `src/copy/overview.ts` | Embryos workstream with the embryo-comparison presentation ADR. |
| Chat over one's own genome (Copilot) | shipped | `/copilot/[scope]` for the self and each adult subject, with per-provider consent grants; with no AI connected the page says "Questions like these become askable as soon as an AI is connected." and explains that Inherit bundles no AI. Family and cohort scopes answer 404 and their Overview boxes link to the domain landing instead. | `src/app/(app)/copilot/[scope]/page.tsx`, `src/app/api/chat/route.ts`, `src/lib/copilot/guard.ts`, `src/copy/copilot/refusals.ts`, `config/allowed-numerals.json`, `src/app/(app)/settings/copilot/page.tsx`, ADR 0004 §5, `e2e/copilot.spec.ts`, `e2e/copilot-refusal.spec.ts` | Family and cohort scopes with their workstreams (the guard takes the scope and is ready for them). The brief's 80-case adversarial evaluation (line 2263) is not built; the guard's 68-row classifier table is a unit test, not that suite. |
| Export | shipped | Settings → Your data → "Export everything": one ZIP with originals, variants, results, consents, legal audit records and saved chats. | `src/app/api/export/route.ts`, `src/app/(app)/settings/data/page.tsx`, `e2e/deletion-export.spec.ts` | — |
| Deletion | shipped | "Delete account" schedules deletion after a seven-day notice, cancellable, with export still available; the scheduled state names the date. | `src/app/api/account/delete/route.ts`, `src/lib/account-deletion.ts`, `src/components/settings/danger-zone.tsx`, `e2e/deletion-export.spec.ts` | — |

## Declared gaps

Three category services are declared gaps with a published reason, never
silent omissions. A gap is declared only where the product says so; two are stated on `/science#not-offered` and the third on the ancestry surface.

| Gap | Declared where | Evidence | Open work |
| --- | --- | --- | --- |
| Relative matching, relatedness or any shared-DNA quantity | `/science#not-offered`: "Inherit does not match you with relatives. It does not work out how much DNA two people share. Your file is compared with public reference data, not with another person’s file." Inherit computes and displays none of it (a search for shared DNA, centimorgan, kinship, IBD and relative matching across `src/` finds no computation and no display); the appeals page states the same (D-016 fixed). | `src/app/(marketing)/science/page.tsx`, `src/app/(marketing)/legal/appeals/page.tsx`, `e2e/legal.spec.ts` | The Family workstream's consented side-by-side comparison must not contradict the sentence; revise it when that ships. |
| Prenatal and newborn screening | `/science#not-offered`: "Inherit does not offer prenatal or newborn screening. Those are clinical tests with their own rules, and a clinic is the right place for them." | `src/app/(marketing)/science/page.tsx`, `e2e/legal.spec.ts` | — |
| Denisovan ancestry | Stated once on the ancestry surface, in the Neanderthal card: "Inherit does not estimate Denisovan ancestry yet, so this number is about Neanderthals only." | `src/copy/ancestry.ts` (`DENISOVAN`), `src/components/results/ancestry/neanderthal-card.tsx`, `e2e/ancestry.spec.ts` (`#neanderthal` present) | Published. |
