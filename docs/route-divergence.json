{
  "purpose": "Every place where the live App Router surface and docs/route-register.json disagree. scripts/route-register-correspondence.test.ts checks which routes exist; scripts/route-gate.ts (pnpm gate:routes) checks the methods they export, the status their redirects emit, the storage buckets their prefixes name and the (route, state) pairs a browser test proves. Both fail when a divergence appears that is not listed here, and fail when a listed one is fixed or misdescribed, so this file cannot quietly drift from reality in either direction.",
  "notAnApproval": "Listing a divergence records it; it does not bless it. The register is the binding response-contract authority, so an unregistered live endpoint has no declared auth mode, response contract or policy. Each entry names what is missing and what closing it needs.",
  "builtButNotRegistered": [
    {
      "path": "/api/withdraw",
      "file": "src/app/api/withdraw/route.ts",
      "methods": [
        "POST"
      ],
      "handler": "inline POST",
      "live": "posted by the form in src/app/(marketing)/withdraw/[token]/page.tsx",
      "why": "The legacy adult-subject invitation response surface. api.withdraw is registered at /api/withdraw/[token] with token pinned to the literal \"session\", which src/app/api/withdraw/session/route.ts implements \u2014 and which, since 2026-09-13, answers the adult confirm/refuse/delete as well as the co-parent refusal. This bare path is the pre-migration form and is defect D-081. Nothing mails a link that reaches it any more; it stays only for the tokens mailed before that change.",
      "priority": "defect D-081",
      "closing": "Delete with the /withdraw/[token] directory once the last pre-2026-09-13 token has expired; see the permissiveDynamicSegment row."
    }
  ],
  "permissiveDynamicSegment": [
    {
      "routeId": "rights.withdraw",
      "path": "/withdraw/[token]",
      "file": "src/app/(marketing)/withdraw/[token]/page.tsx",
      "segment": "token",
      "pinnedTo": [
        "request",
        "session"
      ],
      "why": "The register pins this segment to two literals and marks raw-bearer forbidden. Both literals are built (withdraw/request/route.ts, withdraw/session/page.tsx), and the [token] directory is built too, so /withdraw/<any raw token> still resolves. That is defect D-081. RE-SCOPED 2026-09-13: the adult invitation mail no longer links here. It links at /withdraw/request#<token> like every other mailed token, the adult purpose activates a rights session through api.rights-activate, and /withdraw/session answers confirm, refuse and delete off the cookie. What this directory still serves is the tokens that were mailed BEFORE that change, which live up to 30 days; deleting it today would take away the rights of the people holding them.",
      "priority": "defect D-081",
      "closing": "Delete this directory, the bare POST /api/withdraw beside it, and both of these rows together, once the last token mailed before 2026-09-13 has expired \u2014 30 days by invitation.pending-30d. e2e/adult-subject-invitation.spec.ts keeps one test on this path until then, so the overlap is proven rather than assumed, and that test goes in the same commit."
    }
  ],
  "methodDivergence": [
    {
      "routeId": "api.account-delete",
      "path": "/api/account/delete",
      "file": "src/app/api/account/delete/route.ts",
      "declared": [
        "POST"
      ],
      "exported": [
        "GET",
        "POST"
      ],
      "why": "The registered POST body requires \"required-recent-one-time-operation-nonce\", and the register never says where that nonce comes from. The code answers with an undeclared GET on the same path that mints it: it checks same-origin and a sensitive account context, then calls issue_account_operation_nonce_v1 through the service role. So the mint is real, needed and load-bearing for account deletion, and it is the one verb on this path with no declared auth mode, no rate-limit binding and no response contract.",
      "priority": "Priority 1 \u2014 deletion",
      "closing": "A register entry for the GET verb naming its auth requirements, its rate limit and the nonce response contract. Not a transcription: docs/inherit-v2-brief.md describes no issuance surface for the account operation nonce at all, and the register is derived from the brief and pinned to briefSha256, so the brief has to describe the mint before the register can.",
      "blocker": "authority \u2014 the brief must describe the operation-nonce mint before the register can declare it"
    },
    {
      "routeId": "api.export",
      "path": "/api/export",
      "file": "src/app/api/export/route.ts",
      "declared": [
        "GET",
        "POST"
      ],
      "exported": [
        "GET"
      ],
      "why": "The register describes the asynchronous large-export delivery contract: POST create or open-ready with a session-bound one-time nonce, and a GET that polls status with zero enqueue and zero credential. The code ships the older synchronous route, whose own comment says so \u2014 one GET that streams the whole ZIP inline under maxDuration 300. The POST half is unbuilt, and the built GET is a different operation from the registered GET, so the verb count understates the gap rather than overstating it.",
      "priority": "Priority 1 \u2014 complete download/export",
      "closing": "Build largeExportDeliveryContract: the enqueue and open-ready POST, the polling GET, the principal-bound HttpOnly export cookie and the exports bucket below. Until then the synchronous GET is what exists, and it is the same 300-second finalization ceiling the brief already names as the bottleneck."
    }
  ],
  "redirectStatusDivergence": [],
  "redirectStatusDivergenceNote": "Empty on purpose, and checked in both directions so it cannot stay empty through a regression. The register pins all ten legacy aliases to expectedStatus 308. Eight are pages calling permanentRedirect. /signup and /login were next.config.ts entries with permanent: false, which emits 307 \u2014 a 307 asks the browser back to the dead path every time, so the alias never retires. Both now read permanent: true.",
  "kindDivergence": [
    {
      "routeId": "rights.withdraw",
      "path": "/withdraw/request",
      "file": "src/app/(marketing)/withdraw/request/route.ts",
      "declaredKind": "page",
      "builtKind": "endpoint",
      "why": "The register pins /withdraw/[token] to the literals request and session and calls the route a page. /withdraw/session is a page; /withdraw/request is a route.ts that builds an HTML document as a template string and returns it with its own Content-Security-Policy, nonce and Set-Cookie. It reads as a page to a person, and to the codebase it is not one: it is outside the app layout and navigation, outside pageAuthContract, and invisible to the registered-page sweep in e2e/a11y.spec.ts, which enumerates pages. Its markup is hand-written, so nothing checks its headings, landmarks, contrast or focus order the way every other public page is checked.",
      "priority": "defect D-081 \u2014 adjacent",
      "closing": "It is a route.ts on purpose: it mints a rights-activation candidate and sets a cookie before any markup, which a React page cannot do in the same response. Closing it means either registering the request literal as an endpoint in its own right, with the interstitial contract written down, or moving the mint into a middleware or action so the interstitial can be a page and re-enter the accessibility sweep. The second is the better shape and the larger change."
    }
  ],
  "storageBucketDivergence": [
    {
      "bucket": "genomes-staging",
      "direction": "created-not-declared",
      "createdBy": "supabase/migrations/20260831224054_storage_and_download_sessions.sql",
      "why": "A real bucket holding real uploaded genome bytes: genome_files.storage_bucket defaults to it, storage RLS policies name bucket_id = 'genomes-staging', and an upload lands there before single-logical-sample verification moves it. It has no entry in storagePrefixes, so the bytes users upload sit under no declared prefix shape, no filenameContract, no retentionId and no disposition. This is the sharpest of the five.",
      "priority": "Priority 1 \u2014 upload, durable preparation, deletion",
      "closing": "A storagePrefixes entry mirroring storage.subject-v2 with the staging disposition and the retention id that governs an unverified upload, so the pre-verification window has a written deadline like everything else."
    },
    {
      "bucket": "generated-artifacts",
      "direction": "created-not-declared",
      "createdBy": "supabase/migrations/20260831224054_storage_and_download_sessions.sql",
      "why": "Created as a Storage bucket, and nothing in src writes an object to it. The same literal is used elsewhere as a retention disposition target class over database rows (public.report_artifacts, public.generated_exports, public.download_sessions, public.model_contexts), so the name carries two meanings and only the row meaning is exercised. An empty private bucket is not a leak, but an undeclared one is a place for a later write to land unnoticed.",
      "priority": "Priority 2 \u2014 saved results",
      "closing": "Decide which it is. If derived artifacts are meant to be objects, write the prefix entry; if they are rows only, drop the bucket so the retention target class is the only thing wearing the name."
    },
    {
      "bucket": "exports",
      "direction": "declared-not-created",
      "declaredBy": "storage.export-v1",
      "why": "The bucket the large-export delivery contract writes archives into. It does not exist, which is consistent with POST /api/export being unbuilt above: the whole asynchronous export path is backlog, not a half-built one. The declared access rules \u2014 worker-write-only, authenticated list and select denied, bounded range reads only after api.download-chunk authorization \u2014 therefore protect nothing yet.",
      "priority": "Priority 1 \u2014 complete download/export",
      "closing": "Created with those policies as part of building largeExportDeliveryContract."
    },
    {
      "bucket": "legal-evidence",
      "direction": "declared-not-created",
      "declaredBy": "storage.legal-evidence-v1",
      "why": "Evidence for claim and appeal review, declared with service-only access and application envelope encryption on top of provider at-rest encryption. Nothing creates it because the claim and appeal surfaces it serves are unbuilt.",
      "priority": "Priority 5 \u2014 future-person and claims",
      "closing": "Created with the declared policies and the document-specific data key when evidence ingest is built."
    },
    {
      "bucket": "future-person-identity",
      "direction": "declared-not-created",
      "declaredBy": "storage.future-person-identity-v1",
      "why": "Documentary review for Record-Key, claimant-Recovery-Key and keyless future-person claims, declared with named-human-reviewer-assignment access only and four retention ids. Unbuilt for the same reason.",
      "priority": "Priority 5 \u2014 future-person and claims",
      "closing": "Created with the declared policies and the claim-specific data key when future-person claim review is built."
    }
  ],
  "provenRouteStatesNote": "The (route, state) pairs a browser test title proves today, out of the 288 the register requires. scripts/route-gate.ts recomputes this list from e2e/**/*.spec.ts titles and fails if it differs in either direction, and UNPROVEN_ROUTE_STATE_PAIRS in that script holds the remainder. A title proves a pair when it names the registered route path and the state id as separate, non-overlapping, whole tokens, so \"empty-history\" does not prove \"empty\" and a state id appearing only inside the path proves nothing.",
  "provenRouteStates": [
    "/ complete",
    "/about complete",
    "/auth/forgot-password complete",
    "/auth/forgot-password processing",
    "/auth/reset-password processing",
    "/auth/sign-in processing",
    "/auth/sign-up complete",
    "/auth/sign-up processing",
    "/changelog complete",
    "/copilot/[scope] complete",
    "/copilot/[scope] consent-required",
    "/copilot/[scope] empty",
    "/copilot/[scope] processing",
    "/embryo-analysis jurisdiction-unavailable",
    "/embryos complete",
    "/embryos empty",
    "/embryos jurisdiction-unavailable",
    "/embryos processing",
    "/embryos/[embryoId] consent-required",
    "/embryos/[embryoId] empty",
    "/embryos/[embryoId] jurisdiction-unavailable",
    "/embryos/[embryoId] processing",
    "/embryos/compare consent-required",
    "/embryos/compare empty",
    "/embryos/compare jurisdiction-unavailable",
    "/embryos/compare processing",
    "/embryos/request-data complete",
    "/embryos/request-data jurisdiction-unavailable",
    "/embryos/upload jurisdiction-unavailable",
    "/family complete",
    "/family empty",
    "/family jurisdiction-unavailable",
    "/family processing",
    "/family/[person] consent-required",
    "/family/[person] empty",
    "/family/[person] jurisdiction-unavailable",
    "/family/[person] partial-coverage",
    "/family/[person] complete",
    "/family/[person] not-covered",
    "/family/[person] processing",
    "/family/[person]/permissions complete",
    "/family/[person]/permissions jurisdiction-unavailable",
    "/family/[person]/permissions processing",
    "/family/health-picture complete",
    "/family/health-picture consent-required",
    "/family/health-picture empty",
    "/family/health-picture jurisdiction-unavailable",
    "/family/health-picture not-covered",
    "/family/health-picture partial-coverage",
    "/family/health-picture processing",
    "/family/invite complete",
    "/family/invite jurisdiction-unavailable",
    "/family/invite processing",
    "/family/portrait/[pairId] consent-required",
    "/family/portrait/[pairId] empty",
    "/family/portrait/[pairId] jurisdiction-unavailable",
    "/family/portrait/[pairId] not-covered",
    "/family/portrait/[pairId] processing",
    "/files complete",
    "/files consent-required",
    "/files empty",
    "/files processing",
    "/files/upload complete",
    "/files/upload consent-required",
    "/files/upload processing",
    "/future-person/claim complete",
    "/genome/[subject] complete",
    "/genome/[subject] jurisdiction-unavailable",
    "/genome/[subject] processing",
    "/genome/[subject]/ancestry complete",
    "/genome/[subject]/ancestry empty",
    "/genome/[subject]/ancestry jurisdiction-unavailable",
    "/genome/[subject]/ancestry not-covered",
    "/genome/[subject]/ancestry processing",
    "/genome/[subject]/data complete",
    "/genome/[subject]/data empty",
    "/genome/[subject]/data jurisdiction-unavailable",
    "/genome/[subject]/data processing",
    "/genome/[subject]/data/browser complete",
    "/genome/[subject]/data/browser empty",
    "/genome/[subject]/data/browser jurisdiction-unavailable",
    "/genome/[subject]/data/browser partial-coverage",
    "/genome/[subject]/data/browser processing",
    "/genome/[subject]/reports consent-required",
    "/genome/[subject]/reports empty",
    "/genome/[subject]/reports jurisdiction-unavailable",
    "/genome/[subject]/reports partial-coverage",
    "/genome/[subject]/reports processing",
    "/genome/[subject]/reports complete",
    "/genome/[subject]/reports/[slug] complete",
    "/genome/[subject]/reports/[slug] jurisdiction-unavailable",
    "/genome/[subject]/reports/[slug] empty",
    "/genome/[subject]/reports/[slug] not-covered",
    "/genome/[subject]/reports/[slug] processing",
    "/legal complete",
    "/legal/[artifact] complete",
    "/legal/[artifact]/diff/[from]/[to] complete",
    "/legal/[artifact]/versions/[version] complete",
    "/legal/appeals complete",
    "/legal/consent/[key] complete",
    "/legal/consent/[key]/diff/[from]/[to] complete",
    "/legal/consent/[key]/v/[version] complete",
    "/legal/consents complete",
    "/legal/deceased complete",
    "/legal/future-person complete",
    "/legal/gdpr complete",
    "/legal/gina complete",
    "/legal/incident-response complete",
    "/legal/insurance-and-discrimination complete",
    "/legal/law-enforcement complete",
    "/legal/research-consent complete",
    "/legal/self-hosting complete",
    "/legal/state-genetic-privacy complete",
    "/legal/where-inherit-works complete",
    "/overview awaiting-choice",
    "/overview empty",
    "/overview jurisdiction-unavailable",
    "/overview partial-coverage",
    "/overview complete",
    "/overview not-covered",
    "/genome/[subject]/reports not-covered",
    "/genome/[subject]/data/browser not-covered",
    "/genome/[subject]/data partial-coverage",
    "/genome/[subject]/reports/[slug] partial-coverage",
    "/overview processing",
    "/privacy complete",
    "/providers complete",
    "/science complete",
    "/science/limits complete",
    "/science/positions complete",
    "/settings complete",
    "/settings processing",
    "/settings/consents complete",
    "/settings/consents empty",
    "/settings/consents processing",
    "/settings/copilot complete",
    "/settings/copilot processing",
    "/settings/data complete",
    "/settings/data processing",
    "/terms complete",
    "/withdraw/[token] complete"
  ],
  "unhashableAttestationFieldsNote": "Request fields the register requires that nothing can honestly produce, because the artifact they attest to does not exist. D-083. `policy.jurisdiction` appears nowhere in this repository outside docs/route-register.json \u2014 not in src/, not in the docs, not in a test \u2014 so there is no published version to name and no body to hash; these bodies carry a plain `jurisdictionCode` instead. Owner decision 2026-09-13: record the divergence and keep the code, rather than author a legal artifact to satisfy a field or drop a commitment the brief made about proving which jurisdiction a person accepted under. scripts/route-gate.ts compares this list with the register in BOTH directions, so a sixth route declaring the fields fails until it is recorded here, and the day the artifact is authored and the fields are really served, every row here fails until it is removed.",
  "unhashableAttestationFields": [
    {
      "routeId": "auth.sign-up-submit",
      "path": "/api/auth/sign-up",
      "fields": [
        "jurisdictionAttestationVersion",
        "jurisdictionAttestationHash"
      ],
      "served": "jurisdictionCode"
    },
    {
      "routeId": "api.consent-reaffirm-rights",
      "path": "/api/consents/reaffirm/session",
      "fields": [
        "jurisdictionAttestationVersion",
        "jurisdictionAttestationHash"
      ],
      "served": "jurisdictionCode"
    },
    {
      "routeId": "api.consent-grant-rights",
      "path": "/api/consents/grant/session",
      "fields": [
        "jurisdictionAttestationVersion",
        "jurisdictionAttestationHash"
      ],
      "served": "jurisdictionCode"
    },
    {
      "routeId": "api.invitation-accept",
      "path": "/api/invitations/accept",
      "fields": [
        "jurisdictionAttestationVersion",
        "jurisdictionAttestationHash"
      ],
      "served": "jurisdictionCode"
    },
    {
      "routeId": "api.withdraw",
      "path": "/api/withdraw/[token]",
      "fields": [
        "jurisdictionAttestationVersion",
        "jurisdictionAttestationHash"
      ],
      "served": "jurisdictionCode"
    }
  ],
  "unreadRequiredHeadersNote": "A header the register marks required is part of the contract a caller is held to, so one that no module under src/ so much as names is a promise with no keeper. scripts/route-gate.ts check 4d compares this ledger in both directions: a third unread required header fails until it is recorded here, and the day one of these is really read, its row here fails as stale. Both rows are the same header on the two chunk-upload routes, and it is recorded rather than deleted from the register for the reason check 4b records the unhashable attestation fields: the register goes on saying what it wants, and this says why nothing serves it and what serving it would take. The header beside it on both routes, X-Inherit-CSRF, is built and in production use \u2014 src/lib/embryos/operation-token.ts mints and verifies it and sixteen modules name it \u2014 so this is not a gap in the chunk contract as a whole.",
  "unreadRequiredHeaders": [
    {
      "routeId": "api.embryo-ingest-chunk",
      "path": "/api/embryo-ingest/[session]/chunks/[sequence]",
      "header": "X-Inherit-Chunk-Nonce",
      "registerSource": "requestContract.requiredHeaders: 'one-time-token-bound-to-session-sequence-declared-content-length-and-chunk-sha256'",
      "why": "Three of those four bindings cannot be expressed by anything this repository has. private.consume_embryo_operation_nonce_v1(p_nonce text, p_account_id uuid, p_session_id uuid, p_operation text, p_target_kind text, p_target_id uuid) is the only nonce primitive, and it has no parameter for a sequence, a byte count or a content hash; the sealed envelope in src/lib/embryos/operation-token.ts carries the same six fields and no more, over closed unions that contain no ingest operation and no chunk target kind. The fourth binding is worse than inexpressible: a token bound to the chunk's sha256 can only be minted once the browser holds the bytes, so minting it needs a prior round trip carrying that hash, and that round trip is private.reserve_embryo_ingest_chunk_v1 - the call the token would be protecting.",
      "servedInstead": "public.embryo_ingest_chunks enforces all four values structurally and on every call. (session_id, sequence) is its primary key; sequence must equal embryo_ingest_sessions.expected_next_sequence with no other reservation outstanding; byte_count is checked against declared_capacity_bytes, the 200,000,000-byte session cap and the 50-chunk cap; and a retry at an existing sequence whose sha256 differs calls private.mark_embryo_ingest_failure_v1(session, 'chunk'), which fails the whole session rather than the one request. A replay of the identical bytes is an idempotent resume that allocates no new capacity and no new objects. Authority is re-established per request by public.authorize_embryo_ingest_request_v1, which matches id, account_id, originating_session_id, cookie_hash and origin in one predicate and re-runs private.embryo_ingest_binding_failure_v1, so a revoked auth session or a stale cohort revision fails the chunk.",
      "closing": "An owner decision. Either the register drops this header, and ADR 0020 decision 9 becomes the record of why; or a new nonce primitive is designed that can bind a sequence and a content hash, together with the extra round trip needed to mint one - which would duplicate the reservation call that already carries exactly those values.",
      "intentEvidence": "The intent was real and the repository disagrees with itself about it. src/lib/embryos/ingest-http.ts:106 says to call readIngestChunk \"only after live authority and the two operation tokens have passed\", while src/lib/embryos/guards.ts:14 - the module holding the checks every embryo route runs, in one place so they cannot drift between routes - describes \"the one operation token per request\", which is the convention all four shipped embryo routes follow through csrfOperation. The second token exists in one comment on an unbuilt path and in no primitive; the ingest-http sentence should be corrected by whichever change settles this."
    },
    {
      "routeId": "api.evidence-chunk",
      "path": "/api/evidence/[session]/chunks/[sequence]",
      "header": "X-Inherit-Chunk-Nonce",
      "registerSource": "requestContract.requiredHeaders: 'one-time-token-bound-to-session-sequence-declared-content-length-and-chunk-sha256'",
      "why": "The same header on the legal-evidence ingest path, where less can be concluded: that subsystem has tables (legal_evidence_ingest_sessions, legal_evidence_fragments and six more) but no chunk table, no reserve or commit RPC and no src/lib/evidence, so there is nothing yet for a nonce to be consumed by. The embryo finding above is not carried over to it, because the mechanism that makes the nonce redundant there - the chunk reservation row - has no counterpart here.",
      "servedInstead": "Nothing yet. This route is unbuilt and so is the transport beneath it.",
      "closing": "The legal-evidence ingest slice being designed. Whoever designs it answers this header for this route on its own evidence; if it lands on the embryo answer, both rows go at once."
    }
  ],
  "taskDepthCeilingDivergenceNote": "navigationContract.taskDepthActions and scripts/comprehension/bindings.json both take their action ceilings from docs/inherit-v2-brief.md, and the brief gives T9 two different numbers in two different sentences. The task-depth rule classes T9 as a consent task and caps it at 6 actions; T9's own task statement ends 'Maximum 8 actions.' The register transcribed the rule, the bindings transcribed the statement, and each is faithful to what it read. Resolving it means deciding which brief sentence governs, which is an owner decision and not a gate's to make, so the disagreement is recorded here and scripts/route-gate.ts holds it in both directions: a new ceiling disagreement fails, and changing either number without updating this entry fails too. Until it is settled a protocol run graded against the bindings would pass a participant at 8 actions on a task the register caps at 6, on the accountless rights journey \u2014 the one path a person with no account has to find and delete what is held about them.",
  "taskDepthCeilingDivergence": [
    {
      "taskId": "T9",
      "registerCeiling": 6,
      "boundMaxActions": 8,
      "registerSource": "docs/inherit-v2-brief.md task-depth rule: 'consent tasks T4, T9 <= 6'",
      "bindingsSource": "docs/inherit-v2-brief.md T9 task statement: 'Maximum 8 actions.'",
      "closing": "An owner decision on which brief sentence governs, then one edit to the brief and one to whichever derived file was following the losing sentence."
    }
  ]
}
