# Defect ledger

Append-only. Every admissible defect has an id, the lens that found it, a
reproduction, a severity and a status. Simulated lenses are labelled
`simulated: true` and are never described as user observations.

| Id | Date | Lens | Surface | Reproduction | Severity | Status |
| --- | --- | --- | --- | --- | --- | --- |
| D-001 | 2026-09-03 | on-call engineer | branch topology | `git merge-base --is-ancestor origin/main origin/codex/readability-incident-response-remediation` is false: 32 verified commits were unreachable from `main` | high | fixed (pull request #41) |
| D-002 | 2026-09-03 | on-call engineer | `pnpm gate:readability` | 12 legal long blocks above grade 11 on `/terms`, `/legal/self-hosting`, `/legal/state-genetic-privacy` | medium | fixed (`a4e58b9`) |
| D-003 | 2026-09-03 | on-call engineer | `/science` | The report chips link to `/science#evidence` and the list's "Why?" to `/science#polygenic`; `src/app/(marketing)/science/page.tsx` had neither anchor | medium | fixed (`47bc818`) |
| D-004 | 2026-09-03 | on-call engineer | `pnpm gate:readability` | Extending the extractor to `src/copy/**/*.ts` and `src/emails/` surfaced 32 findings the JSX-only scan had never seen (29 unregistered plain words, one grade-9.1 mandated sentence, one grade-10.5 e-mail sentence, one contraction read as `dont`) | medium | fixed (`47bc818`, `7810ea6`, `31e1d7c`) |
| D-005 | 2026-09-03 | on-call engineer | `pnpm gate:readability` | Removing the stripped tokens `wont`/`doesnt` before the contraction rule landed broke the gate on `What we won't do.` at commit `7810ea6` (caught in the isolated pre-push verification, never on CI) | low | fixed (`4a3e0c6`, superseded by `31e1d7c`) |
| D-006 | 2026-09-03 | adversarial review (brief, privacy lenses) | `/genome/[subject]/reports/[slug]` | h1, breadcrumb and title carried the gene suffix; no category eyebrow; support panel nested inside "How sure we are"; provenance absent from "Where this comes from"; array explanation chosen when a VCF was present; density markers not on the selectors' elements; two generic bullets | medium | fixed (review-fix batch, this branch) |
| D-007 | 2026-09-03 | adversarial review (brief lens) | `/genome/[subject]/reports` | list header rendered the per-variant VCF sentence with no variant; search input removed against §4.4 item 7 | medium | fixed (review-fix batch) |
| D-008 | 2026-09-03 | adversarial review (privacy lens) | subject bar, Overview | an accepted-invitation record (owner null, subject account = invitee) was shown to the invitee as another adult "Uploaded with their permission" and on the bar as "Shared with you"; `minor` records got a permission chip; "Add a file" on other records uploaded to the caller's own record | high | fixed (review-fix batch) |
| D-009 | 2026-09-03 | adversarial review (brief lens) | `/overview` | an annotated file hid a second upload in flight (State C won over B); "regions found" counted with an invented 2% threshold; strings duplicated from their canonical homes; a second disclaimer in the sidebar | medium | fixed (review-fix batch) |
| D-010 | 2026-09-03 | adversarial review (brief lens) | all new surfaces | runtime links are literal paths; `src/lib/primary-routes.ts` (§5.5) does not exist | medium | **closed 2026-09-13, and half of it was stale.** The module exists and 38 files import it; that clause had been true when the defect was written and was not re-read since. The surviving half was real: **33 links across 20 files still spelled a path the module can build**, including `/settings` from the privacy policy, the terms and the deceased-customers page, `/settings/copilot` from the Copilot panel, and `/overview` and `/providers` from the header and footer arrays every page renders. All 33 now consume a route id. What holds it is a sweep in `src/lib/primary-routes.test.ts` that reads every source file for a literal href matching a builder's pattern, in both spellings — the JSX attribute and the `{ href: "/overview" }` object property the nav and footer use, which the first version of the sweep missed. **Its scope is stated rather than implied:** it covers the 22 builder routes with no `[param]` segment, so a literal `/genome/me/reports` and the register's other ~130 routes, which have no builder to consume, are outside it and are not claimed fixed. |
| D-011 | 2026-09-03 | review-fix agent (report surfaces) | `/genome/[subject]/reports/[slug]` | the six skeleton sections sit 40px apart (`space-y-10`) while `docs/density-baseline.json` requires ≥96px between adjacent top-level sections at ≥1024px | low | fixed (this branch): the skeleton container and the library's category sections use the baseline gaps (64px below 768px, 80px to 1023px, 96px from 1024px); `e2e/report-skeleton.spec.ts` measures ≥96px between the six report sections and between the category sections at 1280×800 |
| D-012 | 2026-09-03 | CI run 33750829586 | `e2e/search.spec.ts` | the caffeine test pressed Enter on the first Reports result and expected the CYP1A2 template, but two seed templates match "caffeine" and the ADORA2A one ranks first; the spec now follows the specific link (product behaviour unchanged) | low | fixed (this branch) |
| D-013 | 2026-09-03 | capability audit (read-only agent) | `docs/` | `docs/capability-register.md` did not exist although X15/G7.4 bind it and `docs/canonical-artifacts.md` names it as the authority for ship/withhold status; six capabilities and three declared gaps had no written status anywhere | medium | fixed (this branch): register created with one row per capability, `not shipped` rows named explicitly, and a declared-gaps table |
| D-014 | 2026-09-03 | capability audit (read-only agent) | `/genome/[subject]/reports` | every seeded template is `layer='estimate'`, so the "Specific variants" layer has no templates and its tab and counts render absent; a reader cannot tell that Inherit reads specific variants at all from the list | low | fixed (branch `medicines`, ADR 0021): eleven `variant_call` templates seed the layer; the tab renders with its counts and definition; the acceptance-matrix rows that read the layer (G4.5, A4/G1.15) are updated on landing |
| D-015 | 2026-09-03 | capability audit (read-only agent) | `/genome/[subject]/reports` | the "Medicines" category exists in the taxonomy with zero templates and is dropped from the DOM entirely; no sentence tells the reader pharmacogenomics is absent, which X15 forbids ("renders absent, not empty — it is either populated or registered as `withheld` with a dossier") | medium | fixed (this branch): pharmacogenomics reached a terminal status. `docs/withheld/pharmacogenomics.md` carries the nine elements with the obstacle classified safety (primary) and scientific (supporting), three designs actually built under `docs/withheld/pharmacogenomics/designs/` with the verbatim command, exit code and output of `pnpm gate:templates` and `pnpm gate:readability` for each, ADR 0018, and the register row `withheld`. The list still states the withholding in one place, now as “Inherit does not offer reports about medicines. …” (`MEDICINES_ABSENT`), re-pinned by `e2e/report-skeleton.spec.ts`, which also asserts the sentence carries none of “coming soon”, “soon”, “yet” or “currently”. |
| D-016 | 2026-09-03 | capability audit (read-only agent) | `/legal/appeals` | the sentence "After a relative objects, Inherit turns off relative matching, pages that others can see about that relative, and shared-segment output" implies relative matching and shared-segment output exist; Inherit computes and displays neither (X15 declared gap) | medium | fixed (this branch): the sentence now names what is switched off (every page and result others can see about the relative) and states that Inherit does not match relatives or compute shared DNA; the declared gaps are published at `/science#not-offered` and `e2e/legal.spec.ts` pins both |
| D-017 | 2026-09-03 | W7 part B (fixture generation) | `src/lib/genome/admixture.ts` | the synthetic 168-marker fixture drawn at EUR 0.6 / AFR 0.3 / EAS 0.1 per allele copy is estimated as AMR 0.652 / AFR 0.255 / EAS 0.088 / EUR 0.005 / SAS 0.000: the estimator attributes most European-drawn alleles to the admixed-American reference, so the shipped panel cannot tell those two references apart at this marker count | medium | **THE DESCRIPTION IS WRONG AND THE AREA COLUMN WAS RIGHT, measured 2026-09-14.** Over 200 seeds of exactly the fixture described, drawn by the committed helper in `admixture.test.ts`, the recorded outcome does not occur once: EUR comes back at mean 0.522 (median 0.530, range 0.293-0.800), AMR at mean 0.042 (median 0.000), and **AMR exceeds EUR in 0 of 200 seeds**. What reproduces the recorded numbers is one character of the draw: taking the ALT allele with probability `1 - freqs[pop]` instead of `freqs[pop]` gives EUR 0.000 / AMR 0.588 / AFR 0.139 / EAS 0.273 — the same signature, EUR collapsed and AMR holding the bulk. This is fixture generation, which is what the area column always said; the description then blamed the panel and the remedy chosen from it was to select and re-fit a new marker set behind a licence-audit gate. Not a fix that landed in between: `src/lib/genome/admixture.ts` and `data/ref/aims.json` are byte-identical to 2026-09-03 (the one commit touching either since, 319a214, only added RELIABLE_FRACTION). Both draws are now exercised by `admixture.test.ts` so the distinction is held by code. **What remains is real and much smaller**: at 168 markers the spread is wide (0.293-0.800 for a truth of 0.600) and a mild downward bias persists as markers rise (mean EUR 0.522 at 168, 0.551 at 336, 0.565 at 672, 0.580 at 1,344, 0.584 at 2,688). So the work is the marker-subsampling interval this row's own remedy line already named, not a new panel. **That half is now built and this row is closed, 2026-09-14.** Each share carries a pivotal bootstrap interval over the markers the file supplied, measured against the shipped estimator over 14,400 region-estimates: the true share falls inside it 91.5% of the time overall, 86.2% in the weakest band and 94.9% in the strongest (`docs/ancestry-interval.md`, reproduced by `scripts/ancestry-interval/measure.mts`). The pivot is the design decision and the percentile method was measured and rejected: it has no mechanism for the inward bias a 168-marker panel produces, and its interval contained the truth in 0 of 30 seeds for a person drawn as entirely one population. The surface prints 91.5% as "about nine times in ten" and never the nominal 95%, beside the limit of the test that produced it — the simulated people are mixtures of exactly these five references, which is the estimator's own assumption, so the figure is an upper bound. What remains after this is the panel: the estimator is consistent (0.948 at 168 markers rising to 0.993 at 5,376 for a truth of 1.000), so the width is what 168 markers can tell and narrowing it further is the expensive option this row already named, which stays the owner's call. |
| D-018 | 2026-09-03 | orchestrator (matrix review) | `docs/acceptance-matrix.md` | rows G4.1–G4.3, G4.6 and G4.7 carry paraphrased statements that do not match the brief's G4 gate texts (G4.3 in the brief is the count-class gate, G4.6 the input-quality provenance block, G4.7 citations with access dates); G4.4 and G4.5 were realigned in this pass | low | fixed (this branch): G4.1, G4.2, G4.3, G4.6 and G4.7 now carry the brief's gate text; each row's evidence was re-audited against the tree and names exactly what is built (the relative-figure component and ladder with no rendering template; `data-figure-basis` and the once-per-block marker with no E2E count; the single-class `<Count>` under X5.1's names with no automated gate; the coverage sentence, pills and fixed provenance line with no call rate or liftover status; PMID/DOI citations on every template with no access date). All five stay NO |
| D-019 | 2026-09-03 | orchestrator (G4.4 audit) | `src/components/results/ancestry/lineage-card.tsx` | a haplogroup call renders its matched-of-tested markers as a coverage figure but not the reference tree's version nor an explicit statement that no interval exists, which G4.4 requires of every quantity derived from a reference panel | low | fixed (this branch): each called line names its tree and version (`LINEAGE_TREES`), states "Inherit puts no range on this name…" and gives the tree's resolution limit, beside the existing matched-of-tested coverage figure |
| D-020 | 2026-09-03 | CI run 33775768209 | `src/app/api/files/[id]/process/route.ts` | the admixture row gained `model_id`, `model_version`, `coverage` and `result_state` while the two haplogroup rows in the same bulk insert did not; PostgREST fills a key that some rows omit with null rather than the column default, so `result_state` violated NOT NULL (Postgres 23502) and every file's processing failed, taking ten browser specs with it | high | fixed (this branch): every ancestry row states the four columns explicitly through one `AncestryRow` type (haplogroup rows: no model id, coverage from matched-of-tested, `available` / `partial` / `not_covered`); the unit suite cannot reach this because the database path is exercised only against the real local stack in CI |
| D-021 | 2026-09-03 | CI run 33778596222 | `e2e/genome-data.spec.ts` | the data-page test forbade any percent sign inside a score's list item, but each score renders its seeded ancestry-portability statement, which quotes the source cohort's composition ("53.3% multi-ancestry…"); the assertion was wrong, not the page | low | fixed (this branch): the note carries `data-slot="ancestry-note"` and the exempt comment (provenance about the score's cohort, not a result about the subject); the spec asserts one note per score and no percent text outside it |
| D-022 | 2026-09-03 | orchestrator (G4.4 audit, reading the haplogroup provenance) | `data/ref/haplogroups/`, `docs/dataset-licenses.md` | the licence audit had no row for either haplogroup reference source, and the two records disagree: the repository's provenance note asserts PhyloTree is "free for academic/non-commercial use with citation", while the publisher's own page, read 2026-09-03, states no copyright, licence or terms at all and asks only for a citation. The audit's standing rule is that no non-commercial-licensed source may enter the reference store or seed data, and the mtDNA tree's topology and defining positions are seed data | high (legal surface) | **closed 2026-09-13, and the wrong record was ours.** The owner read both and chose the correction: the publisher imposes no non-commercial term, so the audit's bar on non-commercially-licensed sources was disqualifying this source on the strength of a restriction nobody imposed. `data/ref/haplogroups/PROVENANCE.md` now states what the page states — no copyright, licence or terms, citation asked — with the read date and the correction date, and says in terms that the old wording was ours. `docs/dataset-licenses.md` moves from Verification outstanding to Use. **What this is not:** a legal opinion. Data published with no terms is not data published permissively, both files say so, and counsel may revisit; what changed is that a factual claim about someone else's page is no longer false. The MIT licence on the machine-readable copy still covers that copy's packaging, not the tree. Filed as: resolving it needs the publisher's actual terms (or counsel's reading of data published with none), after which either the provenance note or the maternal-line capability's status changes. The MIT licence on the machine-readable copy covers that copy's packaging, not the tree |
| D-023 | 2026-09-03 | orchestrator (F1 review, reading the grant routine) | `/family/[person]/permissions` | the Portrait row rendered "Turn on", but `grant_directional_purpose_v1` refuses `family.portrait` while the signer's `independent_login_at` is null, and nothing in `src/` ever called `mark_independent_login_v1`: the register places the marker in `auth.callback`, while the password sign-in completes in the browser and never reaches that route, so every Portrait grant would have answered 409 (a control without its mechanism, brief §9 item 21) | medium | fixed (this branch): `src/lib/family/independent-login.ts` stamps the marker from the callback exchange and on the first server-rendered permissions visit (the routine itself proves the session post-dates the acceptance); until it is stamped the Portrait row renders locked with "Portrait needs a sign-in of your own first…" and no control; `e2e/family.spec.ts` asserts the locked row in the acceptance session and the successful grant after a fresh sign-in |
| D-024 | 2026-09-03 | CI run 33787980603 | `e2e/adult-subject-invitation.spec.ts` | the assertion F1 added ("the pre-consent statement sits above the form") located `form` unscoped, which resolves to two elements under the app shell: the account landmark's sign-out form and the invite form; Playwright's strict mode throws | low | fixed (this branch): the locator is scoped to the form containing the email field; the product is unchanged. The same unscoped locator in `e2e/family.spec.ts`'s invite test was masked by the hub failure in the first run and surfaced in run 33789839105 once that passed; scoped the same way |
| D-025 | 2026-09-03 | CI run 33787980603 | `e2e/family.spec.ts` | `expectAxeClean` emulated the dark colour scheme on the already-loaded hub and audited at once; `next-themes` flips the class on the live page and the chrome's `transition-colors` animates for 150 ms, so axe sampled mid-transition text and background colours and reported a serious `color-contrast` violation in dark only. Every other spec loads the page after emulating the theme | low | fixed (this branch): the helper reloads the page in each theme before auditing, as `e2e/a11y.spec.ts` and `e2e/ancestry.spec.ts` do |
| D-026 | 2026-09-03 | CI run 33790730613 | `/family/[person]/permissions` | each row control carried `aria-label="{label} for {name}"`, which replaces the visible "Turn on" / "Turn off" as the accessible name: the browser spec's `getByRole("button", { name: "Turn on" })` could never match (the invite/accept test timed out after 120 s), and the control failed WCAG 2.5.3 (the visible label must be part of the accessible name) | medium | fixed (this branch): the accessible name is now "Turn on {label} for {name}" / "Turn off {label} for {name}" (`rowControlLabel(verb, label, name)`), pinned by `src/components/family/family.test.ts`; the spec's locators are unchanged and now match |
| D-027 | 2026-09-03 | F3 libraries agent (running the Family unit suites) | `src/lib/family/tier2.test.ts` | the tamper assertion replaced the digest's last character with a literal `0`, so whenever the real digest already ends in `0` the "tampered" cookie equals the real one and the test fails; the key is random per run, so the failure is intermittent | low | fixed (this branch): the replacement character is chosen to differ from the real last character |
| D-028 | 2026-09-03 | Family permissions (Portrait row) | `mark_independent_login_v1` excluded only subjects that an accepted invitation targeted, but the Portrait guard reads the data subject's own `self` record, which no invitation targets. Reproduced in CI run 33792405267 (`e2e/family.spec.ts:341`): the invited adult's first request in the acceptance session stamped their self record, so the Portrait row was settable from the session the invitation was accepted in. | high (a sharing guard that never engaged) | fixed (this branch): migration `20260903190000_independent_login_per_account.sql` makes the rule per account (any session that predates an adult-subject invitation this account accepted stamps nothing), with four pgTAP assertions |
| D-029 | 2026-09-03 | Family permissions | No screen could grant `family.heritability`: the five permission rows are the report layers, ancestry, raw data and Portrait, so the two-column health picture was reachable only by a service-role call in the browser suite. Found while reviewing F2's report. | medium | fixed (this branch): a sixth row, "Health picture", on the same directional own-session rules, locked with Portrait until the independent-login marker is set (ADR 0017 §8) |
| D-030 | 2026-09-03 | Carrier panel (runs of homozygosity) | `src/lib/family/roh.ts` measured runs at request time under a 20,000-call read budget, so every real array or sequence file answered `not_measurable` and the 25-in-100 branch could never render in production. Found by the adversarial review of `c6dd140` (carrier-rule lens). | high | fixed (this branch): measured once at ingest from the parsed calls and stored on `genome_files` (migration `20260903200000_genome_files_runs_of_homozygosity.sql`); the reader asks the stored measure of every annotated file of the subject |
| D-031 | 2026-09-03 | Carrier panel (X-linked pattern) | Brief line 346 requires the X-linked arithmetic across 100 pregnancies; the split depends on which parent carries the change on the X, and nothing in Inherit records a person's chromosomal sex. The panel renders the refusal sentence with the reason `sex-unknown` instead. | medium (science and product) | fixed (this branch, 2026-09-14): `public.declare_chromosomal_sex_v1` is the one writer, reached only by `POST /api/chromosomal-sex` from the control on `/settings`, and `subject_demographics.chromosomal_sex` has one reader, `src/lib/family/chromosomal-sex.ts`. DECLARED, never derived — nothing reads X or Y coverage, which diverges from the permission at `brief:1833` on purpose (`docs/protocol/decisions.md`, 2026-09-14). Only the subject may declare, never the account that holds their record; withdrawal is the same call with null. An X-linked pair whose two people have each declared gets `xLinkedCross`; the one reason becomes three, told apart by what would change the answer. 22 pgTAP assertions, mutation-tested against eight mutants with no survivors; browser proof in `e2e/settings.spec.ts` (`12 passed`, no skips, no retries) |
| D-032 | 2026-09-03 | Carrier panel (trigger and naming) | The candidate set required the same rsid in both files and the block never rendered the variant; brief line 346 triggers on a P/LP heterozygous variant in the same gene and requires naming both variants and both classifications. | medium | fixed (this branch): candidates are grouped by gene; a match carries each person's own variant, classification and copies, and the block names them (review, R2) |
| D-033 | 2026-09-03 | Carrier panel (classification) | `isPathogenicClassification` matched the exact strings only, while the reference writer stores ClinVar's list joined with ", " and ClinVar itself uses "/", so "Pathogenic/Likely pathogenic" fell to "nobody yet knows what this change means". | medium | fixed (this branch): labels are split on `/ , ; |`; pathogenic only when every token is pathogenic or likely pathogenic, harmless only when every token is benign or likely benign, anything else unknown (review, R3) |
| D-034 | 2026-09-03 | Carrier panel (empty state) | With no classified reference position (production today) the panel printed "Inherit checked the 0 positions both files cover" above a table whose cells show positions both files do cover. | high | fixed (this branch): an empty classified set renders "Inherit has no classified positions to check yet, so it cannot look for a change you both carry."; the count sentence renders only over a non-empty set (review, R6) |
| D-035 | 2026-09-03 | Carrier panel (two copies) | A person with two changed copies at a classified position was dropped from the panel, which then reported no shared change although both files showed one; brief line 346 says any failed trigger renders the refusal sentence with a reason. | high | fixed (this branch): the pair renders with no probability and the new closed-table reason `two-copies` ("one file shows two changed copies, not one"); decision recorded (review, R4) |
| D-036 | 2026-09-03 | Portrait libraries (distribution) | `distribute()` keyed the below-1-in-100 rule on the dot count after largest-remainder rather than on the share, so a 6-in-1,000 category rendered a solid dot and "about 1 would"; the lending category's sentence disagreed with the figure contract's rounding; a NaN share was filtered away instead of refused; `crossShares` promised a number for absent outcomes; `sentence: null` could pair with `denominator: 1000`. | medium | fixed (this branch): the rule keys on the share, sentences use `naturalFrequency(share, 100)`, NaN and negative shares throw, absent outcomes are absent keys, and one rounding rule decides both the sentence and the rung (review, R8) |
| D-037 | 2026-09-03 | Portrait copy | `ACKNOWLEDGE_BUTTON` was "Continue", banned standalone by brief line 928; the `runs_below_threshold` assumption statement asserted a completed check under the label "What we do not check". | low | fixed (this branch): "Open Portrait"; the statement is written as the assumption it is (review, R7) |
| D-038 | 2026-09-03 | Health picture (consent boundary) | Cells rendered another adult's genotype letters under `family.heritability` alone, and the Overview's carrier line read another adult's rows after checking `carrier_match` only; the register makes `family.heritability` authority for the joint comparison, never for an individual result layer, and `family:carrier-arithmetic` requires all three capabilities. Found by the adversarial review (browser-spec and consent lens). | medium | fixed (this branch): a cell's figure and link need the layer's own grant from that person, else "Not shared with you"; the Overview checks the three capabilities before any read (review, S3–S4) |
| D-039 | 2026-09-03 | `e2e/family-health-picture.spec.ts` | Tests after the second never passed the Tier-2 gate in their own browser context (the gate cookie is keyed to the auth session), the dark-theme axe audit repeated the D-025 pattern, and the independent-login marker was forged with a raw update instead of the routine. Found by the adversarial review before the spec ever ran in CI. | medium (test) | fixed (this branch): a gate helper per test, a reload per theme, the real `mark_independent_login_v1` call (review, S1, S2, S5) |
| D-040 | 2026-09-03 | Carrier panel (runs of homozygosity) | The measure counted any two adjacent same-reading calls as a run and summed their spans, so an array-density file always exceeded the brief's 100 Mb and 0.0156 thresholds (the repository's own `data/samples/synthetic_23andme.txt`: 124 runs, 241 Mb, F_ROH 0.055) and the 25-in-100 branch was reachable only for a sparse VCF; a differences-only VCF with adjacent 1/1 rows counted as measured although the positions between its rows are unrecorded. Found by the adversarial review's verification pass after D-030. | high (science) | fixed (this branch): runs follow McQuillan et al. 2008 (doi:10.1016/j.ajhg.2008.08.007): ≥ 1.5 Mb, ≥ 25 contiguous same-reading calls, at most one heterozygous call inside a run; a file that reports no reference-homozygous call is `not_measurable` (`no-reference-calls`); the denominator stays the autosomal span the file covers and is stated as Inherit's own choice |
| D-041 | 2026-09-03 | Family permissions | The lock reason read "Portrait needs a sign-in of your own first…" on the new Health picture row too, and `e2e/family.spec.ts` picked the Portrait row by a substring filter that this text now matched twice (CI run 33807524096, the first on PR #42). | low | fixed (this branch): the reason is row-neutral ("This row needs a sign-in of your own first…"), the label carries `data-slot="permission-label"`, and the spec filters rows by their exact label |
| D-042 | 2026-09-04 | Copilot output guard | The route checked only `text-delta` chunks and forwarded reasoning parts and tool-input JSON to the client unchecked, so a fabricated number in a reasoning part or a model-authored rsID in a tool input bypassed brief line 2262. Found by the adversarial review (findings 1 and 12). | high | fixed (this branch): `toUIMessageStream` runs with `sendReasoning: false` and `foldStreamChunks` folds text, reasoning, tool inputs, sources and provider-run tool outputs into the one string `checkResponse` reads; a failing turn is replaced whole |
| D-043 | 2026-09-04 | Copilot intent gate | The diagnosis rules' file-fact exemption tested the whole message, so "Do I have haemochromatosis? My file has the variant." was allowed, while "Do I have Neanderthal DNA?", "Do I have rs762551?" and "Do I have MTHFR?" were refused. Found by the adversarial review (finding 5). | high | fixed (this branch): the exemption reaches only the object of the rule's own "have" (a determiner, up to two qualifying words, a file-fact word, an rsID or a capitalised gene symbol); paraphrases ("am I a diabetic", "so I have X then", "is that a disease", "does my genome say I have") are gated |
| D-044 | 2026-09-04 | Copilot intent gate | A comparative about embryos ("Which of them is healthier?", "Is embryo 3 better than embryo 5?", "Compare embryo 2 with embryo 4") reached the model in a cohort scope; a ranking by another name. Found by the adversarial review (finding 6). | high | fixed (this branch): `selection.comparative`, `selection.which-of-them` and `selection.compare-embryos`; a sex question that also compares keeps the sex refusal |
| D-045 | 2026-09-04 | Copilot intent gate | An unspaced dose ("500mg", "1000IU"), "too much iron", "double my folic acid", "any supplement recommendations", "what supplements" and "should I be taking" were not treatment questions to the gate. Found by the adversarial review (finding 7). | high | fixed (this branch): `treatment.dose` reads `\d+\s*unit`, plus `treatment.too-much`, `treatment.adjust-intake` and `treatment.which-supplement`; a genotype letter after a digit ("1 G and 1 A") is not a unit |
| D-046 | 2026-09-04 | Copilot intent gate | The Portrait rule refused any "will/would my child …", including the inheritance and carrier questions Portrait exists to answer ("Could our children inherit this?", "Would a child of ours be a carrier?"). Found by the adversarial review (finding 8). | high | fixed (this branch): the rule refuses trait predictions about one child (be, have, look, grow, become; "how tall will my child be") and exempts inherit, carry and carrier |
| D-047 | 2026-09-04 | Copilot chat route | A gated user turn and its refusal stayed in the client's thread and were resent as history, so the model saw every refused message on the next allowed request. Found by the adversarial review (finding 10). | high | fixed (this branch): `dropGatedTurns` classifies every user turn and drops a gated one with the assistant turn that answered it before `convertToModelMessages`; `e2e/copilot-refusal.spec.ts` reads the provider's history |
| D-048 | 2026-09-04 | Copilot citation check | "Author et al. YEAR" matched by substring ("Sule" inside "Sulem", "2011" inside "20115"), an "et al." without a year, "a study by X", "according to X" and "published in X" were not citations to the check, and `get_prs` never selected `prs_scores.citation`, so a score citation could not be permitted. Found by the adversarial review (findings 2, 4, 11). | medium | fixed (this branch): whole-token matching against the labels, the four new shapes held to the same set, report and score names (`title`, `name`) permitted, `citation` selected |
| D-049 | 2026-09-04 | `docs/route-register.json` | `copilot-refusal-v1` described a JSON body while the route serves the refusal on the UI-message-stream transport with `x-copilot-refusal`. Found by the adversarial review (findings 13, 15). | low | fixed (this branch): the register describes the served contract (status 200, `text/event-stream`, the header enum, one text part, no echo); the route is unchanged |
| D-050 | 2026-09-04 | Copilot chat route | A `minor` or `embryo` subject resolved to the `subject` guard scope although no chat scope exists for either class. Found by the adversarial review (finding 14). | low | fixed (this branch): `guardScopeKindFor` admits `self` and `other_adult`; any other class answers the opaque 404 |
| D-051 | 2026-09-04 | `e2e/copilot-refusal.spec.ts`, `e2e/mock-llm.ts` | Three treatment prompts all reached `treatment.should-i-take`, the header claimed a cohort prompt no route can send, the mock counted only `/chat/completions`, and the axe audit ran on a reloaded, empty thread. Found by the adversarial review (findings 16–19). | low (test) | fixed (this branch): one prompt per treatment rule (pinned by rule id in `guard.test.ts`), the header states the cohort limit, the mock counts every request on any path and exposes the newest history, each theme sends a gated prompt before axe |
| D-052 | 2026-09-04 | Portrait output cards | The brief's line-1349 runs refusal ("These two files look more genetically similar than usual…") rendered for the `runs-unchecked` reason, which also covers a person whose runs were never measured, could not be, or who has no annotated file: the sentence asserted a similarity nothing had measured. Found by the adversarial review (finding 1). | high | fixed (this branch): the reason table splits `runs-above-threshold` (a measured file above a threshold; the refusal) from `runs-unchecked` (the side-by-side page's "could not check" sentence); `subjectRunsState` in `src/lib/family/roh.ts` |
| D-053 | 2026-09-04 | Carrier rule | The gene-level trigger produced the exact 25-in-100 arithmetic for two changes at different positions when each file lacked the other's position, and the card printed "Both files cover the positions this uses"; brief line 1349 says a position one file does not cover is named and never imputed. Found by the adversarial review (finding 2). | high | fixed (this branch): `not-covered` names the file and the position (`uncovered`); a match carries `positionsBothCovered`, and the coverage field is the cannot-calculate sentence when it is false |
| D-054 | 2026-09-04 | Portrait page | The one-sided readings ran only when at least one classified position was shared, so a carrier whose partner's file covers none of the gene's positions, the line-2238 case itself, rendered nothing. Found by the adversarial review (finding 3). | medium | fixed (this branch): `evaluateOneSided` runs whatever `positionsBothCover` is |
| D-055 | 2026-09-04 | Portrait libraries | A match refused because the other side's change was harmless or of unknown meaning counted as the carrier rule's answer for the gene and suppressed the no-second-copy reading for the pathogenic carrier. Found by the adversarial review (finding 4). | medium | fixed (this branch): `answeredGenes` counts only a match where both readings are pathogenic or likely pathogenic; the page renders the one-sided reading in place of that refusal |
| D-056 | 2026-09-04 | Portrait output cards | The runs threshold statement sat under "What we do not check" although the runs were measured, and the viewer's own label ("You") filled third-person sentence slots ("You’s file", "You hasn’t"). Found by the adversarial review (findings 5, 6). | low | fixed (this branch): a "What we checked" field carries the runs statement on an exact block; every sentence about the viewer has a second-person form (`PersonRef`) and the placeholder never reaches a slot |
| D-057 | 2026-09-04 | Portrait blocking screen and delete dialog | The delete dialog said the page opens again only when "you both" turn Portrait on, though deleting revokes the viewer's own grant alone; the acknowledgement form rendered while a grant or sign-in was still missing. Found by the adversarial review (findings 7, 8). | low | fixed (this branch): the dialog names the one grant revoked and the one that stays; the form renders only when the acknowledgement is the viewer's one remaining step |
| D-058 | 2026-09-04 | `src/copy/family/portrait.ts` | The refusals list lacked line 356's talent, athleticism, attractiveness and skin tone, and the copy claimed the height reason was the brief's verbatim although it is a grade-9 rewrite of an example. Found by the adversarial review (findings 9–11). | low | fixed (this branch): four cards added (fifteen in all); the comment and test cite the recorded decision |
| D-059 | 2026-09-04 | Embryo closed shapes (`src/lib/embryos/policy.ts`) | `qc_reasons` accepted any string array and `not_covered_reason` any string, so a value outside the register's closed tables passed the shape and reached the renderers, which then emitted it as `data-reason`. Found by the adversarial review of E1 (R1). | high | fixed (this branch): membership of `QC_REASON_IDS` and `RESULT_NOT_REPORTABLE_REASON_IDS` is validated; a qc reason needs `quality_not_measurable` and a result-level reason `not_covered`; `registeredReason` maps every emitted id |
| D-060 | 2026-09-04 | Embryo QC source strings | `source_laboratory`, `source_assay` and the two method strings were unbounded and printed as received, although the register requires bounded reviewed labels and canonical-artifacts says an original laboratory label is never rendered. Found by the adversarial review of E1 (R2). | high | fixed (this branch): `data/embryo/source_labels.json`, a closed registry withheld and empty today; the shape accepts null or a registered id; renderers print a label's display text or "Not stated by the source laboratory" |
| D-061 | 2026-09-04 | `<CompareCell>` | The embryo's own absolute figure carried the general-population group, so one cell attributed two numbers to the population; the `measured_inconclusive` status rendered the "No one has measured…" sentence, which is false of a tested score. Found by the adversarial review of E1 (R3, R4). | medium | fixed (this branch): the embryo's figure is captioned "for Embryo N"; the inconclusive status has its own sentence with its citation beside the brief-1318 comparison sentence |
| D-062 | 2026-09-04 | `<QcTable>`, `<CompareTable>` | The QC table, a `data-card` holding many `data-subject-id` blocks, lacked `data-compare-surface` (brief line 211); the compare caption said "your DNA" about an embryo's file (X13.1); the mean depth rendered as a bare count outside the figure contract. Found by the adversarial review of E1 (R5, R6, R7). | high | fixed (this branch): the attribute, `EMBRYO_LAYER_DEFINITIONS`, and the `measure` figure kind (value, unit, decimals) with the depth inside the column's block |
| D-063 | 2026-09-04 | `e2e/embryos.spec.ts`, `playwright.config.ts` | The title assertion ignored the root layout's template; the flag-off jurisdiction refusal of design §6.2 had no browser proof; the spec header claimed the unit suite proved the gate. Found by the adversarial review of E1 (R8, R9, R10). | medium (test) | fixed (this branch): the templated title; a second Playwright project against a second server from the same build with the flag unset, running `e2e/embryos.nojurisdiction.spec.ts`; the header states what is and is not proven |
| D-064 | 2026-09-04 | `src/lib/embryos/cohorts.ts`, the result loaders | Every query's `error` was ignored, so a failed read resolved to an empty graph, a 404 or "Still checking the files". Found by the adversarial review of E1 (R11). | medium | fixed (this branch): `rowsOrThrow` and `EmbryoReadError`; the compare and detail pages render the read-failed error state |
| D-065 | 2026-09-04 | `/auth/sign-out`, the embryo gate | Sign-out deleted the Family gate cookie only; the embryo gate's write and read paths shared no code. Found by the adversarial review of E1 (R12, R10). | low | fixed (this branch): `signOutResponse` deletes both cookies; `embryoGateCookie` and `acknowledgedBy` are the one pair, tested end to end |
| D-066 | 2026-09-04 | Embryo consent sentence | A non-parent uploader with no grant from either parent read "Waiting for the other genetic parent". Found by the adversarial review of E1 (R13). | low | fixed (this branch): `analysisConsent` answers `waiting-for-both`; the sentence names "both genetic parents", or the one missing party |
| D-067 | 2026-09-04 | `src/components/uploads/uploader.tsx` | The uploader carried its own unrecognised-format and too-large sentences beside the A.6 home the canonical index names, and refused a PDF as "Unrecognized format" although the canonical PDF row has the preflight refuse it by name. Found while placing the A.6 refusals. | low | fixed (this branch): the preflight reads `sniffFileV2` and the three sentences from `src/copy/upload/errors.ts` |
| D-068 | 2026-09-04 | `docs/capability-register.md` | The Embryo ingest row said the Embryos landing ends "No embryo analysis model is currently allowlisted on this deployment.", a sentence E1 removed. Found while updating the row. | low (docs) | fixed (this branch): the row states what `/embryos/upload` and the landing render |
| D-069 | 2026-09-04 | `e2e/README.md` | The suite index had no row for `embryos.spec.ts` or `embryos.nojurisdiction.spec.ts`. Found while adding the upload flow's assertions. | low (docs) | fixed (this branch): both rows added |
| D-070 | 2026-09-04 | `src/components/embryo/upload/upload-flow.tsx`, `src/lib/embryos/upload-flow.ts` | Two screens of the upload flow exceeded X6.1's seven interactive elements on the repository's measurement basis (nine on the options screen with the shell's search button and desktop attribution link counted). Found by CI run 33924630411 on PR #48. | medium | fixed (this branch): screens cut to five controls of their own — the first two questions on one screen, the options and bases as actions, the PDF refusal and each basis on its own screen; the reducer records the shell's two controls beside the cap |
| D-071 | 2026-09-04 | `src/copy/embryos/upload.ts` | Three flow sentences overclaimed: "Nothing is kept until both parents sign" (the design's draft step keeps a record first; three bases never have both parents sign), the closing sentence named the other parent's email and signature for every path, and a basis label and sentence spoke in a genetic parent's first person after the permission situation. Found by the adversarial review of E2 slice 1 (C3, C4, C5). | medium | fixed (this branch): sentences true on every path, third-person bases |
| D-072 | 2026-09-04 | `src/components/uploads/uploader.tsx` | A laboratory table `sniffV2` had recognised was refused as "unrecognised", whose trigger the brief binds to a null sniff; a multi-sample VCF travelled on to fail at the server; the PDF refusal promised a letter without linking it. Found by the review (C6, U3). | medium | fixed (this branch): the register's `subject_source_not_single_sample` sentence with a link to the Embryo flow for both cohort-shaped kinds; the letter link on the PDF refusal |
| D-073 | 2026-09-04 | `src/lib/genome/parsers/sniff.ts` | A `#CHROM` line cut off by the 64 KiB decode window or a truncated gzip member was read as complete, giving a wrong sample count or a cut sample name; a trailing tab counted an empty sample. Found by the review (C8, U6). | medium (latent: no runtime caller yet reads the count) | fixed (this branch): an unterminated header answers `sampleCount: null`; trailing whitespace is dropped |
| D-074 | 2026-09-04 | `src/lib/genome/parsers/pgt-table.ts` | A comma inside a quoted header cell split the cell, shifting every later column index and yielding a complete plan pointing at the wrong columns. Found by the review (C9). | medium (latent: no mapping route yet) | fixed (this branch): RFC 4180 tokenisation of the header line |
| D-075 | 2026-09-04 | `src/components/embryo/upload/upload-flow.tsx` | The "No" ending appeared in place as a newly mounted live region that nothing focused; the terminal rendered a block-role sentence as a heading; the basis sentence and the endings were live regions that double-announce or are ignored. Found by the review (C10, U1, U7). | low | fixed (this branch): focus moves to the ending's sentence, the PDF screen carries the option's label as its heading, the terminal's sentence is a focused paragraph, no live regions |
| D-076 | 2026-09-04 | `docs/adr/0020-*.md`, `src/copy/upload/errors.ts`, `docs/canonical-artifacts.md` | All three said the file processor consumes the A.6 refusals; no processor code does yet. Found by the review (C7, U8). | low (docs) | fixed (this branch): the consumers that exist are named; the processor's re-sniff and the ingest routes are marked as E0's |

| D-077 | 2026-09-05 | Schema | `subject_principals` | The identity check required a subject or an account on every non-service principal, so an invited genetic parent could not exist before accepting; the invitation mail, contact reference and slot all need that principal. | medium (blocked the co-parent path) | fixed (this branch): the check admits a pending or deleted `genetic_parent` / `identified_donor` principal with both null (`20260905103317_embryo_cohort_runtime.sql`) |
| D-078 | 2026-09-05 | Schema | `attestations.kind` | No kind existed for `attestation.embryo-disposition-rights`, so the matrix's third parent artifact had no typed row. | low | fixed (this branch): `disposition_rights` added to the check |
| D-079 | 2026-09-05 | Mail | `public.claim_mail_outbox` | The delivery-token branch ran only for `adult-subject-invitation`, so a co-parent invitation would have been sent without a link. | medium | fixed (this branch): the branch and the pending-principal invalidation clause cover both invitation purposes |
| D-080 | 2026-09-05 | SQL | `private.consume_embryo_operation_nonce_v1` | The nonce pattern `{16,256}` exceeds PostgreSQL's regular-expression repetition limit of 255 and failed at first call; found by running pgTAP on the local stand-in cluster. | medium (every RPC would have failed) | fixed (this branch): a character-class match plus a length check |
| D-081 | 2026-09-05 | Rights | `src/app/api/jobs/mail/route.ts` (adult invitation) | The adult invitation mail carries the raw token in the URL path (`/withdraw/<token>`), which the register forbids (`urlCredentialInterstitial`: fragment only); embryo mails use the fragment form. | medium (pre-existing) | **closed 2026-09-13, merged as #118.** The adult token no longer travels in a URL path: the mail links at `/withdraw/request#<token>`, the adult purpose opens a rights session through `api.rights-activate`, and `/withdraw/session` answers confirm, refuse and delete off the cookie. What remains is a dated deletion rather than open work — `/withdraw/[token]` and the bare `POST /api/withdraw` stay until the last token mailed before 2026-09-13 expires at 30 days, a browser test holds that path open until then, and both `docs/route-divergence.json` rows carry the deletion condition. Filed and re-scoped as: the interstitial and `api.rights-activate` this row said the adult path "needs" now EXIST and the co-parent path uses them end to end (D-084, closed the same day). What is left is not building them — it is moving the ADULT token onto the same issuance model, which is the harder half. Re-measured 2026-09-10, the defect has two surfaces, both recorded in `docs/route-divergence.json` and held by `scripts/route-register-correspondence.test.ts`: the `/withdraw/[token]` directory implements as an open bearer segment what `rights.withdraw` pins to the literals `request` and `session`, and the pre-migration `POST /api/withdraw` has no register entry at all (`api.withdraw` is `/api/withdraw/[token]` with token fixed to `session`, which `src/app/api/withdraw/session/route.ts` already implements correctly). Closing it is larger than a link change: adult invitations store `token_hash` directly on `subject_invitations`, while `activate_rights_session_v1` resolves only co-parent invitations through `token_hashes`/`token_candidates` and hard-codes the purpose, so the adult token must first move onto the `mail-token-delivery-v1` issuance model. Already-mailed tokens live 30 days, so the old path cannot be removed the same day the new one lands. **Re-measured 2026-09-10 by reading the code, and it has two more surfaces than this entry listed plus one stale blocker.** (a) `src/app/(marketing)/withdraw/[token]/page.tsx` calls `adultInvitationAvailable(token)` on a plain GET, so a link scanner or prefetcher that follows a mailed URL gets a live availability oracle keyed by that URL — against the register's own `tokenSecurityContract.activation`, `scanner-prefetch-GETs-cannot-see-burn-or-use-a-token`. (b) The server mints two further URLs carrying the same raw token: the sign-in bounce at `page.tsx:68` (`/auth/sign-in?next=%2Fwithdraw%2F<token>`, which puts it in a **query string on a different route**) and the post-action receipt redirect in `api/withdraw/route.ts` (`/withdraw/<token>?result=…`). So `platformLogging.rule` — "no secret query or path segment" — is breached by URLs this codebase builds itself, not only by the one it mails. **The blocker recorded above is stale.** It says the adult token must first move onto the `mail-token-delivery-v1` issuance model because adult invitations store `token_hash` directly on `subject_invitations`; since migration `20260906061029` both purposes mint through the same `token_candidates`/`token_hashes` path and only mirror the hash onto `subject_invitations`. The real remaining blocker is narrower: `activate_rights_session_v1` resolves through `private.current_co_parent_invitation_v1`, which hard-filters the co-parent purpose, kind and target and joins the cohort-draft tables, so an adult token resolves a `token_hashes` row and then falls out and activation 404s. **What is NOT a defect here, checked rather than assumed:** `refuse` and `delete` proceed on the token hash with no account or session (only `confirm` requires `p_account_id`), and that is deliberate — G5.4 requires accountless subject rights, and making a person sign up before they can decline the use of their genetic data would be the worse bug. The defect is the transport, not the absence of a session. **Closing order, established 2026-09-10 and unchanged by this entry:** teach `activate_rights_session_v1` an adult branch in a new migration; change the one line in `src/app/api/jobs/mail/route.ts` that builds `applicationUrl('/withdraw/<token>')` to the `fragmentUrl()` form the co-parent mail already uses, so newly mailed links stop using the open segment; extend `/withdraw/session` and `/api/withdraw/session` to serve the adult confirm/refuse/delete operations off the rights cookie; then hold both old surfaces for the full 30-day `invitation.pending-30d` window before deleting them together with their `docs/route-divergence.json` entries in one commit. Removing the GET oracle alone was considered and rejected as a fix on its own: it leaves the token authoritative in the path and re-emitted into two more URLs, so it would read as closed while the transport defect stood. **Surface (a) is CLOSED, 2026-09-13, and it needed nobody's decision.** `src/app/(marketing)/withdraw/[token]/page.tsx` no longer calls `adultInvitationAvailable(token)` on a plain GET, so following a mailed URL no longer answers "is this token still live?" to a link scanner, a mail-security bot or a prefetching browser. The helper had exactly one caller and nothing read its result but the rendering, so it is deleted from `src/lib/adult-invitations.ts` rather than left for the next page to reach for; `adultInvitationTokenHash`, which the POST uses, stays. The page also carries `robots: { index: false, follow: false }` now. The cost is the trade the contract asks for and no copy changed: a person opening an expired or used link sees the actions and learns it failed as `?result=unavailable` after choosing one, rather than being told first — that outcome and its sentence already existed on the POST path. Verified by 58 browser cases across every spec that follows an adult invitation link (portrait, portrait-no-file, family, family-health-picture, adult-subject-invitation, genome-family.nojurisdiction, figures-two-seed): no skips, no retries. **Surfaces (b) and the path token remain, and they are one job, not three:** the raw token is still the URL segment, still in the `next=` query string of both sign-in bounces, and still in the receipt redirect `api/withdraw` sends back here — and each of those needs a token-free destination that does not exist yet, which is the `mail-token-delivery-v1` migration with its thirty-day overlap. **Sized on 2026-09-13 by reading the code rather than calling it large, and the answer is a dependency nobody had drawn.** The transport half is what the row describes and it is unblocked: issue the adult token through `token_candidates`/`token_hashes` at mail time, add an adult branch to `activate_rights_session_v1` (today it calls `private.current_co_parent_invitation_v1`, requires an `embryo_cohort_drafts` row and writes `purpose='co-parent-invitation'`, `target_kind='cohort_draft'` — all three hard-coded), mail `/withdraw/request#<token>`, and give `/withdraw/session` a third mode for the adult purpose. Nothing is deleted: the thirty-day overlap makes the change additive. **Where the confirm lands is the part that was not obvious.** `api.withdraw`'s `confirmBodySelection` says the full `subjectArtifact`-plus-jurisdiction body is required by "only a path-b adult_subject invitation", and `/family/invite` offers no Path B (`e2e/family.spec.ts` asserts it), so today's adult invitation is path A and its confirm belongs to `api.invitation-accept` — whose register body ALSO requires `jurisdictionAttestationVersion` and `jurisdictionAttestationHash`, which is exactly D-083. That does not block this: `src/lib/embryos/routes.ts` ships the co-parent body with `jurisdictionCode` alone and the adult path can follow the same interim. It does mean **neither path's confirm matches the register until D-083's artifact is authored**, and that is an owner act. **Sharpened again on 2026-09-13, and the row's own premise was stale — the third such row this day.** This entry says "adult invitations store `token_hash` directly on `subject_invitations`", which reads as though the modern issuance model were absent. It is not. `create_adult_subject_invitation_v1` already inserts a `token_candidates` row with `purpose='adult-subject-invitation'`, `target_kind='subject_invitation'`; the issuance step already writes `token_hashes` against that candidate and marks it `issued`; and `respond_adult_subject_invitation_v1` already resolves token_hashes → token_candidates → `subject_invitations`. The write to `subject_invitations.token_hash` is a MIRROR kept for the legacy page, not the only home. So step one of the migration is done. **What is actually left is four things.** (i) `activate_rights_session_v1` needs an adult branch: it resolves only `private.current_co_parent_invitation_v1`, requires an `embryo_cohort_drafts` row and writes `purpose='co-parent-invitation'`, `target_kind='cohort_draft'`. (ii) A session-scoped responder, because `respond_adult_subject_invitation_v1` takes a token hash rather than a rights session — its body can be shared. (iii) `/withdraw/session` needs an adult mode; the accept/refuse/delete copy already exists on `/withdraw/[token]`. (iv) ONE LINE of mail: `src/app/api/jobs/mail/route.ts:145` builds `applicationUrl('/withdraw/' + deliveryToken)` for the `adult-subject-invitation` template while every other template calls `fragmentUrl`, which has built `/withdraw/request#<token>` since decision §11.6. That line is the whole reason a raw token is mailed in a path, and it must be flipped LAST, after the activation branch and the session mode exist, so nothing is mailed to a path that cannot yet answer. **CLOSED for newly mailed invitations, 2026-09-13, and the four steps above were built in that order.** (i) `private.current_adult_subject_invitation_v1` resolves the adult chain with the same revision agreement the co-parent resolver requires, and `activate_rights_session_v1` now branches on the token candidate's own purpose, so neither invitation kind can be activated through the other's checks; the adult branch opens a session on the reserved subject (38 pgTAP assertions, twenty-three of them single-field mutations that each refuse activation and leave no session behind). (ii) The response transaction moved into `private.adult_subject_invitation_response_v1` and both entry points call it: `respond_adult_subject_invitation_v1` for the mailed token and `respond_adult_subject_invitation_session_v1` for the cookie session, which consumes a session-bound nonce before reading the invitation and spends the session on any terminal answer (24 pgTAP assertions). Sharing the body rather than copying it was the point: it is the transaction that decides who controls a person's genome, and two copies would drift. (iii) `/withdraw/session` dispatches on the purpose stored on the session, and `/api/withdraw/session` answers `confirm`, `refuse` and `delete` for the adult purpose against the registered `{status, operation}` receipt; which rights holder is answering is decided by the form token the page served, never by a field in the body. (iv) `src/app/api/jobs/mail/route.ts` builds `fragmentUrl(deliveryToken)`. **The raw token is no longer in a path, a query string or a receipt redirect for anything mailed from now on.** **What is deliberately NOT removed:** `/withdraw/[token]` and the bare `POST /api/withdraw` still answer, because tokens mailed before this change live up to 30 days and breaking them would take away the very rights this row is about. Both `docs/route-divergence.json` rows stay open for that window and are re-scoped rather than closed. `e2e/adult-subject-invitation.spec.ts` now drives the whole new journey (mail fragment, scanner-safe GET/HEAD, interstitial, activation, session accept and accountless session refusal, with the token proven absent from every URL and present in exactly one request body) AND keeps a third test on the old path, so the overlap is a guarantee rather than a hope. **What is still not conformant, and it is not this row's:** the adult `confirm` body is the same account-bound acceptance it always was, not the register's `subjectArtifact` shape. That is D-103, filed today, and D-083 gates the jurisdiction half of it. |
| D-082 | 2026-09-05 | Legal | `src/app/(marketing)/legal/insurance-and-discrimination/page.tsx` | The page renders two short sections while the seeded `disclosure.insurance-and-discrimination` artifact carries brief §8 items 1–6; the page and the signed artifact differ. | medium | **closed 2026-09-13.** The page now reads the artifact row, so there is one copy of the text and this renders it. What the public page had been missing is the point of the defect: that GINA reaches neither life, disability nor long-term-care cover; that a result is information about parents, siblings, children and people never met, who did not agree; that any of them can ask Inherit to stop at `/legal/appeals` **without an account**; that asking for a test is itself genetic information under US employment law; and the advice to settle cover before looking at results. An integrity section carries the version, the effective date and the body hash and links the permanent versioned copy, so a reader can check the page against the record instead of trusting it. `e2e/legal.spec.ts` now pins those facts rather than the two old section headings — pinning headings would pass on a page that had dropped the content again. If the row is absent the page says so rather than falling back to prose nobody signed, and rather than 404ing a legal URL. One consequence worth naming: the disclosure's words are no longer in source, so the readability gate no longer scans them; the artifact is versioned, hashed and signed instead, which is a different control and a stronger one for this text. |
| D-083 | 2026-09-05 | Register | `docs/route-register.json#api.invitation-accept` | The body requires a jurisdiction attestation version and hash, but no `policy.jurisdiction` artifact exists in the repository, so nothing can be hashed honestly. | low as filed; **raised in reach, not in severity, on 2026-09-13** | **the silent half is closed 2026-09-13; the artifact is still unauthored.** Owner decision: record the divergence and keep the code, rather than author a legal artifact to satisfy a field or drop a commitment the brief made about proving which jurisdiction a person accepted under. `docs/route-divergence.json` now carries `unhashableAttestationFields` naming all five routes — auth.sign-up-submit, api.consent-reaffirm-rights, api.consent-grant-rights, api.invitation-accept, api.withdraw — the two fields each declares and the `jurisdictionCode` each actually serves. `scripts/route-gate.ts` compares that list with the register in both directions, proven by two planted defects, so a sixth route declaring the fields fails until it is recorded and every row fails the day the fields are really served. A mismatch nobody could see is now one nobody can add to or leave behind. What remains is authorship, which sits with G5.5. As filed: Three things were measured while sizing D-081 and each is worth having before anyone starts. (1) **`jurisdictionAttestation` appears nowhere in the repository outside `docs/route-register.json`** — not in `src/`, not in the docs, not in a test. Nothing implements it, so nothing can be failing to. (2) **`policy.jurisdiction` appears nowhere at all**, including in the register's own artifact list, which is what the row says and is now checked rather than asserted. (3) This is not one route's gap: `api.invitation-accept` AND `api.withdraw`'s path-b adult confirm both require the version and hash, so the artifact gates the confirm on every invitation path, which is why closing D-081 cannot make its confirm register-conformant on its own. **And the deviation is currently justified by a citation that does not resolve.** `src/lib/embryos/routes.ts:288` reads "decision §11.5: `jurisdictionCode` only"; there is no §11.5 anywhere under `docs/` — the only "11.5" in the tree is a font size in the brief. The decision may well have been made; it is not written down where the code sends a reader to look, and a deliberate deviation from the register is exactly the kind that needs to be. |
| D-084 | 2026-09-05 | Rights | `src/app/(marketing)/withdraw/` | The register's `/withdraw/request` interstitial (reads the fragment token, sets the candidate cookie, posts to `api.rights-activate`) and the `/withdraw/session` destination do not exist; the dynamic adult page matches both paths and shows "This link cannot be used". The co-parent mail therefore promises a page that is absent. | medium | **closed 2026-09-13. It was already done, and the row had not been re-read.** All three things this row asked for are in the tree and have been for some time. `src/app/(marketing)/withdraw/request/route.ts` is the interstitial: a generic GET that looks up no target, mints a rights-activation candidate, sets its host-only cookie and serves a nonce-scripted page whose only job is to read the fragment and post it — with `default-src 'none'`, `form-action 'none'` and `noindex,nofollow`. `src/app/(marketing)/withdraw/session/page.tsx` is the destination, and it builds its request from the cookie header alone with the comment "Never derive authority from URL input"; it renders `CoParentReviewForm` and `InvitationRefusalForm`, which are the E2 acceptance screens this row named. `src/app/api/rights/activate/route.ts` calls `readRightsActivationCandidate(request, nonce)` and answers the same opaque 404 when it is absent, which is the candidate cookie check. The end-to-end proof is `e2e/co-parent-invitation.spec.ts`, which follows a mailed `/withdraw/request#<token>` through activation to the review screen and passes. **The static segments also answer this row's last sentence:** `request` and `session` take precedence over `[token]` in the App Router, so the dynamic page no longer shadows them and the co-parent mail no longer promises an absent page. **Worth recording as a pattern rather than a one-off.** This is the second stale open row found this week by reading the code instead of the row — D-010's second clause was the first, and it had been half-true for ten days. An open defect row is a claim about the present, and this file is read as a to-do list. |
| D-085 | 2026-09-05 | Audit | `src/lib/embryos/api.ts` | A blocked closed-shape response writes no legal audit event; `embryo-closed-schema-v1` requires exactly one pseudonymized event per blocked attempt. | low | **closed 2026-09-13**, by the RPC the row itself prescribed. `public.record_blocked_embryo_response_v1(p_consumer)` is service-role only, takes no principal, no target and no payload, and appends one `embryo.response.blocked` event through `private.append_legal_audit_event` with `audit_principal_id` null, the consumer as `route_id` and `{"shape_reference":"embryo-closed-schema-v1"}` as the whole coded context. The consumer is re-validated against the route's own id pattern INSIDE the function, so a payload fragment cannot reach the ledger even from a caller that skipped the route's check — proven by a pgTAP case that passes one and reads back `unregistered`. **A second divergence was found and fixed with it.** The contract's `observabilityEvent` is a template id and two constant slots, and it says the observability sink receives ONLY that; the log line was passing the consumer as a third value and omitting the `reason` slot entirely. The line is now exactly `feature.blocked embryo.closed-schema-serialization unsafe-embryo-response-shape`, and the consumer moved to the audit row, which is where the contract asks for it. A failed audit write does not change the response — the shape is already refused and the caller gets the same fixed 500 — but it is never silent: a second coded line says the ledger is missing an event it is required to hold. Eight pgTAP assertions and four unit cases, including both failure modes of the write. |
| D-086 | 2026-09-05 | Jobs | `src/app/api/jobs/mail/route.ts` | The drain response returns `processed`, `failed` and `pending` counts; the register's `machine-job-result-v1` allows only `{status:"complete", outcome}`. Pre-existing; unchanged by the cohort runtime. | low as filed; the class is medium | **closed 2026-09-13, and wider than this row said.** The row named `jobs.mail`. `machine-job-result-v1` binds SEVEN routes, and six of the seven were returning job internals: the mail and retention drains returned `processed`, `failed` and a `pending` queue depth — for retention, a count of the people part-way through deleting their account, readable by anything holding the shared jobs secret; `jobs.annotation-refresh` returned the batch size and the free text `"reference store fresh"`; `jobs.research-publish` returned the slug of the template it had just published and how many subscribers it had queued a digest for; `jobs.research-refresh` returned one object per source with the release key it had recorded and, on failure, the raw `Error.message`. The contract's own clause names all of those — `targetJobRowObjectRecipientDocumentTemplateSubjectCohortFileAccountErrorFreeTextOrPrivateCounts: "never-returned"` — so this was a privacy-contract breach across a class, not one route's shape drift. Fixed by `src/lib/jobs/machine-result.ts`: one helper builds `{status:"complete", outcome}` with the contract's headers, `jobOutcome` maps a drain's own counters onto `no_work`/`completed`/`completed_with_failures`, and `src/lib/jobs/machine-result.test.ts` reads every bound route's file and fails if a 2xx JSON body is built anywhere but the helper (verified against a deliberately reintroduced violation). The two queue-depth QUERIES went with the fields they fed, so the drains no longer ask the database a question they may not answer; the route tests now make a table read from those routes throw. No count evidence was weakened: the unit suites already asserted the submissions and receipts the counts summarised, the `no_work` case gained the first test it has ever had, and the browser suites read the outbox with the service key — the same query the route used to run, moved to the one caller entitled to the answer. Two further divergences on these routes were found and are NOT fixed here, as D-100 and D-101: widening a contract fix into an endpoint's authority model is how a scoped change becomes an unreviewed one. |
| D-087 | 2026-09-05 | independent transport review | `src/lib/embryos/vcf-transport.ts` | Two phased calls in distinct PS blocks lost PS but kept phased separators; a downstream VCF reader would treat them as one phase set. | high | fixed before publication: remove phase claims when discarding phase-set provenance, preserve allele copies, and reject phased wire records; VCF and table transport regression tests cover both source and bypassed input |
| D-088 | 2026-09-05 | independent transport review | `src/lib/embryos/vcf-transport.ts` | VCF 4.5 samples at POS 100 with LEN 100 and 10 both inherited the cohort END 199 after splitting. | high | fixed before publication: retain validated per-sample LEN and regenerate END 199 and 109 respectively; regression test proves neither sample borrows its sibling's span |
| D-089 | 2026-09-05 | independent transport review | `src/lib/embryos/vcf-transport.ts` | Valid lowercase REF/ALT sequence alleles were refused by the uppercase-only validator. | medium | fixed before publication: normalize sequence alleles; regression test proves identical transport for uppercase and lowercase sources |
| D-090 | 2026-09-05 | independent transport review | `src/lib/embryos/table-transport.ts` | An rsID-mapped table with an unused chromosome column containing a dot or blank discarded server-resolvable rows. | medium | fixed before publication: missing optional chromosome values defer to the server's rsID lookup; both cases have regression tests |

| D-091 | 2026-09-05 | independent database review | `private.embryo_ingest_binding_failure_v1` | The expiry-pair predicate used an unlocked read, allowing concurrent expiry-target changes during a reservation transaction. | high (latent: no route is enabled) | fixed before commit: lock both exact retention rows with FOR SHARE, compare the phase kind and immutable cohort/revision envelope; independent rollback-only verification confirmed the locks |
| D-092 | 2026-09-05 | independent database review | minted ingest due binding | Changing the due recipient kind/revision or either disposition revision still passed the binding check. | medium (latent) | fixed before commit: all four fields checked; four independent mutation regressions |
| D-093 | 2026-09-05 | independent database review | session mint/chunk contention | New profile/auth/cohort lock order can conflict with deletion, chunk writes or another co-parent uploader. | medium (latent) | fixed before commit: explicit NOWAIT, bounded legacy/implicit lock timeout and retriable transaction failure; actual wrapper/reserve cross-session probes preserve durable state |
| D-094 | 2026-09-05 | independent cross-session runtime review | ingest binding exception handler | PostgreSQL's broad 55000 category handler also catches 55P03, turning temporary parent-lock contention into durable failure_pending. | high (latent) | fixed before commit: explicit contention/deadlock rethrow before category fallback; injected SQLSTATE regression and real same-sequence retry prove the session stays open without duplicate receipt or lost due phase |
| D-095 | 2026-09-06 | direct synthetic guard probe | `src/lib/copilot/guard.ts#checkResponse`, `src/app/api/chat/route.ts` | At `8cbece97a1a9a66fbc4953eafc60c180889b2677`, the output check accepted synthetic diagnosis, treatment and embryo-recommendation statements after an allowed prompt because only numerals/citations were checked. The stream fold also inserted whitespace between provider fragments, splitting the tokens read by the checker. No real-user model incident was asserted. | high | fixed by PR70, production merge `f8c43bb`: assertion checks, exact per-part stream assembly, legitimate-explanation regressions and 64 actual-route completion cases. Full CI `34009298500` passed 207 browser cases. See `docs/copilot-output-contract.md` for detection limits; G4.8 remains NO until the complete A.9 scope and evaluation are delivered. |
| D-096 | 2026-09-10 | Rights | `private.clear_revoked_own_report_outputs_v1`, `public.ancestry_results` | Revoking the `ancestry` purpose never deletes the subject's legacy ancestry rows. The live revocation trigger deletes only `public.user_prs` for that grant's `reports.polygenic` runs and every `private.own_analysis_runs` row for the grant. In the whole database only `private.assert_supported_account_fk_shape_v1` and `public.purge_account_deletion_database_v1` mention `ancestry_results`; the only other removal is the reprocess path in `POST /api/files/[id]/process`. Reads are correctly denied — `loadAncestryResultSnapshot` gates canonical and legacy rows through `filterOwnAnalysisFiles(subject, 'ancestry', ...)` and re-confirms after every read — so this is a retention failure, not an access one. `docs/retention.md` registers `purpose.derived-60s` as delete-within-60-seconds for derived rows attributable to the exact revoked subject-and-purpose tuple. | medium | **closed 2026-09-10, by decision, not a code change.** The owner's call is that revoking an analysis purpose does not withdraw already-derived data from the subject's own export, because storage, analysis, sharing and AI permissions are separate and export runs on `raw.export`/`export.share-link`. Its subject scoping is correct — the rows are narrowed to the account's own legacy files — so the absent ancestry-purpose check is the only gap, and it is the intended one. Reads under the revoked purpose already end immediately, which was never in question. `docs/retention.md` now states the exception, so the registered rule matches intent instead of requiring a deletion the product deliberately does not perform. `public.report_observed_calls` was checked in the same pass and was never a defect: `own_copilot_chat_v1`, `own_report_generation_v1` and `own_subject_export_content_v1` all read it, so it is shared across live purposes.  **SUPERSEDED IN PART, 2026-09-12.** This row's closure rested on the decision that revoking analysis does not withdraw already-derived data from the subject's own export. The operator reversed the read half of that on 2026-09-12 (see D-097): revoking `ancestry` now withholds the legacy rows from both the page and the export. What survives of D-096 is the deletion half — those rows are still not deleted, and account deletion or source replacement remain the paths that remove them. |
| D-097 | 2026-09-10 | Rights | `src/app/api/export/route.ts`, `src/lib/exports/own-subject-content.ts`, `private.own_subject_export_content_v1` | The two halves of the same `ancestry.json` behave oppositely after revoking the `ancestry` purpose. The canonical half is withheld: `own_subject_export_content_v1` filters `purpose='ancestry'` against `purpose_grants`, and `own-subject-content.ts` reads twice around its authority check and throws if the two reads differ, with the comment "Unlike raw export permission, ancestry permission may disappear while the source remains. Do not release a buffered result after withdrawal, replacement generation or a different grant, including an empty page." The legacy half is retained: the route's `ancestry_results` read carries no purpose check. So after revocation a person's export contains their legacy ancestry and not their canonical ancestry, in one file. | medium | medium | **CLOSED 2026-09-12 by operator decision, and the decision reversed the one that had been assumed.** Put to the operator as "which read is right", answered "match canonical to legacy — the export keeps both halves", and then REOPENED by me before implementing, because that framing was factually wrong: the canonical half is not withheld by a read guard, it is DELETED. `private.execute_own_report_purge_v1` carries a purge-manifest entry targeting `generated-artifacts` in `private.own_analysis_runs` for the revoked grant, inside the registered 60-second deadline, and `e2e/ancestry-revocation.spec.ts` asserts that row is gone. "Keep both halves" therefore meant retaining derived genetic analysis past a revocation and rewriting the purge's completeness proof, not relaxing a guard. Re-put with that priced in; the operator chose the reverse — MATCH LEGACY TO CANONICAL. Implemented 2026-09-12: `private.own_subject_purpose_grant_v1` and `public.own_subject_purpose_granted_v1` answer the subject-scoped question the legacy read needs, `filterOwnAnalysisFiles` gained an opt-in `gateLegacy`, and both ancestry readers use it. **The gap was wider than this row said.** Legacy rows were never gated on the ancestry page either — `filterOwnAnalysisFiles` returns every legacy file untouched whatever the purpose — so the leak was on a page as well as in an archive, and G5.3a's claim that revocation made them "immediately unreadable" was never true. The rows are still not deleted; the operator declined that option explicitly. |
| D-098 | 2026-09-10 | Documentation | `.env.example`, `docs/self-hosting.md`, `src/app/api/jobs/mail/route.ts` | `NEXT_PUBLIC_APP_URL` was in neither the env template nor the self-hosting guide, and its fallback is the hosted origin: `process.env.NEXT_PUBLIC_APP_URL ?? "https://www.inherit.bio"`. It builds every link inside outbound mail — invitation, rights and withdrawal links, and the cancel/export links in an account-deletion notice. A self-hoster following `docs/self-hosting.md` would set the documented `NEXT_PUBLIC_SITE_URL` and miss this one, sending their users' mail links, and the rights tokens those links carry, to a deployment they do not run. The two variables sit beside each other and fall back in opposite directions: `NEXT_PUBLIC_SITE_URL` falls back to `http://localhost:3000`, which fails visibly and harmlessly. | medium | **documented, not repaired.** Both files now carry the variable and state the hazard. The fallback itself is left alone deliberately: the hosted deployment may rely on it, and I cannot read its environment, so changing it here could break production mail to fix a self-hosting doc. Whether that fallback should fail loudly instead is the open question, and it is an owner call because it turns on what production actually sets. |
| D-099 | 2026-09-12 | Rights | `src/lib/genome/own-analysis-access.ts` | The purpose gate that D-097 added for `ancestry` is missing for the other two own-analysis purposes. `filterOwnAnalysisFiles` returns every legacy file untouched unless the caller passes `gateLegacy`, and only the two ancestry readers pass it, so revoking `reports.monogenic` or `reports.polygenic` still leaves results derived from a LEGACY source readable on the report surfaces and in the export. The canonical half of each refuses correctly. Found while implementing D-097 and deliberately not fixed there: the operator's decision named `ancestry.json`, the fix for the other two is the same shape but touches the report library, the health picture and the export's `reports.json`, and widening a rights change past what was asked is how a scoped decision becomes an unreviewed one. | medium | **closed 2026-09-13.** The same gate now applies to `reports.monogenic` and `reports.polygenic` on every OWN-record reader: Overview's summary, both report surfaces, the personal previews, and the export's legacy `reports.json`, which is gated per layer and rechecked immediately after the build so a revocation landing mid-export cannot ship a buffered result. The Family surfaces deliberately keep the old path: a reader on a relative's record holds no own-subject grant there, so this check would always refuse and would delete legacy sharing rather than gate it — their authority is the counterpart's Family permission, checked before the read. Closed without the operator's line because the brief's own non-negotiables answer it: authority enforced throughout, and revocation immediate. Reverting is one flag per call site. |
| D-100 | 2026-09-13 | Contract sweep while closing D-086 | `docs/route-register.json#routes` (`jobs.run`), `src/app/api/jobs/` | The register carries `jobs.run` at `/api/jobs/run` with `disposition: "kept"`, a POST method policy, a closed query contract, a `worker-job-dispatch-v1` resolver and `successResponseContract: "machine-job-result-v1"`. There is no `src/app/api/jobs/run/route.ts`, and `src/app/api/jobs/` holds only the five directories that do exist. So a registered, kept, contract-bound dispatch endpoint is not implemented, and every reader of the register — including the correspondence test and the contract sweep added for D-086 — has to special-case it. Either the dispatcher was planned and never built, or it was removed without the register being told; the register alone cannot say which, and I will not guess in a document whose whole value is that it matches reality. | medium | **closed 2026-09-13: retired from the register, brief first (corrections item 12).** Read past the first sentence — what follows describes the state when it was filed, not now. Named in `src/lib/jobs/machine-result.test.ts#UNIMPLEMENTED` so the contract sweep skips exactly this id and no other, and so adding the file cannot quietly bypass the check. Closing it means an owner decision: build the dispatcher, or retire the route from the register. **What was done: retired, and the brief went first.** The operator chose to retire rather than build. Asked and answered — but the recommendation I gave was made without checking the brief, and the brief describes the route affirmatively at line 2224 as a test hook "used by `e2e/revocation.spec.ts`, `e2e/embryo-ingest.spec.ts` and §A.13". Removing a register entry the brief describes is the mirror image of `/api/uploads`, where an entry cannot be ADDED because the brief denies the route exists: the register is derived from the brief and pinned to `briefSha256`, so the brief has to move first. That went back to the operator as corrections item 12 and was signed the same day. **Measured before proposing it, which made the case stronger rather than weaker:** the route has no directory, no caller anywhere in `src`, `e2e` or `scripts`, and NEITHER spec the brief names as its user has ever existed in this repository's history. The capability it existed for — not waiting 24 hours inside Playwright — is met by the four job routes the suite actually posts to. **What moved:** the brief's test-hook paragraph and its §A.13 purge assertion; the register's route entry and its seven other references (`workerExecutionBindings`, the `canonical-source-publication-v1` required binding, the `worker-job-enqueue-v1` test-hook sentence, the rate-limit binding, the observability `routeId` enum, `machineAuthBindings` and `responseContractBindings`); `briefSha256`, repinned in the same change the gate demands it be; and `machine-result.test.ts`, whose `UNIMPLEMENTED` set existed only for this id and is gone, so the contract sweep now covers every bound route with no carve-out. |
| D-101 | 2026-09-13 | Contract sweep while closing D-086 | `docs/route-register.json#routes` (`jobs.research-publish`), `src/app/api/jobs/research-publish/route.ts` | The register gives this route `requestContract: { body: "forbidden", query: "forbidden" }` and `policy.mode: "machine-reviewed-publication-only"`. The route reads `const { slug } = await request.json()` and refuses with 400 when it is absent, so the caller names the exact template to publish — the request selects the target, which is the thing every other job route in this domain is written to prevent (`requestTargetOrPrefix: "forbidden"` on retention, `requestMaySelectSubjectTargetSourceOrRows: false` on retention-cron and research-refresh). The authority is a shared operator secret, so this is not a privilege escalation; it is the register describing a due-work drain and the code implementing an operator command. | medium | **closed 2026-09-13; the code moved to the register, not the register to the code.** The owner decided that operator-selected publication was never the intent, so the register's `requestContract` stands and the route is now what it describes. `POST /api/jobs/research-publish` reads nothing from its caller: a body, a query string or a chunked request is `400 invalid_request` before anything is published, exactly as on `jobs.mail` and `jobs.retention`. What is due is decided in the database by `claim_due_research_publication_v1` (migration `20260913080000_research_publication_due_work.sql`): a template is due when it is sitting in the review queue and the newest of its `public.template_reviews` rows decided `approve`. That is the approval record the schema already had and nothing had ever read — a later `re_review` or `retire` takes a template back out of the due set without erasing the approval that preceded it, and a template nobody has decided on is never published, which is the hole the old route left open: the caller could name an unreviewed draft and it would go out. One template per call, oldest approval first, claimed with `for update ... skip locked` and flipped to `published` in the same transaction, so two drains running at once cannot write the changelog entry twice; the route loops five times and the next drain takes the rest. `published_at` is set once and kept, so a report that went back for re-review and was approved again keeps the date its readers first saw. Proven by `supabase/tests/research_publication_due_work.sql` (16 assertions, each rule checked against a mutant function that breaks it: newest-approval-first, any decision counts, any status counts, no status filter, the oldest review revision winning, `published_at` overwritten, and the drain function granted to `authenticated`) and by three browser tests in `e2e/research.spec.ts` — a request naming a slug in the body or the query is refused and publishes nothing, an unapproved draft drains to `no_work` and stays in the queue, and an approved one publishes, reaches the changelog and queues the digest. The four `workerExecutionBindings` checkpoints are NOT all implemented here: the claim and its recheck are (both are the one `for update` in the claim's transaction), the aggregate privacy threshold is not, and widening this fix into that binding is the mistake D-086 named. |
| D-102 | 2026-09-13 | Found while building the `/family/portrait/[pairId] empty` fixture | `src/app/(app)/family/[person]/permissions/page.tsx`, `src/components/family/permission-grant-row.tsx` | **A permission a reader cannot turn on can render with no control and no reason: an empty paragraph.** `PermissionGrantRow` renders `<p data-slot="permission-locked">{disabledReason}</p>` whenever `action` is absent, and the page leaves `disabledReason` undefined for every reason except the independent-login one. `actionFor` returns undefined when `preparePortraitGrant`, `prepareSharedReportGrant` or `prepareHealthPictureGrant` returns null — and `family_report_endpoint_v1`, which all three call, refuses when EITHER account's profile fails its checks, including a date of birth at or over eighteen. Observed in a browser on 2026-09-13: with the granter's account completed and the recipient's not, the Portrait row shows the label and the state `Off`, the "Turn on" control is absent, and nothing stands in its place. Confirmed by rendering `PermissionColumn` with rows carrying neither an action nor a reason: seven `permission-locked` slots, zero controls, and the markup is literally `<p data-slot="permission-locked" class="text-sm text-ink-muted"></p>`. This is a rights surface whose whole job is naming what a person may do and whose step is outstanding, and the brief's non-negotiables require limitations to stay visible. | medium | **open, and the empty paragraph is the defect while the wording is an owner call.** The two are worth separating. That a rights surface renders a blank where an explanation belongs is not a design question — `PermissionGrantRow` should not be able to render a locked row with no reason at all, and making that combination impossible turns a silent blank into a failure a test can catch. WHAT THE REASON SHOULD SAY is the owner call, because the honest one describes the OTHER person's account ("they have not given their date of birth"), and telling one adult that about another is the same disclosure question that keeps `/family/portrait/[pairId] processing` open (see the route-gate history and corrections item 9). A non-disclosing reason is possible — the row can say the control is not available yet without saying whose step it waits on — but that is new user-facing copy on a consent surface, and inventing it inside a ratchet commit is how a scoped change becomes an unreviewed one. Not fixed here for that reason; the fixture that found it works around it by completing both accounts, and `e2e/portrait-no-file.spec.ts` says so in its header. |
| D-103 | 2026-09-13 | Found while moving the adult invitation onto the rights session (D-081) | `docs/route-register.json#api.withdraw`, `supabase/migrations/*_adult_subject_session_response.sql`, `consent.subject-adult` | **The adult `confirm` body cannot be built to its registered shape, because the statements it is supposed to actuate are not published anywhere and the register's count of them disagrees with the count the database records.** `api.withdraw`'s confirm body for an adult-subject invitation requires a `subjectArtifact` with an `artifactPresentationToken`, a `typedName`, and "exact-server-published-four-item-array-with-every-item-individually-actuated" statement keys. The acceptance transaction writes FIVE keys — `age-18-plus`, `mailbox-control`, `no-inviter-access`, `identity-not-verified`, `revocable` — and those five strings appear nowhere in the repository except inside the migration that writes them: no `EMBRYO_ARTIFACT_STATEMENT_KEYS` equivalent publishes them, and no copy anywhere renders one as a sentence a person could tick. The published `consent.subject-adult` body is a single paragraph containing all five ideas. Checked rather than assumed on 2026-09-13: `grep` for each key across `src/`, `docs/` and `data/` returns only the three migrations. | medium | **open, and the blocker is authorship, not engineering.** Building the registered body means splitting a published consent paragraph into individually tickable statements and deciding whether there are four of them or five. Both are legal copy on the surface where a person hands over control of their genome, and writing them here would be inventing the words a reader is told they agreed to. The session confirm therefore does exactly what the mailed-token confirm has always done — an account whose confirmed address is the invited one accepts, and the whole artifact is displayed above the control — which is no worse than the surface it replaces and strictly more informative, since the old page showed the artifact not at all. To close: publish the statement keys and their sentences for `consent.subject-adult`, reconcile four against five in the register, then the presentation token, per-statement actuation and typed name are ordinary work. The jurisdiction half of the same body (`jurisdictionAttestationVersion`, `jurisdictionAttestationHash`) is D-083 and is blocked on the same kind of act. |
| D-104 | 2026-09-13 | Measured while finding out why `e2e/research.spec.ts` cannot run on a developer database | `public.mail_outbox`, `public.claim_mail_outbox`, `docs/route-register.json` | **A mail row that exhausts its attempts is never given a terminal state: it stays `claimed` forever.** `claim_mail_outbox` takes a row that is `queued`, or `claimed` and older than ten minutes, and only while `attempt_count < 10`. Nothing anywhere moves a row out of `claimed` when it reaches ten, and nothing moves a row to `expired` when `expires_at` passes — `grep` for `attempt_count >=`, `attempt_count >` and `state='expired'` across `supabase/migrations` and `src/` returns nothing. Every invalidation in the tree is event-driven (a refusal, a file deletion, a stale recipient authority), so a row whose delivery simply never succeeds has no path to rest. Measured on this machine 2026-09-13: 27 rows sitting at `attempt_count = 10` in state `claimed` since 2026-09-10, plus 24 more at one and two attempts working their way there. The `mail_outbox.state` CHECK constraint declares `failed` and `expired`; `expired` is never written by anything, and `failed` only by the invitation-terminal path. So the schema names two terminal states the system cannot reach for the ordinary case of mail that will not send. | low | **open, and NOT fixed here, because the rule does not exist to transcribe.** What this is not: it is not a permanently poisoned failure signal. A row at ten attempts is no longer claimable, so it stops contributing to a drain's failure count — the degradation is self-limiting, and I checked that rather than assuming the worse reading. What it is: an unbounded set of rows in a non-terminal state, each still naming a recipient principal and a contact reference, which an operator reading the queue cannot tell apart from mail that is genuinely in flight. Closing it means deciding what a dead letter IS here — `failed` at exhaustion and `expired` past `expires_at` is the obvious shape, and the states are already in the constraint — and where the sweep runs. `docs/route-register.json` describes an attempts-and-max_attempts lifecycle for `worker_jobs` and says nothing at all about `mail_outbox`, so writing one is adding a lifecycle the register does not describe, which is how a scoped fix becomes an undeclared surface. An owner says whether the retention job owns this sweep or the mail job retires its own dead rows; the implementation after that is small. |
| D-105 | 2026-09-13 | Measuring priority 2's "complete supported ancestry, lineages and Neanderthal methods" against the code | `scripts/claims-gate.ts`, `data/gates/archaic-allowlist.json` | **A prohibition the brief writes out in full was enforced by nothing.** Brief §7.6 says archaic ancestry is never ranked and names the guard exactly: "`scripts/claims-gate.ts` fails on `/more Neanderthal than/i` or `/Neanderthal (percentile|rank|score)/i` **except** for two pinned exact strings, listed by full sentence (not by file glob) in `data/gates/archaic-allowlist.json`... Positive and negative fixtures are checked in." Measured 2026-09-13: `grep -i "archaic\|neanderthal" scripts/claims-gate.ts` returned nothing, `data/gates` did not exist, and no fixture existed. Nothing in the tree trips the patterns today, so no reader has ever been shown a ranking — but that is because `NeanderthalCard` withholds the estimate, not because anything was stopping it. | medium | **fixed the day it was found, and the timing is the point.** A guard on a capability is worth having BEFORE the capability, not after: the day someone builds the estimate is the day the wording gets written, and that is exactly when a missing gate costs something. `archaicFindings` is exported and the rule now runs over every source module the claims gate already walks plus the report templates — 557 files, reported in the gate's summary line so a sweep that silently read nothing cannot pass, with a floor guard under it. Six checked-in fixtures drive it, positive and negative, and they live in `scripts/claims-gate-archaic.test.ts` rather than under `src/` because a positive fixture is by definition a string the gate must refuse and the sweep reads `src/`. **The allowlist ships empty, on purpose.** Its two pinned sentences do not exist: the pages that would carry them have not been written, because the estimate they qualify is withheld pending a licence-checked marker list. Pinning a sentence that does not exist would be inventing the wording of a refusal, so the file records why it is empty — and empty is the strictest state, permitting no occurrence anywhere. A stale pin is a failure too: a sentence in the list that appears nowhere fails the gate, so the allowlist cannot rot into a permission nobody is using. **What is still not done, and is not this row's:** the Neanderthal estimate itself. `src/copy/ancestry.ts` says "Inherit has not yet built and licence-checked the marker list this needs, and we will not guess", which is the honest state and needs a marker panel plus a licence check — science and counsel, not engineering. |
| D-106 | 2026-09-13 | A production upload failure the owner reported, traced from the Vercel 503 to the live schema | `supabase/migrations`, the deployed application, no drift check anywhere | **Production ran twelve migrations behind the code deployed against it, and every genome upload had been failing since the deploy.** `src/lib/uploads/subject-finalization.ts:157,163` call `read_own_upload_finalization_checkpoint_v1` and `write_own_upload_finalization_checkpoint_v1`; neither existed in the production database, nor did `private.own_upload_finalization_checkpoints`. Every finalize threw, the catch-all returned the opaque `unavailable` 503 the route is designed to return, and the page said "The upload could not finish." Measured 2026-09-13: five finalize attempts, five 503s, zero successes, and those were the ONLY 503s in seven days of production logs. **Nothing detected it and nothing could have.** The route deliberately logs no private detail, so `get_runtime_errors` was empty; CI applies every migration to a fresh database each run, so the suite was green throughout; and no check anywhere compares `supabase/migrations` against what the deployed project has actually applied. The gap is invisible from both ends at once. | high | **Production fixed 2026-09-13 with the owner's explicit authorization, one migration at a time with verification after each; **closed 2026-09-13**, and the guard shipped the same day: `pnpm gate:schema-drift` reads the deployed ledger, compares it with `supabase/migrations` by name, fails when it cannot check, and is deliberately not in CI because there the database is built from those same files seconds earlier. Replayed against that morning's ledger it reports BEHIND by 12 and names them. The ordering that prevents a recurrence is a standing authorization rather than a hope: migrations are applied to production on every merge, and `20260913080000_research_publication_due_work` was the first applied under it.** What made this severe is not the twelve-migration gap but the shape of the failure: the code and the database each looked correct in isolation, and the only signal was a person telling us their upload did not work. A user-reported outage is not a monitoring strategy for a product whose first promise is that a file goes in and results come out. **What closes it:** a check that reads the applied-migration list from the deployed environment and compares it against the repository's, failing when the deployment is behind — run against production after a deploy, not only in CI, because CI's fresh database is exactly the condition that hides this. **One honest caveat about the repair itself:** migration `raw_browse_purpose` carries an intermediate `respond_adult_subject_invitation_v1` that `adult_subject_session_response` replaces outright. Its constraint change and `grant_directional_purpose_v1` were applied and the final responder came from the later migration, so the end state is identical and was verified, but that ledger row covers less than its file does. A future `db push` against a database built strictly from files would produce the same end state; a byte-comparison of the two would not agree. **GUARD BUILT 2026-09-13, and running it against the real ledger corrected its design twice.** `scripts/schema-drift-gate.ts` (`pnpm gate:schema-drift`) reads the deployed `supabase_migrations.schema_migrations` and compares it with `supabase/migrations`. Proof it addresses this defect rather than resembling a fix: replayed against this morning's ledger it reports "BEHIND by 12" and names them, the two checkpoint migrations included. **It fails when it cannot check** — no URL, no connection, no ledger — because a guard against an invisible condition must not have a silent no-op mode, or it reproduces the bug it was written for. It is NOT wired into CI: CI builds its database from these same files seconds earlier, so the comparison there is a tautology, and that tautology is precisely why the suite stayed green through the outage. **First correction: it compared version stamps, and it should never have.** The deployed ledger has never carried this repository's version numbers — every row records when it was applied, so `subjects_and_principals` is `20260831221537` in the repository and `20260831233339` in the database. The first real run reported all 106 missing and all 107 unknown. Names are unique on both sides and match exactly, so the name is the identity this comparison can use; a version-based gate would have been abandoned within a day. **Second correction: one deployed migration has no file and never did.** `install_own_report_retention_scheduler` is an operator-only pg_cron installation whose own preflight refuses unless `current_user` is `postgres` with `pg_cron` preloaded, and which deliberately creates an inactive job. A full-history search finds no such file, ever. It is recorded in `data/gates/operator-applied-migrations.json` with that reason rather than silently tolerated, and an exemption for something the deployment does NOT have also fails, so the list cannot rot into a blanket. **Ledger repair:** `apply_migration` stamps rows with the current time, so the twelve arrived as `202609131431xx`. They were rewritten to the repository's canonical versions, matched one-to-one by name, so the record says which migrations ran. |
| D-107 | 2026-09-13 | Found while closing D-101 | `public.template_reviews`, no route, no page | **Publication is gated on a review nobody can record through the product.** D-101 put `/api/jobs/research-publish` behind `public.template_reviews`: a report template publishes when the newest of its review rows decided `approve`. That table has existed since `20260831224126` and has never had a writer — no route inserts into it, no page renders the queue, and there is no reviewer role in the application at all. The gate is real and it is closed, so nothing publishes without a human decision, which is the safe direction to fail; but the only way to record that decision today is a service-role client against the database. Two further pieces are named here so they are not lost: the register's `workerExecutionBindings.jobs.research-publish` lists four checkpoints and only two are implemented (claim and recheck, both inside the claim's transaction) — the aggregate privacy threshold and the coded publication outcome are not; and a template whose evidence level the publication CHECK constraints refuse will sit approved at the head of the queue making every drain report `completed_with_failures`, which is loud but stuck. | medium | **open.** A review queue needs a surface: a page listing templates in `review` with their draft body, citations and evidence level, an approve/re-review/retire control writing one `template_reviews` row per decision under a reviewer principal, and an audit trail of who decided what. That is a feature, not a defect fix, and it does not block D-101: with no reviewer surface the research library simply publishes nothing new, and every already-published report is unaffected. `e2e/research.spec.ts` inserts the approval row directly with the service key and says in the test that it is standing in for the reviewer this defect asks for. The route is also on no schedule: `vercel.json` runs `research-refresh`, `annotation-refresh`, `mail` and the retention cron, and a drain with no caller never drains. That is deliberate for now — with no reviewer surface nothing is ever due, so a fifth scheduled invocation would buy an empty query a day — and it is the last piece to add when the surface exists. |
| D-108 | 2026-09-13 | Measured while building the nine consent gates item 7 asked for | `docs/inherit-v2-brief.md` G2.2, `docs/route-register.json` (seven Family and Embryo routes) | **Seven routes are required to declare a state that nothing on them can render, and forbidden from saying so.** G2.2's forbidden-`n/a` list ends with "`consent-required` on any Family or Embryo Analysis route", which item 7 read correctly and which the owner answered by choosing to build the nine gates. Two could be and now are: `/family/[person]` (implemented and untitled — the paused-sharing branch) and `/family/health-picture` (built — a paused pair was falling into the empty state and being told to turn on what they had already turned on). The other seven were then read one page at a time, and none has a recorded revocable consent that a page-level refusal could require. On `/family` and `/embryos` consent is a property of an ITEM the page lists — a person's pause, a cohort's analysis grant — and both already render that line per item, while the route-level refusal on those hubs is the jurisdiction one, which suppresses the whole surface and is proven. `/family/[person]/permissions` is the page where consent is given, so a refusal there is circular. `/family/invite` and `/embryos/request-data` read no consent at all: a form and a letter. `/embryos/upload` signs its artifacts DURING a flow whose draft route (E0) does not exist. `/embryo-analysis` is 36 lines of public copy with one render, already proven as `jurisdiction-unavailable`, and the register's own projection for its consent state is a panel of prerequisites rather than a refusal. | medium | **closed 2026-09-13 by the amendment, not by building anything.** The owner chose to amend rather than accept seven permanently unproven pairs. G2.2's second forbidden `n/a` now reaches only a Family or Embryo Analysis route that renders a result, a record or a permission of a named person, and names three cases it does not reach: consent that is a property of an ITEM the page lists, the page on which that consent is given, and a page that reads no consent at all. `briefSha256` repinned in the same change, because the register is derived from the brief and the brief moves first. **The exception is structured, not prose:** each of the seven routes carries `consentRequiredException.kind` from a closed list, and the route gate fails a waiver claiming none of them, one outside the list, `item-level` without naming what carries the refusal instead, or an exception left on a route that no longer waives the state — four planted defects, one per rule. So the guard got narrower with the amendment rather than looser: before, any consent waiver on these paths failed; now it fails unless the register says which case applies and, for the item-level claim, points at a state that is itself proven. Ratchet 34 to 27. Originally filed as: The prohibition's purpose — stopping a Family or Embryo route quietly declaring that consent does not apply to it — is served wherever consent is a property of the page, and is not served where it is a property of an item the page lists, where the page IS where consent is given, or where no consent artifact exists in the schema. There it forces a permanent unproven pair and invites someone to satisfy it by building a refusal nobody asked for, which would be adding a product requirement to satisfy a register declaration. Corrections item 13 puts the narrow amendment to the owner, with the alternative stated plainly: leave G2.2 as it is and accept seven permanently unproven pairs, which is honest and costs only that the ratchet never reaches zero. `scripts/route-gate.ts` enforces the prohibition as written, so neither this row nor item 13 can be acted on by anyone quietly editing a register entry. |
| D-109 | 2026-09-14 | CI run 538 failed the name gate on a commit nobody wrote | `scripts/name-gate.ts` (the commit-message scan), `data/allowed-external-names.json` | **Every CI run after the previous pull request merged would have failed, on a commit message GitHub composed.** The gate scans commit messages as well as files, which is its most valuable half; a squash-merge copies the pull-request body into the commit on the default branch, and that body ends with the tool's own attribution link on `claude.com`. The allowlist already carries the same organisation, reviewed, with `claude.ai` among its aliases for exactly the session link every commit trailer holds — the alias list simply predated the second domain. | medium (build) | fixed (this branch): `claude.com` joins the existing Anthropic entry's aliases and the entry's reason now names both places the hosts appear, so the row says why rather than just what. The gate is otherwise untouched, and the denylist half is unchanged. The reason it reached CI is the second half of this row: `gate:names` needs `NAME_DENYLIST_FILE` to run at all, so local verification had been skipping it entirely. It runs from a one-token file outside the repository, which exercises the three public rules and the whole commit scan; the README now says so. |
| D-110 | 2026-09-14 | Found while trying to wire D-103's approved statements into the copy registry | `src/components/embryo/co-parent-review-form.tsx` (the inline `STATEMENTS` map), `scripts/readability-gate.ts` | **The sentences a person ticks to consent are the only copy in the product nobody measures.** The readability gate holds a long block to grade 9, or 11 on a marketing legal path, and it scans `src/copy/**` and the app tree. The six adult- and embryo-consent statements live in a `Record<string, string>` inside a component, which the block scanner does not pick up, so none has ever been graded. Measured by hand on 2026-09-14: "I understand that there is no outcome data..." is **11.68**, "The other genetic parent is named truthfully..." is **11.70**, "I have read the warning about false statements..." is **9.13**. Three of six are above the limit that governs every ordinary paragraph beside them. | medium (comprehension) | open. Two decisions, and both are the owner's rather than mine. (1) Whether a consent statement is legal copy for the gate's purpose — the repository already says marketing legal pages may sit at 11 while their SUMMARY is held to 9, and a statement someone ticks is closer to the summary than to the body. (2) Whether the three above the limit are rewritten. What is NOT open is where they live: all of them belong in the copy registry under one measured rule, and moving them is what makes the rule apply. **This blocks the D-103 half of the wiring**: its approved `revocable` sentence measures 9.08 against the same limit, so the five statements cannot enter the copy registry as approved. Two candidate rewrites and their grades are in `docs/protocol/copy-for-signature.md`; approved copy is not edited to pass a gate without the signature being renewed. |
| D-111 | 2026-09-14 | density verifier | `docs/density-baseline.json` capture artifacts | Three pinned harness hashes had drifted from the files they name and nothing noticed: `reproduce.sh` (parameterised for Linux in #120), `capture.mjs` (`ready`/`measure` extracted to `measure.mjs` in #121) and `verify.mjs`. The pin worked the first time the verifier was run by hand and failed immediately on `capture.mjs`; the root cause is that `scripts/density-baseline/verify.mjs` was not a `pnpm` script and not a CI job, so nothing ever ran it. | medium | fixed (this branch): hashes repinned, `measure.mjs` added as a pinned artifact, `pnpm density:verify` added. NOT closed as CI: the job checks out at depth 1 and the verifier reads the frozen baseline commit through `git archive`, so wiring it in needs the history fetched first. |
| D-112 | 2026-09-14 | density comparison (G2.5) | 22 successor surfaces, first viewport | Brief X6.2 requires a successor's ink coverage to be ≤ 60% of its predecessor's. Measured 2026-09-14 across 22 routes × 2 viewports with both halves on one machine: **3 of 44 within the rule, 41 outside it**, median ratio 0.9703, range 0.3657–1.5002 — and all three passes are on rows with no honest predecessor (D-113), so among the **28 measurements whose predecessors were real rendered pages there are none**. The rewrite is about as inky as the product it replaced, which is the outcome X6.2's own summary line says the relative rule exists to prevent. Twenty public rows, which no file feeds, are the unarguable subset: median 0.9473, none within 0.6, three denser than their predecessors (`/changelog` 1.1366 and 1.0996, `/` 1.0782 and 1.0673, `/providers` 1.0711). | high | open. Not an engineering defect with a patch: either the surfaces get substantially sparser or X6.2's threshold moves, and the second is the operator's signature — filed as item 14 of `docs/protocol/brief-corrections-proposed.md`. Evidence: `docs/evidence/density-post-change/`. |
| D-113 | 2026-09-14 | density comparison (G2.5) | baseline capture fixture | Sixteen of the 44 comparison rows have **no honest predecessor**. First recorded as a fixture confound — different synthetic sources on the two halves — which understated it. `scripts/density-baseline/supabase-fixture.mjs` is a PostgREST stub, and three of the four derived layers the baseline's authenticated pages showed are literals in that file: the admixture result (EUR 0.54, AFR 0.18, EAS 0.12, SAS 0.10, AMR 0.06, `markersUsed` 82, "82 of 120 ancestry markers covered in this synthetic fixture"), the mtDNA and Y calls (H, matched 5 of 7; not determined) and the three polygenic scores (percentiles 62/33/78, coverages 0.82/0.74/0.69). Only `variant_calls` came from the sample. The baseline's ancestry page names 120 markers; the shipped estimator reads 168. | medium | closed as not-applicable rather than fixed. No upload on the successor side can reproduce a number that was never computed, so X6.2's own clause governs: absolute budgets apply and the relative comparison does not. Uploading the baseline's sample to HEAD was measured and rejected — it covers 5 of the 168 shipped ancestry markers, 3%, below the reliability floor, so it would trade one incomparable row for another. It changes no conclusion: strike all 16 and the remaining 28 still contain no row that meets the rule. |
| D-114 | 2026-09-14 | density comparison (G2.5) | `/overview` at 390×844 | The largest rise in the relative comparison: ink coverage 0.0642 → 0.1158, **1.8039×** its predecessor. The predecessor showed four number cards above the fold; the successor showed the plain-language distinction between a statistical estimate and a specific-variant report as two headed paragraphs, over a persistent bottom navigation bar the predecessor did not have. The operator judged the mobile figure a defect and the desktop one (0.9953) not. | medium | fixed in part (this branch): the two layer definitions became keyboard-operable disclosures whose summary is the count line and its short gloss — the pattern `/genome/[subject]/reports` already uses for these same two sentences. 245 of 574 first-viewport characters moved behind them; **1.8039 → 1.5002** at 390×844 and 0.9953 → 0.8738 at 1280×800. Still over the rule, and the remainder is structural: three domain cards with descriptions plus the bottom bar, against four numerals and no bar. Going below 1.0 means removing content or navigation, which is a product decision rather than a density fix. |
| D-115 | 2026-09-14 | absolute density budgets (G2.5) | `docs/density-baseline.json` methodology | The prose measure is wrong in two independent ways. (1) **Element type**: it reads every rendered `p` **or `li`**, anywhere in the document, where the brief says "prose `<p>`" (line 197), "No prose `<p>` in `(app)`" (line 491) and explains the 45ch floor by arithmetic on the content column (line 569) — a three-character list item cannot clear a floor meant for body prose. (2) **Box versus line**: it reads the element's bounding box, and a block paragraph fills its column, so a short sentence in a wide container reports a wide measure while rendering no long line at all. The adjacent-section gap has a third, smaller problem: it reads `next.top - previous.bottom`, the space **between** boxes, while `src/components/legal/legal-page.tsx` separates its sections with `border-t` plus `py-8` **inside** each, so `/about` reports 0px where a reader sees 64px. | medium | MEASURED 2026-09-14, and the size of it took two passes to get right — the first estimate was a guess ("roughly 57 of 83"), the second measured only the element type ("about six") and missed the box-versus-line half. `measure.mjs` now records all three readings side by side, added rather than substituted so every number already recorded stays comparable. Result: element type moves the 45ch floor from 21 misses to **15** (six rows); box-versus-line shows `/files` at a 112ch box against a **56ch line** and `/` at 112ch against **59ch** (two rows). **Eight of the 83 are the measurement.** The same distinction found **seven real long-line defects the box basis could not separate out** — the legal and about surfaces rendered 85ch LINES — fixed in D-121. The section-gap misses stand: 64px is still short of 96px. |
| D-116 | 2026-09-14 | ancestry resolution (task 41) | `docs/ancestry-resolution.md` | The document's own claim that gnomAD's HGDP+1kGP set reaches seven regions **including OCE** is true of the callset and false of the public API it proposed reaching it through. Measured over all 168 panel markers: the API returns 73 named populations and **no Oceanian population at any marker** — no Papuan, no Bougainville, no Melanesian — nor San, Mbuti or Biaka. OCE was half the argument for preferring this set over 1000 Genomes. | medium | closed 2026-09-14. The operator ruled to take the bulk callset, and it was read without downloading it — the release ships a tabix index, so 168 markers cost about a second each through HTTP range requests. All 78 populations are present at every marker, Oceania included. |
| D-117 | 2026-09-14 | ancestry resolution (task 41) | reference set | The sample-size floor falls almost entirely on the same people the set already covers worst. Of 73 populations, 22 carry fewer than ten people — Bantu South Africa and Surui 1, Karitiana 2, Colombian 4, Bantu Kenya and Pima 5, then fifteen East and Southeast Asian populations at 6–9. Verified not to be a badly-covered-marker artefact: for almost all of them the median count across markers equals the minimum. A European has ~12 candidate labels; someone of Indigenous-American ancestry has one (Maya, 20 people); someone of Papuan ancestry has none at any floor. | high | REDUCED, not closed, 2026-09-14. Measured against the full callset and the release's own `hgdp_tgp_meta.genetic_region` mapping rather than a reading of names: at a ten-person floor every region keeps at least two populations — AFR 11, AMR 7, CSA 13, EAS 8, EUR 12, MID 4, OCE 2. East Asia is the severe loss (15 populations at 6–10 people) and the floor is load-bearing (at fifteen people Oceania falls to zero). The inequality is smaller than the API made it look and it is still real. |
| D-118 | 2026-09-14 | ancestry resolution (task 41) | the proposed naming rule | `docs/ancestry-resolution.md` proposed printing a specific population name only when its interval supports it, asserting that the interval measures the leave-one-out instability. **It does not, and neither do two other candidates.** Measured over 8 populations × 2 conditions × 6 seeds at 51 populations and 168 markers: the interval's low bound separates best at t=0.60, where it names 45.8% of represented people correctly and still names 16.7% of unrepresented ones; the fitted log-likelihood separates *negatively* almost everywhere; label agreement reaches +0.27 and still names 35.4% of unrepresented people. Confidence runs the wrong way in the cases that matter: a Peruvian with Peru removed is called Maya at 0.80 with **100 of 100 resamples agreeing**, while a Gujarati the set does contain is called Gujarati at 0.32 with 26 of 100. | high | open, and it blocks the design rather than the code: a single specific population name gated on a confidence measure has no gate. Showing the resample distribution instead of deciding from it is not ruled out and is an owner decision. `scripts/ancestry-resolution/measure-naming-rule.mts` reproduces every figure. |
| D-119 | 2026-09-14 | absolute density budgets (G2.5) | `src/components/site/app-shell.tsx` | Every authenticated surface rendered **16px** of primary-content left padding at 390×844 against the brief's 24px minimum (`thresholds.mobile390.primaryContentLeftPaddingPxMin`): the shell's `<main>` carried `px-4` below `md`. Eight of the 44 measurements, and the only absolute miss in the set that a measurement alone settles rather than a design decision. | medium | fixed (this branch), **stepped at 360px after the first attempt broke a different rule**. `<main>` and the account header carry `px-4` below 360px and `px-6` from 360px up. Raising it at every width narrowed `/genome/[subject]/data/browser` by 16px and pushed igv.js's own overflow from the **446 CSS px** recorded in `docs/accessibility-divergence.json` to **454**, failing the 320px reflow check — caught by CI on run 561 and reproduced locally. Full-bleeding the widget below `md` was tried first and moved nothing, so the overflow is not the widget's width. The density minimum is measured at 390px and reflow at 320px, so the breakpoint satisfies both rather than trading one for the other; ADR-0029's rule that accessibility wins a collision was not needed, because there is no collision at a viewport either rule actually names. Verified both ways: all eight surfaces read 24 at 390px, and the reflow sweep passes with the browser page back at 446. |
| D-120 | 2026-09-14 | ancestry resolution (task 41) | the panel, on North African ancestry | Measured on the full callset: scored on the REGION of the top population rather than its name, the estimator is right 71 of 72 times when the person's population is in the reference set and 62 of 72 when it is not — but the ten failures are not spread out. **Mozabite fails 6 of 6**: an Algerian whose population is absent is called Sardinian, French or Finnish, so MID is read as EUR. Bedouin fails 2 of 6. Every other target, including both Oceanian populations, is 5 or 6 of 6. | high | open, and it is a disclosure rather than a fix: this panel's 168 markers cannot separate North Africa from southern Europe, and any region-level surface built on it has to say so where a reader will see it. Reproduces from `scripts/ancestry-resolution/measure-naming-rule.mts` with the callset source. |
| D-121 | 2026-09-14 | absolute density budgets (G2.5) | `src/components/legal/legal-page.tsx` | Seven surfaces — the six legal pages and `/about` — rendered body prose at **85 characters per line** at 1280×800, against the brief's 68ch cap (X6.3). The container is `max-w-3xl`, a pixel width, and `ch` scales with the font size, so a 14px body column that looks narrow measures wide. Invisible on the contract's box-width basis, which reported the same 85ch for surfaces whose longest line was 63ch; found only once the longest RENDERED line was measured (D-115). | medium | fixed (this branch): `max-w-prose` on the prose blocks rather than on the column, so headings keep the wider measure. Verified by re-capture on the line basis — 85ch before, 63–65ch after, on all seven — and the relative-comparison median falls from 0.9703 to 0.9371 in the same change. |
| D-122 | 2026-09-14 | ancestry resolution (task 41) | the panel, on every reader whose ancestry sits between two regions | D-120 asks whether the LARGEST share lands on the right region. A reader is not shown an argmax; they are shown numbers, and the wrong ones are large. Measured over all 78 reference populations, 20 simulated people each, drawn from those populations' own published frequencies and fitted against the region table built from the same callset — so this is the panel at its most favourable, with the person's own population present in the reference set. **16 of the 69 non-AMR cohorts put at least 0.10 of the mixture on a region that is not their own; 8 put at least 0.20.** The confusion is bidirectional and concentrated: a Sardinian is told 0.229 Middle East and a Tuscan 0.162; a Druze is told 0.282 Europe and a Palestinian 0.156; a Mozabite 0.210 Africa. South and Central Asia leaks the same way — Balochi 0.236 Europe, Makrani 0.234 Middle East, Brahui 0.223 Europe, Kalash 0.179 Middle East, Pathan 0.178 Europe — and Hazara puts 0.326 on East Asia against 0.394 on its own region. Northern and western Europe is clean by comparison (CEU 0.004 MID, FIN 0.000, GBR 0.012, Russian 0.000, Basque 0.017). The AMR cohorts are excluded from the count because "admixed American" denotes admixture rather than a place, so a mixture answer is not necessarily wrong there; they are the largest numbers in the table all the same (CLM 0.397 EUR, PUR 0.373 EUR). | high | open. This is not noise that more resamples would shrink: it is the mean over 20 draws, and it is what the panel believes. Printing 0.229 next to "Middle East" for a Sardinian reader is an invented magnitude, which the project's own rule against invented numbers forbids. No region surface ships without either a disclosure carrying these figures or a merge of the regions the panel cannot separate. Reproduces from `scripts/ancestry-resolution/measure-region-confusion.mts`. **Extended 2026-09-14: both cheap fixes were measured and neither works.** (a) More markers: subsampling the panel with each person's own population held out, the non-AMR counts run 32/21/9 at 21 markers, 22/11/4 at 84 and 21/11/5 at 168 — flat from 84 on, and Druze sits at 0.48 at every panel size. (b) Fitting all 78 populations and rolling their shares up into regions: with the person's own population present this looks like a free win (10/2/0 against the region fit's 16/8/1), and HELD OUT it is worse than what it replaces (14 cohorts above 0.20 against 11, 5 above 0.30 against 2) — a Druze without Druze in the set becomes 0.525 Europe. Removing one population barely moves a regional average but deletes the only component that could represent that person, so the fine model reaches for neighbours and neighbours cross region borders. The capped region fit stands. Held out — the real-world case — its own figures are 20 of 69 non-AMR cohorts at >=0.10, 11 at >=0.20, 2 at >=0.30, with Druze 0.457 EUR, Sardinian 0.292 MID and Melanesian 0.224 EAS; treat the present-population figures above as the ceiling and these as the expectation. **The obvious remedy also backfires:** merging EUR and MID — the boundary this defect names — takes cohorts above 0.30 from 2 to 7, because Balochi, Brahui, Pathan, Makrani and Kalash were splitting their error between the two and the merge collects it into one bucket (Balochi → EUR+MID 0.402). A merge concentrates errors that were previously spread. Merging EUR+MID+CSA does work (9/5/2 against 20/11/2, mean worst 0.055 against 0.084) at the cost of the Europe/South Asia split the estimator ships today. Reproduces from `measure-region-merges.mts`. **A remedy that does work, measured 2026-09-14:** merge per READER rather than per product — fit the seven regions, then combine EUR/MID/CSA into one reported row only when two or more of them carry >=0.10 for that reader. Held out, by mean-of-max, this takes the non-AMR counts from 27/14/11 to 10/4/2, matching a global EUR+MID+CSA merge (10/4/2) while merging for 21% of readers instead of 100% and keeping 2.1 reported rows against the global merge's 1.9. Its cost is real and must ship with it: the rule cannot tell genuine two-region ancestry from confusion and merges both, so a reader with real European and South Asian parentage is shown one figure where two were true. Reproduces from `measure-adaptive-merge.mts`. **Update 2026-09-15:** the owner selected the strict `>0.10`/all-three rule and expandable uncertain components. The versioned reference and actual-fitter held-out measurement are complete; the implementation is under release verification. It merges 329/1,560 equally sampled synthetic draws (21.09%), with 10/4/2 tail counts; 265 reports differ from the earlier hot-only rule. See `data/ref/AIMS_SEVEN_REGION_PROVENANCE.md`. The scientific ambiguity, subgroup limitations and disproportionate loss of detail for mixed ancestry remain open; this is not calibrated component resolution. |

| D-123 | 2026-09-15 | Authority-path code review | Family ancestry page | The shared ancestry page passes counterpart subjects into `loadAncestryResultSnapshot`. That loader applies the own-subject purpose gate to legacy files and owning-account RPCs to canonical files. `own-analysis-access.ts` explicitly states that the own-subject gate must not govern Family: a counterpart holds a separately scoped Family permission, not the owner's own-subject grant. Thus a valid counterpart ancestry grant can still yield no rows. | high | fixed and released 2026-09-17. Exact-head CI at `12807a9` passed 5,097 unit tests, 3,151 SQL assertions including the 93-assertion Family file, and 424 browser cases with the synthetic Family ancestry journey. The migration was applied to the Inherit project as hosted version `20260917180516` (`family_ancestry_shared_authority`, source SHA256 `5a4de206…`) after a read-only catalog preflight and a DDL guard; the postflight matched all 13 function bodies, ACLs, the private table and its three triggers byte for byte, and the name-based ledger comparison found no drift. A rollback-only production probe with fresh synthetic identities passed 93/93 checks — current sharing, canonical v1/v2/v3 and legacy reads, fresh confirmation replacing an old grant, nonce replay, owner and recipient sessions, owner-purpose and directional withdrawal, pause/resume, source drift, immutability, stale binding, unrelated recipient, prepared-source refusal, pagination, operational-proof cleanup on revocation, revision, terminal transitions and parent deletion with the sibling direction preserved, and every file status — with zero residue across 22 fixture-keyed tables and no mail row. #128 merged as `6588ca2` carrying the exact CI-tested tree; deployment `dpl_JBJLAveM91qXaYNDRB2fvBtj8EE4` is READY on `www.inherit.bio`, eight public routes pass anonymous Chromium checks and the runtime error scan is empty. Not established, and kept open on their own records: an authenticated Family journey in production (browser evidence is CI-only), time-only expiry proof cleanup, prepared-source Family ancestry (explicitly unavailable) and the broader Family graph export/deletion gaps. See `docs/evidence/family-ancestry-release-20260917/`. |


| D-124 | 2026-09-15 | Upload recovery and concurrency review | Original finalization | An interrupted request before its first validation checkpoint cannot resume. Later retries reuse an expired claim without exclusive ownership; copy verification reads saved digest state but never writes partial progress. A failed superseded request can consequently claim cleanup of current work. A retryable route response also needs to retain the original browser upload handle. | high | open: a separately versioned finalizer now rotates expiring claims under the upload lock, checkpoints complete verified ranges and preserves transient failures. Existing authority, expiry and full-hash checks remain. Independent review and 145 focused unit tests pass; fresh database/browser CI and hosted recovery evidence are still required. See `docs/finalization-fenced-recovery.md`. This does not establish background dispatch or monthly WGS capacity. |
| D-125 | 2026-09-17 | CI run 35260238418 (browser suite on the documentation-only pull request #129 tree) | `/genome/[subject]/ancestry` for a Family counterpart (shared ancestry reader) | `e2e/family-ancestry.spec.ts:30` failed at its last step with 423 of 424 cases passing: after the owner turned off their own ancestry purpose, the viewer's reload rendered the deliberate "This page isn't available" page instead of the empty-state sentence "No ancestry result is shared yet." The failure artifact's page snapshot shows the not-found page. The identical tree passed the same case in the exact-head run 34981687918 and in the post-merge run 35258394909, and the re-run of the failed job (attempt 2 of 35260238418, browser step 43.5 minutes against 35.0 on the post-merge run) passed as well, so this is one failure in four runs of one tree and a slower runner alone does not reproduce it. The reader confirms every captured page in one locked check (`FOR SHARE NOWAIT`), which returns false on a contested lock as well as on any change between capture and confirmation, and it withheld the whole capture on that single false; the page renders a withheld capture as not-found. The run was slower than the two green ones (the accessibility sweep took 14.4 minutes against 11.4), which widens the window in which the owner's post-withdrawal page refresh overlaps the viewer's confirmation. The contending writer is not identified from the artifact. | medium | hardened, cause not isolated (this branch). The reader now retries once: when the locked confirmation of a capture returns false, it waits 250 ms (`CONTESTED_CONFIRMATION_RETRY_DELAY_MS`), rechecks the caller's session and Family capability, captures every page again and confirms the fresh capture under the same locks. A capture is still shown only after a true confirmation of that same capture; a scope failure is never retried; a second false, or a refused fresh capture, denies and closes the reader as before, so a genuine withdrawal still denies. Three unit tests cover the retried confirmation, the second denial staying closed and no retry after a refused fresh capture (30 pass across the reader and page suites). The retry covers an unconfirmed locked check, not a refused first capture, and one green run cannot show that it covers the observed failure, whose incidence was one run in four. Fresh CI is required. |
| D-126 | 2026-09-17 | retention executor audit (read-only survey, verified by `git grep`) | `source.revocation-7d` disposition; cohort restriction notice | `docs/retention.md` registers `source.revocation-7d` ("Delete within 7 days"; targets include `user_variants`, embryo variants and scores, canonical sources, originals and new and legacy Storage objects) for a revoked class or source consent, a stopped or deleted subject and a whole-cohort restriction, and `supabase/migrations/20260831224119_retention_dispositions.sql` inserts it into the registry under the `revocationDispositionWorker` class. Nothing executes it: no `retention_phase_registry` entry, no claim or finish function, no phase in `POST /api/jobs/retention`, and no reference in `src/`, `supabase/tests/` or `e2e/` (`git grep source.revocation-7d` finds only docs, the route register and the registry insert). `restrict_embryo_cohort_v1` deletes embryo figures, scores, QC and variants inline and touches no Storage object and no retention row, while `src/emails/cohort-restriction-notice.tsx` tells both parents "The source files are deleted within 7 days." Every existing residue proof reads `genomes` only (`e2e/file-deletion.spec.ts`, `e2e/account-deletion-purge.spec.ts`); no test reads `genomes-staging` or `generated-artifacts` after a purge, and no test backdates a revocation to prove the day-seven boundary in both directions. | medium | open. No stored data is affected today: embryo ingest is not shipped (G2.6), so no embryo source object exists for the sentence to be false about, and user-requested file deletion is synchronous. The gap becomes a broken promise the day ingest lands. Closing it is implementation, not a test: a retention row on each registered trigger with an immutable seven-day deadline, a claim and finish pair that freezes a manifest of that subject's objects across all three buckets, a job phase that deletes through the Storage API and refuses to finish at any residue, a pgTAP proof that the claim refuses before the deadline and completes only at zero residue, and a browser proof with service-role downloads in every bucket. **Trigger map, same day, verified against the migrations:** none of the three registered triggers can reach a source today. No class or source consent revocation exists as an action: `public.subject_consents` rows (`self_source`, `adult_source`, `embryo_source`, `upload_class`) are only superseded on re-signature or removed by account deletion, `POST /api/consents/[id]/revoke` resolves purpose and cloud-model grants only, and the consents settings page lists `consent_grants` alone. `private.adult_subject_invitation_response_v1` (refuse or delete) shreds contact, marks the principal deleted and the subject purged, and touches no file, variant, source or object; third-party adult uploads are not accepted yet (G2.6). `restrict_embryo_cohort_v1` says in its own comment that source-object purge belongs to the withdrawal slice, and the schema already carries embryo object paths (`genome_storage_objects.cohort_id`, `embryo_ingest_fragments.object_id`) with a ready enumeration in `prepare_embryo_ingest_unwind_v1`. The only path that reaches Storage is account deletion. The executor to mirror is the `purpose.derived-60s` chain (`prepare_own_report_purge_v1` → immutability triggers → `execute_own_report_purge_v1` with its residue refusal → `drainOwnReportRevocations`) combined with the three-bucket manifest form of `claim_due_account_deletion_v1`; `supabase/tests/v2_contracts.sql` pins the phase registry at 53 rows and moves with any new phase. **Owner decision needed before implementation:** whether a class or source consent revocation action exists at all (route, register entry, copy), since without it the executor can only be driven by subject stop and cohort restriction, and both of those trigger on data that cannot be stored yet. G5.3a stays NO. **Decided 2026-09-18 and folded into immediate deletion.** The class keeps its ID and moves to `inlineEventDriven` (`supabase/migrations/20260918113000_source_revocation_inline.sql`, which refuses to run unless exactly one registry row moves); no worker, phase or separate consent screen is planned, and the register wording is "immediately, and within 7 days at the latest". Executors as they now exist: self file deletion (`DELETE /api/files/[id]` → `prepare_genome_file_deletion_v1`/`finish_genome_file_deletion_v1`, whose bodies moved verbatim into `private.prepare_genome_file_deletion_body_v1`/`private.finish_genome_file_deletion_body_v1`: Storage ACK then `storage.objects` absence, cascades of `user_variants`, `user_prs`, `ancestry_results`, `report_observed_calls` and the private runs, prepared R2 artifacts payload-tombstoned through `finish_own_prepared_cleanup_v1`), cohort restriction (derived rows inline; `supabase/tests/embryo_cohort_runtime.sql` now seeds one row per derived table and asserts zero after, plus zero cohort objects and sessions), and account deletion (three-bucket manifest). Added, the seven-day backstop the wording still needed: `claim_due_genome_file_deletion_v1` (claim token, 15-minute retry delay measured from the record's start or last attempt, `for update skip locked`, one record per claim), `prepare_own_prepared_file_cleanup_claimed_v1`, `finish_genome_file_deletion_claimed_v1` and `fail_genome_file_deletion_claim_v1`, drained by `POST /api/jobs/retention` (`src/lib/uploads/file-deletion-backstop.ts`) with the record's own bucket and name and never a caller-supplied target; `supabase/tests/genome_file_deletion.sql` proves the claim is refused before the delay, granted after backdating, not handed to a second worker, released on failure, and that the claimed finish refuses while `storage.objects` lists the name and succeeds once absent with every `auth.sessions` row deleted; `e2e/file-deletion.spec.ts` strands a real deletion, ages it, posts the job and asserts zero residue in `genomes`, `genomes-staging` and `generated-artifacts`; `e2e/account-deletion-purge.spec.ts` reads the same three buckets after the purge. Not added and still open: the constraint refusing `own_preparation_config.enabled` with `artifact_provider='supabase'`. Seven pgTAP files (`own_preparation_checkpoints`, `own_preparation_jobs`, `own_preparation_retry_sequence`, `own_prepared_export_content`, `own_prepared_member_authorization`, `own_prepared_publication`, `own_prepared_report_authority`) enable that provider and assert its reserve, register and publish path, so the guard needs their rewrite first; until then an enabled Supabase-provider configuration would keep every later deletion pending (`cleanup.ts` returns unresolved for that provider) and make "immediately" false. Production is disabled with 0 jobs. `src/emails/cohort-restriction-notice.tsx` is unchanged on purpose: `restrict_embryo_cohort_v1` deletes no source object and no embryo source can exist before ingest ships, so neither "now" nor "within 7 days" describes an executed deletion; the sentence changes with the cohort source executor. **Applied to the Inherit project 2026-09-18.** After the read-only DDL guard passed at 10:04 UTC, both migrations were applied with their exact repository text as hosted versions `20260918100630` (`own_preparation_monthly_cap`) and `20260918100756` (`source_revocation_inline`); the postflight found every function body byte-equal to the repository, the registry row in `inlineEventDriven`, and the month ledger empty with RLS on; a rollback-only probe passed 14/14 (`docs/evidence/priority1-foundations-release-20260918/`). CI run 35327691091 on the identical tree (`9a2491c`, merged as `adfd0a7` in PR #136) carried the new pgTAP and browser proofs green. Closed by decision and evidence; the provider guard and the cohort source executor stay recorded above as open work. |
| D-127 | 2026-09-18 | main post-merge run 35319131306 (browser suite on the documentation-only pull request #135 merge, `ca88d9d`) | `e2e/portrait-no-file.spec.ts` fixture (`acknowledge`) | `e2e/portrait-no-file.spec.ts:254` failed at its first assertion after sign-in with 425 of 426 cases passing: `/family/portrait/[pairId]` rendered the blocking screen (`[data-slot="portrait-blocking"]`, count 1) for account A after the fixture had completed every step for both adults and asserted, from the database, no file and two live Portrait grants. The identical tree passed the case on pull request run 35315053234 an hour earlier. The failure artifact (10537965764) carries no page snapshot, so the missing step is not recorded. Read at source: the fixture's `acknowledge` helper checked the box, clicked the button and returned at once, and the next statement signed out through `page.request.post("/auth/sign-out")`. The form's own request (`POST /api/family/acknowledge`, then `router.refresh()`) can be overtaken by that sign-out on a slow runner (this run's browser suite took 29.3 minutes against about 26 on green runs), so the acknowledgement is refused or never sent and `portrait_acknowledged_at` stays null for that adult. The fixture's database claim covered files and grants but not the two stamps the page checks (`portrait_acknowledged_at`, `independent_login_at`), so the state passed through to the test. The page's behaviour is correct: a missing step renders the blocking screen. | medium | fixed at the fixture (this branch): `acknowledge` now waits for the acknowledgement response (200) and for the refreshed page to drop the form before returning, and the fixture's final claim reads both pair subjects from `subjects` and requires both stamps to be set. The product is unchanged and no assertion was weakened. The single re-run of the failed job was spent to confirm the first case; its result is recorded in the G1.5 row. The two acknowledgement clicks in `e2e/portrait.spec.ts` already wait on the refreshed page and needed no change. Attempt 2 of run 35319131306, the single re-run of the failed job, passed at 08:51:45 UTC on the unchanged tree; the fixed fixture passed on runs 35322437702 (`f7e7a2d`) and 35327691091 (`9a2491c`, the tree merged as `adfd0a7`). Closed. |
| D-128 | 2026-09-18 | fixture work for the hosted proof (`scripts/synthetic-wgs-fixture.test.ts`, gVCF mode) | `src/lib/genome/input-provenance.ts` (`countInputLines`, `listed-calls-v1`) and `src/lib/genome/observed-calls.ts` | A synthetic single-sample gVCF of 400 reference blocks and 100 called sites (`G,<NON_REF>` in ALT, as a haplotype caller writes a call inside a gVCF) parses to 100 variant records through `parseVcf`, while the provenance counter that observes the same stream reports `called 0, noCall 0, blocks 500`: its block test is `ALT includes "<NON_REF>"`, which is true of every gVCF row, and `observedVcfPointCall` accepts only a single-base ALT, so even with the block test narrowed to a bare `<NON_REF>` the called sites would count as unsupported. The report's inputs are right; the coverage explanation for a gVCF would say the file contributed no called sites. Reproduced in the fixture test, which asserts only the row total until this is fixed. | medium | fixed on this branch, by the owner's decision of 18 September to correct it under the same version strings (eight migrations pin `listed-calls-v1` and a check constraint pins `vcf-literal-diploid-snp-v1`; the change is confined to gVCF rows, which no reader had ever counted as calls, so no stored VCF snapshot changes meaning). `countInputLines` now files a row under blocks when INFO carries `END=` or `SVLEN=`, FORMAT carries `LEN`, or ALT carries `<NON_REF>` with no concrete alternate called (`isReferenceBlockRow`); otherwise `observedVcfPointCall` drops the trailing `<NON_REF>` allele and reads the site, refusing it unless one copy carries the concrete alternate, so `A,<NON_REF>` with `0/0` still never becomes a reference finding, as `observed-calls.test.ts` records. Proven by the new gVCF case in `input-provenance.test.ts` (nine rows: 2 called, 5 blocks, 1 unsupported, parser output unchanged), eight new observed-call cases, and the synthetic gVCF fixture now counting 400 blocks and 100 calls. Green on pull request run 35362848403 (`060dbc8`), merged as `73b298b` (PR #141). Closed. |
| D-129 | 2026-09-18 | route-state work on `/family/[person] not-covered` (`e2e/family-coverage-states.spec.ts`, the two-account journey with a file that covers nothing the product reads) | `private` shared-report capture, readiness mode (`20260907153506_health_picture_canonical_results.sql`, the `hasReports` expression) and the Family hub card (`src/app/(family-hub)/family/page.tsx` `cardState`, `src/components/family/person-card.tsx`) | Another adult has shared both report layers, their file is prepared and both layers were generated, and the file reaches no report. Past the Tier-2 gate the person page says so honestly ("… file covers none of the … reports"). The hub card says "No shared results yet" — the line that means a result is still to come — because readiness computes `hasReports` as `exists(item where covered = true)`, so a completed run that covers nothing is reported to the other adult as not having happened. The card cannot tell "no result yet" from "no result possible", and it errs toward the false "not yet". The spec pins the misreading rather than endorsing it. | medium | fixed and released. The honest card for a completed run is "Reports ready" whatever it covers, with the person page carrying the coverage sentence past the gate as it already does, so readiness now asks whether a completed shared run exists rather than whether it covered anything: `supabase/migrations/20260918213000_shared_report_readiness_completed_runs.sql` replaces the two readers (`public.family_shared_report_results_v1` and `private.health_picture_own_page_v1`) with bodies that differ from the deployed ones only by the dropped `covered` conjunct, keeping grants, authority, receipts and content mode as they are; it discloses nothing the gate withholds, since "Reports ready" already renders for a covered file before the gate. Proven by one pgTAP case in `supabase/tests/family_shared_report_results.sql` (the fixture run completed again through the real wrapper with its one report captured as not covered: readiness true, content carrying the report as captured, no payload in the readiness response) and by `e2e/family-coverage-states.spec.ts`, whose hub assertion on the not-covered journey now reads "Reports ready" and refuses the awaiting line. **Released 18 September 2026, 23:18 UTC**: PR #145 merged as `c78d287` (its run 35401497676 green, the first execution of the migration, the pgTAP case and the hub assertion), the production deployment of that commit READY at 23:16:01 UTC, then the migration applied to the Inherit project as hosted version `20260918231857` after a read-only preflight and a DDL guard, with a byte-equal postflight and a 13/13 rollback-only probe; both completed report runs in production were announced under both predicates, so no card changed (`docs/evidence/shared-report-readiness-release-20260918/`). Closed. `/family not-covered` stays a finding in `docs/protocol/brief-corrections-proposed.md` for its own reason: the card has nothing to say about coverage. |
| D-130 | 2026-09-21 | correcting the `genomes-staging` row in `docs/route-divergence.json`, which meant reading every path that names a staging bucket | `public.claim_due_account_deletion_v1` (`supabase/migrations/20260901034000_account_deletion_purge_runtime.sql:416`) and `public.prepare_embryo_ingest_unwind_v1` (`supabase/migrations/20260905203457_embryo_ingest_unwind_runtime.sql:415`) | Both functions build a Storage manifest, and both enumerate upload-session staging objects under the literal `'genomes-staging'`. Both predate `20260906133807_cutover_subject_upload_transport.sql`, which dropped `genomes_staging_create_once`; since that cutover every dedicated-role staging object is written to `genomes`, and `upload_sessions.storage_bucket` says so on every such row. Neither builder has been replaced: grepping each name across `supabase/migrations/` returns one `create function` and its two grant lines. So the account-deletion manifest's third union leg, guarded by `so.bucket_id = 'genomes-staging'`, matches nothing for any post-cutover session, and the manifest - the record of exactly which objects deletion promised to remove - does not name the staging object. **It is inert today, and the first draft of this entry claimed it blocked account deletion. That was wrong.** Checked case by case rather than assumed. Post-cutover session (`token_jti is not null`) with `status <> 'promoted'`: the fence at `20260907134656:155-162` fires on its *first* disjunct, `u.token_jti is not null and u.status <> 'promoted'`, which defers deletion for every in-flight session whether or not any bytes exist - so the manifest changes nothing. `status = 'promoted'`: nothing is left to enumerate, because `src/lib/uploads/subject-finalization.ts` removes the staging key at its `staging-removed` phase *before* calling `complete_own_upload_finalization_v1`, and the promoted final object carries a `genome_files` row the manifest's second leg already covers. Legacy session (`token_jti is null`): the bytes really are in `genomes-staging` and the literal is correct for them. So no live deletion is wrong. What is wrong is that two records of the same fact disagree, and the guarantee is carried by a fence that blocks rather than by a manifest that deletes. The embryo-ingest half is the one that will bite: `prepare_embryo_ingest_unwind_v1` has no equivalent fence - `20260905203457:423-425` says so in terms, there is deliberately no Storage ACK or terminal graph-purge RPC yet - so the wrong bucket would be baked into the manifest its executor consumes. Ingest is unbuilt (G2.6), so nothing runs it today. Found by reading, not by a failing test: nothing exercises either manifest against a post-cutover session, which is why the cutover could leave two builders behind without anything going red. | low | open, deliberately unfixed here. The correction is one column reference in each builder - `us.storage_bucket` instead of the literal, exactly what `20260907134656` already did to the fence - but `create or replace function` restates the whole body, so fixing the account-deletion half alone is a ~200-line restatement to change two words in a path that is currently inert, which is more risk than the defect. That half should be corrected the next time the function is opened for another reason, or on its own with a pgTAP proof that stages an object in `genomes`, claims a deletion and asserts the manifest names it - planted by restoring the literal and watching exactly that assertion fail. The ingest half belongs in the embryo-ingest E0 work, where `prepare_embryo_ingest_unwind_v1` is on the path anyway and no fence exists to hide the error; it must not ship carrying the literal. |
| D-131 | 2026-09-21 | CI run 35611746310 on the chunk-door pull request: the new pgTAP file emitted 55 passing assertions, then exited 3 with `No plan found in TAP output` | `public.embryo_ingest_sessions.source_format` and `.reference_build` (`supabase/migrations/20260905192551_embryo_ingest_session_lifecycle.sql:12-13`), read by `private.bind_embryo_fragment_object_v1` (`supabase/migrations/20260905203457_embryo_ingest_unwind_runtime.sql:25-28`) | The proof reserved a chunk carrying one fragment. psql answered `ERROR: ingest object binding unavailable`, raised by the before-insert trigger on `embryo_ingest_fragments`, reached from `private.reserve_embryo_ingest_chunk_v1` line 87 through the new public wrapper - so the delegation being proved worked, and the fragment is what could not land. The trigger refuses unless the session has `status='open'`, a non-null `account_id` and `upload_id`, a `source_format` in (`vcf`,`gvcf`,`pgt_table`) and a non-null `reference_build`. Both columns are declared nullable and **nothing writes either one**: `private.create_embryo_ingest_session_v1` names 24 columns in its insert and neither is among them, no `update public.embryo_ingest_sessions` anywhere sets them, and grepping both names across `supabase/`, `src/` and `worker/` returns only the column definitions, the HTTP authorization payload that reads them back (`20260905202656:63`) and this trigger. So no chunk carrying even a single fragment can be reserved today, by any caller - the fragment is the whole point of a chunk, so the transport is closed at its last step. Same shape as the mapping route's challenge store: a column created, read and given a retention order, never populated. | medium | open, and it is a missing step rather than a bug: embryo ingest is unbuilt (G2.6), so nothing runs this path in production. It is a named blocker for the ingest work - the chunk route cannot carry a fragment until the step that declares the uploaded file's format and reference build writes these two columns, and that step does not exist. The pgTAP file now asserts the closure instead of tripping over it: two `ok(... is null)` lines and a `throws_ok` for `55000 ingest object binding unavailable`, so the day a writer lands, those three fail and the fragment-carrying proof is restored. Delegation is meanwhile proved with an empty fragment array, which the reservation accepts and which still writes the receipt row, its `stored` transition and the session's next sequence. **Where it belongs is not an open question - the register already says, and names files that do not exist.** `referenceBuild` appears exactly once in `docs/route-register.json`: on `api.embryo-ingest-mapping`, as the second of two `oneOfClosedBodies` on its PUT, `{challengeId, referenceBuild: enum[GRCh37, GRCh38, unknown], nonce}`. That is the **fallback**, not the source. `policyContracts.genome-build-inference-v1` is the source: the server compares the input positions against both builds, accepts one only on at least 1000 discriminating positions and at least 0.99 agreement, and may reach `decision-required` - which is the only thing that issues that challenge - with `unsolicitedClientBuildDeclarationOrThresholdOverride` explicitly `forbidden`. Its `implementationTarget` is `src/lib/genome/build-inference.ts` and its `referenceData` is `data/ref/build-discriminating-sites.json`; **neither file exists**. `source_format` is the same shape one field over: `policyContracts.genetic-file-ingest-v1` puts detection on the server (`classify-as-pgt_table-only-when-at-least-three-of-sample-embryo-rsid-genotype-chrom-pos-resolve`, and `serverPreflight` says to sniff the first decoded bytes and never trust extension, content type or browser result), and the chunk route carries both contracts. So the work is: build the inference stage the register specifies, then give it an RPC that writes the two columns on the session before the first chunk reserves - `reserve_embryo_ingest_chunk_v1` takes neither as a parameter, so it cannot be folded in there. Only after that is the mapping route's build challenge reachable, which is a second reason it is the last piece rather than the first. **And the reference data is the blocker, measured rather than assumed.** The shape `build-discriminating-sites.json` needs is an rsID to (GRCh37 pos, GRCh38 pos) table, because a bare position discriminates nothing - a number is consistent with either coordinate system, and only a site whose two builds disagree, matched to the position the input actually carries for it, is a vote. The repository already holds 134 rows of exactly that shape, at `data/ref/grch38_to_37.json`, built by `scripts/generate-synthetic-sample.ts:50` against Ensembl REST `/map/human/GRCh38/{chr}:{pos}..{pos}/GRCh37` at build time and cached so the generator stays offline-reproducible. Every position set in the repository together is short of the floor, counted rather than estimated: `aims.json` 168, `aims-seven-region.json` the same 168, the PGS panels 424, 223 and 50, that cache 134 - **980 distinct rsIDs in total**, against a minimum of 1000. Even taking every site the product holds from any source, and generously assuming every one of them discriminates, the repository is twenty short of the number the register requires before a build may be accepted at all. The chain at `data/ref/chain/GRCh37_to_GRCh38.chain.gz` (Ensembl, provenance recorded, already parsed by `src/lib/genome/liftover.ts`) can supply the second coordinate for any site, but it is a coordinate mapping, not a site list. And the floor is on positions **compared in the input**, not on rows in the file, so to overlap 1000 sites of an arbitrary lab-specific PGT table the reference set has to be far larger than 1000 - a common-SNP panel, tens of thousands of sites. Choosing that panel is an owner decision with a licence and a provenance entry in `docs/dataset-licenses.md` attached, in the shape `data/ref/AIMS_PROVENANCE.md` already sets. **Not invented here**, which is why this defect stops at naming it. Once the panel is chosen, the derivation is mechanical and the two files follow. |
| D-132 | 2026-09-21 | verifying the deployed migration ledger against `main`, because fresh CI cannot prove a production schema and the goal says to check the live state rather than trust a merge | `public.jurisdictionDenied` (`src/lib/embryos/guards.ts:38`), the four Embryo routes it gates, and the two migrations `main` carries that production does not | Read `supabase_migrations.schema_migrations` in project `zuvloczwgrayonqabnss` on 2026-09-21 and compared it to `supabase/migrations` at `main` **by name**, because hosted versions and repository filenames disagree for several 5 September entries. Production holds **118** applied migrations and `main` carries **119**, so production is **behind by exactly two**, both authored today: `20260921130000 embryo_cohort_finalize_public_door` and `20260921150000 embryo_ingest_chunk_public_doors`. The one production-only name, `install_own_report_retention_scheduler`, is the operator-only migration with no repository file that `docs/protocol/decisions.md:2848` already records, so it is not drift. The deployed application calls one of the two missing functions: `src/app/api/embryo-cohorts/route.ts:79` invokes `finalize_embryo_cohort_ingest_v1` through the service role. It never reaches it, and **the reason is not the one that looks obvious**. It is not that `realJurisdictions` is empty, though it is - counted today at **0** entries in `data/jurisdictions.json` against 249 selectable codes. It is that `jurisdictionDenied` returns the 403 **unconditionally** whenever `INHERIT_TEST_JURISDICTION` is unset: it calls `resolveCapability(null, 'embryo_analysis', ...)` and reads only `decision.userFacingCopy`, discarding the permit-or-deny verdict, and it passes `null` rather than the reader's declared jurisdiction. That is deliberate and documented - the function's own comment says only the TEST-LOCAL fixture is enabled in E0, so the flag decides and the resolve is there to source the sentence rather than to reach a verdict - so this is not a bug report and the shape is not the `/settings/people` fault G8.5 records, where an unconditional refusal was mistaken for a working gate. | low | open as an ordering constraint, not a fault: nothing is broken today and nothing here asks for a change today. What it records is that **three things have to land in one order, and each is invisible from the other two**. A signed jurisdiction review alone changes nothing on these routes, because the flag decides and not the verdict. Changing `jurisdictionDenied` to honour the verdict alone turns the 403 into a 500, because `finalize_embryo_cohort_ingest_v1` does not exist in production. Applying the two migrations alone changes nothing a reader can see. The order is therefore: the signed review (G5.5), then the guard taught to read the verdict and the reader's own declared jurisdiction rather than `null`, then the two migrations applied with preflight, postflight and a receipt - never as a side effect of a merge, which is the standing rule for this project and the reason they are still unapplied after their pull requests merged today. Two adjacent readings are left alone rather than corrected, because each is true as written. G5.1b says the Embryo routes apply the resolver; they do call it, and in E0 a verdict and the flag agree on every input, so the sentence is not false today - it becomes false on the day of the first review, which is the day this row is for. And `e2e/embryos.nojurisdiction.spec.ts` proves the refused branch of all four routes with the flag unset, which is exactly the behaviour above and stays correct either way. Filed so that the release which finally answers G5.5 does not discover these two steps one 403 and one 500 at a time. |
| D-133 | 2026-09-21 | CI run 35624921056 refused two task-depth journeys that uploaded the committed 23andMe sample, and the refusal turned out to be about the sample rather than the journeys | `data/samples/synthetic_23andme.txt` and its generator `scripts/generate-synthetic-sample.ts:122-133`, against `policyContracts.genome-liftover-v1` | Ran the shipped parser and the shipped chain over the committed file rather than inferring from the HTTP status. It parses perfectly - **2135 records, 0 skipped**, build read as `GRCh37` from its own header by `parseArray` - and then **850 of those 2135 positions fail to lift to GRCh38, a loss of 0.3981** against the `maximumUnmappedFraction` of **0.05** the register sets. `subject-normalization.ts:217` therefore refuses with `liftover_loss` and `/api/files/[id]/process` answers **422**, so the file cannot complete preparation and nothing downstream of preparation can run on it. **The cause is the filler, and the generator's own header says what it was for.** That header states the file writes GRCh37 positions obtained by mapping each template's GRCh38 position back through the bundled chain, `exercising the liftover path on upload`. The 135 template variants really are mapped that way and really do lift. The other 2000 rows are not mapped at all: `for (let i = 0; i < 2000; i++)` draws `chrom = 1 + floor(rand()*22)` and `pos = 1_000_000 + floor(rand()*200_000_000)` uniformly, with no reference to chromosome lengths and no reference to the chain, so a large share of them name coordinates the chain has nothing to map and some name coordinates past the end of their chromosome. The file was built to exercise liftover and fails it, on positions invented to be filler. | medium | open. **Two claims rest on this file and neither survives the measurement, so both are corrected in the same change rather than left standing.** Row A4 of the acceptance matrix marks a full pass for `Synthetic 23andMe sample renders 100+ reports with genotype-specific results`, describing the sample as `(GRCh37, liftover-exercising)` - right about the intent, wrong about the outcome - and cites `e2e/upload-vcf.spec.ts` as its evidence. That spec does not upload this sample; **no browser test anywhere uploads it**, checked across the whole of `e2e/`. So the row's mark rested on a test that never exercised the claim, which is the failure mode G8.5 warns about, and it is moved to partial with the reason appended. And `docs/self-hosting.md` offered this file as the first of two samples a self-hoster uploads on their first run, which today ends in a 422 they have no way to interpret. **Correcting that line found a second fault on it, and the first correction was wrong too.** The obvious move was to point at the other of the two, `HG001_GRCh38_chr20-22.vcf.gz`, and that is worse rather than better: `data/samples/PROVENANCE.md` records it as a historical artifact that no current browser or pipeline test uses as genetic input, whose active parser role the synthetic pipeline fixture replaced on 15 September 2026, and states that current tests exclude public benchmark genotypes as inputs. So the guide was offering a reader a choice between a file that 422s and real reference material the repository had already stopped using, and the first fix swapped one for the other. The line now names the third file in that directory, `synthetic-pipeline-grch38.vcf.gz` - synthetic, GRCh38 so the liftover step cannot refuse it, and carrying 73 of the catalogue's 146 rsIDs present and 73 absent so a first run shows a covered result and an honest not-covered one - and says plainly that **no test uploads any of the three through the browser**, so the recommendation rests on `pipeline-integration.test.ts` and the file's build rather than on a proven end-to-end run. **The fix is not attempted here, because its blast radius is larger than it looks.** Making the filler liftable - drawing it from the chain's own mapped blocks rather than from a uniform range - regenerates the file, and three committed things read it: two assertions in `scripts/comprehension/bindings.test.ts`, one in `src/lib/family/roh.test.ts`, and a hash-pinned capture in `docs/density-baseline.json` whose ceiling may not rise. A regeneration has to be taken with those four in hand, and the density ceiling checked before and after, which is a session of its own rather than a line in this one. |
