# Defect ledger

Append-only. Every admissible defect has an id, the lens that found it, a
reproduction, a severity and a status. Simulated lenses are labelled
`simulated: true` and are never described as user observations.

| Id | Date | Lens | Surface | Reproduction | Severity | Status |
| --- | --- | --- | --- | --- | --- | --- |
| D-001 | 2026-09-03 | on-call engineer | branch topology | `git merge-base --is-ancestor origin/main origin/codex/readability-incident-response-remediation` is false: 32 verified commits were unreachable from `main` | high | fixed (pull request #41) |
| D-002 | 2026-09-03 | on-call engineer | `pnpm gate:readability` | 12 legal long blocks above grade 11 on `/terms`, `/legal/self-hosting`, `/legal/state-genetic-privacy` | medium | fixed (`a4e58b9`) |
| D-003 | 2026-09-03 | on-call engineer | `/science` | The report chips link to `/science#evidence` and the list's "Why?" to `/science#polygenic`; `src/app/(marketing)/science/page.tsx` had neither anchor | medium | fixed (`47bc818`) |
| D-004 | 2026-09-03 | on-call engineer | `pnpm gate:readability` | Extending the extractor to `src/copy/**/*.ts` and `src/emails/` surfaced 32 findings the JSX-only scan had never seen (29 unregistered plain words, one grade-9.1 mandated sentence, one grade-10.5 e-mail sentence, one contraction read as `dont`) | medium | fixed (`47bc818`, `7810ea6`, `31e1d7c`) |
| D-005 | 2026-09-03 | on-call engineer | `pnpm gate:readability` | Removing the stripped tokens `wont`/`doesnt` before the contraction rule landed broke the gate on `What we won't do.` at commit `7810ea6` (caught in the isolated pre-push verification, never on CI) | low | fixed (`4a3e0c6`, superseded by `31e1d7c`) |
| D-006 | 2026-09-03 | adversarial review (brief, privacy lenses) | `/genome/[subject]/reports/[slug]` | h1, breadcrumb and title carried the gene suffix; no category eyebrow; support panel nested inside "How sure we are"; provenance absent from "Where this comes from"; array explanation chosen when a VCF was present; density markers not on the selectors' elements; two generic bullets | medium | fixed (review-fix batch, this branch) |
| D-007 | 2026-09-03 | adversarial review (brief lens) | `/genome/[subject]/reports` | list header rendered the per-variant VCF sentence with no variant; search input removed against §4.4 item 7 | medium | fixed (review-fix batch) |
| D-008 | 2026-09-03 | adversarial review (privacy lens) | subject bar, Overview | an accepted-invitation record (owner null, subject account = invitee) was shown to the invitee as another adult "Uploaded with their permission" and on the bar as "Shared with you"; `minor` records got a permission chip; "Add a file" on other records uploaded to the caller's own record | high | fixed (review-fix batch) |
| D-009 | 2026-09-03 | adversarial review (brief lens) | `/overview` | an annotated file hid a second upload in flight (State C won over B); "regions found" counted with an invented 2% threshold; strings duplicated from their canonical homes; a second disclaimer in the sidebar | medium | fixed (review-fix batch) |
| D-010 | 2026-09-03 | adversarial review (brief lens) | all new surfaces | runtime links are literal paths; `src/lib/primary-routes.ts` (§5.5) does not exist | medium | **closed 2026-09-13, and half of it was stale.** The module exists and 38 files import it; that clause had been true when the defect was written and was not re-read since. The surviving half was real: **33 links across 20 files still spelled a path the module can build**, including `/settings` from the privacy policy, the terms and the deceased-customers page, `/settings/copilot` from the Copilot panel, and `/overview` and `/providers` from the header and footer arrays every page renders. All 33 now consume a route id. What holds it is a sweep in `src/lib/primary-routes.test.ts` that reads every source file for a literal href matching a builder's pattern, in both spellings — the JSX attribute and the `{ href: "/overview" }` object property the nav and footer use, which the first version of the sweep missed. **Its scope is stated rather than implied:** it covers the 22 builder routes with no `[param]` segment, so a literal `/genome/me/reports` and the register's other ~130 routes, which have no builder to consume, are outside it and are not claimed fixed. |
| D-011 | 2026-09-03 | review-fix agent (report surfaces) | `/genome/[subject]/reports/[slug]` | the six skeleton sections sit 40px apart (`space-y-10`) while `docs/density-baseline.json` requires ≥96px between adjacent top-level sections at ≥1024px | low | fixed (this branch): the skeleton container and the library's category sections use the baseline gaps (64px below 768px, 80px to 1023px, 96px from 1024px); `e2e/report-skeleton.spec.ts` measures ≥96px between the six report sections and between the category sections at 1280×800 |
| D-012 | 2026-09-03 | CI run 33750829586 | `e2e/search.spec.ts` | the caffeine test pressed Enter on the first Reports result and expected the CYP1A2 template, but two seed templates match "caffeine" and the ADORA2A one ranks first; the spec now follows the specific link (product behaviour unchanged) | low | fixed (this branch) |
| D-013 | 2026-09-03 | capability audit (read-only agent) | `docs/` | `docs/capability-register.md` did not exist although X15/G7.4 bind it and `docs/canonical-artifacts.md` names it as the authority for ship/withhold status; six capabilities and three declared gaps had no written status anywhere | medium | fixed (this branch): register created with one row per capability, `not shipped` rows named explicitly, and a declared-gaps table |
| D-014 | 2026-09-03 | capability audit (read-only agent) | `/genome/[subject]/reports` | every seeded template is `layer='estimate'`, so the "Specific variants" layer has no templates and its tab and counts render absent; a reader cannot tell that Inherit reads specific variants at all from the list | low | fixed (branch `medicines`, ADR 0021): eleven `variant_call` templates seed the layer; the tab renders with its counts and definition; the acceptance-matrix rows that read the layer (G4.5, A4/G1.15) are updated on landing |
| D-015 | 2026-09-03 | capability audit (read-only agent) | `/genome/[subject]/reports` | the "Medicines" category exists in the taxonomy with zero templates and is dropped from the DOM entirely; no sentence tells the reader pharmacogenomics is absent, which X15 forbids ("renders absent, not empty — it is either populated or registered as `withheld` with a dossier") | medium | fixed (this branch): pharmacogenomics reached a terminal status. `docs/withheld/pharmacogenomics.md` carries the nine elements with the obstacle classified safety (primary) and scientific (supporting), three designs actually built under `docs/withheld/pharmacogenomics/designs/` with the verbatim command, exit code and output of `pnpm gate:templates` and `pnpm gate:readability` for each, ADR 0018, and the register row `withheld`. The list still states the withholding in one place, now as “Inherit does not offer reports about medicines. …” (`MEDICINES_ABSENT`), re-pinned by `e2e/report-skeleton.spec.ts`, which also asserts the sentence carries none of “coming soon”, “soon”, “yet” or “currently”. |
| D-016 | 2026-09-03 | capability audit (read-only agent) | `/legal/appeals` | the sentence "After a relative objects, Inherit turns off relative matching, pages that others can see about that relative, and shared-segment output" implies relative matching and shared-segment output exist; Inherit computes and displays neither (X15 declared gap) | medium | fixed (this branch): the sentence now names what is switched off (every page and result others can see about the relative) and states that Inherit does not match relatives or compute shared DNA; the declared gaps are published at `/science#not-offered` and `e2e/legal.spec.ts` pins both |
| D-017 | 2026-09-03 | W7 part B (fixture generation) | `src/lib/genome/admixture.ts` | the synthetic 168-marker fixture drawn at EUR 0.6 / AFR 0.3 / EAS 0.1 per allele copy is estimated as AMR 0.652 / AFR 0.255 / EAS 0.088 / EUR 0.005 / SAS 0.000: the estimator attributes most European-drawn alleles to the admixed-American reference, so the shipped panel cannot tell those two references apart at this marker count | medium | open — science dimension (X16.6): the panel, the threshold and a marker-subsampling interval are its work; the surface already states that neighbouring regions are similar and that no range exists yet |
| D-018 | 2026-09-03 | orchestrator (matrix review) | `docs/acceptance-matrix.md` | rows G4.1–G4.3, G4.6 and G4.7 carry paraphrased statements that do not match the brief's G4 gate texts (G4.3 in the brief is the count-class gate, G4.6 the input-quality provenance block, G4.7 citations with access dates); G4.4 and G4.5 were realigned in this pass | low | fixed (this branch): G4.1, G4.2, G4.3, G4.6 and G4.7 now carry the brief's gate text; each row's evidence was re-audited against the tree and names exactly what is built (the relative-figure component and ladder with no rendering template; `data-figure-basis` and the once-per-block marker with no E2E count; the single-class `<Count>` under X5.1's names with no automated gate; the coverage sentence, pills and fixed provenance line with no call rate or liftover status; PMID/DOI citations on every template with no access date). All five stay NO |
| D-019 | 2026-09-03 | orchestrator (G4.4 audit) | `src/components/results/ancestry/lineage-card.tsx` | a haplogroup call renders its matched-of-tested markers as a coverage figure but not the reference tree's version nor an explicit statement that no interval exists, which G4.4 requires of every quantity derived from a reference panel | low | fixed (this branch): each called line names its tree and version (`LINEAGE_TREES`), states "Inherit puts no range on this name…" and gives the tree's resolution limit, beside the existing matched-of-tested coverage figure |
| D-020 | 2026-09-03 | CI run 33775768209 | `src/app/api/files/[id]/process/route.ts` | the admixture row gained `model_id`, `model_version`, `coverage` and `result_state` while the two haplogroup rows in the same bulk insert did not; PostgREST fills a key that some rows omit with null rather than the column default, so `result_state` violated NOT NULL (Postgres 23502) and every file's processing failed, taking ten browser specs with it | high | fixed (this branch): every ancestry row states the four columns explicitly through one `AncestryRow` type (haplogroup rows: no model id, coverage from matched-of-tested, `available` / `partial` / `not_covered`); the unit suite cannot reach this because the database path is exercised only against the real local stack in CI |
| D-021 | 2026-09-03 | CI run 33778596222 | `e2e/genome-data.spec.ts` | the data-page test forbade any percent sign inside a score's list item, but each score renders its seeded ancestry-portability statement, which quotes the source cohort's composition ("53.3% multi-ancestry…"); the assertion was wrong, not the page | low | fixed (this branch): the note carries `data-slot="ancestry-note"` and the exempt comment (provenance about the score's cohort, not a result about the subject); the spec asserts one note per score and no percent text outside it |
| D-022 | 2026-09-03 | orchestrator (G4.4 audit, reading the haplogroup provenance) | `data/ref/haplogroups/`, `docs/dataset-licenses.md` | the licence audit had no row for either haplogroup reference source, and the two records disagree: the repository's provenance note asserts PhyloTree is "free for academic/non-commercial use with citation", while the publisher's own page, read 2026-09-03, states no copyright, licence or terms at all and asks only for a citation. The audit's standing rule is that no non-commercial-licensed source may enter the reference store or seed data, and the mtDNA tree's topology and defining positions are seed data | high (legal surface) | open — both sources now have rows marked verification outstanding; resolving it needs the publisher's actual terms (or counsel's reading of data published with none), after which either the provenance note or the maternal-line capability's status changes. The MIT licence on the machine-readable copy covers that copy's packaging, not the tree |
| D-023 | 2026-09-03 | orchestrator (F1 review, reading the grant routine) | `/family/[person]/permissions` | the Portrait row rendered "Turn on", but `grant_directional_purpose_v1` refuses `family.portrait` while the signer's `independent_login_at` is null, and nothing in `src/` ever called `mark_independent_login_v1`: the register places the marker in `auth.callback`, while the password sign-in completes in the browser and never reaches that route, so every Portrait grant would have answered 409 (a control without its mechanism, brief §9 item 21) | medium | fixed (this branch): `src/lib/family/independent-login.ts` stamps the marker from the callback exchange and on the first server-rendered permissions visit (the routine itself proves the session post-dates the acceptance); until it is stamped the Portrait row renders locked with "Portrait needs a sign-in of your own first…" and no control; `e2e/family.spec.ts` asserts the locked row in the acceptance session and the successful grant after a fresh sign-in |
| D-024 | 2026-09-03 | CI run 33787980603 | `e2e/adult-subject-invitation.spec.ts` | the assertion F1 added ("the pre-consent statement sits above the form") located `form` unscoped, which resolves to two elements under the app shell: the account landmark's sign-out form and the invite form; Playwright's strict mode throws | low | fixed (this branch): the locator is scoped to the form containing the email field; the product is unchanged. The same unscoped locator in `e2e/family.spec.ts`'s invite test was masked by the hub failure in the first run and surfaced in run 33789839105 once that passed; scoped the same way |
| D-025 | 2026-09-03 | CI run 33787980603 | `e2e/family.spec.ts` | `expectAxeClean` emulated the dark colour scheme on the already-loaded hub and audited at once; `next-themes` flips the class on the live page and the chrome's `transition-colors` animates for 150 ms, so axe sampled mid-transition text and background colours and reported a serious `color-contrast` violation in dark only. Every other spec loads the page after emulating the theme | low | fixed (this branch): the helper reloads the page in each theme before auditing, as `e2e/a11y.spec.ts` and `e2e/ancestry.spec.ts` do |
| D-026 | 2026-09-03 | CI run 33790730613 | `/family/[person]/permissions` | each row control carried `aria-label="{label} for {name}"`, which replaces the visible "Turn on" / "Turn off" as the accessible name: the browser spec's `getByRole("button", { name: "Turn on" })` could never match (the invite/accept test timed out after 120 s), and the control failed WCAG 2.5.3 (the visible label must be part of the accessible name) | medium | fixed (this branch): the accessible name is now "Turn on {label} for {name}" / "Turn off {label} for {name}" (`rowControlLabel(verb, label, name)`), pinned by `src/components/family/family.test.ts`; the spec's locators are unchanged and now match |
| D-027 | 2026-09-03 | F3 libraries agent (running the Family unit suites) | `src/lib/family/tier2.test.ts` | the tamper assertion replaced the digest's last character with a literal `0`, so whenever the real digest already ends in `0` the "tampered" cookie equals the real one and the test fails; the key is random per run, so the failure is intermittent | low | fixed (this branch): the replacement character is chosen to differ from the real last character |
| D-028 | 2026-09-03 | Family permissions (Portrait row) | `mark_independent_login_v1` excluded only subjects that an accepted invitation targeted, but the Portrait guard reads the data subject's own `self` record, which no invitation targets. Reproduced in CI run 33792405267 (`e2e/family.spec.ts:341`): the invited adult's first request in the acceptance session stamped their self record, so the Portrait row was settable from the session the invitation was accepted in. | high (a sharing guard that never engaged) | fixed (this branch): migration `20260903190000_independent_login_per_account.sql` makes the rule per account (any session that predates an adult-subject invitation this account accepted stamps nothing), with four pgTAP assertions |
| D-029 | 2026-09-03 | Family permissions | No screen could grant `family.heritability`: the five permission rows are the report layers, ancestry, raw data and Portrait, so the two-column health picture was reachable only by a service-role call in the browser suite. Found while reviewing F2's report. | medium | fixed (this branch): a sixth row, "Health picture", on the same directional own-session rules, locked with Portrait until the independent-login marker is set (ADR 0017 §8) |
| D-030 | 2026-09-03 | Carrier panel (runs of homozygosity) | `src/lib/family/roh.ts` measured runs at request time under a 20,000-call read budget, so every real array or sequence file answered `not_measurable` and the 25-in-100 branch could never render in production. Found by the adversarial review of `c6dd140` (carrier-rule lens). | high | fixed (this branch): measured once at ingest from the parsed calls and stored on `genome_files` (migration `20260903200000_genome_files_runs_of_homozygosity.sql`); the reader asks the stored measure of every annotated file of the subject |
| D-031 | 2026-09-03 | Carrier panel (X-linked pattern) | Brief line 346 requires the X-linked arithmetic across 100 pregnancies; the split depends on which parent carries the change on the X, and nothing in Inherit records a person's chromosomal sex. The panel renders the refusal sentence with the reason `sex-unknown` instead. | medium (science and product) | open — needs a sourced writer for chromosomal sex; the Portrait libraries' `xLinkedCross` is ready to render the split once one exists. The reason phrase was corrected to say what is not recorded (review, R5) |
| D-032 | 2026-09-03 | Carrier panel (trigger and naming) | The candidate set required the same rsid in both files and the block never rendered the variant; brief line 346 triggers on a P/LP heterozygous variant in the same gene and requires naming both variants and both classifications. | medium | fixed (this branch): candidates are grouped by gene; a match carries each person's own variant, classification and copies, and the block names them (review, R2) |
| D-033 | 2026-09-03 | Carrier panel (classification) | `isPathogenicClassification` matched the exact strings only, while the reference writer stores ClinVar's list joined with ", " and ClinVar itself uses "/", so "Pathogenic/Likely pathogenic" fell to "nobody yet knows what this change means". | medium | fixed (this branch): labels are split on `/ , ; |`; pathogenic only when every token is pathogenic or likely pathogenic, harmless only when every token is benign or likely benign, anything else unknown (review, R3) |
| D-034 | 2026-09-03 | Carrier panel (empty state) | With no classified reference position (production today) the panel printed "Inherit checked the 0 positions both files cover" above a table whose cells show positions both files do cover. | high | fixed (this branch): an empty classified set renders "Inherit has no classified positions to check yet, so it cannot look for a change you both carry."; the count sentence renders only over a non-empty set (review, R6) |
| D-035 | 2026-09-03 | Carrier panel (two copies) | A person with two changed copies at a classified position was dropped from the panel, which then reported no shared change although both files showed one; brief line 346 says any failed trigger renders the refusal sentence with a reason. | high | fixed (this branch): the pair renders with no probability and the new closed-table reason `two-copies` ("one file shows two changed copies, not one"); decision recorded (review, R4) |
| D-036 | 2026-09-03 | Portrait libraries (distribution) | `distribute()` keyed the below-1-in-100 rule on the dot count after largest-remainder rather than on the share, so a 6-in-1,000 category rendered a solid dot and "about 1 would"; the lending category's sentence disagreed with the figure contract's rounding; a NaN share was filtered away instead of refused; `crossShares` promised a number for absent outcomes; `sentence: null` could pair with `denominator: 1000`. | medium | fixed (this branch): the rule keys on the share, sentences use `naturalFrequency(share, 100)`, NaN and negative shares throw, absent outcomes are absent keys, and one rounding rule decides both the sentence and the rung (review, R8) |
| D-037 | 2026-09-03 | Portrait copy | `ACKNOWLEDGE_BUTTON` was "Continue", banned standalone by brief line 928; the `runs_below_threshold` assumption statement asserted a completed check under the label "What we do not check". | low | fixed (this branch): "Open Portrait"; the statement is written as the assumption it is (review, R7) |
| D-038 | 2026-09-03 | Health picture (consent boundary) | Cells rendered another adult's genotype letters under `family.heritability` alone, and the Overview's carrier line read another adult's rows after checking `carrier_match` only; the register makes `family.heritability` authority for the joint comparison, never for an individual result layer, and `family:carrier-arithmetic` requires all three capabilities. Found by the adversarial review (browser-spec and consent lens). | medium | fixed (this branch): a cell's figure and link need the layer's own grant from that person, else "Not shared with you"; the Overview checks the three capabilities before any read (review, S3–S4) |
| D-039 | 2026-09-03 | `e2e/family-health-picture.spec.ts` | Tests after the second never passed the Tier-2 gate in their own browser context (the gate cookie is keyed to the auth session), the dark-theme axe audit repeated the D-025 pattern, and the independent-login marker was forged with a raw update instead of the routine. Found by the adversarial review before the spec ever ran in CI. | medium (test) | fixed (this branch): a gate helper per test, a reload per theme, the real `mark_independent_login_v1` call (review, S1, S2, S5) |
| D-040 | 2026-09-03 | Carrier panel (runs of homozygosity) | The measure counted any two adjacent same-reading calls as a run and summed their spans, so an array-density file always exceeded the brief's 100 Mb and 0.0156 thresholds (the repository's own `data/samples/synthetic_23andme.txt`: 124 runs, 241 Mb, F_ROH 0.055) and the 25-in-100 branch was reachable only for a sparse VCF; a differences-only VCF with adjacent 1/1 rows counted as measured although the positions between its rows are unrecorded. Found by the adversarial review's verification pass after D-030. | high (science) | fixed (this branch): runs follow McQuillan et al. 2008 (doi:10.1016/j.ajhg.2008.08.007): ≥ 1.5 Mb, ≥ 25 contiguous same-reading calls, at most one heterozygous call inside a run; a file that reports no reference-homozygous call is `not_measurable` (`no-reference-calls`); the denominator stays the autosomal span the file covers and is stated as Inherit's own choice |
| D-041 | 2026-09-03 | Family permissions | The lock reason read "Portrait needs a sign-in of your own first…" on the new Health picture row too, and `e2e/family.spec.ts` picked the Portrait row by a substring filter that this text now matched twice (CI run 33807524096, the first on PR #42). | low | fixed (this branch): the reason is row-neutral ("This row needs a sign-in of your own first…"), the label carries `data-slot="permission-label"`, and the spec filters rows by their exact label |
| D-042 | 2026-09-04 | Copilot output guard | The route checked only `text-delta` chunks and forwarded reasoning parts and tool-input JSON to the client unchecked, so a fabricated number in a reasoning part or a model-authored rsID in a tool input bypassed brief line 2262. Found by the adversarial review (findings 1 and 12). | high | fixed (this branch): `toUIMessageStream` runs with `sendReasoning: false` and `foldStreamChunks` folds text, reasoning, tool inputs, sources and provider-run tool outputs into the one string `checkResponse` reads; a failing turn is replaced whole |
| D-043 | 2026-09-04 | Copilot intent gate | The diagnosis rules' file-fact exemption tested the whole message, so "Do I have haemochromatosis? My file has the variant." was allowed, while "Do I have Neanderthal DNA?", "Do I have rs762551?" and "Do I have MTHFR?" were refused. Found by the adversarial review (finding 5). | high | fixed (this branch): the exemption reaches only the object of the rule's own "have" (a determiner, up to two qualifying words, a file-fact word, an rsID or a capitalised gene symbol); paraphrases ("am I a diabetic", "so I have X then", "is that a disease", "does my genome say I have") are gated |
| D-044 | 2026-09-04 | Copilot intent gate | A comparative about embryos ("Which of them is healthier?", "Is embryo 3 better than embryo 5?", "Compare embryo 2 with embryo 4") reached the model in a cohort scope; a ranking by another name. Found by the adversarial review (finding 6). | high | fixed (this branch): `selection.comparative`, `selection.which-of-them` and `selection.compare-embryos`; a sex question that also compares keeps the sex refusal |
| D-045 | 2026-09-04 | Copilot intent gate | An unspaced dose ("500mg", "1000IU"), "too much iron", "double my folic acid", "any supplement recommendations", "what supplements" and "should I be taking" were not treatment questions to the gate. Found by the adversarial review (finding 7). | high | fixed (this branch): `treatment.dose` reads `\d+\s*unit`, plus `treatment.too-much`, `treatment.adjust-intake` and `treatment.which-supplement`; a genotype letter after a digit ("1 G and 1 A") is not a unit |
| D-046 | 2026-09-04 | Copilot intent gate | The Portrait rule refused any "will/would my child …", including the inheritance and carrier questions Portrait exists to answer ("Could our children inherit this?", "Would a child of ours be a carrier?"). Found by the adversarial review (finding 8). | high | fixed (this branch): the rule refuses trait predictions about one child (be, have, look, grow, become; "how tall will my child be") and exempts inherit, carry and carrier |
| D-047 | 2026-09-04 | Copilot chat route | A gated user turn and its refusal stayed in the client's thread and were resent as history, so the model saw every refused message on the next allowed request. Found by the adversarial review (finding 10). | high | fixed (this branch): `dropGatedTurns` classifies every user turn and drops a gated one with the assistant turn that answered it before `convertToModelMessages`; `e2e/copilot-refusal.spec.ts` reads the provider's history |
| D-048 | 2026-09-04 | Copilot citation check | "Author et al. YEAR" matched by substring ("Sule" inside "Sulem", "2011" inside "20115"), an "et al." without a year, "a study by X", "according to X" and "published in X" were not citations to the check, and `get_prs` never selected `prs_scores.citation`, so a score citation could not be permitted. Found by the adversarial review (findings 2, 4, 11). | medium | fixed (this branch): whole-token matching against the labels, the four new shapes held to the same set, report and score names (`title`, `name`) permitted, `citation` selected |
| D-049 | 2026-09-04 | `docs/route-register.json` | `copilot-refusal-v1` described a JSON body while the route serves the refusal on the UI-message-stream transport with `x-copilot-refusal`. Found by the adversarial review (findings 13, 15). | low | fixed (this branch): the register describes the served contract (status 200, `text/event-stream`, the header enum, one text part, no echo); the route is unchanged |
| D-050 | 2026-09-04 | Copilot chat route | A `minor` or `embryo` subject resolved to the `subject` guard scope although no chat scope exists for either class. Found by the adversarial review (finding 14). | low | fixed (this branch): `guardScopeKindFor` admits `self` and `other_adult`; any other class answers the opaque 404 |
| D-051 | 2026-09-04 | `e2e/copilot-refusal.spec.ts`, `e2e/mock-llm.ts` | Three treatment prompts all reached `treatment.should-i-take`, the header claimed a cohort prompt no route can send, the mock counted only `/chat/completions`, and the axe audit ran on a reloaded, empty thread. Found by the adversarial review (findings 16–19). | low (test) | fixed (this branch): one prompt per treatment rule (pinned by rule id in `guard.test.ts`), the header states the cohort limit, the mock counts every request on any path and exposes the newest history, each theme sends a gated prompt before axe |
| D-052 | 2026-09-04 | Portrait output cards | The brief's line-1349 runs refusal ("These two files look more genetically similar than usual…") rendered for the `runs-unchecked` reason, which also covers a person whose runs were never measured, could not be, or who has no annotated file: the sentence asserted a similarity nothing had measured. Found by the adversarial review (finding 1). | high | fixed (this branch): the reason table splits `runs-above-threshold` (a measured file above a threshold; the refusal) from `runs-unchecked` (the side-by-side page's "could not check" sentence); `subjectRunsState` in `src/lib/family/roh.ts` |
| D-053 | 2026-09-04 | Carrier rule | The gene-level trigger produced the exact 25-in-100 arithmetic for two changes at different positions when each file lacked the other's position, and the card printed "Both files cover the positions this uses"; brief line 1349 says a position one file does not cover is named and never imputed. Found by the adversarial review (finding 2). | high | fixed (this branch): `not-covered` names the file and the position (`uncovered`); a match carries `positionsBothCovered`, and the coverage field is the cannot-calculate sentence when it is false |
| D-054 | 2026-09-04 | Portrait page | The one-sided readings ran only when at least one classified position was shared, so a carrier whose partner's file covers none of the gene's positions, the line-2238 case itself, rendered nothing. Found by the adversarial review (finding 3). | medium | fixed (this branch): `evaluateOneSided` runs whatever `positionsBothCover` is |
| D-055 | 2026-09-04 | Portrait libraries | A match refused because the other side's change was harmless or of unknown meaning counted as the carrier rule's answer for the gene and suppressed the no-second-copy reading for the pathogenic carrier. Found by the adversarial review (finding 4). | medium | fixed (this branch): `answeredGenes` counts only a match where both readings are pathogenic or likely pathogenic; the page renders the one-sided reading in place of that refusal |
| D-056 | 2026-09-04 | Portrait output cards | The runs threshold statement sat under "What we do not check" although the runs were measured, and the viewer's own label ("You") filled third-person sentence slots ("You’s file", "You hasn’t"). Found by the adversarial review (findings 5, 6). | low | fixed (this branch): a "What we checked" field carries the runs statement on an exact block; every sentence about the viewer has a second-person form (`PersonRef`) and the placeholder never reaches a slot |
| D-057 | 2026-09-04 | Portrait blocking screen and delete dialog | The delete dialog said the page opens again only when "you both" turn Portrait on, though deleting revokes the viewer's own grant alone; the acknowledgement form rendered while a grant or sign-in was still missing. Found by the adversarial review (findings 7, 8). | low | fixed (this branch): the dialog names the one grant revoked and the one that stays; the form renders only when the acknowledgement is the viewer's one remaining step |
| D-058 | 2026-09-04 | `src/copy/family/portrait.ts` | The refusals list lacked line 356's talent, athleticism, attractiveness and skin tone, and the copy claimed the height reason was the brief's verbatim although it is a grade-9 rewrite of an example. Found by the adversarial review (findings 9–11). | low | fixed (this branch): four cards added (fifteen in all); the comment and test cite the recorded decision |
| D-059 | 2026-09-04 | Embryo closed shapes (`src/lib/embryos/policy.ts`) | `qc_reasons` accepted any string array and `not_covered_reason` any string, so a value outside the register's closed tables passed the shape and reached the renderers, which then emitted it as `data-reason`. Found by the adversarial review of E1 (R1). | high | fixed (this branch): membership of `QC_REASON_IDS` and `RESULT_NOT_REPORTABLE_REASON_IDS` is validated; a qc reason needs `quality_not_measurable` and a result-level reason `not_covered`; `registeredReason` maps every emitted id |
| D-060 | 2026-09-04 | Embryo QC source strings | `source_laboratory`, `source_assay` and the two method strings were unbounded and printed as received, although the register requires bounded reviewed labels and canonical-artifacts says an original laboratory label is never rendered. Found by the adversarial review of E1 (R2). | high | fixed (this branch): `data/embryo/source_labels.json`, a closed registry withheld and empty today; the shape accepts null or a registered id; renderers print a label's display text or "Not stated by the source laboratory" |
| D-061 | 2026-09-04 | `<CompareCell>` | The embryo's own absolute figure carried the general-population group, so one cell attributed two numbers to the population; the `measured_inconclusive` status rendered the "No one has measured…" sentence, which is false of a tested score. Found by the adversarial review of E1 (R3, R4). | medium | fixed (this branch): the embryo's figure is captioned "for Embryo N"; the inconclusive status has its own sentence with its citation beside the brief-1318 comparison sentence |
| D-062 | 2026-09-04 | `<QcTable>`, `<CompareTable>` | The QC table, a `data-card` holding many `data-subject-id` blocks, lacked `data-compare-surface` (brief line 211); the compare caption said "your DNA" about an embryo's file (X13.1); the mean depth rendered as a bare count outside the figure contract. Found by the adversarial review of E1 (R5, R6, R7). | high | fixed (this branch): the attribute, `EMBRYO_LAYER_DEFINITIONS`, and the `measure` figure kind (value, unit, decimals) with the depth inside the column's block |
| D-063 | 2026-09-04 | `e2e/embryos.spec.ts`, `playwright.config.ts` | The title assertion ignored the root layout's template; the flag-off jurisdiction refusal of design §6.2 had no browser proof; the spec header claimed the unit suite proved the gate. Found by the adversarial review of E1 (R8, R9, R10). | medium (test) | fixed (this branch): the templated title; a second Playwright project against a second server from the same build with the flag unset, running `e2e/embryos.nojurisdiction.spec.ts`; the header states what is and is not proven |
| D-064 | 2026-09-04 | `src/lib/embryos/cohorts.ts`, the result loaders | Every query's `error` was ignored, so a failed read resolved to an empty graph, a 404 or "Still checking the files". Found by the adversarial review of E1 (R11). | medium | fixed (this branch): `rowsOrThrow` and `EmbryoReadError`; the compare and detail pages render the read-failed error state |
| D-065 | 2026-09-04 | `/auth/sign-out`, the embryo gate | Sign-out deleted the Family gate cookie only; the embryo gate's write and read paths shared no code. Found by the adversarial review of E1 (R12, R10). | low | fixed (this branch): `signOutResponse` deletes both cookies; `embryoGateCookie` and `acknowledgedBy` are the one pair, tested end to end |
| D-066 | 2026-09-04 | Embryo consent sentence | A non-parent uploader with no grant from either parent read "Waiting for the other genetic parent". Found by the adversarial review of E1 (R13). | low | fixed (this branch): `analysisConsent` answers `waiting-for-both`; the sentence names "both genetic parents", or the one missing party |
| D-067 | 2026-09-04 | `src/components/uploads/uploader.tsx` | The uploader carried its own unrecognised-format and too-large sentences beside the A.6 home the canonical index names, and refused a PDF as "Unrecognized format" although the canonical PDF row has the preflight refuse it by name. Found while placing the A.6 refusals. | low | fixed (this branch): the preflight reads `sniffFileV2` and the three sentences from `src/copy/upload/errors.ts` |
| D-068 | 2026-09-04 | `docs/capability-register.md` | The Embryo ingest row said the Embryos landing ends "No embryo analysis model is currently allowlisted on this deployment.", a sentence E1 removed. Found while updating the row. | low (docs) | fixed (this branch): the row states what `/embryos/upload` and the landing render |
| D-069 | 2026-09-04 | `e2e/README.md` | The suite index had no row for `embryos.spec.ts` or `embryos.nojurisdiction.spec.ts`. Found while adding the upload flow's assertions. | low (docs) | fixed (this branch): both rows added |
| D-070 | 2026-09-04 | `src/components/embryo/upload/upload-flow.tsx`, `src/lib/embryos/upload-flow.ts` | Two screens of the upload flow exceeded X6.1's seven interactive elements on the repository's measurement basis (nine on the options screen with the shell's search button and desktop attribution link counted). Found by CI run 33924630411 on PR #48. | medium | fixed (this branch): screens cut to five controls of their own — the first two questions on one screen, the options and bases as actions, the PDF refusal and each basis on its own screen; the reducer records the shell's two controls beside the cap |
| D-071 | 2026-09-04 | `src/copy/embryos/upload.ts` | Three flow sentences overclaimed: "Nothing is kept until both parents sign" (the design's draft step keeps a record first; three bases never have both parents sign), the closing sentence named the other parent's email and signature for every path, and a basis label and sentence spoke in a genetic parent's first person after the permission situation. Found by the adversarial review of E2 slice 1 (C3, C4, C5). | medium | fixed (this branch): sentences true on every path, third-person bases |
| D-072 | 2026-09-04 | `src/components/uploads/uploader.tsx` | A laboratory table `sniffV2` had recognised was refused as "unrecognised", whose trigger the brief binds to a null sniff; a multi-sample VCF travelled on to fail at the server; the PDF refusal promised a letter without linking it. Found by the review (C6, U3). | medium | fixed (this branch): the register's `subject_source_not_single_sample` sentence with a link to the Embryo flow for both cohort-shaped kinds; the letter link on the PDF refusal |
| D-073 | 2026-09-04 | `src/lib/genome/parsers/sniff.ts` | A `#CHROM` line cut off by the 64 KiB decode window or a truncated gzip member was read as complete, giving a wrong sample count or a cut sample name; a trailing tab counted an empty sample. Found by the review (C8, U6). | medium (latent: no runtime caller yet reads the count) | fixed (this branch): an unterminated header answers `sampleCount: null`; trailing whitespace is dropped |
| D-074 | 2026-09-04 | `src/lib/genome/parsers/pgt-table.ts` | A comma inside a quoted header cell split the cell, shifting every later column index and yielding a complete plan pointing at the wrong columns. Found by the review (C9). | medium (latent: no mapping route yet) | fixed (this branch): RFC 4180 tokenisation of the header line |
| D-075 | 2026-09-04 | `src/components/embryo/upload/upload-flow.tsx` | The "No" ending appeared in place as a newly mounted live region that nothing focused; the terminal rendered a block-role sentence as a heading; the basis sentence and the endings were live regions that double-announce or are ignored. Found by the review (C10, U1, U7). | low | fixed (this branch): focus moves to the ending's sentence, the PDF screen carries the option's label as its heading, the terminal's sentence is a focused paragraph, no live regions |
| D-076 | 2026-09-04 | `docs/adr/0020-*.md`, `src/copy/upload/errors.ts`, `docs/canonical-artifacts.md` | All three said the file processor consumes the A.6 refusals; no processor code does yet. Found by the review (C7, U8). | low (docs) | fixed (this branch): the consumers that exist are named; the processor's re-sniff and the ingest routes are marked as E0's |

| D-077 | 2026-09-05 | Schema | `subject_principals` | The identity check required a subject or an account on every non-service principal, so an invited genetic parent could not exist before accepting; the invitation mail, contact reference and slot all need that principal. | medium (blocked the co-parent path) | fixed (this branch): the check admits a pending or deleted `genetic_parent` / `identified_donor` principal with both null (`20260905103317_embryo_cohort_runtime.sql`) |
| D-078 | 2026-09-05 | Schema | `attestations.kind` | No kind existed for `attestation.embryo-disposition-rights`, so the matrix's third parent artifact had no typed row. | low | fixed (this branch): `disposition_rights` added to the check |
| D-079 | 2026-09-05 | Mail | `public.claim_mail_outbox` | The delivery-token branch ran only for `adult-subject-invitation`, so a co-parent invitation would have been sent without a link. | medium | fixed (this branch): the branch and the pending-principal invalidation clause cover both invitation purposes |
| D-080 | 2026-09-05 | SQL | `private.consume_embryo_operation_nonce_v1` | The nonce pattern `{16,256}` exceeds PostgreSQL's regular-expression repetition limit of 255 and failed at first call; found by running pgTAP on the local stand-in cluster. | medium (every RPC would have failed) | fixed (this branch): a character-class match plus a length check |
| D-081 | 2026-09-05 | Rights | `src/app/api/jobs/mail/route.ts` (adult invitation) | The adult invitation mail carries the raw token in the URL path (`/withdraw/<token>`), which the register forbids (`urlCredentialInterstitial`: fragment only); embryo mails use the fragment form. | medium (pre-existing) | open, and re-scoped on 2026-09-13: the interstitial and `api.rights-activate` this row said the adult path "needs" now EXIST and the co-parent path uses them end to end (D-084, closed the same day). What is left is not building them — it is moving the ADULT token onto the same issuance model, which is the harder half. Re-measured 2026-09-10, the defect has two surfaces, both recorded in `docs/route-divergence.json` and held by `scripts/route-register-correspondence.test.ts`: the `/withdraw/[token]` directory implements as an open bearer segment what `rights.withdraw` pins to the literals `request` and `session`, and the pre-migration `POST /api/withdraw` has no register entry at all (`api.withdraw` is `/api/withdraw/[token]` with token fixed to `session`, which `src/app/api/withdraw/session/route.ts` already implements correctly). Closing it is larger than a link change: adult invitations store `token_hash` directly on `subject_invitations`, while `activate_rights_session_v1` resolves only co-parent invitations through `token_hashes`/`token_candidates` and hard-codes the purpose, so the adult token must first move onto the `mail-token-delivery-v1` issuance model. Already-mailed tokens live 30 days, so the old path cannot be removed the same day the new one lands. **Re-measured 2026-09-10 by reading the code, and it has two more surfaces than this entry listed plus one stale blocker.** (a) `src/app/(marketing)/withdraw/[token]/page.tsx` calls `adultInvitationAvailable(token)` on a plain GET, so a link scanner or prefetcher that follows a mailed URL gets a live availability oracle keyed by that URL — against the register's own `tokenSecurityContract.activation`, `scanner-prefetch-GETs-cannot-see-burn-or-use-a-token`. (b) The server mints two further URLs carrying the same raw token: the sign-in bounce at `page.tsx:68` (`/auth/sign-in?next=%2Fwithdraw%2F<token>`, which puts it in a **query string on a different route**) and the post-action receipt redirect in `api/withdraw/route.ts` (`/withdraw/<token>?result=…`). So `platformLogging.rule` — "no secret query or path segment" — is breached by URLs this codebase builds itself, not only by the one it mails. **The blocker recorded above is stale.** It says the adult token must first move onto the `mail-token-delivery-v1` issuance model because adult invitations store `token_hash` directly on `subject_invitations`; since migration `20260906061029` both purposes mint through the same `token_candidates`/`token_hashes` path and only mirror the hash onto `subject_invitations`. The real remaining blocker is narrower: `activate_rights_session_v1` resolves through `private.current_co_parent_invitation_v1`, which hard-filters the co-parent purpose, kind and target and joins the cohort-draft tables, so an adult token resolves a `token_hashes` row and then falls out and activation 404s. **What is NOT a defect here, checked rather than assumed:** `refuse` and `delete` proceed on the token hash with no account or session (only `confirm` requires `p_account_id`), and that is deliberate — G5.4 requires accountless subject rights, and making a person sign up before they can decline the use of their genetic data would be the worse bug. The defect is the transport, not the absence of a session. **Closing order, established 2026-09-10 and unchanged by this entry:** teach `activate_rights_session_v1` an adult branch in a new migration; change the one line in `src/app/api/jobs/mail/route.ts` that builds `applicationUrl('/withdraw/<token>')` to the `fragmentUrl()` form the co-parent mail already uses, so newly mailed links stop using the open segment; extend `/withdraw/session` and `/api/withdraw/session` to serve the adult confirm/refuse/delete operations off the rights cookie; then hold both old surfaces for the full 30-day `invitation.pending-30d` window before deleting them together with their `docs/route-divergence.json` entries in one commit. Removing the GET oracle alone was considered and rejected as a fix on its own: it leaves the token authoritative in the path and re-emitted into two more URLs, so it would read as closed while the transport defect stood. **Surface (a) is CLOSED, 2026-09-13, and it needed nobody's decision.** `src/app/(marketing)/withdraw/[token]/page.tsx` no longer calls `adultInvitationAvailable(token)` on a plain GET, so following a mailed URL no longer answers "is this token still live?" to a link scanner, a mail-security bot or a prefetching browser. The helper had exactly one caller and nothing read its result but the rendering, so it is deleted from `src/lib/adult-invitations.ts` rather than left for the next page to reach for; `adultInvitationTokenHash`, which the POST uses, stays. The page also carries `robots: { index: false, follow: false }` now. The cost is the trade the contract asks for and no copy changed: a person opening an expired or used link sees the actions and learns it failed as `?result=unavailable` after choosing one, rather than being told first — that outcome and its sentence already existed on the POST path. Verified by 58 browser cases across every spec that follows an adult invitation link (portrait, portrait-no-file, family, family-health-picture, adult-subject-invitation, genome-family.nojurisdiction, figures-two-seed): no skips, no retries. **Surfaces (b) and the path token remain, and they are one job, not three:** the raw token is still the URL segment, still in the `next=` query string of both sign-in bounces, and still in the receipt redirect `api/withdraw` sends back here — and each of those needs a token-free destination that does not exist yet, which is the `mail-token-delivery-v1` migration with its thirty-day overlap. **Sized on 2026-09-13 by reading the code rather than calling it large, and the answer is a dependency nobody had drawn.** The transport half is what the row describes and it is unblocked: issue the adult token through `token_candidates`/`token_hashes` at mail time, add an adult branch to `activate_rights_session_v1` (today it calls `private.current_co_parent_invitation_v1`, requires an `embryo_cohort_drafts` row and writes `purpose='co-parent-invitation'`, `target_kind='cohort_draft'` — all three hard-coded), mail `/withdraw/request#<token>`, and give `/withdraw/session` a third mode for the adult purpose. Nothing is deleted: the thirty-day overlap makes the change additive. **Where the confirm lands is the part that was not obvious.** `api.withdraw`'s `confirmBodySelection` says the full `subjectArtifact`-plus-jurisdiction body is required by "only a path-b adult_subject invitation", and `/family/invite` offers no Path B (`e2e/family.spec.ts` asserts it), so today's adult invitation is path A and its confirm belongs to `api.invitation-accept` — whose register body ALSO requires `jurisdictionAttestationVersion` and `jurisdictionAttestationHash`, which is exactly D-083. That does not block this: `src/lib/embryos/routes.ts` ships the co-parent body with `jurisdictionCode` alone and the adult path can follow the same interim. It does mean **neither path's confirm matches the register until D-083's artifact is authored**, and that is an owner act. **Sharpened again on 2026-09-13, and the row's own premise was stale — the third such row this day.** This entry says "adult invitations store `token_hash` directly on `subject_invitations`", which reads as though the modern issuance model were absent. It is not. `create_adult_subject_invitation_v1` already inserts a `token_candidates` row with `purpose='adult-subject-invitation'`, `target_kind='subject_invitation'`; the issuance step already writes `token_hashes` against that candidate and marks it `issued`; and `respond_adult_subject_invitation_v1` already resolves token_hashes → token_candidates → `subject_invitations`. The write to `subject_invitations.token_hash` is a MIRROR kept for the legacy page, not the only home. So step one of the migration is done. **What is actually left is four things.** (i) `activate_rights_session_v1` needs an adult branch: it resolves only `private.current_co_parent_invitation_v1`, requires an `embryo_cohort_drafts` row and writes `purpose='co-parent-invitation'`, `target_kind='cohort_draft'`. (ii) A session-scoped responder, because `respond_adult_subject_invitation_v1` takes a token hash rather than a rights session — its body can be shared. (iii) `/withdraw/session` needs an adult mode; the accept/refuse/delete copy already exists on `/withdraw/[token]`. (iv) ONE LINE of mail: `src/app/api/jobs/mail/route.ts:145` builds `applicationUrl('/withdraw/' + deliveryToken)` for the `adult-subject-invitation` template while every other template calls `fragmentUrl`, which has built `/withdraw/request#<token>` since decision §11.6. That line is the whole reason a raw token is mailed in a path, and it must be flipped LAST, after the activation branch and the session mode exist, so nothing is mailed to a path that cannot yet answer. **CLOSED for newly mailed invitations, 2026-09-13, and the four steps above were built in that order.** (i) `private.current_adult_subject_invitation_v1` resolves the adult chain with the same revision agreement the co-parent resolver requires, and `activate_rights_session_v1` now branches on the token candidate's own purpose, so neither invitation kind can be activated through the other's checks; the adult branch opens a session on the reserved subject (38 pgTAP assertions, twenty-three of them single-field mutations that each refuse activation and leave no session behind). (ii) The response transaction moved into `private.adult_subject_invitation_response_v1` and both entry points call it: `respond_adult_subject_invitation_v1` for the mailed token and `respond_adult_subject_invitation_session_v1` for the cookie session, which consumes a session-bound nonce before reading the invitation and spends the session on any terminal answer (24 pgTAP assertions). Sharing the body rather than copying it was the point: it is the transaction that decides who controls a person's genome, and two copies would drift. (iii) `/withdraw/session` dispatches on the purpose stored on the session, and `/api/withdraw/session` answers `confirm`, `refuse` and `delete` for the adult purpose against the registered `{status, operation}` receipt; which rights holder is answering is decided by the form token the page served, never by a field in the body. (iv) `src/app/api/jobs/mail/route.ts` builds `fragmentUrl(deliveryToken)`. **The raw token is no longer in a path, a query string or a receipt redirect for anything mailed from now on.** **What is deliberately NOT removed:** `/withdraw/[token]` and the bare `POST /api/withdraw` still answer, because tokens mailed before this change live up to 30 days and breaking them would take away the very rights this row is about. Both `docs/route-divergence.json` rows stay open for that window and are re-scoped rather than closed. `e2e/adult-subject-invitation.spec.ts` now drives the whole new journey (mail fragment, scanner-safe GET/HEAD, interstitial, activation, session accept and accountless session refusal, with the token proven absent from every URL and present in exactly one request body) AND keeps a third test on the old path, so the overlap is a guarantee rather than a hope. **What is still not conformant, and it is not this row's:** the adult `confirm` body is the same account-bound acceptance it always was, not the register's `subjectArtifact` shape. That is D-103, filed today, and D-083 gates the jurisdiction half of it. |
| D-082 | 2026-09-05 | Legal | `src/app/(marketing)/legal/insurance-and-discrimination/page.tsx` | The page renders two short sections while the seeded `disclosure.insurance-and-discrimination` artifact carries brief §8 items 1–6; the page and the signed artifact differ. | medium | **closed 2026-09-13.** The page now reads the artifact row, so there is one copy of the text and this renders it. What the public page had been missing is the point of the defect: that GINA reaches neither life, disability nor long-term-care cover; that a result is information about parents, siblings, children and people never met, who did not agree; that any of them can ask Inherit to stop at `/legal/appeals` **without an account**; that asking for a test is itself genetic information under US employment law; and the advice to settle cover before looking at results. An integrity section carries the version, the effective date and the body hash and links the permanent versioned copy, so a reader can check the page against the record instead of trusting it. `e2e/legal.spec.ts` now pins those facts rather than the two old section headings — pinning headings would pass on a page that had dropped the content again. If the row is absent the page says so rather than falling back to prose nobody signed, and rather than 404ing a legal URL. One consequence worth naming: the disclosure's words are no longer in source, so the readability gate no longer scans them; the artifact is versioned, hashed and signed instead, which is a different control and a stronger one for this text. |
| D-083 | 2026-09-05 | Register | `docs/route-register.json#api.invitation-accept` | The body requires a jurisdiction attestation version and hash, but no `policy.jurisdiction` artifact exists in the repository, so nothing can be hashed honestly. | low as filed; **raised in reach, not in severity, on 2026-09-13** | open: the acceptance body carries `jurisdictionCode` only until the artifact is authored and seeded. Three things were measured while sizing D-081 and each is worth having before anyone starts. (1) **`jurisdictionAttestation` appears nowhere in the repository outside `docs/route-register.json`** — not in `src/`, not in the docs, not in a test. Nothing implements it, so nothing can be failing to. (2) **`policy.jurisdiction` appears nowhere at all**, including in the register's own artifact list, which is what the row says and is now checked rather than asserted. (3) This is not one route's gap: `api.invitation-accept` AND `api.withdraw`'s path-b adult confirm both require the version and hash, so the artifact gates the confirm on every invitation path, which is why closing D-081 cannot make its confirm register-conformant on its own. **And the deviation is currently justified by a citation that does not resolve.** `src/lib/embryos/routes.ts:288` reads "decision §11.5: `jurisdictionCode` only"; there is no §11.5 anywhere under `docs/` — the only "11.5" in the tree is a font size in the brief. The decision may well have been made; it is not written down where the code sends a reader to look, and a deliberate deviation from the register is exactly the kind that needs to be. |
| D-084 | 2026-09-05 | Rights | `src/app/(marketing)/withdraw/` | The register's `/withdraw/request` interstitial (reads the fragment token, sets the candidate cookie, posts to `api.rights-activate`) and the `/withdraw/session` destination do not exist; the dynamic adult page matches both paths and shows "This link cannot be used". The co-parent mail therefore promises a page that is absent. | medium | **closed 2026-09-13. It was already done, and the row had not been re-read.** All three things this row asked for are in the tree and have been for some time. `src/app/(marketing)/withdraw/request/route.ts` is the interstitial: a generic GET that looks up no target, mints a rights-activation candidate, sets its host-only cookie and serves a nonce-scripted page whose only job is to read the fragment and post it — with `default-src 'none'`, `form-action 'none'` and `noindex,nofollow`. `src/app/(marketing)/withdraw/session/page.tsx` is the destination, and it builds its request from the cookie header alone with the comment "Never derive authority from URL input"; it renders `CoParentReviewForm` and `InvitationRefusalForm`, which are the E2 acceptance screens this row named. `src/app/api/rights/activate/route.ts` calls `readRightsActivationCandidate(request, nonce)` and answers the same opaque 404 when it is absent, which is the candidate cookie check. The end-to-end proof is `e2e/co-parent-invitation.spec.ts`, which follows a mailed `/withdraw/request#<token>` through activation to the review screen and passes. **The static segments also answer this row's last sentence:** `request` and `session` take precedence over `[token]` in the App Router, so the dynamic page no longer shadows them and the co-parent mail no longer promises an absent page. **Worth recording as a pattern rather than a one-off.** This is the second stale open row found this week by reading the code instead of the row — D-010's second clause was the first, and it had been half-true for ten days. An open defect row is a claim about the present, and this file is read as a to-do list. |
| D-085 | 2026-09-05 | Audit | `src/lib/embryos/api.ts` | A blocked closed-shape response writes no legal audit event; `embryo-closed-schema-v1` requires exactly one pseudonymized event per blocked attempt. | low | **closed 2026-09-13**, by the RPC the row itself prescribed. `public.record_blocked_embryo_response_v1(p_consumer)` is service-role only, takes no principal, no target and no payload, and appends one `embryo.response.blocked` event through `private.append_legal_audit_event` with `audit_principal_id` null, the consumer as `route_id` and `{"shape_reference":"embryo-closed-schema-v1"}` as the whole coded context. The consumer is re-validated against the route's own id pattern INSIDE the function, so a payload fragment cannot reach the ledger even from a caller that skipped the route's check — proven by a pgTAP case that passes one and reads back `unregistered`. **A second divergence was found and fixed with it.** The contract's `observabilityEvent` is a template id and two constant slots, and it says the observability sink receives ONLY that; the log line was passing the consumer as a third value and omitting the `reason` slot entirely. The line is now exactly `feature.blocked embryo.closed-schema-serialization unsafe-embryo-response-shape`, and the consumer moved to the audit row, which is where the contract asks for it. A failed audit write does not change the response — the shape is already refused and the caller gets the same fixed 500 — but it is never silent: a second coded line says the ledger is missing an event it is required to hold. Eight pgTAP assertions and four unit cases, including both failure modes of the write. |
| D-086 | 2026-09-05 | Jobs | `src/app/api/jobs/mail/route.ts` | The drain response returns `processed`, `failed` and `pending` counts; the register's `machine-job-result-v1` allows only `{status:"complete", outcome}`. Pre-existing; unchanged by the cohort runtime. | low as filed; the class is medium | **closed 2026-09-13, and wider than this row said.** The row named `jobs.mail`. `machine-job-result-v1` binds SEVEN routes, and six of the seven were returning job internals: the mail and retention drains returned `processed`, `failed` and a `pending` queue depth — for retention, a count of the people part-way through deleting their account, readable by anything holding the shared jobs secret; `jobs.annotation-refresh` returned the batch size and the free text `"reference store fresh"`; `jobs.research-publish` returned the slug of the template it had just published and how many subscribers it had queued a digest for; `jobs.research-refresh` returned one object per source with the release key it had recorded and, on failure, the raw `Error.message`. The contract's own clause names all of those — `targetJobRowObjectRecipientDocumentTemplateSubjectCohortFileAccountErrorFreeTextOrPrivateCounts: "never-returned"` — so this was a privacy-contract breach across a class, not one route's shape drift. Fixed by `src/lib/jobs/machine-result.ts`: one helper builds `{status:"complete", outcome}` with the contract's headers, `jobOutcome` maps a drain's own counters onto `no_work`/`completed`/`completed_with_failures`, and `src/lib/jobs/machine-result.test.ts` reads every bound route's file and fails if a 2xx JSON body is built anywhere but the helper (verified against a deliberately reintroduced violation). The two queue-depth QUERIES went with the fields they fed, so the drains no longer ask the database a question they may not answer; the route tests now make a table read from those routes throw. No count evidence was weakened: the unit suites already asserted the submissions and receipts the counts summarised, the `no_work` case gained the first test it has ever had, and the browser suites read the outbox with the service key — the same query the route used to run, moved to the one caller entitled to the answer. Two further divergences on these routes were found and are NOT fixed here, as D-100 and D-101: widening a contract fix into an endpoint's authority model is how a scoped change becomes an unreviewed one. |
| D-087 | 2026-09-05 | independent transport review | `src/lib/embryos/vcf-transport.ts` | Two phased calls in distinct PS blocks lost PS but kept phased separators; a downstream VCF reader would treat them as one phase set. | high | fixed before publication: remove phase claims when discarding phase-set provenance, preserve allele copies, and reject phased wire records; VCF and table transport regression tests cover both source and bypassed input |
| D-088 | 2026-09-05 | independent transport review | `src/lib/embryos/vcf-transport.ts` | VCF 4.5 samples at POS 100 with LEN 100 and 10 both inherited the cohort END 199 after splitting. | high | fixed before publication: retain validated per-sample LEN and regenerate END 199 and 109 respectively; regression test proves neither sample borrows its sibling's span |
| D-089 | 2026-09-05 | independent transport review | `src/lib/embryos/vcf-transport.ts` | Valid lowercase REF/ALT sequence alleles were refused by the uppercase-only validator. | medium | fixed before publication: normalize sequence alleles; regression test proves identical transport for uppercase and lowercase sources |
| D-090 | 2026-09-05 | independent transport review | `src/lib/embryos/table-transport.ts` | An rsID-mapped table with an unused chromosome column containing a dot or blank discarded server-resolvable rows. | medium | fixed before publication: missing optional chromosome values defer to the server's rsID lookup; both cases have regression tests |

| D-091 | 2026-09-05 | independent database review | `private.embryo_ingest_binding_failure_v1` | The expiry-pair predicate used an unlocked read, allowing concurrent expiry-target changes during a reservation transaction. | high (latent: no route is enabled) | fixed before commit: lock both exact retention rows with FOR SHARE, compare the phase kind and immutable cohort/revision envelope; independent rollback-only verification confirmed the locks |
| D-092 | 2026-09-05 | independent database review | minted ingest due binding | Changing the due recipient kind/revision or either disposition revision still passed the binding check. | medium (latent) | fixed before commit: all four fields checked; four independent mutation regressions |
| D-093 | 2026-09-05 | independent database review | session mint/chunk contention | New profile/auth/cohort lock order can conflict with deletion, chunk writes or another co-parent uploader. | medium (latent) | fixed before commit: explicit NOWAIT, bounded legacy/implicit lock timeout and retriable transaction failure; actual wrapper/reserve cross-session probes preserve durable state |
| D-094 | 2026-09-05 | independent cross-session runtime review | ingest binding exception handler | PostgreSQL's broad 55000 category handler also catches 55P03, turning temporary parent-lock contention into durable failure_pending. | high (latent) | fixed before commit: explicit contention/deadlock rethrow before category fallback; injected SQLSTATE regression and real same-sequence retry prove the session stays open without duplicate receipt or lost due phase |
| D-095 | 2026-09-06 | direct synthetic guard probe | `src/lib/copilot/guard.ts#checkResponse`, `src/app/api/chat/route.ts` | At `8cbece97a1a9a66fbc4953eafc60c180889b2677`, the output check accepted synthetic diagnosis, treatment and embryo-recommendation statements after an allowed prompt because only numerals/citations were checked. The stream fold also inserted whitespace between provider fragments, splitting the tokens read by the checker. No real-user model incident was asserted. | high | fixed by PR70, production merge `f8c43bb`: assertion checks, exact per-part stream assembly, legitimate-explanation regressions and 64 actual-route completion cases. Full CI `34009298500` passed 207 browser cases. See `docs/copilot-output-contract.md` for detection limits; G4.8 remains NO until the complete A.9 scope and evaluation are delivered. |
| D-096 | 2026-09-10 | Rights | `private.clear_revoked_own_report_outputs_v1`, `public.ancestry_results` | Revoking the `ancestry` purpose never deletes the subject's legacy ancestry rows. The live revocation trigger deletes only `public.user_prs` for that grant's `reports.polygenic` runs and every `private.own_analysis_runs` row for the grant. In the whole database only `private.assert_supported_account_fk_shape_v1` and `public.purge_account_deletion_database_v1` mention `ancestry_results`; the only other removal is the reprocess path in `POST /api/files/[id]/process`. Reads are correctly denied — `loadAncestryResultSnapshot` gates canonical and legacy rows through `filterOwnAnalysisFiles(subject, 'ancestry', ...)` and re-confirms after every read — so this is a retention failure, not an access one. `docs/retention.md` registers `purpose.derived-60s` as delete-within-60-seconds for derived rows attributable to the exact revoked subject-and-purpose tuple. | medium | **closed 2026-09-10, by decision, not a code change.** The owner's call is that revoking an analysis purpose does not withdraw already-derived data from the subject's own export, because storage, analysis, sharing and AI permissions are separate and export runs on `raw.export`/`export.share-link`. Its subject scoping is correct — the rows are narrowed to the account's own legacy files — so the absent ancestry-purpose check is the only gap, and it is the intended one. Reads under the revoked purpose already end immediately, which was never in question. `docs/retention.md` now states the exception, so the registered rule matches intent instead of requiring a deletion the product deliberately does not perform. `public.report_observed_calls` was checked in the same pass and was never a defect: `own_copilot_chat_v1`, `own_report_generation_v1` and `own_subject_export_content_v1` all read it, so it is shared across live purposes.  **SUPERSEDED IN PART, 2026-09-12.** This row's closure rested on the decision that revoking analysis does not withdraw already-derived data from the subject's own export. The operator reversed the read half of that on 2026-09-12 (see D-097): revoking `ancestry` now withholds the legacy rows from both the page and the export. What survives of D-096 is the deletion half — those rows are still not deleted, and account deletion or source replacement remain the paths that remove them. |
| D-097 | 2026-09-10 | Rights | `src/app/api/export/route.ts`, `src/lib/exports/own-subject-content.ts`, `private.own_subject_export_content_v1` | The two halves of the same `ancestry.json` behave oppositely after revoking the `ancestry` purpose. The canonical half is withheld: `own_subject_export_content_v1` filters `purpose='ancestry'` against `purpose_grants`, and `own-subject-content.ts` reads twice around its authority check and throws if the two reads differ, with the comment "Unlike raw export permission, ancestry permission may disappear while the source remains. Do not release a buffered result after withdrawal, replacement generation or a different grant, including an empty page." The legacy half is retained: the route's `ancestry_results` read carries no purpose check. So after revocation a person's export contains their legacy ancestry and not their canonical ancestry, in one file. | medium | medium | **CLOSED 2026-09-12 by operator decision, and the decision reversed the one that had been assumed.** Put to the operator as "which read is right", answered "match canonical to legacy — the export keeps both halves", and then REOPENED by me before implementing, because that framing was factually wrong: the canonical half is not withheld by a read guard, it is DELETED. `private.execute_own_report_purge_v1` carries a purge-manifest entry targeting `generated-artifacts` in `private.own_analysis_runs` for the revoked grant, inside the registered 60-second deadline, and `e2e/ancestry-revocation.spec.ts` asserts that row is gone. "Keep both halves" therefore meant retaining derived genetic analysis past a revocation and rewriting the purge's completeness proof, not relaxing a guard. Re-put with that priced in; the operator chose the reverse — MATCH LEGACY TO CANONICAL. Implemented 2026-09-12: `private.own_subject_purpose_grant_v1` and `public.own_subject_purpose_granted_v1` answer the subject-scoped question the legacy read needs, `filterOwnAnalysisFiles` gained an opt-in `gateLegacy`, and both ancestry readers use it. **The gap was wider than this row said.** Legacy rows were never gated on the ancestry page either — `filterOwnAnalysisFiles` returns every legacy file untouched whatever the purpose — so the leak was on a page as well as in an archive, and G5.3a's claim that revocation made them "immediately unreadable" was never true. The rows are still not deleted; the operator declined that option explicitly. |
| D-098 | 2026-09-10 | Documentation | `.env.example`, `docs/self-hosting.md`, `src/app/api/jobs/mail/route.ts` | `NEXT_PUBLIC_APP_URL` was in neither the env template nor the self-hosting guide, and its fallback is the hosted origin: `process.env.NEXT_PUBLIC_APP_URL ?? "https://www.inherit.bio"`. It builds every link inside outbound mail — invitation, rights and withdrawal links, and the cancel/export links in an account-deletion notice. A self-hoster following `docs/self-hosting.md` would set the documented `NEXT_PUBLIC_SITE_URL` and miss this one, sending their users' mail links, and the rights tokens those links carry, to a deployment they do not run. The two variables sit beside each other and fall back in opposite directions: `NEXT_PUBLIC_SITE_URL` falls back to `http://localhost:3000`, which fails visibly and harmlessly. | medium | **documented, not repaired.** Both files now carry the variable and state the hazard. The fallback itself is left alone deliberately: the hosted deployment may rely on it, and I cannot read its environment, so changing it here could break production mail to fix a self-hosting doc. Whether that fallback should fail loudly instead is the open question, and it is an owner call because it turns on what production actually sets. |
| D-099 | 2026-09-12 | Rights | `src/lib/genome/own-analysis-access.ts` | The purpose gate that D-097 added for `ancestry` is missing for the other two own-analysis purposes. `filterOwnAnalysisFiles` returns every legacy file untouched unless the caller passes `gateLegacy`, and only the two ancestry readers pass it, so revoking `reports.monogenic` or `reports.polygenic` still leaves results derived from a LEGACY source readable on the report surfaces and in the export. The canonical half of each refuses correctly. Found while implementing D-097 and deliberately not fixed there: the operator's decision named `ancestry.json`, the fix for the other two is the same shape but touches the report library, the health picture and the export's `reports.json`, and widening a rights change past what was asked is how a scoped decision becomes an unreviewed one. | medium | **closed 2026-09-13.** The same gate now applies to `reports.monogenic` and `reports.polygenic` on every OWN-record reader: Overview's summary, both report surfaces, the personal previews, and the export's legacy `reports.json`, which is gated per layer and rechecked immediately after the build so a revocation landing mid-export cannot ship a buffered result. The Family surfaces deliberately keep the old path: a reader on a relative's record holds no own-subject grant there, so this check would always refuse and would delete legacy sharing rather than gate it — their authority is the counterpart's Family permission, checked before the read. Closed without the operator's line because the brief's own non-negotiables answer it: authority enforced throughout, and revocation immediate. Reverting is one flag per call site. |
| D-100 | 2026-09-13 | Contract sweep while closing D-086 | `docs/route-register.json#routes` (`jobs.run`), `src/app/api/jobs/` | The register carries `jobs.run` at `/api/jobs/run` with `disposition: "kept"`, a POST method policy, a closed query contract, a `worker-job-dispatch-v1` resolver and `successResponseContract: "machine-job-result-v1"`. There is no `src/app/api/jobs/run/route.ts`, and `src/app/api/jobs/` holds only the five directories that do exist. So a registered, kept, contract-bound dispatch endpoint is not implemented, and every reader of the register — including the correspondence test and the contract sweep added for D-086 — has to special-case it. Either the dispatcher was planned and never built, or it was removed without the register being told; the register alone cannot say which, and I will not guess in a document whose whole value is that it matches reality. | medium | **open.** Named in `src/lib/jobs/machine-result.test.ts#UNIMPLEMENTED` so the contract sweep skips exactly this id and no other, and so adding the file cannot quietly bypass the check. Closing it means an owner decision: build the dispatcher, or retire the route from the register. |
| D-101 | 2026-09-13 | Contract sweep while closing D-086 | `docs/route-register.json#routes` (`jobs.research-publish`), `src/app/api/jobs/research-publish/route.ts` | The register gives this route `requestContract: { body: "forbidden", query: "forbidden" }` and `policy.mode: "machine-reviewed-publication-only"`. The route reads `const { slug } = await request.json()` and refuses with 400 when it is absent, so the caller names the exact template to publish — the request selects the target, which is the thing every other job route in this domain is written to prevent (`requestTargetOrPrefix: "forbidden"` on retention, `requestMaySelectSubjectTargetSourceOrRows: false` on retention-cron and research-refresh). The authority is a shared operator secret, so this is not a privilege escalation; it is the register describing a due-work drain and the code implementing an operator command. | medium | **open, and deliberately not fixed alongside D-086.** Making the route match its register means it publishes whatever the review queue has approved and takes no input, which changes what the endpoint IS, changes how `e2e/research.spec.ts` drives the pipeline, and needs a rule for which approved drafts are due. The alternative — amending the register to permit the body — is an owner call about whether operator-selected publication is the intended design. The D-086 change touched only the response half and left this exactly as it was. |
| D-102 | 2026-09-13 | Found while building the `/family/portrait/[pairId] empty` fixture | `src/app/(app)/family/[person]/permissions/page.tsx`, `src/components/family/permission-grant-row.tsx` | **A permission a reader cannot turn on can render with no control and no reason: an empty paragraph.** `PermissionGrantRow` renders `<p data-slot="permission-locked">{disabledReason}</p>` whenever `action` is absent, and the page leaves `disabledReason` undefined for every reason except the independent-login one. `actionFor` returns undefined when `preparePortraitGrant`, `prepareSharedReportGrant` or `prepareHealthPictureGrant` returns null — and `family_report_endpoint_v1`, which all three call, refuses when EITHER account's profile fails its checks, including a date of birth at or over eighteen. Observed in a browser on 2026-09-13: with the granter's account completed and the recipient's not, the Portrait row shows the label and the state `Off`, the "Turn on" control is absent, and nothing stands in its place. Confirmed by rendering `PermissionColumn` with rows carrying neither an action nor a reason: seven `permission-locked` slots, zero controls, and the markup is literally `<p data-slot="permission-locked" class="text-sm text-ink-muted"></p>`. This is a rights surface whose whole job is naming what a person may do and whose step is outstanding, and the brief's non-negotiables require limitations to stay visible. | medium | **open, and the empty paragraph is the defect while the wording is an owner call.** The two are worth separating. That a rights surface renders a blank where an explanation belongs is not a design question — `PermissionGrantRow` should not be able to render a locked row with no reason at all, and making that combination impossible turns a silent blank into a failure a test can catch. WHAT THE REASON SHOULD SAY is the owner call, because the honest one describes the OTHER person's account ("they have not given their date of birth"), and telling one adult that about another is the same disclosure question that keeps `/family/portrait/[pairId] processing` open (see the route-gate history and corrections item 9). A non-disclosing reason is possible — the row can say the control is not available yet without saying whose step it waits on — but that is new user-facing copy on a consent surface, and inventing it inside a ratchet commit is how a scoped change becomes an unreviewed one. Not fixed here for that reason; the fixture that found it works around it by completing both accounts, and `e2e/portrait-no-file.spec.ts` says so in its header. |
| D-103 | 2026-09-13 | Found while moving the adult invitation onto the rights session (D-081) | `docs/route-register.json#api.withdraw`, `supabase/migrations/*_adult_subject_session_response.sql`, `consent.subject-adult` | **The adult `confirm` body cannot be built to its registered shape, because the statements it is supposed to actuate are not published anywhere and the register's count of them disagrees with the count the database records.** `api.withdraw`'s confirm body for an adult-subject invitation requires a `subjectArtifact` with an `artifactPresentationToken`, a `typedName`, and "exact-server-published-four-item-array-with-every-item-individually-actuated" statement keys. The acceptance transaction writes FIVE keys — `age-18-plus`, `mailbox-control`, `no-inviter-access`, `identity-not-verified`, `revocable` — and those five strings appear nowhere in the repository except inside the migration that writes them: no `EMBRYO_ARTIFACT_STATEMENT_KEYS` equivalent publishes them, and no copy anywhere renders one as a sentence a person could tick. The published `consent.subject-adult` body is a single paragraph containing all five ideas. Checked rather than assumed on 2026-09-13: `grep` for each key across `src/`, `docs/` and `data/` returns only the three migrations. | medium | **open, and the blocker is authorship, not engineering.** Building the registered body means splitting a published consent paragraph into individually tickable statements and deciding whether there are four of them or five. Both are legal copy on the surface where a person hands over control of their genome, and writing them here would be inventing the words a reader is told they agreed to. The session confirm therefore does exactly what the mailed-token confirm has always done — an account whose confirmed address is the invited one accepts, and the whole artifact is displayed above the control — which is no worse than the surface it replaces and strictly more informative, since the old page showed the artifact not at all. To close: publish the statement keys and their sentences for `consent.subject-adult`, reconcile four against five in the register, then the presentation token, per-statement actuation and typed name are ordinary work. The jurisdiction half of the same body (`jurisdictionAttestationVersion`, `jurisdictionAttestationHash`) is D-083 and is blocked on the same kind of act. |
| D-104 | 2026-09-13 | Measured while finding out why `e2e/research.spec.ts` cannot run on a developer database | `public.mail_outbox`, `public.claim_mail_outbox`, `docs/route-register.json` | **A mail row that exhausts its attempts is never given a terminal state: it stays `claimed` forever.** `claim_mail_outbox` takes a row that is `queued`, or `claimed` and older than ten minutes, and only while `attempt_count < 10`. Nothing anywhere moves a row out of `claimed` when it reaches ten, and nothing moves a row to `expired` when `expires_at` passes — `grep` for `attempt_count >=`, `attempt_count >` and `state='expired'` across `supabase/migrations` and `src/` returns nothing. Every invalidation in the tree is event-driven (a refusal, a file deletion, a stale recipient authority), so a row whose delivery simply never succeeds has no path to rest. Measured on this machine 2026-09-13: 27 rows sitting at `attempt_count = 10` in state `claimed` since 2026-09-10, plus 24 more at one and two attempts working their way there. The `mail_outbox.state` CHECK constraint declares `failed` and `expired`; `expired` is never written by anything, and `failed` only by the invitation-terminal path. So the schema names two terminal states the system cannot reach for the ordinary case of mail that will not send. | low | **open, and NOT fixed here, because the rule does not exist to transcribe.** What this is not: it is not a permanently poisoned failure signal. A row at ten attempts is no longer claimable, so it stops contributing to a drain's failure count — the degradation is self-limiting, and I checked that rather than assuming the worse reading. What it is: an unbounded set of rows in a non-terminal state, each still naming a recipient principal and a contact reference, which an operator reading the queue cannot tell apart from mail that is genuinely in flight. Closing it means deciding what a dead letter IS here — `failed` at exhaustion and `expired` past `expires_at` is the obvious shape, and the states are already in the constraint — and where the sweep runs. `docs/route-register.json` describes an attempts-and-max_attempts lifecycle for `worker_jobs` and says nothing at all about `mail_outbox`, so writing one is adding a lifecycle the register does not describe, which is how a scoped fix becomes an undeclared surface. An owner says whether the retention job owns this sweep or the mail job retires its own dead rows; the implementation after that is small. |
| D-105 | 2026-09-13 | Measuring priority 2's "complete supported ancestry, lineages and Neanderthal methods" against the code | `scripts/claims-gate.ts`, `data/gates/archaic-allowlist.json` | **A prohibition the brief writes out in full was enforced by nothing.** Brief §7.6 says archaic ancestry is never ranked and names the guard exactly: "`scripts/claims-gate.ts` fails on `/more Neanderthal than/i` or `/Neanderthal (percentile|rank|score)/i` **except** for two pinned exact strings, listed by full sentence (not by file glob) in `data/gates/archaic-allowlist.json`... Positive and negative fixtures are checked in." Measured 2026-09-13: `grep -i "archaic\|neanderthal" scripts/claims-gate.ts` returned nothing, `data/gates` did not exist, and no fixture existed. Nothing in the tree trips the patterns today, so no reader has ever been shown a ranking — but that is because `NeanderthalCard` withholds the estimate, not because anything was stopping it. | medium | **fixed the day it was found, and the timing is the point.** A guard on a capability is worth having BEFORE the capability, not after: the day someone builds the estimate is the day the wording gets written, and that is exactly when a missing gate costs something. `archaicFindings` is exported and the rule now runs over every source module the claims gate already walks plus the report templates — 557 files, reported in the gate's summary line so a sweep that silently read nothing cannot pass, with a floor guard under it. Six checked-in fixtures drive it, positive and negative, and they live in `scripts/claims-gate-archaic.test.ts` rather than under `src/` because a positive fixture is by definition a string the gate must refuse and the sweep reads `src/`. **The allowlist ships empty, on purpose.** Its two pinned sentences do not exist: the pages that would carry them have not been written, because the estimate they qualify is withheld pending a licence-checked marker list. Pinning a sentence that does not exist would be inventing the wording of a refusal, so the file records why it is empty — and empty is the strictest state, permitting no occurrence anywhere. A stale pin is a failure too: a sentence in the list that appears nowhere fails the gate, so the allowlist cannot rot into a permission nobody is using. **What is still not done, and is not this row's:** the Neanderthal estimate itself. `src/copy/ancestry.ts` says "Inherit has not yet built and licence-checked the marker list this needs, and we will not guess", which is the honest state and needs a marker panel plus a licence check — science and counsel, not engineering. |
