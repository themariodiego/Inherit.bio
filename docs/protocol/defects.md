# Defect ledger

Append-only. Every admissible defect has an id, the lens that found it, a
reproduction, a severity and a status. Simulated lenses are labelled
`simulated: true` and are never described as user observations.

| Id | Date | Lens | Surface | Reproduction | Severity | Status |
| --- | --- | --- | --- | --- | --- | --- |
| D-001 | 2026-09-03 | on-call engineer | branch topology | `git merge-base --is-ancestor origin/main origin/codex/readability-incident-response-remediation` is false: 32 verified commits were unreachable from `main` | high | fixed (pull request #41) |
| D-002 | 2026-09-03 | on-call engineer | `pnpm gate:readability` | 12 legal long blocks above grade 11 on `/terms`, `/legal/self-hosting`, `/legal/state-genetic-privacy` | medium | fixed (`a4e58b9`) |
| D-003 | 2026-09-03 | on-call engineer | `/science` | The report chips link to `/science#evidence` and the list's "Why?" to `/science#polygenic`; `src/app/(marketing)/science/page.tsx` had neither anchor | medium | fixed (`47bc818`) |
| D-004 | 2026-09-03 | on-call engineer | `pnpm gate:readability` | Extending the extractor to `src/copy/**/*.ts` and `src/emails/` surfaced 32 findings the JSX-only scan had never seen (29 unregistered plain words, one grade-9.1 mandated sentence, one grade-10.5 e-mail sentence, one contraction read as `dont`) | medium | fixed (`47bc818`, `7810ea6`, `31e1d7c`) |
| D-005 | 2026-09-03 | on-call engineer | `pnpm gate:readability` | Removing the stripped tokens `wont`/`doesnt` before the contraction rule landed broke the gate on `What we won't do.` at commit `7810ea6` (caught in the isolated pre-push verification, never on CI) | low | fixed (`4a3e0c6`, superseded by `31e1d7c`) |
| D-006 | 2026-09-03 | adversarial review (brief, privacy lenses) | `/genome/[subject]/reports/[slug]` | h1, breadcrumb and title carried the gene suffix; no category eyebrow; support panel nested inside "How sure we are"; provenance absent from "Where this comes from"; array explanation chosen when a VCF was present; density markers not on the selectors' elements; two generic bullets | medium | fixed (review-fix batch, this branch) |
| D-007 | 2026-09-03 | adversarial review (brief lens) | `/genome/[subject]/reports` | list header rendered the per-variant VCF sentence with no variant; search input removed against §4.4 item 7 | medium | fixed (review-fix batch) |
| D-008 | 2026-09-03 | adversarial review (privacy lens) | subject bar, Overview | an accepted-invitation record (owner null, subject account = invitee) was shown to the invitee as another adult "Uploaded with their permission" and on the bar as "Shared with you"; `minor` records got a permission chip; "Add a file" on other records uploaded to the caller's own record | high | fixed (review-fix batch) |
| D-009 | 2026-09-03 | adversarial review (brief lens) | `/overview` | an annotated file hid a second upload in flight (State C won over B); "regions found" counted with an invented 2% threshold; strings duplicated from their canonical homes; a second disclaimer in the sidebar | medium | fixed (review-fix batch) |
| D-010 | 2026-09-03 | adversarial review (brief lens) | all new surfaces | runtime links are literal paths; `src/lib/primary-routes.ts` (§5.5) does not exist | medium | open — task #12 |
| D-011 | 2026-09-03 | review-fix agent (report surfaces) | `/genome/[subject]/reports/[slug]` | the six skeleton sections sit 40px apart (`space-y-10`) while `docs/density-baseline.json` requires ≥96px between adjacent top-level sections at ≥1024px | low | fixed (this branch): the skeleton container and the library's category sections use the baseline gaps (64px below 768px, 80px to 1023px, 96px from 1024px); `e2e/report-skeleton.spec.ts` measures ≥96px between the six report sections and between the category sections at 1280×800 |
| D-012 | 2026-09-03 | CI run 33750829586 | `e2e/search.spec.ts` | the caffeine test pressed Enter on the first Reports result and expected the CYP1A2 template, but two seed templates match "caffeine" and the ADORA2A one ranks first; the spec now follows the specific link (product behaviour unchanged) | low | fixed (this branch) |
| D-013 | 2026-09-03 | capability audit (read-only agent) | `docs/` | `docs/capability-register.md` did not exist although X15/G7.4 bind it and `docs/canonical-artifacts.md` names it as the authority for ship/withhold status; six capabilities and three declared gaps had no written status anywhere | medium | fixed (this branch): register created with one row per capability, `not shipped` rows named explicitly, and a declared-gaps table |
| D-014 | 2026-09-03 | capability audit (read-only agent) | `/genome/[subject]/reports` | every seeded template is `layer='estimate'`, so the "Specific variants" layer has no templates and its tab and counts render absent; a reader cannot tell that Inherit reads specific variants at all from the list | low | fixed (branch `medicines`, ADR 0021): eleven `variant_call` templates seed the layer; the tab renders with its counts and definition; the acceptance-matrix rows that read the layer (G4.5, A4/G1.15) are updated on landing |
| D-015 | 2026-09-03 | capability audit (read-only agent) | `/genome/[subject]/reports` | the "Medicines" category exists in the taxonomy with zero templates and is dropped from the DOM entirely; no sentence tells the reader pharmacogenomics is absent, which X15 forbids ("renders absent, not empty — it is either populated or registered as `withheld` with a dossier") | medium | fixed (this branch): pharmacogenomics reached a terminal status. `docs/withheld/pharmacogenomics.md` carries the nine elements with the obstacle classified safety (primary) and scientific (supporting), three designs actually built under `docs/withheld/pharmacogenomics/designs/` with the verbatim command, exit code and output of `pnpm gate:templates` and `pnpm gate:readability` for each, ADR 0018, and the register row `withheld`. The list still states the withholding in one place, now as “Inherit does not offer reports about medicines. …” (`MEDICINES_ABSENT`), re-pinned by `e2e/report-skeleton.spec.ts`, which also asserts the sentence carries none of “coming soon”, “soon”, “yet” or “currently”. |
| D-016 | 2026-09-03 | capability audit (read-only agent) | `/legal/appeals` | the sentence "After a relative objects, Inherit turns off relative matching, pages that others can see about that relative, and shared-segment output" implies relative matching and shared-segment output exist; Inherit computes and displays neither (X15 declared gap) | medium | fixed (this branch): the sentence now names what is switched off (every page and result others can see about the relative) and states that Inherit does not match relatives or compute shared DNA; the declared gaps are published at `/science#not-offered` and `e2e/legal.spec.ts` pins both |
| D-017 | 2026-09-03 | W7 part B (fixture generation) | `src/lib/genome/admixture.ts` | the synthetic 168-marker fixture drawn at EUR 0.6 / AFR 0.3 / EAS 0.1 per allele copy is estimated as AMR 0.652 / AFR 0.255 / EAS 0.088 / EUR 0.005 / SAS 0.000: the estimator attributes most European-drawn alleles to the admixed-American reference, so the shipped panel cannot tell those two references apart at this marker count | medium | open — science dimension (X16.6): the panel, the threshold and a marker-subsampling interval are its work; the surface already states that neighbouring regions are similar and that no range exists yet |
| D-018 | 2026-09-03 | orchestrator (matrix review) | `docs/acceptance-matrix.md` | rows G4.1–G4.3, G4.6 and G4.7 carry paraphrased statements that do not match the brief's G4 gate texts (G4.3 in the brief is the count-class gate, G4.6 the input-quality provenance block, G4.7 citations with access dates); G4.4 and G4.5 were realigned in this pass | low | fixed (this branch): G4.1, G4.2, G4.3, G4.6 and G4.7 now carry the brief's gate text; each row's evidence was re-audited against the tree and names exactly what is built (the relative-figure component and ladder with no rendering template; `data-figure-basis` and the once-per-block marker with no E2E count; the single-class `<Count>` under X5.1's names with no automated gate; the coverage sentence, pills and fixed provenance line with no call rate or liftover status; PMID/DOI citations on every template with no access date). All five stay NO |
| D-019 | 2026-09-03 | orchestrator (G4.4 audit) | `src/components/results/ancestry/lineage-card.tsx` | a haplogroup call renders its matched-of-tested markers as a coverage figure but not the reference tree's version nor an explicit statement that no interval exists, which G4.4 requires of every quantity derived from a reference panel | low | fixed (this branch): each called line names its tree and version (`LINEAGE_TREES`), states "Inherit puts no range on this name…" and gives the tree's resolution limit, beside the existing matched-of-tested coverage figure |
| D-020 | 2026-09-03 | CI run 33775768209 | `src/app/api/files/[id]/process/route.ts` | the admixture row gained `model_id`, `model_version`, `coverage` and `result_state` while the two haplogroup rows in the same bulk insert did not; PostgREST fills a key that some rows omit with null rather than the column default, so `result_state` violated NOT NULL (Postgres 23502) and every file's processing failed, taking ten browser specs with it | high | fixed (this branch): every ancestry row states the four columns explicitly through one `AncestryRow` type (haplogroup rows: no model id, coverage from matched-of-tested, `available` / `partial` / `not_covered`); the unit suite cannot reach this because the database path is exercised only against the real local stack in CI |
| D-021 | 2026-09-03 | CI run 33778596222 | `e2e/genome-data.spec.ts` | the data-page test forbade any percent sign inside a score's list item, but each score renders its seeded ancestry-portability statement, which quotes the source cohort's composition ("53.3% multi-ancestry…"); the assertion was wrong, not the page | low | fixed (this branch): the note carries `data-slot="ancestry-note"` and the exempt comment (provenance about the score's cohort, not a result about the subject); the spec asserts one note per score and no percent text outside it |
| D-022 | 2026-09-03 | orchestrator (G4.4 audit, reading the haplogroup provenance) | `data/ref/haplogroups/`, `docs/dataset-licenses.md` | the licence audit had no row for either haplogroup reference source, and the two records disagree: the repository's provenance note asserts PhyloTree is "free for academic/non-commercial use with citation", while the publisher's own page, read 2026-09-03, states no copyright, licence or terms at all and asks only for a citation. The audit's standing rule is that no non-commercial-licensed source may enter the reference store or seed data, and the mtDNA tree's topology and defining positions are seed data | high (legal surface) | open — both sources now have rows marked verification outstanding; resolving it needs the publisher's actual terms (or counsel's reading of data published with none), after which either the provenance note or the maternal-line capability's status changes. The MIT licence on the machine-readable copy covers that copy's packaging, not the tree |
| D-023 | 2026-09-03 | orchestrator (F1 review, reading the grant routine) | `/family/[person]/permissions` | the Portrait row rendered "Turn on", but `grant_directional_purpose_v1` refuses `family.portrait` while the signer's `independent_login_at` is null, and nothing in `src/` ever called `mark_independent_login_v1`: the register places the marker in `auth.callback`, while the password sign-in completes in the browser and never reaches that route, so every Portrait grant would have answered 409 (a control without its mechanism, brief §9 item 21) | medium | fixed (this branch): `src/lib/family/independent-login.ts` stamps the marker from the callback exchange and on the first server-rendered permissions visit (the routine itself proves the session post-dates the acceptance); until it is stamped the Portrait row renders locked with "Portrait needs a sign-in of your own first…" and no control; `e2e/family.spec.ts` asserts the locked row in the acceptance session and the successful grant after a fresh sign-in |
| D-024 | 2026-09-03 | CI run 33787980603 | `e2e/adult-subject-invitation.spec.ts` | the assertion F1 added ("the pre-consent statement sits above the form") located `form` unscoped, which resolves to two elements under the app shell: the account landmark's sign-out form and the invite form; Playwright's strict mode throws | low | fixed (this branch): the locator is scoped to the form containing the email field; the product is unchanged. The same unscoped locator in `e2e/family.spec.ts`'s invite test was masked by the hub failure in the first run and surfaced in run 33789839105 once that passed; scoped the same way |
| D-025 | 2026-09-03 | CI run 33787980603 | `e2e/family.spec.ts` | `expectAxeClean` emulated the dark colour scheme on the already-loaded hub and audited at once; `next-themes` flips the class on the live page and the chrome's `transition-colors` animates for 150 ms, so axe sampled mid-transition text and background colours and reported a serious `color-contrast` violation in dark only. Every other spec loads the page after emulating the theme | low | fixed (this branch): the helper reloads the page in each theme before auditing, as `e2e/a11y.spec.ts` and `e2e/ancestry.spec.ts` do |
| D-026 | 2026-09-03 | CI run 33790730613 | `/family/[person]/permissions` | each row control carried `aria-label="{label} for {name}"`, which replaces the visible "Turn on" / "Turn off" as the accessible name: the browser spec's `getByRole("button", { name: "Turn on" })` could never match (the invite/accept test timed out after 120 s), and the control failed WCAG 2.5.3 (the visible label must be part of the accessible name) | medium | fixed (this branch): the accessible name is now "Turn on {label} for {name}" / "Turn off {label} for {name}" (`rowControlLabel(verb, label, name)`), pinned by `src/components/family/family.test.ts`; the spec's locators are unchanged and now match |
| D-027 | 2026-09-03 | F3 libraries agent (running the Family unit suites) | `src/lib/family/tier2.test.ts` | the tamper assertion replaced the digest's last character with a literal `0`, so whenever the real digest already ends in `0` the "tampered" cookie equals the real one and the test fails; the key is random per run, so the failure is intermittent | low | fixed (this branch): the replacement character is chosen to differ from the real last character |
| D-028 | 2026-09-03 | Family permissions (Portrait row) | `mark_independent_login_v1` excluded only subjects that an accepted invitation targeted, but the Portrait guard reads the data subject's own `self` record, which no invitation targets. Reproduced in CI run 33792405267 (`e2e/family.spec.ts:341`): the invited adult's first request in the acceptance session stamped their self record, so the Portrait row was settable from the session the invitation was accepted in. | high (a sharing guard that never engaged) | fixed (this branch): migration `20260903190000_independent_login_per_account.sql` makes the rule per account (any session that predates an adult-subject invitation this account accepted stamps nothing), with four pgTAP assertions |
| D-029 | 2026-09-03 | Family permissions | No screen could grant `family.heritability`: the five permission rows are the report layers, ancestry, raw data and Portrait, so the two-column health picture was reachable only by a service-role call in the browser suite. Found while reviewing F2's report. | medium | fixed (this branch): a sixth row, "Health picture", on the same directional own-session rules, locked with Portrait until the independent-login marker is set (ADR 0017 §8) |
| D-030 | 2026-09-03 | Carrier panel (runs of homozygosity) | `src/lib/family/roh.ts` measured runs at request time under a 20,000-call read budget, so every real array or sequence file answered `not_measurable` and the 25-in-100 branch could never render in production. Found by the adversarial review of `c6dd140` (carrier-rule lens). | high | fixed (this branch): measured once at ingest from the parsed calls and stored on `genome_files` (migration `20260903200000_genome_files_runs_of_homozygosity.sql`); the reader asks the stored measure of every annotated file of the subject |
| D-031 | 2026-09-03 | Carrier panel (X-linked pattern) | Brief line 346 requires the X-linked arithmetic across 100 pregnancies; the split depends on which parent carries the change on the X, and nothing in Inherit records a person's chromosomal sex. The panel renders the refusal sentence with the reason `sex-unknown` instead. | medium (science and product) | open — needs a sourced writer for chromosomal sex; the Portrait libraries' `xLinkedCross` is ready to render the split once one exists. The reason phrase was corrected to say what is not recorded (review, R5) |
| D-032 | 2026-09-03 | Carrier panel (trigger and naming) | The candidate set required the same rsid in both files and the block never rendered the variant; brief line 346 triggers on a P/LP heterozygous variant in the same gene and requires naming both variants and both classifications. | medium | fixed (this branch): candidates are grouped by gene; a match carries each person's own variant, classification and copies, and the block names them (review, R2) |
| D-033 | 2026-09-03 | Carrier panel (classification) | `isPathogenicClassification` matched the exact strings only, while the reference writer stores ClinVar's list joined with ", " and ClinVar itself uses "/", so "Pathogenic/Likely pathogenic" fell to "nobody yet knows what this change means". | medium | fixed (this branch): labels are split on `/ , ; |`; pathogenic only when every token is pathogenic or likely pathogenic, harmless only when every token is benign or likely benign, anything else unknown (review, R3) |
| D-034 | 2026-09-03 | Carrier panel (empty state) | With no classified reference position (production today) the panel printed "Inherit checked the 0 positions both files cover" above a table whose cells show positions both files do cover. | high | fixed (this branch): an empty classified set renders "Inherit has no classified positions to check yet, so it cannot look for a change you both carry."; the count sentence renders only over a non-empty set (review, R6) |
| D-035 | 2026-09-03 | Carrier panel (two copies) | A person with two changed copies at a classified position was dropped from the panel, which then reported no shared change although both files showed one; brief line 346 says any failed trigger renders the refusal sentence with a reason. | high | fixed (this branch): the pair renders with no probability and the new closed-table reason `two-copies` ("one file shows two changed copies, not one"); decision recorded (review, R4) |
| D-036 | 2026-09-03 | Portrait libraries (distribution) | `distribute()` keyed the below-1-in-100 rule on the dot count after largest-remainder rather than on the share, so a 6-in-1,000 category rendered a solid dot and "about 1 would"; the lending category's sentence disagreed with the figure contract's rounding; a NaN share was filtered away instead of refused; `crossShares` promised a number for absent outcomes; `sentence: null` could pair with `denominator: 1000`. | medium | fixed (this branch): the rule keys on the share, sentences use `naturalFrequency(share, 100)`, NaN and negative shares throw, absent outcomes are absent keys, and one rounding rule decides both the sentence and the rung (review, R8) |
| D-037 | 2026-09-03 | Portrait copy | `ACKNOWLEDGE_BUTTON` was "Continue", banned standalone by brief line 928; the `runs_below_threshold` assumption statement asserted a completed check under the label "What we do not check". | low | fixed (this branch): "Open Portrait"; the statement is written as the assumption it is (review, R7) |
| D-038 | 2026-09-03 | Health picture (consent boundary) | Cells rendered another adult's genotype letters under `family.heritability` alone, and the Overview's carrier line read another adult's rows after checking `carrier_match` only; the register makes `family.heritability` authority for the joint comparison, never for an individual result layer, and `family:carrier-arithmetic` requires all three capabilities. Found by the adversarial review (browser-spec and consent lens). | medium | fixed (this branch): a cell's figure and link need the layer's own grant from that person, else "Not shared with you"; the Overview checks the three capabilities before any read (review, S3–S4) |
| D-039 | 2026-09-03 | `e2e/family-health-picture.spec.ts` | Tests after the second never passed the Tier-2 gate in their own browser context (the gate cookie is keyed to the auth session), the dark-theme axe audit repeated the D-025 pattern, and the independent-login marker was forged with a raw update instead of the routine. Found by the adversarial review before the spec ever ran in CI. | medium (test) | fixed (this branch): a gate helper per test, a reload per theme, the real `mark_independent_login_v1` call (review, S1, S2, S5) |
| D-040 | 2026-09-03 | Carrier panel (runs of homozygosity) | The measure counted any two adjacent same-reading calls as a run and summed their spans, so an array-density file always exceeded the brief's 100 Mb and 0.0156 thresholds (the repository's own `data/samples/synthetic_23andme.txt`: 124 runs, 241 Mb, F_ROH 0.055) and the 25-in-100 branch was reachable only for a sparse VCF; a differences-only VCF with adjacent 1/1 rows counted as measured although the positions between its rows are unrecorded. Found by the adversarial review's verification pass after D-030. | high (science) | fixed (this branch): runs follow McQuillan et al. 2008 (doi:10.1016/j.ajhg.2008.08.007): ≥ 1.5 Mb, ≥ 25 contiguous same-reading calls, at most one heterozygous call inside a run; a file that reports no reference-homozygous call is `not_measurable` (`no-reference-calls`); the denominator stays the autosomal span the file covers and is stated as Inherit's own choice |
| D-041 | 2026-09-03 | Family permissions | The lock reason read "Portrait needs a sign-in of your own first…" on the new Health picture row too, and `e2e/family.spec.ts` picked the Portrait row by a substring filter that this text now matched twice (CI run 33807524096, the first on PR #42). | low | fixed (this branch): the reason is row-neutral ("This row needs a sign-in of your own first…"), the label carries `data-slot="permission-label"`, and the spec filters rows by their exact label |
| D-042 | 2026-09-04 | Copilot output guard | The route checked only `text-delta` chunks and forwarded reasoning parts and tool-input JSON to the client unchecked, so a fabricated number in a reasoning part or a model-authored rsID in a tool input bypassed brief line 2262. Found by the adversarial review (findings 1 and 12). | high | fixed (this branch): `toUIMessageStream` runs with `sendReasoning: false` and `foldStreamChunks` folds text, reasoning, tool inputs, sources and provider-run tool outputs into the one string `checkResponse` reads; a failing turn is replaced whole |
| D-043 | 2026-09-04 | Copilot intent gate | The diagnosis rules' file-fact exemption tested the whole message, so "Do I have haemochromatosis? My file has the variant." was allowed, while "Do I have Neanderthal DNA?", "Do I have rs762551?" and "Do I have MTHFR?" were refused. Found by the adversarial review (finding 5). | high | fixed (this branch): the exemption reaches only the object of the rule's own "have" (a determiner, up to two qualifying words, a file-fact word, an rsID or a capitalised gene symbol); paraphrases ("am I a diabetic", "so I have X then", "is that a disease", "does my genome say I have") are gated |
| D-044 | 2026-09-04 | Copilot intent gate | A comparative about embryos ("Which of them is healthier?", "Is embryo 3 better than embryo 5?", "Compare embryo 2 with embryo 4") reached the model in a cohort scope; a ranking by another name. Found by the adversarial review (finding 6). | high | fixed (this branch): `selection.comparative`, `selection.which-of-them` and `selection.compare-embryos`; a sex question that also compares keeps the sex refusal |
| D-045 | 2026-09-04 | Copilot intent gate | An unspaced dose ("500mg", "1000IU"), "too much iron", "double my folic acid", "any supplement recommendations", "what supplements" and "should I be taking" were not treatment questions to the gate. Found by the adversarial review (finding 7). | high | fixed (this branch): `treatment.dose` reads `\d+\s*unit`, plus `treatment.too-much`, `treatment.adjust-intake` and `treatment.which-supplement`; a genotype letter after a digit ("1 G and 1 A") is not a unit |
| D-046 | 2026-09-04 | Copilot intent gate | The Portrait rule refused any "will/would my child …", including the inheritance and carrier questions Portrait exists to answer ("Could our children inherit this?", "Would a child of ours be a carrier?"). Found by the adversarial review (finding 8). | high | fixed (this branch): the rule refuses trait predictions about one child (be, have, look, grow, become; "how tall will my child be") and exempts inherit, carry and carrier |
| D-047 | 2026-09-04 | Copilot chat route | A gated user turn and its refusal stayed in the client's thread and were resent as history, so the model saw every refused message on the next allowed request. Found by the adversarial review (finding 10). | high | fixed (this branch): `dropGatedTurns` classifies every user turn and drops a gated one with the assistant turn that answered it before `convertToModelMessages`; `e2e/copilot-refusal.spec.ts` reads the provider's history |
| D-048 | 2026-09-04 | Copilot citation check | "Author et al. YEAR" matched by substring ("Sule" inside "Sulem", "2011" inside "20115"), an "et al." without a year, "a study by X", "according to X" and "published in X" were not citations to the check, and `get_prs` never selected `prs_scores.citation`, so a score citation could not be permitted. Found by the adversarial review (findings 2, 4, 11). | medium | fixed (this branch): whole-token matching against the labels, the four new shapes held to the same set, report and score names (`title`, `name`) permitted, `citation` selected |
| D-049 | 2026-09-04 | `docs/route-register.json` | `copilot-refusal-v1` described a JSON body while the route serves the refusal on the UI-message-stream transport with `x-copilot-refusal`. Found by the adversarial review (findings 13, 15). | low | fixed (this branch): the register describes the served contract (status 200, `text/event-stream`, the header enum, one text part, no echo); the route is unchanged |
| D-050 | 2026-09-04 | Copilot chat route | A `minor` or `embryo` subject resolved to the `subject` guard scope although no chat scope exists for either class. Found by the adversarial review (finding 14). | low | fixed (this branch): `guardScopeKindFor` admits `self` and `other_adult`; any other class answers the opaque 404 |
| D-051 | 2026-09-04 | `e2e/copilot-refusal.spec.ts`, `e2e/mock-llm.ts` | Three treatment prompts all reached `treatment.should-i-take`, the header claimed a cohort prompt no route can send, the mock counted only `/chat/completions`, and the axe audit ran on a reloaded, empty thread. Found by the adversarial review (findings 16–19). | low (test) | fixed (this branch): one prompt per treatment rule (pinned by rule id in `guard.test.ts`), the header states the cohort limit, the mock counts every request on any path and exposes the newest history, each theme sends a gated prompt before axe |
| D-052 | 2026-09-04 | Portrait output cards | The brief's line-1349 runs refusal ("These two files look more genetically similar than usual…") rendered for the `runs-unchecked` reason, which also covers a person whose runs were never measured, could not be, or who has no annotated file: the sentence asserted a similarity nothing had measured. Found by the adversarial review (finding 1). | high | fixed (this branch): the reason table splits `runs-above-threshold` (a measured file above a threshold; the refusal) from `runs-unchecked` (the side-by-side page's "could not check" sentence); `subjectRunsState` in `src/lib/family/roh.ts` |
| D-053 | 2026-09-04 | Carrier rule | The gene-level trigger produced the exact 25-in-100 arithmetic for two changes at different positions when each file lacked the other's position, and the card printed "Both files cover the positions this uses"; brief line 1349 says a position one file does not cover is named and never imputed. Found by the adversarial review (finding 2). | high | fixed (this branch): `not-covered` names the file and the position (`uncovered`); a match carries `positionsBothCovered`, and the coverage field is the cannot-calculate sentence when it is false |
| D-054 | 2026-09-04 | Portrait page | The one-sided readings ran only when at least one classified position was shared, so a carrier whose partner's file covers none of the gene's positions, the line-2238 case itself, rendered nothing. Found by the adversarial review (finding 3). | medium | fixed (this branch): `evaluateOneSided` runs whatever `positionsBothCover` is |
| D-055 | 2026-09-04 | Portrait libraries | A match refused because the other side's change was harmless or of unknown meaning counted as the carrier rule's answer for the gene and suppressed the no-second-copy reading for the pathogenic carrier. Found by the adversarial review (finding 4). | medium | fixed (this branch): `answeredGenes` counts only a match where both readings are pathogenic or likely pathogenic; the page renders the one-sided reading in place of that refusal |
| D-056 | 2026-09-04 | Portrait output cards | The runs threshold statement sat under "What we do not check" although the runs were measured, and the viewer's own label ("You") filled third-person sentence slots ("You’s file", "You hasn’t"). Found by the adversarial review (findings 5, 6). | low | fixed (this branch): a "What we checked" field carries the runs statement on an exact block; every sentence about the viewer has a second-person form (`PersonRef`) and the placeholder never reaches a slot |
| D-057 | 2026-09-04 | Portrait blocking screen and delete dialog | The delete dialog said the page opens again only when "you both" turn Portrait on, though deleting revokes the viewer's own grant alone; the acknowledgement form rendered while a grant or sign-in was still missing. Found by the adversarial review (findings 7, 8). | low | fixed (this branch): the dialog names the one grant revoked and the one that stays; the form renders only when the acknowledgement is the viewer's one remaining step |
| D-058 | 2026-09-04 | `src/copy/family/portrait.ts` | The refusals list lacked line 356's talent, athleticism, attractiveness and skin tone, and the copy claimed the height reason was the brief's verbatim although it is a grade-9 rewrite of an example. Found by the adversarial review (findings 9–11). | low | fixed (this branch): four cards added (fifteen in all); the comment and test cite the recorded decision |
| D-059 | 2026-09-04 | Embryo closed shapes (`src/lib/embryos/policy.ts`) | `qc_reasons` accepted any string array and `not_covered_reason` any string, so a value outside the register's closed tables passed the shape and reached the renderers, which then emitted it as `data-reason`. Found by the adversarial review of E1 (R1). | high | fixed (this branch): membership of `QC_REASON_IDS` and `RESULT_NOT_REPORTABLE_REASON_IDS` is validated; a qc reason needs `quality_not_measurable` and a result-level reason `not_covered`; `registeredReason` maps every emitted id |
| D-060 | 2026-09-04 | Embryo QC source strings | `source_laboratory`, `source_assay` and the two method strings were unbounded and printed as received, although the register requires bounded reviewed labels and canonical-artifacts says an original laboratory label is never rendered. Found by the adversarial review of E1 (R2). | high | fixed (this branch): `data/embryo/source_labels.json`, a closed registry withheld and empty today; the shape accepts null or a registered id; renderers print a label's display text or "Not stated by the source laboratory" |
| D-061 | 2026-09-04 | `<CompareCell>` | The embryo's own absolute figure carried the general-population group, so one cell attributed two numbers to the population; the `measured_inconclusive` status rendered the "No one has measured…" sentence, which is false of a tested score. Found by the adversarial review of E1 (R3, R4). | medium | fixed (this branch): the embryo's figure is captioned "for Embryo N"; the inconclusive status has its own sentence with its citation beside the brief-1318 comparison sentence |
| D-062 | 2026-09-04 | `<QcTable>`, `<CompareTable>` | The QC table, a `data-card` holding many `data-subject-id` blocks, lacked `data-compare-surface` (brief line 211); the compare caption said "your DNA" about an embryo's file (X13.1); the mean depth rendered as a bare count outside the figure contract. Found by the adversarial review of E1 (R5, R6, R7). | high | fixed (this branch): the attribute, `EMBRYO_LAYER_DEFINITIONS`, and the `measure` figure kind (value, unit, decimals) with the depth inside the column's block |
| D-063 | 2026-09-04 | `e2e/embryos.spec.ts`, `playwright.config.ts` | The title assertion ignored the root layout's template; the flag-off jurisdiction refusal of design §6.2 had no browser proof; the spec header claimed the unit suite proved the gate. Found by the adversarial review of E1 (R8, R9, R10). | medium (test) | fixed (this branch): the templated title; a second Playwright project against a second server from the same build with the flag unset, running `e2e/embryos.nojurisdiction.spec.ts`; the header states what is and is not proven |
| D-064 | 2026-09-04 | `src/lib/embryos/cohorts.ts`, the result loaders | Every query's `error` was ignored, so a failed read resolved to an empty graph, a 404 or "Still checking the files". Found by the adversarial review of E1 (R11). | medium | fixed (this branch): `rowsOrThrow` and `EmbryoReadError`; the compare and detail pages render the read-failed error state |
| D-065 | 2026-09-04 | `/auth/sign-out`, the embryo gate | Sign-out deleted the Family gate cookie only; the embryo gate's write and read paths shared no code. Found by the adversarial review of E1 (R12, R10). | low | fixed (this branch): `signOutResponse` deletes both cookies; `embryoGateCookie` and `acknowledgedBy` are the one pair, tested end to end |
| D-066 | 2026-09-04 | Embryo consent sentence | A non-parent uploader with no grant from either parent read "Waiting for the other genetic parent". Found by the adversarial review of E1 (R13). | low | fixed (this branch): `analysisConsent` answers `waiting-for-both`; the sentence names "both genetic parents", or the one missing party |
| D-067 | 2026-09-04 | `src/components/uploads/uploader.tsx` | The uploader carried its own unrecognised-format and too-large sentences beside the A.6 home the canonical index names, and refused a PDF as "Unrecognized format" although the canonical PDF row has the preflight refuse it by name. Found while placing the A.6 refusals. | low | fixed (this branch): the preflight reads `sniffFileV2` and the three sentences from `src/copy/upload/errors.ts` |
| D-068 | 2026-09-04 | `docs/capability-register.md` | The Embryo ingest row said the Embryos landing ends "No embryo analysis model is currently allowlisted on this deployment.", a sentence E1 removed. Found while updating the row. | low (docs) | fixed (this branch): the row states what `/embryos/upload` and the landing render |
| D-069 | 2026-09-04 | `e2e/README.md` | The suite index had no row for `embryos.spec.ts` or `embryos.nojurisdiction.spec.ts`. Found while adding the upload flow's assertions. | low (docs) | fixed (this branch): both rows added |
| D-070 | 2026-09-04 | `src/components/embryo/upload/upload-flow.tsx`, `src/lib/embryos/upload-flow.ts` | Two screens of the upload flow exceeded X6.1's seven interactive elements on the repository's measurement basis (nine on the options screen with the shell's search button and desktop attribution link counted). Found by CI run 33924630411 on PR #48. | medium | fixed (this branch): screens cut to five controls of their own — the first two questions on one screen, the options and bases as actions, the PDF refusal and each basis on its own screen; the reducer records the shell's two controls beside the cap |
| D-071 | 2026-09-04 | `src/copy/embryos/upload.ts` | Three flow sentences overclaimed: "Nothing is kept until both parents sign" (the design's draft step keeps a record first; three bases never have both parents sign), the closing sentence named the other parent's email and signature for every path, and a basis label and sentence spoke in a genetic parent's first person after the permission situation. Found by the adversarial review of E2 slice 1 (C3, C4, C5). | medium | fixed (this branch): sentences true on every path, third-person bases |
| D-072 | 2026-09-04 | `src/components/uploads/uploader.tsx` | A laboratory table `sniffV2` had recognised was refused as "unrecognised", whose trigger the brief binds to a null sniff; a multi-sample VCF travelled on to fail at the server; the PDF refusal promised a letter without linking it. Found by the review (C6, U3). | medium | fixed (this branch): the register's `subject_source_not_single_sample` sentence with a link to the Embryo flow for both cohort-shaped kinds; the letter link on the PDF refusal |
| D-073 | 2026-09-04 | `src/lib/genome/parsers/sniff.ts` | A `#CHROM` line cut off by the 64 KiB decode window or a truncated gzip member was read as complete, giving a wrong sample count or a cut sample name; a trailing tab counted an empty sample. Found by the review (C8, U6). | medium (latent: no runtime caller yet reads the count) | fixed (this branch): an unterminated header answers `sampleCount: null`; trailing whitespace is dropped |
| D-074 | 2026-09-04 | `src/lib/genome/parsers/pgt-table.ts` | A comma inside a quoted header cell split the cell, shifting every later column index and yielding a complete plan pointing at the wrong columns. Found by the review (C9). | medium (latent: no mapping route yet) | fixed (this branch): RFC 4180 tokenisation of the header line |
| D-075 | 2026-09-04 | `src/components/embryo/upload/upload-flow.tsx` | The "No" ending appeared in place as a newly mounted live region that nothing focused; the terminal rendered a block-role sentence as a heading; the basis sentence and the endings were live regions that double-announce or are ignored. Found by the review (C10, U1, U7). | low | fixed (this branch): focus moves to the ending's sentence, the PDF screen carries the option's label as its heading, the terminal's sentence is a focused paragraph, no live regions |
| D-076 | 2026-09-04 | `docs/adr/0020-*.md`, `src/copy/upload/errors.ts`, `docs/canonical-artifacts.md` | All three said the file processor consumes the A.6 refusals; no processor code does yet. Found by the review (C7, U8). | low (docs) | fixed (this branch): the consumers that exist are named; the processor's re-sniff and the ingest routes are marked as E0's |

