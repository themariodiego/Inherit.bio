# Test diff register

Baseline: `864736979c92a08ba77e8580d61946eba6864918` (`8647369`).

This register covers test and test-runner changes after the baseline. A change
is listed only when it strengthens or extends an assertion; weakened, deleted,
skipped, quarantined, or retry-dependent tests are not permitted.

| Path | Kind | Reason | Strengthened or replacement assertion |
| --- | --- | --- | --- |
| `playwright.config.ts` | Runner strengthened | G1.5 requires zero retries in every environment and machine-readable proof of zero skipped results. | `retries` is unconditionally `0`; the JSON reporter writes `test-results/results.json`; `scripts/run-e2e.ts` makes `pnpm e2e` fail when any result is skipped or has `retry > 0`. |
| `e2e/adult-subject-invitation.spec.ts` | New E2E coverage | Covers the TEST-LOCAL adult-subject invitation Path A reservation and acceptance boundary. | Follows the captured email link, proves the raw token is absent from durable mail payloads, binds the subject to the matching recipient account, and proves the inviter has zero directional grants and the subject has zero files. |
| `scripts/secret-gate.test.ts` | New unit coverage | Pins the G1.17 credential detector against future provider formats and false-positive regressions. | Detects provider tokens, JWTs, private-key headers, credential-bearing URLs, and generic secret assignments while rejecting documented placeholders and environment lookups. |
| `scripts/name-gate.test.ts` | New unit coverage | Pins G1.9/G6 normalization, carve-out precedence, and evaluative-distance behavior. | Detects lowercase-domain, camelCase, kebab-case, snake_case and compact forms; proves the provider carve-out is path-limited and absent for commit messages; checks the exact 200-character proximity boundary. |
| `scripts/readability.test.ts` | New unit coverage | Pins the G1.10 scorer before corpus remediation begins. | Proves opaque-token normalization, deterministic sentence/word/syllable counts, contraction handling, and all ten versioned calibration grades within the 0.2 tolerance. |
| `scripts/readability-gate.test.ts` | New unit coverage | Prevents the static G1.10 extractor from inventing composite copy blocks or regressing remediated provider, report-template, privacy-policy, GINA, deceased-account, law-enforcement, research-consent, Copilot setup, appeals, Future Person, GDPR, or incident-response copy. | Proves nested copy containers are scored separately, inline markup stays inside its visible block, `alt`/`title` strings remain independently covered, and every displayed provider field plus all fifteen report categories, the privacy policy, GINA explainer, deceased-account policy, law-enforcement policy, research-consent policy, Copilot setup page, appeals policy, Future Person Charter, GDPR status page, and incident-response policy remains free of readability findings. |
| `e2e/copilot.spec.ts` | Strengthened setup-copy assertion | Keeps the existing cloud-provider consent flow aligned with the remediated plain-language endpoint state without losing any required setup clause. | The no-provider state must retain the API-key analogy, explicit per-use consent, typical penny cost, provider and exact-data-class dialog, revocation, Ollama and LM Studio options, OpenAI-compatible setting, infrastructure boundary, and hosted-demo localhost limit; the cloud flow, tool call, citations, and revocation behavior remain unchanged. |
| `e2e/upload-vcf.spec.ts` | Updated copy assertion | Keeps the existing local-only genome-browser provenance check aligned with the remediated sentence. | Upload, processing, locus rendering, rsID search, genotype, and external-network assertions are unchanged; only the expected provenance sentence changes. |
| `e2e/legal.spec.ts` | Strengthened legal-policy assertion | Keeps the deceased-account page aligned with the v2 policy instead of checking only that it mentions death and a certificate. | The rendered page must refuse new deceased-person uploads; cover an account holder, another adult, a genetic parent's embryo record, and a future-person record; require human review and 30 days' notice; honor recorded wishes; and protect a living relative's genotype without consent. |
| `e2e/legal.spec.ts` | Strengthened law-enforcement-policy assertion | Replaces the former topic-and-zero check with the v2 disclosure contract. | The rendered page must require a warrant or equal judicial order for content, reject a subpoena alone and resist genetic-data subpoenas, give affected-person notice, cover another adult and future-person records, ban forensic matching, and publish explicit received/resisted/complied zeros by jurisdiction. |
| `e2e/legal.spec.ts` | Strengthened research-consent-policy assertion | Replaces the former one-word topic check with the v2 future-research contract. | The rendered page must state there is no user-data research; require separate purpose/recipient-class opt-ins, named recipients, published independent review, and prospective withdrawal limits; exclude embryo and other-adult data; and forbid model development or training under research consent. |
| `e2e/legal.spec.ts` | New appeals-policy assertion | Adds the public appeals route to the rendered legal-policy regression set. | The page must cover account-free objections, non-consensual adult uploads, relative objections, the 60-second cross-account restriction and written confirmation, named-human-only review, fixed acknowledgement and resolution clocks, and public-response privacy. |
| `e2e/legal.spec.ts` | New Future Person Charter assertion | Adds the public Charter route to the rendered legal-policy regression set. | All six rights and their parent-data boundary must remain; right five must prohibit insurer, employer, school, outside-AI, and unresisted court disclosure while requiring a local model for non-self genomes; right six must retain the claim window and deletion rule; and the page must retain third-party enforceability, release boundaries, and jurisdiction-specific service gating. |
| `e2e/legal.spec.ts` | New GDPR-status assertion | Adds the public GDPR route to the rendered legal-policy regression set. | The page must retain the Article 6 and Article 9 bases, registered purpose keys, rights deadline, transfer and impact-assessment publication gates, and honest EU/UK restriction while the controller contact, DPO, and two representatives remain missing. |
| `e2e/legal.spec.ts` | New incident-response assertion | Adds the public incident-response route to the rendered legal-policy regression set. | The page must retain safe reporting instructions, the four-hour assessment start, containment, no-loss incident records, GDPR and US notice clocks, direct non-account-holder and claimant notice, the missing-key disclosure, and the dated zero-incident history. |
| `e2e/report-gate.spec.ts` | Strengthened result-content assertions | The report page now renders results through the figure contract and the six-heading skeleton; the prose pins ("Your genotype", "Sources", the old not-covered sentence) no longer exist. | Gated HTML and DOM must contain no `data-figure-kind="genotype"` node, none of "Your file does not cover this variant." / "This is a limit of your file, not a result about you.", and (FGFR2) no "A/G" or "One copy of the A risk allele"; revealed pages must carry the genotype node with `[data-slot="figure-value"]` text "A/C" or "A/G"; the not-covered state must render the exact §2 §4.5 VCF first sentence followed by the exact limit sentence; the heading "Where this comes from" replaces the text "Sources"; "Not now" must land on `/genome/me/reports#cancer` (nine-category id) with `#cancer` visible. Every gate, support-panel and localStorage assertion is unchanged. |
| `e2e/legal.spec.ts` | Strengthened report-disclaimer assertion | The report surface ships the one §5 §6.1 not-diagnostic line instead of the former informational wording. | `report-disclaimer` must have the exact text "This is not a diagnosis. Inherit is not a doctor and no clinician has reviewed this. Talk to a qualified professional before acting on anything here." (full-text match, not a substring regex). |
| `e2e/report-skeleton.spec.ts` | New E2E coverage | Pins the report skeleton, figure contract and layer/category presentation on `/genome/me`, `/genome/me/reports` and two report pages against the tiny GRCh38 fixture. | Six fixed h2s with fixed ids in DOM order; exactly one `[data-claim-block][data-subject-id]`; one `[data-figure-kind="genotype"][data-figure-class="estimate"][data-figure-basis="observed"][data-provenance^="computed:"]` node reading "A/C"; the exact partial-state sentence; zero percentile nodes; the exact not-diagnostic line; `What this doesn’t mean` is exactly one `li` (`It does not say what will happen to you.`) on the covered report and exactly two (that bullet then `A missing result is not a negative result.`) on the not-covered report; breadcrumb "My Genome / {name} / Reports / Caffeine metabolism" with hub and list links; subject-bar chip "You" and "1 file" → `/files`; chip row "Statistical estimates" and "Emerging"/"Preliminary"; footer links to `/genome/me/data` and `/copilot/me?report={slug}`; the not-covered strings at the body `--ink` colour with "2 supporting studies" and "Your file covered 0 of the 2 positions this estimate uses."; on the list, the estimate definition exactly once, the count `{seeded template count} statistical estimates` with `data-figure-class` (the number is read from `data/templates` by `seededTemplateCount()` in `e2e/helpers.ts`, never hard-coded), the eight seeded category headings in taxonomy order with `#cancer` present and `#medicines` absent, no "Polygenic scores", no percent sign in any h2; on the hub, h1 "My Genome", tiles Reports/Ancestry/Copilot and exactly one default-variant button "Add a file". |
| `e2e/report-skeleton.spec.ts` | Strengthened report-page and list assertions | The report page now renders the report name (title up to the gene suffix) as its h1, the nine-category eyebrow, a `Sources` h3 with a per-variant provenance row, and the support panel after the skeleton; the list restores the labelled search box and renames the category strip to `Filter reports`. | Covered report: `main h1` is exactly "Caffeine metabolism"; the first `main article header p` is "Food, drink and metabolism"; exactly six `h2` inside `[data-slot="report-skeleton"]`; exactly one `[data-claim-block][aria-label="CYP1A2 rs762551"][data-density-primary-claim="true"]`; "Your result" contains no `[data-slot="variant-locus"]` line; "Where this comes from" has a level-3 heading "Sources", a link "rs762551" → `https://www.ncbi.nlm.nih.gov/snp/rs762551` and the text "CYP1A2 · rs762551 · chr15:74749576 C→A". Not-covered report: six skeleton `h2`, `main h1` "Alzheimer's disease" (U+0027 from seed), `support-panel` visible and not inside the skeleton, "Your result" has exactly two `[data-slot="variant-locus"]` lines reading "APOE · rs429358" then "APOE · rs7412". List: exactly two `[data-slot="count"][data-figure-class="estimate"]` nodes, the first matching `/^\d+ statistical estimates? covered by your file$/`, no "Your file does not cover this variant." text; `details > summary` is "Filter reports" and visible; `getByLabel("Search reports by title, gene, or category")` has id `report-search` and type `search`; filling "cyp1a2" leaves exactly one `a[href^="/genome/me/reports/"]` and only the "Food, drink and metabolism" heading; clearing it restores the eight seeded headings. Every earlier assertion in the spec is unchanged. |
| `e2e/report-gate.spec.ts` | Strengthened gated-header assertion | The gated response must keep the report name and the provenance heading visible while withholding the result. | While gated: `main h1` is exactly "Alzheimer's disease" and a level-3 heading "Sources" is visible, in addition to the existing `/Alzheimer's disease/` heading, "Where this comes from" heading and `report-disclaimer` pins. Every gate, leak, support-panel and localStorage assertion is unchanged. |

| `e2e/response-headers.spec.ts` | New E2E coverage | The route register binds the `authenticatedUserData` header profile to every page or endpoint that can read or write user, subject, consent, chat, file or derived data; nothing asserted it before. | On the signed-out 307 redirect from a protected page, on signed-in product pages and on `/api/export`: `cache-control` is `private, no-store`, `cdn-cache-control` and `vercel-cdn-cache-control` are `no-store`, `pragma` is `no-cache`, `referrer-policy` is `same-origin`, `x-content-type-options` is `nosniff`, `content-security-policy` is `frame-ancestors 'none'` and `x-frame-options` is `DENY`; the public `/` carries no `cdn-cache-control` header. |
| `e2e/overview.spec.ts` | New E2E coverage | Pins the X9 Overview structure and the X6.1 density basis before the density gate exists; the previous `/overview` carried no assertions beyond the sign-in landing regex. | State A: `h1` "Overview" and exactly four headings in order; the State-A lede; the "Start here" strip with `/providers` as a plain link and no `/example/report` link while the route is absent; exactly one `[data-variant="default"]` ("I have a DNA file" → `/files/upload`); nine `[data-overview-box]` links with accessible names exactly Reports/Ancestry/Copilot/Individual risks/Portrait/Copilot/Upload/Compare your embryos/Copilot, each one link with a ≤12-word description, every target answering 2xx; zero `[data-figure-kind]`; ≤7 interactive elements in the first viewport at 1280×800 and 390×844 on the capture-harness basis (excluding `nav`, `a[href="#main"]`, `[data-copilot-entry]`); primary target ≥44px; Family `h2` below 800px and ≥96px gaps between `[data-density-top-level-section]` at 1280. Phone: one visible `nav[aria-label="App"]` with five 44px-tall labelled links (≥13px), one `aria-current="page"`, heavier active weight, no button/hamburger, content ending above the bar. State C (tiny VCF ingested): "Just you so far.", "No embryo files added.", `[data-metric-value]` equal to `seededTemplateCount()` as "{n} statistical estimates" with a 1–12-word sibling note and the definition sentence, no specific-variant half, the ancestry too-few line, exactly one primary ("Open my reports" → `/genome/me/reports`), the starter line "4 reports to read first…" with the four covered slugs in category-rank order, the not-diagnostic sentence, and ≤12 first-viewport interactives at both sizes. |
| `src/lib/primary-routes.test.ts` | Extended unit assertion | The global search builds settings hrefs from route ids, so the runtime mirror gains `settings.data`, `settings.copilot`, `settings.people` and `settings.consents`; the exact-id-set assertion must name them or fail. | `EXPECTED_IDS` lists the four new ids (the set equality still fails on any other addition or removal); each must exist in `docs/route-register.json#routes` with the same path; the byte-for-byte href check adds `/settings/data`, `/settings/copilot`, `/settings/people` and `/settings/consents`. Every earlier assertion is unchanged. |
| `e2e/search.spec.ts` | New E2E coverage | Pins the §2 §1.3 global search: one header button, the shortcut, the labelled modal dialog, the four groups in order with the subject chip on subject-derived rows, and destinations only. | On `/overview` and `/genome/me`: a header `button` named exactly "Search" outside every `nav`, ≥44px tall, `aria-keyshortcuts="Meta+K Control+K"`, a `kbd` hint reading `⌘K` or `Ctrl K`; `ControlOrMeta+k` and the button open `role="dialog"` named "Search Inherit" with `aria-modal="true"` and the input labelled "Find a person, a report or a page" focused; the empty sentence "Results show here as you type."; Escape hides the dialog and focuses the button. `caffeine`: `group` "Reports" containing `a[href="/genome/me/reports/caffeine-metabolism-cyp1a2-rs762551"]` with text "Caffeine metabolism" and `[data-slot="search-chip"]` "You"; the group label is a `p`; ArrowDown/ArrowUp move focus between the first link and the input; Enter on the link lands on the report and hides the dialog. `settings`: `group` "Settings" with `a[href="/settings"]` and no chip. The self subject's `display_label` (read from `subjects`): `group` "People and embryos" with `a[href="/genome/me"]` containing the label and the chip "You"; `zzqqxx`: `role="status"` with `aria-live="polite"` reading "No results for this search. Try another word." and zero groups. On every populated dialog: zero `[data-figure-kind]`, zero headings, no `%` and no `[ACGT]/[ACGT]` in the text, ≤4 group labels all from the mandated order and in that order, ≤8 links per group. `/api/search?q=caffeine` answers 401 signed out; signed in, 1–4 non-empty groups in the mandated order, 1–8 results each, every result exactly `{label, href}` or `{label, href, chip}`, no `%` or genotype pair in a label, every href root-relative, and the caffeine row chipped "You". |
| `e2e/report-skeleton.spec.ts` | Extended E2E assertion | Defect D-011: the six report sections sat 40px apart and the library's category sections 32px apart, below `docs/density-baseline.json`'s 96px minimum at ≥1024px. | At 1280×800, the gap (next top minus previous bottom, DOM order) between each adjacent `[data-slot="report-skeleton"] [data-density-top-level-section]` (exactly six) on the covered report, and between each adjacent `[data-library-layer="estimate"] [data-density-top-level-section]` (one per seeded category heading) on the list, is ≥95.5px. Every earlier assertion is unchanged. |
| `e2e/legal.spec.ts` | Strengthened copy assertion (D-016) and new route | The appeals sentence implied relative matching and shared-segment output exist; Inherit has neither (X15 declared gap). `/science` now publishes the two declared gaps and joins the placeholder-free, on-topic route table. | `/legal/appeals` must match `/turns off every page and result that others can see about that relative/i` and `/does not match relatives and does not compute shared DNA/i` (replacing `/turns off relative matching/i` and `/shared-segment output/i`); `/science` must render below 400, contain the heading "What Inherit does not do", `/does not match you with relatives/i`, `/does not work out how much DNA two people share/i` and `/does not offer prenatal or newborn screening/i`, contain no placeholder and exceed 800 characters. Every other assertion is unchanged. |
| `e2e/report-skeleton.spec.ts` | Extended list assertion (D-015) | The empty Medicines category was dropped from the DOM with no statement; X15 forbids a silent absence. | On the list, exactly one `[data-slot="category-absent"][data-category="medicines"]` with the exact text "Inherit has no reports about medicines. How a body handles a medicine depends on more than one DNA position. A report built from one position would say less than it seems to."; `#medicines` still has count 0. Every earlier assertion is unchanged. |
| `src/components/results/ancestry/ancestry.test.ts` | Extended unit assertion (D-019) | A haplogroup call is a quantity read against a reference tree, so G4.4 requires the tree and its version, an explicit statement that no range exists, and the resolution limit in plain words; the card rendered only the matched-of-tested coverage figure. | The mother's-line card's markup contains `treeLine(LINEAGE_TREES.mother)`, `LINEAGE_NO_RANGE` and `LINEAGE_RESOLUTION_LIMIT` in addition to every earlier assertion. |
| `e2e/family.spec.ts` | New E2E coverage | The Family surfaces had no browser coverage: `/family` was a marketing page and the four app routes rendered a jurisdiction placeholder. | Signed out, `/family` keeps the "Not available in any production jurisdiction yet" panel and the "If a child is born from this" panel in the server-rendered HTML with the Future Person Charter link, and renders no `[data-figure-kind]` or `[data-claim-block]`. Signed in: `h1` "Family", ≤6 headings, exactly one `main [data-variant="default"]` reading "Add another adult" → `/family/invite`, every rendered `main a[href^="/"]` answering 200, each of the three tiles either linking or carrying a non-empty `[data-slot="tile-blocked"]` sentence, "Just you so far." with nobody, ≤12 first-viewport interactives at 1280×800 and 390×844 on the capture-harness basis, and zero serious or critical axe violations in both themes. `/family/invite`: `[data-slot="pre-consent-statement"]` has the exact brief §5 §5.2 sentence, sits above the `form` and inside no `details`; the "Invite them." heading and the "A note for them" field are present; "They can’t use Inherit themselves" has count 0. The flow: A invites B with a note, the mail carries the note as words and not as an href, B accepts through their own account, ingests the tiny GRCh38 fixture onto their own self subject, and turns on "Statistical estimates" from their own session — after which exactly one live `purpose_grants` row exists for B's subject with purpose `reports.polygenic`, the opposite column carries `data-settable="false"`, five `Only {name} can turn this on.` sentences and zero controls. A's card then reads "Reports ready" with no file count; `/family/s-{invited}` shows the exact Tier-2 checkbox and session sentence while `page.content()` contains no `data-figure-kind`, no `data-claim-block` and none of the four covered slugs, and neither `localStorage` nor `sessionStorage` mentions the gate; after ticking it, the four covered report links, the exact "has not shared Specific variants with you." sentence, the baseline-absence sentence exactly once, the "Shared with you" chip and the `Family / {name}` breadcrumb; opening a report gives one `[data-claim-block]` attributed to B's own self subject id and none attributed to the handle. Pause makes the card read "Sharing paused", the person page render the paused sentence with no figures and `/genome/s-{invited}/reports` answer 404 on the very next request; resume restores it; stop names B in its dialog, lists the three deleted kinds, writes the tombstone matching `/^Sharing ended on .+\. \d+ results built from this pairing were deleted\./`, leaves the person page with the nothing-shared sentence and zero figures, answers 404 for the reports list and the report page, and leaves zero unrevoked `purpose_grants` rows for B's subject. |
| `e2e/adult-subject-invitation.spec.ts` | Strengthened invite-screen assertion | `/family/invite` now carries the brief §5 §5.2 pre-consent statement, which nothing asserted. | Before anything is entered, `[data-slot="pre-consent-statement"]` must have the exact text "Comparing two people’s DNA can show that they are related, or not related, in ways neither expected. Inherit cannot un-see this.", must not sit inside any `details`, and must render above the form's bounding box. Every earlier assertion (the mail, the opaque token's absence from the durable payload, the acceptance binding, the inviter's zero directional grants and the record's zero files) is unchanged. |
| `src/lib/family/{graph,access,tier2,grant-token}.test.ts` | New unit coverage | The Family people graph, the jurisdiction-and-consent access rules, the Tier-2 session memory and the single-use presentation token are all new and decide what one adult may see about another. | Graph: the inviter resolves the invited record as a handle over the invitee's own self subject and the invitee resolves the inviter's self subject, each with `s-{uuid}` and never `me`; a `minor` is excluded from every source; a pause is a flag that deletes no grant row; the two grant directions stay separate sets; a counterpart reachable only through a live grant still resolves; nobody is ordered by anything but name; the self placeholder "You" is never printed as another person's name; the eight directional purposes are exactly those the grant transaction accepts. Access: TEST-LOCAL only under the flag, an unset jurisdiction as `unreviewed` with non-empty copy, a permitted actor blocked by an unreviewed contributor (G5.1b), a pause emptying the live set without changing the rows, each layer mapping to one purpose, and the outbound direction never reading as permission to see. Tier-2: a session cookie with no `Max-Age`, `httpOnly`, `Secure`, `SameSite=Lax`; a keyed digest that changes with the account and with the session and contains neither; equality only on the exact digest; the session id read from the access token or nothing. Token: round-trips exactly the endpoints it was minted for with a fresh 16–256-character whitespace-free nonce; refuses a tampered payload, a foreign signature, a bare payload and a stale token; and the sharing-operation envelope and the grant envelope never verify as each other. |
| `src/copy/family/family.test.ts` | New unit coverage | Every Family string is new, and the mandated ones must ship character-for-character. | The brief's quoted strings byte-for-byte ("Add another adult", "Just you so far.", the three card lines, "If a child is born from this", the Tier-2 checkbox and session sentence, "Show what’s shared", the no-file sentence, the baseline-absence sentence, the Copilot local-only sentence, both column headings, "Only {name} can turn this on.", the asymmetry line, "Pause sharing", "Stop sharing", the tombstone, the stop-dialog sentence, the pre-consent statement, "Invite them.", the Path B link text and "Invite another adult"); shared strings read from their one home (the nav label, `STATE_C.justYou`, `NOT_DIAGNOSTIC`, the Overview box label, `LAYER_LABELS`, `KIND_CHIPS.adult_shared`); the five permission rows in order with one-sentence consequences and the three state words; `PATH_B_AVAILABLE` false; typographic apostrophes everywhere; no sentence over 25 words and grade ≤ 9 for every block of 15+ words, graded with registered terms replaced as the gate does; every short-role string built from `data/plain-vocabulary.json`; and no registered term in a heading, button or label outside four named exemptions. |
| `src/components/family/family.test.ts` | New unit coverage | The Family components render another adult's identity and the controls that share their data. | `renderToStaticMarkup`: a person card carries the disc, the name, the "Shared with you" chip, one `[data-slot="person-state"]` line and no file count; the four card states map to their exact strings; a list of two people carries no `aria-sort`, no ranking word and no `[data-figure-kind]`; the result gate renders the exact checkbox, the session sentence and one action, nothing pre-ticked and no claim block or figure; a permission column renders the five rows all off with no master switch, and the column this session may not set carries `data-settable="false"`, five "Only {name} can turn this on." sentences and zero controls; a row shows a glyph plus one of On/Off/Expired with an accessible control name and no colour-only state; the sharing actions offer pause and stop, resume only while paused, and open no dialog until the destructive action is chosen; and another account's own `self` record is never chipped "You". |
| `src/components/subjects/subject-bar.test.ts` | Extended unit assertion | The subject bar now takes `fileCount: number \| null` (a Family record withholds the count until a report layer is live) and `subjectKind` had to learn that another account's `self` record is shared with the viewer, not the viewer's own. | New: `subjectKind(selfRecord, otherAccount)` is `adult_shared` while `subjectKind(selfRecord, ownAccount)` stays `self`; `fileCount: null` renders no `[data-slot="subject-files"]` while the chip still reads "Shared with you". Every earlier assertion is unchanged. |
| `scripts/eslint/no-raw-figure.test.ts` | Strengthened glob assertion | The Family domain landing moved into its own route group and the Family components are new, so both join the result surfaces the figure-contract rule covers; a count check would have passed a wrong list. | The former "expected five result-surface globs" length check is replaced by an exact ordered list of the seven globs, which fails on any addition, removal or reordering. Every rule case in the spec is unchanged. |
| `src/lib/primary-routes.test.ts` | Extended unit assertion | The Family surfaces link to the invite screen, the permissions screen, the Future Person Charter and the where-Inherit-works page, and the register's binding rule forbids a spelled path. | `EXPECTED_IDS` gains `family.invite`, `family.permissions`, `legal.future-person` and `legal.where-inherit-works` (the set equality still fails on any other addition or removal), and each must exist in `docs/route-register.json#routes` with the same path. Every earlier assertion is unchanged. |
No pre-existing E2E test has been weakened, deleted, skipped, or quarantined in
this change set. Existing design-token assertions are unchanged.
| `e2e/upload-vcf.spec.ts` | Strengthened copy assertion | The ancestry page's grey state now renders the mandated §4.6 sentence instead of the former three-clause empty state; the three regex pins that matched the old wording are replaced by the exact sentence. | `[data-slot="grey-state"]` inside `admixture` has text matching `/^Your file covers only \d+ of \d+ ancestry markers — too few to draw a map\. This is a limit of the file, not a result about you\.$/` (the counts are measured from the GIAB subset, never fixed). Every other assertion in the spec — the mtDNA and Y notes, the XX gloss, the hidden list, the `Show the unreliable raw numbers anyway` summary, `/proportions are unreliable/` and the five list items — is unchanged. |
| `e2e/ancestry.spec.ts` | New E2E coverage | Pins the ancestry surface (brief §4.6, A.8, G4.4, X16.5; acceptance 30–34) against the tiny GRCh38 fixture (grey state) and the synthetic 168-marker fixture `e2e/fixtures/aims-mixed-grch38.vcf` (shown state). | Grey state: the exact sentence `Your file covers only 0 of {panel size} ancestry markers — too few to draw a map. This is a limit of the file, not a result about you.` (the panel size read from `data/ref/aims.json`), no `[data-slot="ancestry-chip"]`, no `[data-slot="well-supported-toggle"]`, no visible text node containing `%` outside `details`, the mtDNA and Y empty-state notes, `#neanderthal` present, `archaic-hominin` absent from `page.content()`, zero serious or critical axe violations in both themes. Shown state: every `[data-figure-kind="ancestry-share"]` has a `figure-value` matching `/^\d+\.\d%$/` and a `figure-unit` matching `/^\(\d+\.\d–\d+\.\d%\)$/` or equal to `no range yet`, and no other text node matches `/\d+(\.\d+)?%/`; visible `region-row` values + both chip values = 100.0 ± 0.1 with the toggle on, then off, both chips present in both states; toggling changes the count of visible rows and of focusable paths; Tab order over the paths equals descending share; clicking a path opens `[data-slot="region-panel"]` naming that region with focus on `Close`, Escape closes it and returns focus to the path; a background click closes it; no region name, path `aria-label`, chip or figcaption contains a word from `data/ref/regions/label-denylist.json` (loaded from the file); every filled path references a `radialGradient` whose last stop has `stop-opacity="0"` and whose feather is ≥ 15% of the path's `getBBox()` width; ≤ 12 interactive elements in the first 800px at 1280×800 on the capture-harness basis; only first-party origins requested and no request to `/geo/`. |
| `src/components/figures/figures.test.ts`, `src/lib/figures/claim-block.test.ts` | Extended unit assertion | The figure contract gained the ancestry-share range disjunction (`{ low, high }` or `{ unavailable: true }`), one-decimal shares, body size without a range and `ClaimBlock.renderFigures` (W7 part A). | An ancestry share with a range still renders its mandatory range (the `@ts-expect-error` on a spec without `range` holds); `no range yet` renders as the unit at body size when the range is unavailable; `renderFigures` receives one node per spec in order and the modelled marker still renders once; the claim-block row expects `43.2%` and `(38.0–48.0%)`, plus a row for `{ unavailable: true }` → `no range yet`. |
| `src/lib/ancestry/present.test.ts`, `src/lib/ancestry/regions.test.ts`, `src/lib/geo/project.test.ts`, `src/lib/geo/topojson.test.ts`, `src/components/results/ancestry/ancestry.test.ts` | New unit coverage | Pins the ancestry arithmetic, the region set, the map geometry and the regions section's markup before any browser test runs. | Bands half-open at 0.02/0.10/0.30 and never changed by display rounding; `apportionShares` over 1,000 seeded simplex vectors sums to exactly 100.0, is stable across toggle states, within 0.1 of naive rounding, zero stays 0.0; shown + u + h = 100.0 in both states and u = 0.0 for every estimator output; `lowerBound` falls back to the point; `fillOpacity` 0.005 → 0.15, 0 → hairline; the A.8 accessible-name forms; `POPS` ↔ regions is a bijection, codes match the `ref_regions` regex, `min_markers = ceil(RELIABLE_FRACTION × AIMS.length)`, `n_total = Σ n` with the phase 3 totals, no display name or accessible name matches the denylist and the denylist names no allowed external organisation; the projection's corners and two sampled cities land inside the viewBox; the in-repo TopoJSON reader equals `topojson-client` on the committed file, which holds exactly `land` plus the five region objects within 180 KB; the regions section renders one `[data-claim-block][data-subject-id]`, every ancestry share with its unit and the marker once, five focusable paths in descending order each with a gradient ending at `stop-opacity="0"` and `data-fill-opacity ≥ 0.15`, both chips in both toggle states summing with the visible rows to 100.0, a dashed hairline for a share of 0, and a grey state with no `%` outside the disclosure and exactly one list; the lineage and Neanderthal cards render their mandated sentences and never `archaic-hominin`. |
| `e2e/upload-vcf.spec.ts` | Retargeted and extended E2E assertion | The genome browser's genotype is now an observed `genotype` figure inside one claim block per results table (W8), so the former loose-text regex would match nothing; the block and the four-level breadcrumb are pinned beside it. | The region query asserts exactly one `[data-claim-block][data-subject-id]` wrapping exactly one `table` and the breadcrumb `My Genome / {name} / Data / Genome browser` with the name read from the subject bar; the rsID re-search reads `/^[ACGT](\/[ACGT])?$/` from `[data-figure-kind="genotype"] [data-slot="figure-value"]` in the first row (visible and matching) instead of from any text node. The upload, processing, canvas, first-party sentence, gene-search and ancestry assertions are unchanged. |
| `e2e/genome-data.spec.ts` | New E2E coverage | Pins the expert path (brief §7.3, §1.4–§1.6, §2.2, X4, X6.1, X13) on the tiny GRCh38 fixture: the two pages' structure, the figure contract on every genotype, the search states and the three entry points. | `?q=rs762551`: `main h1` count 1 reading "Genome browser"; breadcrumb `My Genome / {name} / Data / Genome browser` with links `/genome/me` and `/genome/me/data`; one `[data-subject-bar][data-subject-id]`; zero `main .eyebrow`; exactly one `[data-claim-block][data-subject-id]` wrapping one `table`; `#results` present; `table th` reads Variant / Position / Gene / Your two letters; zero `table [title]`; exactly one `[data-figure-kind]`, being `genotype` with `data-figure-class="variant-call"`, `data-figure-basis="observed"`, `data-provenance="computed:genome/browser"` and value `A/C`; zero percentile and absolute figures; one row whose first cell is `rs762551` and which contains `chr15:74749576` and `CYP1A2`; `main :is(h1,…,h6)` reads exactly Genome browser / Results / Region; the region section contains `chr15:74744576-74754576` and the exact first-party sentence; the track canvas appears within 60 s; exactly one `[data-variant="default"]` reading "Search"; ≤ 12 first-viewport interactives at 1280×800 on the capture-harness basis after the canvas is ready. `?q=CYP1A2`: one block; the `rs762551` row's figure value `A/C` with `chr15:74749576` and no not-covered text; every other row contains "Not covered by your file" and no figure; one figure on the page; zero `table [title]`. `?q=BRCA1`: `main [role="status"]` contains "Inherit’s reference has no clinical variants for BRCA1."; no `#results`, no figure. `?q=caffeine`: exactly one `main a[href="/genome/me/reports/caffeine-metabolism-cyp1a2-rs762551"]` reading "Caffeine metabolism"; link "Browse the full report library" → `/genome/me/reports`; no figure. `?q=zzz`: text matching `/^No reference variants known for “zzz”\./`; link "Or start from your reports." → `/genome/me/reports`; no `#results`, no figure. `/genome/me/data`: `main h1` "Data and methods"; breadcrumb `My Genome / {name} / Data` with the hub link; one outline `a[data-slot="button"]` each to `/genome/me/data/browser` and `/files`; zero default-variant buttons; `[data-figure-kind="coverage"]` count equals the score-panel `li` count and both equal the `[data-claim-block][data-subject-id]` count inside the list, all > 0; no list item matches `/\d%/`; zero percentile figures. `/settings` and `/genome/me/ancestry`: link "Data and methods" → `/genome/me/data`. |
| `e2e/helpers.ts`, `e2e/overview.spec.ts` | Helper lifted | `firstViewportInteractives` moves from `e2e/overview.spec.ts` into `e2e/helpers.ts` so `genome-data.spec.ts` pins the X6.1 budget on the same capture-harness basis. | The function body is unchanged and exported; `overview.spec.ts` imports it. Every overview assertion is unchanged. |
| `src/lib/genome/search-guidance.test.ts` | Moved unit coverage (from `src/components/browse/`), one assertion retired with its constant, two added | The guidance module moves with the browser rewrite; its hard-coded report titles (one already stale against `data/templates`) are dropped in favour of titles read from `report_templates` at render, and the three example-query chips it fed are removed by decision, so `SEARCH_EXAMPLES` no longer exists. | Every keyword → slug, partial-typing, case and null case is unchanged (`slugs` replaces `reports[].slug`; `"zzz"` joins the null cases); new: every suggested slug exists in `data/templates` (read from disk, so a retired template fails the suite) and every topic id has a plain-English label in `src/copy/genome/data.ts`. The `SEARCH_EXAMPLES` length assertion is retired with the constant. |
| `src/copy/genome/data.test.ts`, `src/lib/genome/locus.test.ts` | New unit coverage | The expert-path copy and the locus parser were inline in the browser page and unchecked; the parser's chromosome class carried a stray space. | Copy: `DATA_H1` equals `DATA_AND_METHODS`; the browser headings, search label and button, `Positions are on GRCh38.` and the first-party sentence character-for-character; the search-state builders (`Results for rs762551`, `Your file does not cover rs762551 (CYP1A2).`, the clinical-gene first sentence, `Looking for caffeine? These reports cover it.`); the track control names distinct; every heading, label and button word in `data/plain-vocabulary.json` and none of the five forbidden terms; typographic apostrophes, no sentence over 25 words, grade ≤ 9 for every string of 15+ words. Locus: a single position centres a 10 kb window; ranges with or without `chr`, with grouping commas, reversed or starting at 0; X, Y, MT and M map to 23, 24, 25; `chr99`, `chrT` and `chr1X` report an unknown chromosome; `chr M` and `M T` (the former stray-space matches) are not loci; rsIDs, gene symbols and words are not loci; `locusAround`, `locusSpanning` and the ungrouped `formatLocus` output. |
| `e2e/report-skeleton.spec.ts` | Re-pinned copy assertion, one assertion added | `MEDICINES_ABSENT` changed from "Inherit has no reports about medicines." to "Inherit does not offer reports about medicines." when pharmacogenomics became a `withheld` capability (ADR 0018, `docs/withheld/pharmacogenomics.md`): a withholding states that the capability is not offered, and "has no reports" reads as an inventory gap that will fill. | The absence paragraph is still asserted character-for-character, now against the new sentence, and a new assertion requires it to contain none of "coming soon", "soon", "yet" or "currently" — the four words §8 forbids in a withheld capability’s UI state where the testable condition depends on outside parties. Every other assertion in the spec is unchanged: the nine-category order with Medicines absent, `#medicines` at count 0, exactly one `[data-slot="category-absent"]` paragraph. |
| `scripts/validate-templates.test.ts` | New unit coverage | `scripts/validate-templates.ts` had no test of its own, and this work added the §6.4 blocklist rows (`\bdosage\b`, `\bsupplement\b`, `we recommend you take`) to `BANNED_PATTERNS` under the label `treatment advice (§6.4)`. | `bannedLanguage` returns the §6.4 label for each of the three phrases and for a mixed-case match; matches on word boundaries only, so `supplementary`, `dosages` and "we recommend you talk" are not findings; the five pre-existing rows still fire and a template tripping several reports them in pattern order; `BANNED_PATTERNS` carries exactly six labels with the §6.4 row last. |
| `src/lib/genome/pharmacogenomics-withheld.test.ts` | New unit coverage | Pins the structural failures the withheld dossier cites (`docs/withheld/pharmacogenomics.md` element 4), reading the three built designs from `docs/withheld/pharmacogenomics/designs/` through `import.meta.url` so the evidence and the tests cannot drift apart. | The design-3 TPMT diplotype caller as a pure function; `indeterminate` for `*1/*3A` versus `*3B/*3C` on identical unphased input; `bad ref/alt` on rs1142345’s two alt alleles with the two impossible genotype keys the validator would derive; the seed’s `refVariants` rule dropping the second alt silently; `resolveVariant` unrecognized for `T/G` and `C/G`; every hemizygous chromosome-23 call unrecognized, the reference base included; `categoryFor` throwing on the design-2 slug and landing every re-slugged template in `food-drink-metabolism`; and no percent sign or numeric multiplier in any design. Every assertion names its rule. |
| `e2e/adult-subject-invitation.spec.ts` | Scoped locator | The F1 assertion that the pre-consent statement sits above the form located `form` unscoped and matched the app shell's sign-out form as well (D-024). | The form's bounding box is read from the `form` containing the "Their email address" field; the ordering assertion and everything after it are unchanged. |
| `e2e/family.spec.ts` | Helper corrected; two assertions added | `expectAxeClean` audited a page whose theme had just been flipped in place (D-025); and the Portrait row's independent-login precondition was untested (D-023). | Each theme is audited after `page.reload()`; in the invitee's acceptance session the settable column's Portrait row renders `[data-slot="permission-locked"]` with the exact sentence and no control while `subjects.independent_login_at` is null; after sign-out and a fresh sign-in the row is settable, "Turn on" answers "On", the marker is set, and the live grants are exactly `family.portrait` and `reports.polygenic`. |
| `src/components/family/family.test.ts` | New unit assertion | `<PermissionColumn>` gained a per-row `lockedReason` for the settable column (the Portrait row before the marker). | A settable column with one locked row renders `data-settable="true"`, four controls, one `permission-locked` slot carrying `INDEPENDENT_LOGIN_REQUIRED`, and all five states. |
| `e2e/family-health-picture.spec.ts` | New E2E coverage | `/family/health-picture` rendered a jurisdiction placeholder and had no browser coverage, and the carrier rule had no end-to-end proof at all. | Two accounts ingest `e2e/fixtures/carrier-pair-grch38.vcf`, four synthetic classified positions are inserted through the admin client and removed in `afterAll`, and both accounts turn on `family.heritability` toward each other through `grant_directional_purpose_v1`. The page then pins: `h1` "Family health picture", breadcrumbs `Family / Family health picture`, ≤6 headings; the Tier-2 gate first, with `page.content()` carrying no `data-figure-kind`, no `data-claim-block`, no `data-compare-surface`, no `25 in 100` and none of the four gene names; after the exact checkbox, `[data-slot="comparison-banner"]` equal to the brief's banner and inside no `details`; exactly one `[data-trade-off-panel]`, outside any `details`, carrying "Inherit does not rank embryos and does not recommend one.", the joint-selection sentence, the availability sentence and one `[data-slot="trade-off-row"]` per column; one `[data-compare-surface]` per `data-layer` with one `<caption>` each; every `[data-slot="health-picture-cell"] [data-claim-block]` carrying exactly one `data-subject-id` and no `data-subject-pair`, over exactly the two data-subject ids; zero `[aria-sort]`, zero `th button`, zero buttons inside a compare surface; both `[data-slot="column-footer"]` equal to the baseline-absence sentence; and zero matches for `/centimorgan|\bcM\b|kinship|shared DNA|related to/i` in `page.content()` (acceptance 20). The carrier panel: four blocks, all attributed to the one `data-subject-pair`; exactly one carrying `[data-figure-basis="exact"]`, whose `[data-slot="carrier-sentence"]` equals the brief's 25-in-100 sentence and whose single `[data-exact-marker]` equals the exactness label; exactly one block in the whole page containing `25 in 100` and exactly one containing `1 in 4` (acceptance 17); the other three carrying the no-probability sentence with their named reason, no exact figure and no marker; zero `[data-modelled-marker]`. Budgets: ≤24 first-viewport interactives at 1280×800 (acceptance 33) and zero serious or critical axe violations in both themes (acceptance 34). The Overview then shows one `[data-subject-pair]` line whose link reads "1 carrier match to look at" and points at `/family/health-picture#carrier-matches`, with no `[data-figure-kind]` and no value. After `revoke_directional_purpose_v1` on one direction, the next request renders the two-people sentence, zero carrier panels, zero compare surfaces, zero claim blocks and no `[data-subject-pair]` on the Overview. |
| `src/lib/family/{carrier-pair,roh}.test.ts` | New unit coverage | The carrier trigger rule and the runs-of-homozygosity measure are new, and they decide whether one number about two people's children is printed at all. | Carrier: both files reading one changed copy of a pathogenic or likely pathogenic autosomal-recessive change, with both files below the runs thresholds, gives exactly `0.25` and nothing else; each of the seven named reasons is produced by its own case (dominant pattern, harmless classification, uncertain and conflicting classifications, a no-call and a haploid call, a null and an `other` inheritance mode and a missing registry row, an X-linked pattern, a file above the threshold and a file that could not be measured); every refusal sentence contains no digit and no `0%`; a position with no classification, a position only one file reports, a file showing no changed copy, a person with two changed copies, a position neither file could read and a reference row with no gene name are all not candidates; `countCarrierMatches` counts only the probability branch; matches are ordered by position alone. Runs: a 120 Mb run of same-reading calls is above the total the brief states; a short run inside a wide span is below both; a file whose rows are all differences is `not_measurable`; only autosomes count and the answer does not depend on row order; and two files' measures are only ever asked separately. |
| `src/copy/family/health-picture.test.ts` | New unit coverage | Every string of the side-by-side surface is new, and the mandated ones must ship character-for-character. | The brief's quoted strings byte-for-byte (the comparison banner, the no-ranking statement, the joint-selection sentence, the availability sentence, the per-column line, the two-people sentence, the empty-panel sentence, the not-in-file cell and the baseline-absence sentence); the 25-in-100 sentence reassembled around `naturalFrequency(0.25, 100)` so the copy and the rendered figure cannot drift; the reason table's keys equal to `CARRIER_REASONS` with all six design phrases pinned and every refusal sentence free of digits; shared strings read from their one home (the two last report headings, the layer labels and definitions, the not-diagnostic, no-range, provenance and counsellor lines, and the person page's baseline sentence); no relatedness word anywhere in the corpus; typographic apostrophes only; every sentence ≤25 words and every block of 15+ words at grade ≤9 with registered terms replaced as the gate replaces them; every short-role string built from `data/plain-vocabulary.json`; and no registered term in a heading, button or label outside one named exemption. |
| `src/components/family/health-picture.test.ts` | New unit coverage | The five components of the side-by-side surface render another adult's genotypes and the one Mendelian fraction Inherit prints. | `renderToStaticMarkup`: the table carries `data-compare-surface` and `data-card`, one claim block per cell each naming one subject and no pair, no `aria-sort`, no `<th>` button, no button at all, no ranking word, one `<caption>`, the absence sentence in both column footers, observed `genotype` figures with provenance `computed:genome/reports`, the not-covered word, and one link only for the column whose report permission is live; a cell with no file and a cell whose files disagree render the word and zero figures; the pair block carries exactly one `data-subject-pair`, no `data-subject-id`, both subject chips, two `carrier-status` figures, an `exact` figure with provenance `computed:family/carrier-pair`, exactly one exact marker and no modelled marker, and a `[data-slot="carrier-sentence"]` whose text equals the brief's sentence; the no-probability block's sentence equals the reason sentence and carries no `25 in 100`, no `1 in 4`, no exact figure and no marker; the panel states what it checked when a pair shares nothing and renders one block per match otherwise; the trade-off panel states the three mandated lines, adds no heading, collapses nothing and carries no figure. |
| `src/lib/primary-routes.test.ts` | Extended unit assertion | `/family/health-picture` is now a destination (the Overview carrier line links to it), and the register's binding rule forbids a spelled path. | `EXPECTED_IDS` gains `family.health-picture` (the set equality still fails on any other addition or removal), and it must exist in `docs/route-register.json#routes` at the same path. Every earlier assertion is unchanged. |
| `src/components/family/family.test.ts` | Re-pinned assertion | The permission control's accessible name now starts with its visible verb (D-026, WCAG 2.5.3). | A revoke control renders `aria-label="Turn off Ancestry for Bo"`; the glyph, state word and no-colour assertions are unchanged. |
| `src/lib/family/tier2.test.ts` | Corrected assertion | The tamper check could produce the real digest when it ended in `0` (D-027). | The tampered cookie's last character is chosen to differ from the real one; the assertion is otherwise unchanged. |
| `supabase/tests/family_sharing.sql` | New assertions (4) | D-028: the independent-login marker is a per-account rule. | The session an invitation was accepted in returns 0 and leaves the account's self record unstamped; a session opened after the acceptance returns 1 and sets it. plan(81) → plan(85). |
| `e2e/family.spec.ts`, `src/components/family/family.test.ts`, `src/copy/family/family.test.ts` | Re-pinned assertions | The permissions page gains a sixth row, "Health picture" (`family.heritability`, ADR 0017, D-029). | Row counts 5 → 6; the ids and labels list the sixth row; the row is locked with the Portrait row while the independent-login marker is unset. |
| `e2e/family.spec.ts` | Re-pinned locators | D-041: rows are selected by their exact label (`[data-slot="permission-label"]`), never by a substring of the whole row. | The Portrait and estimates rows resolve to one element each with six rows present; the lock-reason assertion is unchanged. |
| `e2e/portrait.spec.ts` | New E2E coverage | `/family/portrait/[pairId]` rendered a jurisdiction placeholder and had no browser coverage; the brief names a `portrait` suite (line 2308) and G5.9 its specific harms. | Two accounts paired without an invitation through `grant_directional_purpose_v1`, the marker through `mark_independent_login_v1`, both files the synthetic carrier-pair fixture: with only A's grant the blocking screen carries `data-state="consent-required"`, the heading "Portrait is waiting for you and Another adult", the register's body, three server-derived step lines (the viewer's in the second person), the consents link to `/settings/consents`, the banner pair verbatim and inside no `details`, both chips with no file count, zero `[data-figure-kind]`, zero `[data-claim-block]`, zero `main img, main canvas, main svg[role=img]`, no gene name and no `25 in 100` in `page.content()`; A's acknowledgement through the real checkbox stamps `subjects.portrait_acknowledged_at` for A's subject only, a POST naming B's subject is refused and B's stays null, and the refreshed screen drops A's line and offers no checkbox; after B's grant and `acknowledge_portrait_v1` the pair is `current` and A meets the Tier-2 gate with no figure, claim block, dot, gene or header sentence in the HTML and nothing in device storage; the page proper: the header sentence and the principle verbatim, ≤6 headings, one `[data-slot="portrait-output"]` and one pair-attributed `[data-claim-block]` per carried gene and none for the two uncarried, `[data-slot="segregation"]` once per block, `[data-slot="chance-not-prediction"]` and one `[data-slot="how-sure"]` in every block and none inside `details`, exactly one `[data-exact-marker]` in the `carrier-pair` block with the derivation "1 in 4 (25%) affected · 2 in 4 (50%) carriers · 1 in 4 (25%) neither", three `[data-figure-basis="exact"]`, 100 `[data-slot="outcome-dot"]` split 25/50/25 by `data-outcome`, three bar segments, three legend items with their words, the three mandated sentences, a figcaption, the `details[data-slot="outcome-table"]` with its summary and table, the covered-against-known line and both variant lines; only one block contains `25 in 100` or `1 in 4` and no `[data-modelled-marker]` exists; every refused match carries the side-by-side page's sentence with its reason, zero exact figures, zero dots, two status figures and no pregnancy split; `main` text has no `0%`, no `0 in 100` and no relatedness word; five `[data-slot="trait-card"]` each with the unregistered sentence and no figure; the delete button visible with no dialog; the refusals in `page.request.get` HTML with `id="not-shown"`, every `data-refusal-id` of the registry (≥8), no figure or claim block, no percentage or natural frequency in the section text, the link to `/science`, and no singular-child phrasing in the HTML or in any non-test file under `src/`; `[data-finding]` texts byte-equal between A's and B's sessions and naming nobody; ≤12 first-viewport interactives at both viewports, zero `main [data-variant="default"]`, axe serious/critical zero in both themes after `page.reload()`; with every classification removed `[data-slot="portrait-empty"]` reads the no-classified-positions sentence with zero blocks while the trait cards, the refusals and the banner remain; B's "Delete Portrait" → "Delete this Portrait" revokes only B's `family.portrait` grant, leaves A's live, returns the pair to `pending`, leaves zero `portrait_results`, and both B's and A's next request is the blocking screen naming the grant step with zero blocks and zero figures. |
| `src/lib/family/portrait.test.ts` | New unit coverage | The pair rule and the one-sided readings are new pure functions; every branch of the blocking screen must be proved without a database. | `evaluatePortraitPreconditions` answers `ok` only with every step done for both people from either account; `not-authorised` for a viewer holding neither account, a revoked or purged pair, a minor or purged record; `paused` before any step; the missing steps per person in pair order and step order (account alone for a person with none; independent login, own-session grant, acknowledgement otherwise); a grant counts only when signed by the person's own account toward the other, and a `current` pair status stands for nothing; `pairSides` and `ownPortraitGrantId` find the viewer's own side and only the viewer's own grant. `evaluateOneSided` yields `no-second-copy` with the known/covered counts where the other file reports a known position and shows no change, `not-covered` with the carrier's rsid where it reports none, from either side; nothing for a gene the carrier rule answered, for two carriers or none, for a dominant or unrecorded pattern, for two copies or an uncountable reading; `geneCoverage` counts pathogenic positions only, case-insensitively; no reading carries a probability or `0%`. |
| `src/components/family/portrait/portrait.test.ts` | New unit coverage | The Portrait components render the highest-risk surface's markup; G5.9 and lines 358–364 fix its shape. | `renderToStaticMarkup`: the banner's two sentences in a section with no `details` and no button; the pair bar with both subject ids, both chips, no file count, no link and no pair attribution; the blocking screen's heading, body, step lines (third person for the other, second for the viewer), consents link, the acknowledgement card only for the viewer's own step, unticked, with exactly one primary; the recessive card as one pair-attributed block with both chips, the exactness label once and no modelled marker, the derivation string, three exact natural-frequency figures at 100, the cross from one copy each, 100 `outcome-dot` spans, no `img`/`canvas`/`svg[role=img]`/`role="img"`, three bar segments, the three treatments (26 solid, 51 half, 26 empty counting the legend swatches) with solid, dashed and dotted borders, the legend words, the three mandated sentences, a figure with figcaption, the table fallback in `details` with three count cells, the covered-against-known line, the segregation sentence once, the chance line, the counsellor line, both variant lines, "How sure we are" with its four fields and no X-linked assumption, no heading, no `0%`, no `0 in 100`, no singular-child or relatedness phrasing; every refused reason renders the side-by-side sentence, two status figures, no exact basis, marker, fraction or dot, and the runs reason the brief's sentence; the one-sided card the no-second-copy or cannot-calculate sentence, the reading in words, no natural frequency, no dots, no `0%`; every trait card the unregistered sentence with no figure, claim block or digit in its text; the refusals list eleven `data-refusal-id` cards (≥8) with every line and reason, one link, no details, no figure and no percentage or natural frequency; the delete control one destructive button and no dialog until asked. |
| `src/copy/family/portrait.test.ts` | New unit coverage | Every Portrait string is new or newly rendered, and the mandated ones must ship character-for-character. | The brief's quoted strings byte-for-byte (the banner pair, the header sentence, the both-genomes state, the principle, the segregation sentence, the chance line, the exactness label equal to `EXACT_MARKER`, the derivation, the distribution and sub-1% sentences, the X-linked sentence, the no-second-copy sentence, the runs refusal, the cannot-calculate sentence, the two RhD sentences, the chromosomal-sex expectation, the refusals heading and its §4 prefix, "See these numbers as a table", the intelligence reason); the blocking heading, names phrase, step lines, action and checkbox; the known-changes line in the plural and the singular; every outcome, pattern, assumption, reason and trait key named and no other; eleven refusals with unique ids and one-sentence reasons; no monogenic zero in any string and the derivation naming only outcomes that occur; no singular-child, "the two of you" or relatedness phrasing; shared strings read from their one home; typographic apostrophes; ≤25 words per sentence and grade ≤9 for every non-mandated block; every short role in registered plain words with no registered term. |
| `src/lib/family/carrier-pair.ts` (`CarrierPairSummary.genotypes`) | Interface extended, assertions unchanged | Portrait's one-sided readings decide over the same genotype maps the carrier rule read, so the rule's summary now returns them rather than the page reading the files twice. | The existing carrier-pair suite is unchanged; `resolveCarrierPair` returns empty maps when nothing was read and the maps it read otherwise. |
| `src/components/family/health-picture-table.tsx` (`HealthPictureColumn.files: number \| null`) | Interface widened, assertions unchanged | The Portrait pair bar reuses `<SubjectChip>` and must render no file count before any grant is live. | A null count renders no `[data-slot="subject-files"]`; every existing health-picture assertion passes a number and is unchanged. |
