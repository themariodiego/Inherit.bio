You are building **part B of W7, the Ancestry surface** of Inherit (repo at /home/user/Inherit.bio, Next.js 16.3 App Router, React 19, TypeScript strict, Tailwind v4, Supabase). Part A (already in the tree, uncommitted or committed — check `git status` and `git log -3`) built the foundations: `scripts/build-region-geometry.ts`, `public/geo/regions.topo.json` + `GEOMETRY_PROVENANCE.md`, `src/lib/geo/{topojson,project}.ts`, `data/ref/regions/{regions.json,label-denylist.json,PROVENANCE.md}`, `src/lib/ancestry/{panel,regions,present}.ts` with tests, and the figure-contract change (`range: {low,high} | {unavailable:true}`, `ANCESTRY_RANGE_UNAVAILABLE = "no range yet"` in `src/lib/figures/contract.ts`, one-decimal shares, body size without a range, `ClaimBlock.renderFigures`). **Read every one of those files first**, plus the full design at `/tmp/claude-0/-home-user-Inherit-bio/b3c1c41b-e9f8-5631-9c73-1b5e9b8faf86/scratchpad/w7/design.md` (sections 3, 4, 5, 6, 7 are binding for you) and the recorded decisions in `docs/protocol/decisions.md` under the heading "2026-09-03 — Ancestry surface (W7)". Build exactly what the design says; where part A's actual exports differ from the design's names, follow part A's code and tell me.

Read `AGENTS.md` and `node_modules/next/dist/docs/` guides relevant to App Router pages and client components before writing page code (this Next.js differs from training data; `PageProps<"/genome/[subject]/ancestry">` is the page props type, `params` is a Promise).

## Binding constraints (from the specification; violations are defects)
- Do not invent or approximate a citation, an identifier, a statistic, a dataset, a reviewer name or a signed record. Every number on the surface comes from the stored result, the panel constants or the region file; never retype 168, 42 or a date — import `PANEL`, `MIN_MARKERS`, `AIMS.length`, `RELIABLE_FRACTION`.
- No comparator company is named anywhere; no denylist of company names is committed (the label denylist holds demonyms/ethnonyms only).
- No real person's genome enters the repository: the synthetic fixture is generated by a committed script from a seeded PRNG and documented as describing no real person.
- Visual identity is frozen: use existing tokens/classes from `src/app/globals.css` (`--forest`, `--line`, `--paper`, `--card`, `--ink`, `--ink-muted`, `--subject-0…7`, `display`, `eyebrow`); do not add colours or fonts.
- Every user-facing string lives in `src/copy/ancestry.ts` (the readability gate extracts `src/copy/**`); grade ≤ 9; use U+2019 apostrophes (’) in prose and U+2014 (—) where the design mandates an em dash; contractions are fine. Short-role strings (headings, buttons, labels, table headers, chips) are checked word by word against `data/plain-vocabulary.json` — run `pnpm gate:readability` and register genuinely plain words you need there (alphabetical position, lower-case), never a jargon term. Terms in `data/jargon.json` (e.g. `marker`, `ancestry`, `haplogroup`) may not appear in a heading, button, label or chip; `haplogroup` renders through `<TermDefinition term="haplogroup">` on first use; "marker" gets the ≤25-word gloss the design gives.
- Every numeric value on the surface renders through `<Figure>`/`<ClaimBlock>` from `src/components/figures/*` with the four contract attributes; the ESLint rule `inherit/no-raw-figure` is enforced on `src/components/results/**` and, after you remove the ancestry ignore from `eslint.config.mjs`, on the page too. The only permitted raw counts are inside the mandated grey-state sentence with the exact exemption comment the design quotes.
- Fewer claims, not more caveats. No "regions found" count anywhere (D26). No text saying results are "coming soon".
- Never a mocked Supabase. E2E runs against the real local stack in CI only (no Docker here): write the spec, keep `pnpm exec playwright test --list` passing, and make every assertion derivable from the code you wrote.
- Identifier hygiene for the secret gate: never name a constant ending in `TOKEN`, `SECRET` or `KEY` assigned a long literal.
- No model identifiers anywhere in files.

## Deliverables (design §4, §6, §7)
1. `src/copy/ancestry.ts` — every string (h1, h2s, eyebrows, table caption/headers, toggle label `Show only what’s well supported`, chip labels, `RANGE_UNAVAILABLE`, `PANEL_LINE` built from `PANEL`, `markersLine(k)`, `RESOLUTION_LIMIT`, identity sentence, `NO_SEGMENTED_CONTROL` sentence, sub-continental sentence, lineage sentences (§4 §7.5 mother’s line; §2 no-Y lead), Neanderthal card body + Denisovan sentence, grey-state sentence, `Treat these as statistical noise…`, region panel strings, close button, "Where this comes from" text, marker gloss).
2. `src/components/results/ancestry/` — `ancestry-regions.tsx` ("use client"), `ancestry-map.tsx`, `region-list.tsx`, `region-panel.tsx`, `lineage-card.tsx`, `neanderthal-card.tsx`, plus `ancestry.test.ts` (renderToStaticMarkup) exactly as design §4.1 and §6.1 specify (data-slots, aria, tab order, gradient contract, `data-density-pixel-exclusion="map-tile"`, hairline at 0, chips in both toggle states, no `%` outside the disclosure in the grey state, one list element in the grey-state section). Toggle: a `<button role="switch" aria-checked>` styled to ≥44px hit target, `data-slot="well-supported-toggle"`, default on. Region panel: hover/focus opens without moving focus; Enter/Space/click opens and moves focus to Close; Escape or outside click closes and returns focus to the path; never modal, never navigation.
3. `src/app/(app)/genome/[subject]/ancestry/page.tsx` — rewritten as a server composition (design §4.1): `Breadcrumbs` (`My Genome / {displayLabel} / Ancestry` using `NAV_LABELS["my-genome"]` and `route()` from `src/lib/primary-routes.ts`), `SubjectBar` with `getSubjectFileCount` and `viewerAccountId`, h1 `What your file supports`, the regions section, lineage cards, Neanderthal card, "Where this comes from", `Data and methods` link via `route("genome.data", …)`. `generateMetadata` title `"{displayLabel} · Ancestry"` (follow `src/app/(app)/genome/[subject]/page.tsx`). Keep `data-testid="admixture"|"mtdna"|"ydna"` on the sections the existing E2E pins. Six headings max (§4.2). Engines (`src/lib/genome/admixture.ts`, `haplogroups.ts`) untouched.
4. `src/app/api/files/[id]/process/route.ts` — decision 16: on the admixture row also write `model_id`, `model_version` (from `PANEL`), `coverage: markersUsed / AIMS.length`, `result_state` (`available` when coverage ≥ RELIABLE_FRACTION, else `partial`; `not_covered` when markersUsed is 0). Nothing else changes; the page never depends on these columns.
5. `e2e/fixtures/generate-aims-vcf.ts` + committed `e2e/fixtures/aims-mixed-grch38.vcf` (168 rows at the `AIMS` GRCh38 positions, genotypes drawn with a seeded mulberry32 PRNG at weights EUR 0.6 / AFR 0.3 / EAS 0.1 so at least one region falls below 2%; header lines like `e2e/fixtures/tiny-grch38.vcf`); entry with SHA-256 in `e2e/fixtures/PROVENANCE.md` stating it describes no real person; run the generator to produce the file and record the command.
6. `e2e/ancestry.spec.ts` — serial, two users, exactly the assertions of design §6.2 (grey-state exact sentence with `0 of 168` via the tiny fixture, no chips/toggle, no visible `%` outside the disclosure, lineage empty states, `#neanderthal` present, `archaic-hominin` absent, axe both themes; mixed fixture: figure-unit contract on every ancestry-share, no other `%` text node, sum rule 100.0 ± 0.1 in both toggle states with both chips present, toggling changes visible rows/paths, tab order = descending share, panel open/close/focus return, background click closes, denylist check loaded from the JSON, gradient last stop `stop-opacity="0"` and feather ≥ 15% of `getBBox()` width, ≤ 12 interactive elements in the first 800px at 1280×800, first-party origins only and no request to `/geo/`). Reuse `createConfirmedUser`, `signIn`, `ingestFileAs`, `adminClient` from `e2e/helpers.ts` and the polling pattern of `e2e/overview.spec.ts:252-274`.
7. `e2e/upload-vcf.spec.ts` — replace only the three failing regex pins (design §4.6) with the exact mandated sentence; every other assertion unchanged.
8. `eslint.config.mjs` — delete the ancestry ignore line only (the variant browser ignore stays).
9. `docs/adr/0013-offline-map-rendering.md` (content per design §7) + a row in `docs/adr/README.md`; rows in `docs/test-diff-register.md` (format: `| spec | kind | why | what is asserted |`, match the existing rows' style), `docs/canonical-artifacts.md` (rows for `public/geo/regions.topo.json` + provenance, `data/ref/regions/label-denylist.json`, `src/copy/ancestry.ts` — follow the existing table sections), `e2e/README.md` (spec row), `docs/dataset-licenses.md` (only if part A did not already add the Natural Earth row and the 1000 Genomes panel URL). Do NOT edit `docs/protocol/*`, `docs/acceptance-matrix.md` or create `docs/capability-register.md` — the orchestrator does those.

## Verification before you report (all must pass; paste the tail of each)
```
pnpm typecheck && pnpm lint && pnpm test && pnpm gate:readability && pnpm gate:templates && pnpm gate:legal && pnpm gate:secrets && pnpm exec playwright test --list
pnpm build
```
If a gate fails, fix the cause (register a plain word, rewrite a sentence, move a string) — never weaken a test, skip, or add an exemption beyond the one mandated grey-state comment.

## Report (this is the only thing I read; be exact)
- Files created / changed (paths).
- Every deviation from the design or from part A's names, with the reason.
- The exact strings pinned in the two specs (grey-state sentence, toggle label, chip labels, panel title form).
- Plain-vocabulary words you registered.
- Command outputs' final lines (typecheck, lint, test counts, gate:readability block count, playwright --list count, build).
- Anything left undone.
