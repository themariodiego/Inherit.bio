# Embryos design — `/embryos`, `/embryos/upload`, `/embryos/request-data`, `/embryos/compare`, `/embryos/[embryoId]`

Read-only design, 2026-09-03. `brief:N` = `/home/user/Inherit.bio/docs/inherit-v2-brief.md` line N; every other path is relative to `/home/user/Inherit.bio/`. Governing text, in precedence order (X0.1 `brief:2364`): X2.4 `brief:2402`, X3.4 `brief:2414`, X4 `brief:2422-2430`, X6 `brief:2442-2446`, X7.3 `brief:2456`, X10.2–X10.4 `brief:2482-2486`, X11.2 `brief:2492`, X12 `brief:2498-2502`, X13 `brief:2510-2512`, X14.1 `brief:2518`; gates G2.2 `brief:2584`, G2.6 `brief:2596`, G4.5 `brief:2632`, G5.1b `brief:2641`, G5.3 `brief:2643`, G5.4 `brief:2645`, G7.1 `brief:2664`, G8.2/G8.3 `brief:2672-2673`; then §4 §6 `brief:1372-1417` (which "wins on what a surface may say", `brief:1145`), §2 §6 `brief:370-402`, §3 §3.4 `brief:689`, §3 §8.5 `brief:1019-1030`, §5 §2.8 `brief:1720-1735`, §5 §5.3 `brief:1833-1835`, A.6 `brief:2176-2211`, A.7 `brief:2213-2244`, A.10 `brief:2269-2270`; acceptance 21–27 `brief:479-485`, 33–36 `brief:491-494`; ADR 0016; the conflict rows of `docs/canonical-artifacts.md:19-23`; and the repository facts below. Conventions stay those of `docs/design/w9-family-surfaces.md`: the eight states, the Tier-2 gate at the domain boundary, one `<ClaimBlock>` per cell, the G4.5 panel, per-block denominators (`brief:2784`).

## 0. Repository facts that bound the design

- **All five pages are placeholders.** `src/app/(app)/embryos/{page,upload/page,request-data/page,compare/page,[embryoId]/page}.tsx` each render `<CapabilityUnavailable>` unconditionally (hard-coded copy "Not available in this jurisdiction yet…", `src/components/capability-unavailable.tsx:25-29`); the landing adds "If a child is born from this" and "No embryo analysis model is currently allowlisted on this deployment." The register's denial contract instead takes `userFacingCopy` from `data/jurisdictions.json` ("This part of Inherit is not available here because its legal review is not complete."). No `src/lib/embryos/`, `src/components/embryo/`, `src/copy/embryos/`, `src/copy/upload/errors.ts` (the register's canonical home for A.6 codes, `docs/canonical-artifacts.md:81`), `src/lib/embryos/qc-policy.ts` or `src/lib/embryos/policy.ts` (canonical homes, `docs/canonical-artifacts.md:161-162`) exists. `src/lib/legal/` holds only `artifacts.ts`; the jurisdiction reader `src/lib/legal/jurisdictions.ts` (X12.1) is absent. `src/lib/primary-routes.ts:34-36` builds `embryos.index`, `embryos.upload`, `embryos.compare` only — `embryos.request-data` and `embryos.detail` are missing (D-010).
- **Register.** `docs/route-register.json` routes 49–53: `embryos.index` (hub, 64rem, `product-result`), `embryos.upload` and `embryos.request-data` (flow, `restricted-flow`: `partial-coverage`/`not-covered` are `n/a` with the profile's reason), `embryos.compare` (wide-data, 90rem, `product-result`, query `?cohort=` optional: absent → "resolve the most recent readable eligible cohort by created_at descending then id ascending or render the registered zero-cohort blocking state with no private row fetch"; unknown → `resource-not-found-page-v1`), `embryos.detail` (standard). Every page route is guarded by `embryo_analysis` with response guards `embryo_single_locus`, `embryo_statistical_estimate`, `carrier_match` and contracts `resource-authorization-v1`, `jurisdiction-v1`, `embryo-autosomal-only-v1`. Depth budget: `embryos.index 1, embryos.upload 1, embryos.request-data 2, embryos.compare 1, embryos.detail 3`. API routes exist only in the register: `api.embryo-cohort-drafts`, `api.embryo-cohorts`, `api.embryo-ingest-mapping|chunk|complete` (`auth: upload-session`), `api.embryo-record-key-cards(-rights)`, `api.embryo-disposition`, `api.embryo-withdraw` (alias of `api.cohort-restrict`, `jurisdictionBypass: true`); **none has a handler under `src/app/api/`** (grep `embryo|cohort|ordinal` there hits only `jobs/mail`, `jobs/retention`, `search`). `policyContracts.embryo-autosomal-only-v1` fixes the closed shapes (`rscEmbryoListItem`, `rscEmbryoDetail`, `rscEmbryoComparison` with `context_counts`, `embryos`, `result_rows`, `trade_offs {statement_copy_id ∈ embryo.tradeoffs.exists|none-measurable, conflicts[]}`, `standing_statement` copy id `embryo.standing-statement`), the eight-key `EmbryoFinding` (`brief:1982`; mirrored by `private.valid_embryo_findings`, `supabase/migrations/20260831224116_chat_scope_and_context.sql:78-104`), `forbiddenShapeFields` (sex, karyotype, `sample_column`, `lab_identifier`, …), `filtersAndSorts` ("columnOrder: ascending-immutable-sample_ordinal-only; resultRowOrder: ascending-registry-condition_id-only; interactiveSortControl: forbidden"), `qcProjection` (reason ids `embryo_call_rate|embryo_parent_discordant|contamination|dropout_too_high|qc_review_required`; result reasons add `insufficient_coverage|source_call_disputed|within_family_validation_unavailable|sex_combined_model_unavailable`; copy ids `embryo.qc.quality-check-not-passed`, `embryo.qc.not-measurable-from-file`, `embryo.qc.source-not-stated`, `embryo.qc.dropout-not-measured`, `embryo.context.{analysed,quality-check-passed,not-measurable}`, `embryo.result.insufficient-coverage`, `embryo.within-family.not-tested`), and `payloadBoundaryContract.embryoIngestSessionLimits` (200,000,000 uncompressed bytes, 50 chunks, 64 sample columns, 2 concurrent sessions per account).
- **Schema (Platform-owned, present).** `supabase/migrations/20260831223301_embryo_cohorts_and_authority.sql`: `embryo_cohort_drafts`, `embryo_draft_participants`, `embryo_cohorts` (no `label`, `clinic_label` or `lab_label` column — A.3's `brief:2086` was not implemented; `status in ('upload_pending','ingesting','active','restricted','purge_queued','purged','claimed_bound')`, `retention_expires_at`, `uploaded_at`/`qc_failed_at` at most one), `embryo_participant_sets` (the five sets), `embryo_basis_bindings`, `embryo_donor_attributions`, `embryos` (`sample_ordinal 0..63`, **`display_label` generated as `'Embryo ' || (sample_ordinal + 1)`**, `status in ('pending','qc_pass','qc_marginal','qc_fail','excluded','stored','transferred','donated','discarded','claimed_bound')`; **no `lab_identifier`** — `docs/canonical-artifacts.md:23` supersedes `brief:2088`), `embryo_qc` (the twenty projected fields; `imputation_performed` constrained false), `embryo_scores` (finding jsonb with `check (not (finding ?| array['sex','embryo_sex','karyotype','rank','score','recommendation']))`), `embryo_figures` (`absolute_risk|interval|natural_frequency|within_family`), `suppressions`, `embryo_variants` (`chromosome between 1 and 22`), `embryo_ingest_sessions|fragments|fragment_handle_maps`, `embryo_disposition_proposals`, `future_person_record_key_hashes|print_rights`; `genome_files.cohort_id/is_cohort_file/sample_count/source_publication_state`; `genome_file_type` gains `vcf_multisample`, `pgt_table`; the `user_variants_embryo_autosomal_only` trigger and `subject_demographics_reject_embryo` trigger (`…221537:149-175`). `condition_registry` (`category` limited to the five §6.3 categories, `inheritance_mode` nullable, no seed) and `risk_models` (`sex_basis = 'combined'`, `age_band = 'lifetime'`, `prevalence_basis = 'lifetime_risk'`, `within_family_status`, no seed) in `…224126:20-52`. `worker_jobs.kind` includes `split_cohort_vcf` (`output_kind 'ingest.normalize'`, binding `embryo-ingest-fragment-set`) and `score_embryo` (`embryo.single-locus|statistical-estimate|carrier-match`, binding `cohort-source-set`) (`…224034:123-147`); `private.enqueue_worker_job_v2` and `private.resource_authorized_v1(…,'cohort',…)` exist (`…224035:3-80`; the cohort branch authorises the cohort **owner** only — co-parents are reached through grants). `chats.scope_kind = 'cohort'` with `cohort_id` and `cohort_authority_fingerprint` (`…224116:4-49`). Retention phases for every `embryo.*` row are registered (`…224119:32-39, 83-94`) and the sensitive-purge target order lists `embryo_scores`, `embryo_qc`, `embryo_figures`, `embryo_variants`, the ingest working state. Every embryo table revokes `anon, authenticated` (X3.3).
- **Absent.** No RPC creates a draft, finalises a cohort, opens an ingest session, accepts a chunk, completes, restricts a cohort or records a disposition (grep `create or replace function private.*(embryo|cohort)` → only the two triggers and the jsonb validator). `private.finalize_upload` **rejects** cohort sessions (`…031000_upload_runtime.sql:33`), `POST /api/uploads` binds every upload to the caller's `self` subject (`src/app/api/uploads/route.ts:63-81`), `src/lib/genome/parsers/sniff.ts` has no `%PDF-` branch, no sample-column count and no `pgt_table` heuristic (A.6 `brief:2178`), `src/lib/genome/parsers/pgt-table.ts`, `data/ref/lab-tables/column-synonyms.json`, `data/ref/build-discriminating-sites.json`, `src/lib/genome/build-inference.ts` do not exist. The worker (`worker/src/index.ts`) claims `annotate_vcf` only; the register names `scripts/worker.ts` as the canonical entrypoint (`workerExecutionBindings`) — a Platform discrepancy. `job_time_stats(kind)` (`brief:2225`) does not exist. `src/app/api/export/route.ts` has no embryo partition. `data/embryo/allowed_conditions.json` is the committed empty registry (`"status": "withheld_until_calibrated_models_are_registered"`, `"conditions": []`, the five allowed categories, eighteen forbidden classes); the register says the empty array "is valid and must not be populated until the model and review constraints are satisfied". `data/citations.json`, `data/claims.json`, `docs/figures-register.json`, `docs/anti-patterns.md`, `data/counsellors/` and `content/legal/**` do not exist.
- **Jurisdiction.** `data/jurisdictions.json` capabilities include `embryo_analysis`, `embryo_single_locus`, `embryo_statistical_estimate`, `carrier_match`; `realJurisdictions` is `{}`; the default real row is `unreviewed` for each with `userFacingCopy` "This part of Inherit is not available here because its legal review is not complete."; `TEST-LOCAL` permits all under `INHERIT_TEST_JURISDICTION=1` (`playwright.config.ts:37`, CI). No UK/IE/CZ/DE/CH/IT/ES/CA/IN/US row of §5 §5.2 (`brief:1821-1826`) is committed; none may be invented (C5 `brief:2734`). `profiles.jurisdiction_code` exists and nothing in `src/` reads it.
- **Consent artifacts.** `consent.upload-embryo`, `attestation.embryo-parentage`, `charter.future-person` are named by the brief (`brief:1657`) and by the register's `caseArtifactMatrix`; no `content/legal/**` file or migration seeds them (grep → none). The embryo consent template that `gate:legal` must find the §4 §6.1 sentence in (`brief:1378`) does not exist.
- **Figure contract** (`src/lib/figures/contract.ts`): kinds `absolute|relative|difference-pp|natural-frequency|percentile|coverage|interval|ancestry-share|genotype|carrier-status`; classes `variant-call|estimate|ancestry|quality`; bases `observed|modelled` (W9's requested `exact` is not shipped and Embryos does not need it); `<ClaimBlock>` takes `renderFigures`, `densityPrimaryClaim`; `<Figure>` renders `absolute` and `ancestry-share` in Fraunces at `text-2xl` (`figure.tsx:30,55`) — inside a table cell that collides with `brief:620` ("Fraunces is forbidden in … table cells and headers"). `carrier-status` remains uninstantiated. `scripts/eslint/no-raw-figure.mjs:37` already lists `src/app/(app)/embryos/**/*.tsx`.
- **Subject chips.** `src/lib/subjects.ts` lists embryo subjects only for `owner_account_id`/`subject_account_id` matches — a co-parent who is a `requiredUploadPrincipal` but not the owner sees nothing, so the Overview's State E (`src/app/(app)/overview/page.tsx:148-161, 231-247`) never fires for them. `subjectKind` returns `"embryo"` → chip "Embryo" (`KIND_CHIPS.embryo`, `brief:207`). `subjectColourIndex` (`src/lib/subject-colour.ts:21-24`) hashes every non-self subject onto a `--subject-N` token — **X2.4 forbids that for embryos** (`brief:2402`). `SubjectBar` offers "Add a file" on `self` only (`subject-bar.tsx:87`) and links the file count to `/files`, which lists the session's own `genome_files` and would not list a service-role-only embryo canonical source.
- **Overview.** State E counts `embryo_qc.qc_verdict = 'pass'` and `= 'fail'` (`overview/page.tsx:242-246`); the register's `qcProjection.contextStrip` defines `not_measurable` as every embryo that produces no figure (fail **and** the marginal no-figure band). `embryos.copilot` resolves to `route("copilot.scope", { scope: cohortId })` only once `COPILOT_GROUP_SCOPES_AVAILABLE` (`src/copy/overview.ts:40`, false; `/copilot/[scope]` resolves subjects only, `copilot/[scope]/page.tsx:26`). `e2e/overview.spec.ts` pins States A and C ("No embryo files added." at `:255`) and never State E. Search lists embryo subjects under "People and embryos" with href `route("genome.subject", …)` (`src/app/api/search/route.ts:64-72`) — wrong target for an embryo.
- **Templates and categories.** All 151 templates are `estimate/single_locus`; the legacy categories inside the §6.3 allowlist hold 70 templates (heart-cardiovascular 10, lifestyle-wellness 10, metabolic-obesity 10, gastrointestinal 10, autoimmune 10 + `allergic-sensitization-il13`, cancer-risk 10, reproductive-family 10); the excluded categories hold 81. None is a `variant_call`, so no carrier finding is derivable from the seed, and `condition_registry` is empty, so no template is embryo-eligible today.
- **Vocabulary.** `data/jargon.json` bars from headings, buttons, labels, table headers and status text (X7.3): `laboratory`, `cohort`, `condition`, `sample`, `estimate`, `evidence`, `genetic`, `autosomal`, `chromosome`, `model`, `carrier`, `variant`, `coverage`, `call rate`, `percentile`, `probability`. `data/plain-vocabulary.json` (658 words) lacks, among words the Embryo strings need in short roles: `added`, `checked`, `compared`, `passed`, `measured`, `measurable`, `quality`, `column`, `copy`, `each`, `every`, `same`, `different`, `apart`, `label`, `folder`, `zip`, `pdf`, `spreadsheet`, `text`, `four`, `six`, `seven`, `eight`, `too`, `which`, `letter`. Adding words is precedented (`c255e8a`, `4f0fa75`).
- **Ownership** (`docs/protocol/ownership.md`): Embryo Analysis owns `src/app/(app)/embryos/`, `src/components/embryo/`, `src/lib/embryos/`; `supabase/migrations/`, `scripts/`, `.github/workflows/`, the nav and `globals.css` are Platform; `src/app/api/**` (except chat), `src/lib/subjects.ts`, `src/components/subjects/`, `src/copy/upload/`, `worker/` are unlisted; `data/jurisdictions.json` and `content/legal/` are the consent stream; `src/app/api/chat/`, `src/copy/copilot/` are Copilot; `src/app/(app)/overview/` is IA; `docs/` the orchestrator. Every change outside the owned paths is a request row for `docs/protocol/decisions.md`.
- **Density.** `docs/density-baseline.json` records no `/embryos/*` route, so only the absolute floors apply: white space ≥ 0.62 on the landing/detail/flows and ≥ 0.45 on `/embryos/compare` (X6.2); ≤ 12 interactives on the landing and detail, ≤ 7 on each flow step, ≤ 24 on compare (X6.1, `brief:197,491`); ≤ 480 visible characters on the landing, ≤ 700 elsewhere (X6.3); heading cap 6 per app surface (`brief:627`); `/embryos` h1 "Embryos", `/embryos/compare` h1 "Compare embryos" (`brief:535-536`).

## 1. Information architecture

### 1.1 Pages, headings, breadcrumbs, chips

| Route (register id) | h1 | Other headings (≤ 6 total; report bodies unlimited) | Breadcrumbs (`brief:195`) | Subject bar |
| --- | --- | --- | --- | --- |
| `/embryos` (`embryos.index`) | "Embryos" (`brief:535`; nav identity) | h2 "Your embryos" (the cohort list) — 2 headings; the three tiles `Upload` · `Compare your embryos` · `Ask Copilot` are title-role links, not headings (`brief:372,627`) | none (hub) | none; each embryo renders as a chip (§1.2) inside its cohort card |
| `/embryos/upload` (`embryos.upload`) | "Upload embryo files" — no: `files` is fine but the flow h1 must be plain; default "Add embryo files" (§3 §6.5's action word, `brief:930`) | one h2 per step, at most one visible per step ("Step N of 5", `brief:1083`) | `Embryos / Add embryo files` | none |
| `/embryos/request-data` (`embryos.request-data`) | "How to get your embryo files" (the Overview action label, `brief:235,372`) | h2 "The email to send" | `Embryos / How to get your embryo files` | none |
| `/embryos/compare` (`embryos.compare`) | "Compare embryos" (`brief:536`) | h2 "Side by side", h2 "Quality check", h2 "How sure we are", h2 "Where this comes from" — 5 headings; the standing statement, the context strip and the trade-off panel are non-heading blocks above the table | `Embryos / Compare embryos` (joint surface; every column header carries the full embryo chip, `brief:211`) | none (W9 open decision 11 applied) |
| `/embryos/[embryoId]` (`embryos.detail`) | the display label, e.g. "Embryo 3" (`brief:205`: display name first) | the six X13.1 h2s in the embryo variant (`src/copy/reports/headings.ts:29-31`) — a report body, so the six h2 exceed the cap lawfully (`brief:627`) | `Embryos / Embryo 3` | yes, chip "Embryo", no file count, no "Add a file" (§1.2) |

Titles: "Embryos", "Add embryo files · Embryos", "How to get your embryo files · Embryos", "Compare embryos", "Embryo 3 · Embryos" (repository pattern `"{display name} · {section}"`).

### 1.2 The embryo chip: ordinal, never a laboratory label, never sex

Identity is the server-generated `embryos.display_label` ("Embryo 1" … "Embryo 64"), derived only from `sample_ordinal` (`docs/canonical-artifacts.md:23`; X2.4 `brief:2402` "Embryo identity is carried by the laboratory-derived display label as text" is read through the canonical row: the label is derived from the laboratory's **column order**, never from its text). The chip renders: a 24px disc with the initial "E" as text (`brief:199,207`) in the neutral inset ground with a `--line-strong` border — **identical for every embryo** (`brief:689`: "identical in colour, weight, size and order treatment"); the label; the kind chip "Embryo" (`KIND_CHIPS.embryo`); and, on the compare header only, the QC chip "Quality check not passed" when `qc_verdict = 'fail'` (`brief:392`). Request to the owner of `src/components/subjects/`: `subjectColourIndex` returns `null` for `subject_class = 'embryo'` and `SubjectBar` renders the neutral disc for null; `fileCount: number | null` (W9's request) with null rendering nothing — the detail page passes null, because `/files` cannot list the canonical source. Nothing on any embryo surface renders a sample column name, a cycle label, a clinic label, a colour token, an ordinal badge, a rank, or any of `forbiddenShapeFields`; the E2E greps `page.content()` for `/\b(sex|male|female|XX|XY|chrX|chrY|chrM|karyotype)\b/i` and asserts zero.

### 1.3 The cohort graph (replaces `listSubjectsForAccount` for Embryos)

`src/lib/embryos/cohorts.ts` (server-only), no schema change:

```ts
export interface EmbryoCohortView {
  id: string;
  status: "upload_pending" | "ingesting" | "active" | "restricted" | "purge_queued" | "purged" | "claimed_bound";
  createdAt: string;                // the cohort's only safe label: "Embryos added on {date}"
  embryoCount: number;
  viewerRole: "required_upload_principal" | "nonparent_uploader_owner";
  requiredUploadPrincipalAccountIds: string[];   // contributors for G5.1b
  analysisGranted: boolean;         // live embryo.analysis grant from every requiredUploadPrincipal
  embryos: { id: string; subjectId: string; sampleOrdinal: number; displayLabel: string; status: EmbryoStatus }[];
  retentionExpiresAt: string;
}
export function listCohortsForAccount(accountId: string): Promise<EmbryoCohortView[]>;
export function resolveCohortForAccount(accountId: string, cohortId: string | null): Promise<EmbryoCohortView | null>;
export function resolveEmbryoForAccount(accountId: string, embryoId: string): Promise<{ cohort: EmbryoCohortView; embryo: EmbryoCohortView["embryos"][number] } | null>;
```

Sources: `embryo_participant_sets` (`set_kind = 'required_upload_principals'`, `revoked_at is null`) joined to `subject_principals.account_id`, plus account-owned `embryo_third_party` cohorts where the actor is the current non-parent uploader owner (`resourceAuthorizationBindings.routes.embryos.index.targetSource`); `embryos` ordered by `sample_ordinal`; `purpose_grants ⋈ directional_grants` for `embryo.analysis` per principal (`analysis-eligibility-v1`: "purposeGrantsFromEveryRequiredUploadPrincipal"). A donor-subject-only principal, provider, reviewer or unrelated custody owner is excluded. `resolveCohortForAccount(…, null)` applies the register's absent-query rule. Nothing pending, unpublished or restricted is listed (`canonical-source-publication-v1.readInvariant`: no ID, label, count or existence signal) — a `restricted` cohort resolves to null for every account (`brief:2156`). Request to IA: State E and the `embryos.copilot` target read `listCohortsForAccount`; request to Search: embryo rows link to `route("embryos.detail", { embryoId })`.

### 1.4 Access, the jurisdiction gate and the eight states

`src/lib/embryos/access.ts#embryoCapability(viewerAccountId, contributorAccountIds, key)` → `permitted | prohibited | unreviewed`, consuming the same reader as Family's F1 (`src/lib/legal/jurisdictions.ts`, requested from the consent stream; until it ships, one shared reader lives under `src/lib/family/access.ts` and Embryos imports it). Under `INHERIT_TEST_JURISDICTION=1` it reads `TEST-LOCAL` (X12.3 `brief:2502`); otherwise each account's `profiles.jurisdiction_code` against `realJurisdictions`/`defaultRealJurisdiction`, unset → `unreviewed` (`brief:2498`); acting account **and** every `requiredUploadPrincipal` (G5.1b `brief:2641`, X12.2 `brief:2500`). The route guard is `embryo_analysis`; result layers add `embryo_single_locus`, `embryo_statistical_estimate`, `carrier_match` (register guards). The `jurisdiction-unavailable` render is the JSON `userFacingCopy` inside the `<CapabilityUnavailable>` frame plus the future-person link (`public-embryo-analysis.stateProjection.jurisdiction-unavailable`; `brief:1803`), never the frame's hard-coded sentence. §5 §5.4 (ii)–(iii) (`brief:1839`): the same copy renders as a permanent, non-dismissible line on the `/embryos` landing card and as the first screen of `/embryos/upload`. Rights routes (`api.embryo-withdraw`, `api.cohort-restrict`, `rights.withdraw`) bypass the gate (`jurisdictionBypass: true`; X3.4).

The eight register states (G2.2 `brief:2584`; `consent-required` and `jurisdiction-unavailable` may never be `n/a` on an Embryo route):

| State | `/embryos` | `/embryos/upload` | `/embryos/request-data` | `/embryos/compare` | `/embryos/[embryoId]` |
| --- | --- | --- | --- | --- | --- |
| `empty` | no cohort: the §2 §6.0 empty state, one primary "How to get your embryo files" → `/embryos/request-data` (`brief:372`) | no draft: step 1 (question 1) | no cohort: the letter renders anyway (static copy) with "Back to Embryos" | zero cohorts: the registered zero-cohort blocking state, no private row fetch: "No embryo files added yet." + the same primary | cohort `upload_pending`: "The laboratory’s file for this embryo has not been added yet." |
| `processing` | cohort `ingesting`: the `JobProgress` panel with the five embryo steps (`brief:1048`) and `job_time_stats('split_cohort_vcf')` or the `'<20'` sentence (`brief:2225`) | session open / `sanitization_pending`: the same panel | co-parent invitation pending: "Waiting for the other genetic parent to sign." | cohort `ingesting`: blocking screen with the panel; zero findings fetched | embryo `pending`: same |
| `partial-coverage` | n/a — declared with the structural reason "the hub renders no result" | n/a (register profile) | n/a (register profile) | a finding with `coverage_state: partial` → the cell renders its figure with the word "Partly read" (§3 §3.2 glyph + word) | same |
| `complete` | cohorts listed with their embryo chips and status lines | cohort published: "Files added" with links to compare and each embryo | the letter, copied to the clipboard | the matrix (today: QC columns, zero result rows, the honest registry sentence) | QC block + findings (today: the honest registry sentence) |
| `not-covered` | n/a (structural) | n/a | n/a | `coverageFailureFinding` → `embryo.result.insufficient-coverage` (§2.4) | same |
| `error` | load failure → `ErrorState` | an A.6 rejection by code (`src/copy/upload/errors.ts`) | clipboard failure: "Copy did not work. Select the text and copy it yourself." | closed-shape failure → `ErrorState` naming the page, no value (the register's `embryo-closed-schema-v1` 500) | same |
| `consent-required` | a cohort without a live `embryo.analysis` grant from every parent: card line "Waiting for {role} to turn on analysis" and, for the viewer's own missing grant, the just-in-time switch (§5 §2.4 `brief:1686`: one switch, one consequence sentence, no modal) | the other genetic parent has not signed → the evidence-pending step naming the missing person and step | the viewer has not signed `consent.upload-embryo`: the letter still renders and the page states the next step | grant missing from any parent → blocking screen, zero findings fetched or serialised (G5.1 `brief:2639`) | same |
| `jurisdiction-unavailable` | the `userFacingCopy` line on the card; every tile blocked | `capability-denied-page-v1` | same | same | same |

Overview states (`brief:221-240`) map onto the Embryos card only: A/B/C unchanged; **E** (`brief:240`): the three counts from `contextStrip()` in `src/lib/embryos/qc-policy.ts` (request to IA: replace the inline `qc_verdict` counts so "{q} could not be measured" equals the register's `not_measurable` definition), primary "Compare your embryos" → `route("embryos.compare", { query: { cohort } })`.

### 1.5 The Tier-2 gate (`brief:968-970`)

"Any embryo comparison" sits behind the same explicit, session-scoped gate as Family: checkbox "I understand this can tell me something I can’t un-know.", the sentence "You won’t be asked again until you sign out.", one primary "Show my embryos", never `localStorage`. Request to Family/F1: generalise `src/components/family/result-gate.tsx` and `POST /api/family/acknowledge` to `src/components/results/result-gate.tsx` (§3 §4.3 `brief:749`) and `POST /api/results/acknowledge` with `domain ∈ family | embryos`, one httpOnly cookie per domain (`hmacSecret(user.id + ":" + session id + ":" + domain)`, cleared by `/auth/sign-out`). `src/lib/embryos/tier2.ts#acknowledged(user)` is read by `/embryos/compare` and `/embryos/[embryoId]`; while false the page renders the gate and fetches nothing derived. `e2e/embryo-compare.spec.ts` asserts exactly one gate per session across compare → detail → compare (`brief:970`). Counsellor routing renders `COUNSELLOR_NO_ROUTE` until `data/counsellors/directory.json` exists (`brief:972`).

### 1.6 Laboratory-consent and retention rules the surfaces state

- **Upload preconditions, in order, each its own screen** (§5 §2.8 `brief:1722`): jurisdiction permits `embryo_analysis` → the uploader signs `consent.upload-embryo` under Tier-2 friction (`brief:1752`, the criminal-liability sentence verbatim) → both genetic parents evidenced (the co-parent signs `attestation.embryo-parentage` and `consent.upload-embryo` in their own account, `brief:1726`; "A bare two-parent attestation by one person is never sufficient and must not be offered in the UI", `brief:1727`; acceptance 21 `brief:479`) → `disclosure.insurance-and-discrimination` acknowledged (item 6 `brief:1961`) → `charter.future-person` displayed in full and acknowledged → upload. Single-parent bases (`brief:1729-1735`) each have a named screen with the mandated sentences (`brief:1731`, `brief:1734`); contested parentage blocks outright (`brief:1735`). Class (d) (`brief:1633`, `brief:991`): the uploader is recorded only as non-parent uploader-owner; every authority set comes from `embryo-basis-authority-v1`. The Record Key Card is delivered at finalisation (`brief:1793`; `canonical-artifacts.md:39`; `cohort-created-v1`), one per embryo, only to `recordKeyRecipients`.
- **The upload-time notice** (`brief:383`; `upload-time-rights-notice-v1.embryo`): after the worker publishes, every `noticeRecipient` other than the actor receives the plain-language notice, a read-only view of what the uploader can see, the retention maximum, and a `/withdraw/[token]` link whose `embryo-parent-withdrawal` purpose offers `refuse | delete | export` (`rights.withdraw.operationsByPurpose`); `delete` dispatches `cohort.restriction-whole`: access ends on the next query, derived rows within 60 seconds, sources within 7 days, user-facing "within 7 days" (X3.4 `brief:2414`; `docs/retention.md:13-15,70`). An upload attempted with only one parent's attestation is rejected and the control never enables (`brief:383`).
- **Retention lines the product states** (A.13(c) `brief:2333`; `docs/retention.md:56,61-62`, whose numbers are canonical): on `/embryos` beneath each cohort, "Inherit deletes these files 24 months after they were added or last analysed, whichever is later, unless every genetic parent renews." and, after a disposition, "Donated or discarded: deleted 90 days after that was recorded." / "Transferred: kept for the future person until the date on the Record Key Card." The rights page states the maximum in days (G5.4 `brief:2645`) from `rightsEmbryoWithdrawal.retentionMaximumDays`, computed from the same rows.
- **Either parent may stop everything** (`brief:2156`; ADR 0009 by name): a "Stop and delete these files" destructive action on `/embryos` for a `dispositionAuthority` → `api.embryo-withdraw` (tier 3 confirmation of `brief:936`: typed phrase, the itemised list), tombstone on every recipient's landing "Sharing ended on {date}. Every result built from these files was deleted." (mirrors `brief:342`).

## 2. Each surface

### 2.1 `/embryos`

Order: the jurisdiction line (§1.4; permanent), h1, the empty state or the cohort list, the three tiles, the standing statement (§2 §6.0 `brief:372`: "with the standing statement (§6.2) below"), the retention line, `NOT_DIAGNOSTIC`. Empty state (`EmptyState`, four parts `brief:930`; strings from §2 §6.0 `brief:372`, IA authoritative on names per G2.1): heading "No embryo files added yet.", sentence one "When a laboratory’s file is added, each embryo appears here.", sentence two "Not every clinic produces one. Ask yours for the data behind the report.", the one `Action` "How to get your embryo files" → `/embryos/request-data`. Cohort card (`data-card`, no subject colour): group label "Embryos added on {date}" (`createdAt`; `embryo_cohorts` has no label column and "cohort" is jargon), the embryo chips in ordinal order each linking to `/embryos/[embryoId]`, one status line per embryo from a closed table — `"Checking the file"` (pending), `"Ready"` (qc_pass), `"Ready, with a thinner file"` (qc_marginal), `"Quality check not passed"` (qc_fail, `brief:392`), `"Stored"`, `"Transferred"`, `"Donated"`, `"Discarded"` — the analysis line "Waiting for {role} to turn on analysis" when a grant is missing, and one link "Compare these embryos" → `route("embryos.compare", { query: { cohort } })`. Tiles: "Upload" → `/embryos/upload`; "Compare your embryos" → the newest readable cohort's compare page, else the card sentence; "Ask Copilot" → `route("copilot.scope", { scope: "c-{cohortId}" })` once `COPILOT_GROUP_SCOPES_AVAILABLE` and the Copilot page serves the cohort scope, else the blocking sentence "Copilot for embryos runs only on a model you host yourself. It is not connected on this site yet." (`canonical-artifacts.md:42`; `copilotCohortUnavailablePage`). Never a dead link (`decisions.md:125-127`). Budget: h1 + 3 tiles + 1 primary + ≤ 4 chips per card ≤ 12 interactives; ≤ 480 characters means tile descriptions ≤ 12 words and status lines ≤ 4 words. "Sex" and "rank" tokens appear nowhere on the page.

### 2.2 `/embryos/upload` and `/embryos/request-data`

**Steps** (≤ 5, "Step N of 5", `brief:1083`; ≤ 7 interactives per step; one primary per step):

1. *Jurisdiction and the three questions* (`brief:374-377`, verbatim): "Did your clinic do genetic testing on your embryos?" — Yes / No / I’m not sure; `No` ends the flow with "Inherit needs data from a genetic test the laboratory already ran. Without it there is nothing to read."; "Who did the testing?" — free text, **not persisted** (the brief's "stored only as a label" `brief:376` is superseded: `embryo_cohorts` has no lab column and `docs/canonical-artifacts.md:23` forbids source labels; the answer routes the flow only, and the screen says so: "Inherit does not keep this name."); "What did they send you?" — the four illustrated options (`brief:377`) with "A PDF report only" now ending in the refusal state (§2.2 below), plus the secondary link "I don’t know — let me upload it and you tell me".
2. *Whose embryos* (`brief:980-991`): options 3 and 4 with their exact attestation checkboxes — "These are my own embryos and I am a genetic parent." and "Both genetic parents have given me permission to upload these embryos to Inherit. I can show that permission if asked." — then the basis selection (two evidenced parents / donor-gamete-anonymous / parent-deceased / sole-legal-disposition-authority, `closed-embryo-cohort-draft-v1`), the second parent's contact email (mandatory, `brief:383`), the four checkboxes and the Tier-2 signature block (`brief:1752`), the identified-donor option (`brief:1732`, optional). POST `api.embryo-cohort-drafts` (E0).
3. *Evidence pending* (`consent-required`): "Waiting for {role} to sign in their own account." with the reminder rule (7 days, `docs/retention.md:36`) and the 30-day draft deadline in words (`embryo.cohort-draft-30d`); the single-authority cases upload their reviewed document here (`legal-evidence-ingest-v1`, E0) and wait for named-human review (`brief:1733-1734`, automated approval forbidden).
4. *Acknowledgements and the Record Key Cards*: the insurance disclosure (`brief:1950-1961`, item 6 shown) and the Charter in full; POST `api.embryo-cohorts`; the printable Record Key Cards render once, only for a `recordKeyRecipient` (`cohort-created-v1.recipientRule`), with the closing date "provisional until the files are checked".
5. *The file* (`UploadDropzone`, `brief:723,980`): browser preflight refuses `%PDF-` before the first chunk (`genetic-file-ingest-v1.browserPreflight`; ADR 0016 §"Closed genetic formats"), `sniffV2` classifies (`brief:2178`), the browser decompresses, frames complete logical lines, maps each source sample column to a server-issued random handle in ephemeral memory and rewrites the stream so no source label crosses the request boundary (`embryo-ingest-session-v1.identity`), then `PUT api.embryo-ingest-chunk` in sequence (≤ `ingestChunkMaximumBytes`), `api.embryo-ingest-mapping` for the ≤ 4 column decisions or the build challenge (`brief:2190-2192`; "I don’t know" → `/embryos/request-data`), `POST …/complete`, then the `JobProgress` panel ("Checking the file", "Reading the embryo’s DNA spots", "Matching to the current map", "Comparing your embryos", "Done", `brief:1048`) polling `worker_jobs` progress notes only.

**Formats** (`brief:379` read through ADR 0016 and `docs/canonical-artifacts.md:22`): accepted `VCF`, `VCF.GZ`, `gVCF`, multi-sample VCF (`vcf_multisample`), `pgt_table` (`.csv`/`.tsv` under the header rule `brief:2188`), zip archives of these (each member classified; archive bombs fail closed). **PDF is rejected before any durable byte** — the brief's "stored, hashed, listed" PDF (`brief:379`) and acceptance 27 (`brief:485`) are superseded by the canonical row; the refusal is `pdf_not_data` verbatim (`brief:2201`): "This is a PDF, not genetic data. The numbers in it are conclusions, not the genotypes we need. Ask your clinic or lab for the raw data file — a VCF, or a CSV of genotypes per embryo. We have a letter you can send them." with the link to `/embryos/request-data`. Every A.6 code and message (`brief:2200-2209`) lives once in `src/copy/upload/errors.ts` (≤ 240 characters, no allele, `brief:2196`); `too_large` reads its `{n}` from `payloadBoundaryContract`, never a second limit.

**Partial failure, A.10** (`docs/canonical-artifacts.md:20`; `brief:2222`): while `split_cohort_vcf` runs, the client sees only the coarse panel (`canonical-source-publication-v1.progressProjection`: no ordinal, count, hash or existence signal). After the terminal transaction the client sees every ordinal at once: each either published (`qc_pass|qc_marginal`) or `qc_fail` with its closed reason and no source ("Not measurable" column, `brief:392`); the job is `partial`; the Record Key Card date addendum is mailed. A pre-publication failure (`ingest_abandoned_no_source`) shows "The upload did not finish. No genetic data was kept, and every Record Key Card from this upload is now invalid." and returns to step 5. Nothing is ever shown for one embryo before the whole set publishes.

**`/embryos/request-data`**: h1, the verbatim letter in a `<blockquote>` (`brief:381`: "Please could you send me the genetic data files from the preimplantation genetic testing (PGT) on my embryos — the genotype or sequence files behind the report, not the report itself. Labs usually call these VCF files, genotype call files, or ‘the raw data’. I would like one file per embryo, or one file with a column per embryo." — first laboratory-acronym use reads "PGT (preimplantation genetic testing)", `brief:2269`), one primary "Copy this email" (clipboard), the sentence "Inherit reads VCF files, VCF.GZ, gVCF and genotype tables. It cannot read a PDF." (the closed list only, ADR 0016 §Consequences), a link back. Budget ≤ 150 KB (`brief:2317`).

### 2.3 `/embryos/[embryoId]`

Fixed order: breadcrumbs; `SubjectBar` (chip "Embryo", null file count); h1; the Tier-2 gate or, once acknowledged: the `ReportSkeleton` in variant `"embryo"` (`headingText`, `src/copy/reports/headings.ts:36-39`) applied once to the embryo's `rscEmbryoDetail`:

- **"What this is"**: the standing statement (§4 §6.1 `brief:1376`, verbatim, `data-density-required-accuracy`), `PROVENANCE_LINE` reworded for the file ("Inherit did not produce this data. It came from a laboratory that Inherit has not audited." — the §5 §1.6 sentence `brief:1645` mentions "a consumer testing company"; for an embryo file only the laboratory is true; open decision 5), and "This is a limit of the file the laboratory sent, not a result about this embryo." wherever the adult surface renders `LIMIT_OF_FILE` (`brief:400`).
- **"Your result"** (heading unchanged: X13.1 permits exactly two substitutions, `brief:2512`): per allowed category (§6.3 `brief:398`: `Heart and circulation`, `Food, drink and metabolism`, `Immune system and allergies`, `Cancer`, `Having children` — carrier status only), a `<th scope="rowgroup">`-style label (not a heading) and one `<ClaimBlock subject={{ subjectId }}>` per finding (§2.4 cell contract). The four excluded categories never render, and no template outside `data/embryo/allowed_conditions.json` renders (acceptance 26 `brief:484`). **Today** the registry is empty, so the section renders exactly one sentence, once: "Inherit has no calibrated model registered for embryos yet, so it shows no result for any condition. The quality check below is real; the results are not built." ("yet" is permitted: registering a model is within the operator's control, `brief:2680`).
- **"What this doesn’t mean"**: "It does not say what will happen to any child." (the adult generic bullet re-pointed away from "you", `brief:400`) and "A missing result is not a negative result." when any finding is not covered (`WHAT_THIS_DOESNT_MEAN_NOT_COVERED`).
- **"How sure we are"** (X13; `brief:414`: "embryo QC metric detail" is the one collapsible): the QC block — always visible: `coverage` figure "read {sites_called} of the {sites_expected} positions this needs", the verdict word ("Quality check passed" / "Passed, with a thinner file: the data for this embryo is thinner than we would like." (`brief:2229`) / "Quality check not passed" + the reason sentence from `src/copy/upload/errors.ts`), the dropout sentence "Not measured — the ranges below are wider because of this." when null (`brief:1402`), "Not stated by the source laboratory" for every null source field (`brief:1398`), "not measurable from this file" for every null computed metric (`brief:2229`); one `Disclosure` "See the full quality table" with the twenty fields. No sex-and-age baseline (`brief:400`, X13.1).
- **"What this does and does not tell you"** (`brief:400`): the no-action string "There is nothing here that sets this embryo apart from the others." when nothing is reportable, else the per-finding sentences; the not-diagnostic line stays under "Your result" (skeleton).
- **"Where this comes from"**: the risk-model citation per finding when one exists (none does), the registry status sentence, the QC `computed_at` date.

The page never renders `"a result about you"` or `"What you might do"` (acceptance 26). A `minor`, adult or self segment, an unpublished, restricted or foreign embryo answers 404 (`resource-not-found-page-v1`, no existence signal).

### 2.4 `/embryos/compare`

**Above the table, in order, all non-collapsible** (X13 `brief:2510`; `brief:414`): (1) the standing statement (`brief:1376`, verbatim; copy id `embryo.standing-statement`; `data-density-required-accuracy`); (2) the context strip — three counts only, `data-metric-value` + note: "{n} embryos analysed", "{p} passed the quality check", "{q} not measurable" (`brief:393`; `contextCounts` from `qcProjection.contextStrip`); (3) the trade-off panel `<section data-trade-off-panel>` (G4.5 `brief:2632`; X10.3 `brief:2484`; §2 §6.2 `brief:391`; §3 §8.5 `brief:1027`; §4 §6.8 `brief:1411-1412`; §9 item 19 `brief:2710`): line one, always, "The embryo with the lowest estimate for one condition often does not have the lowest estimate for another."; the exact string "Inherit does not rank embryos and does not recommend one."; "If you care about more than one condition, you cannot have the best of each. Choosing for one moves the others."; then `statement_copy_id`: `embryo.tradeoffs.exists` → "No embryo is first on every row." plus one line per real conflict from the full matrix "Embryo {a} has the lowest {condition_x} risk and the highest {condition_y} risk." (§4's wording, open decision 3), or `embryo.tradeoffs.none-measurable` → "There is no trade-off to show: too little could be measured to set one row against another."; and the availability statement "This page shows {n} embryos because the laboratory sent {n} files. Inherit shows nothing about any embryo it has no file for." **Never** a per-embryo lead count, a composite, a "best" (`tradeOffs.forbidden`; `brief:387`); the §3 §8.5 count sentence (`brief:1027`, "lowest estimate on 7 of 12") and the §4 §6.8 "how many conditions each embryo is lowest on" (`brief:1411`) are not rendered — X10.3 forbids the integer and governs.

**The table** (`<table data-compare-surface data-card role="grid">` at ≥ 1024px, `brief:1030`; sticky first column; ≤ 8 columns visible with horizontal scroll; row height ≥ 56px; arrow-key grid navigation `brief:1052`; the condition-first pattern below 1024px and on phones, §2.4.1). Columns = embryos in ascending `sample_ordinal`, **every** embryo including `qc_fail` with its full column (`brief:392`; `comparisonFailureProjection`); rows = conditions in ascending registry `condition_id`, one `<table>` per layer, never mixed (X5.1 `brief:2434`; acceptance 7): `Specific variants` (carrier findings, "Having children" only, `carrierFinding.categoryRule`) and `Statistical estimates`, each `<caption>` carrying the layer definition once (`LAYER_DEFINITIONS`). Column header: the full embryo chip (§1.2) inside `data-subject-id`, the QC chip when failed; **no `<th>` is a button, none carries `aria-sort`, no control on the page changes column or row order** (`brief:2242`, `filtersAndSorts`); "Sorting by a row is permitted" (`brief:388`) is superseded by X10.3 and the register and no sort banner exists. Column footer for every embryo: "Read {sites_called} of {sites_expected} positions" (`coverage` figure) and, when failed or marginal, the reason with its measured numbers (`comparisonFailureProjection.footer`: call rate, parent concordance as §3 figures, `brief:392`). Colour: no `--dir-*`, `--evidence-*`, `--state-*` or `--subject-*` token anywhere on the route (§3 §3.4 `brief:689`; the redundancy words carry the meaning).

**Cell contract**, one `<ClaimBlock subject={{ subjectId: embryoSubjectId }}>` per cell, A.10 `brief:2270` "exactly one number or one named reason plus at most one short qualifier":
1. `absoluteRiskFinding` (future): the `absolute` figure with its block denominator and the `interval` ("could reasonably be", `brief:1252`) as the one number plus one qualifier; the comparator group in the block is "people in the general population" (`brief:400`), the `matched_baseline` as the block's second `absolute`/`natural-frequency` (`brief:1230-1232`, per-block denominator `brief:2784`); the `MODELLED_MARKER` once per block; the within-family status as the block's third element — the interval figure when `measured`, else the exact untested string; **the comparison line and the three other comparators relocate to the row's "How sure we are" entry below the table** (X13; §4 §6.3 `brief:1386`: the lead sentence "…compared with picking one of these embryos at random." renders inside a `<RelativeFigure>` with both absolutes, the `difference-pp` with the gloss "(percentage points, not percent)" and the NNS sentence "About {NNS} couples would need to choose this way for one case to be avoided." (`brief:1388`); the other three under the label "Other ways of comparing" opening with "These four numbers describe the same embryo. They differ because they compare it with different things.", smallest in largest type — as a labelled scroll-reachable block, not a `<details>` (open decision 4)). Ties: two cells whose difference interval includes zero render "≈ Too close to tell apart" and no ordinal (`brief:390`); "These embryos are not distinguishable for this trait using this data." (`brief:2242`) renders in the row's "How sure we are" entry.
2. `carrierFinding`: the `carrier-status` figure (class `variant-call`, `observed`, provenance `computed:embryos/carrier`) with words "one copy found" / "no copy found at the positions covered" / "two copies found"; the confirmation sentence per §4 §4 item 3 (`brief:1332`) reworded for the file in the row's provenance entry; "carrier" never in a header.
3. `coverageFailureFinding`: the word "Not read" (glyph + word `missing`) and, in the footer entry, "Too little of this score is in the file the laboratory sent to give a number. {matched} of {n_variants} positions were found." (`brief:1306` reworded per `brief:400`).
4. `finding: null`: the reason word from the closed table — "Not measurable" (every `qcReasonId`, `brief:392`), "No population figure" (`sex_combined_model_unavailable` → the `brief:400` sentence in the entry), "Not tested between siblings" (`within_family_validation_unavailable` → the exact `brief:389` string in the entry: "No one has measured whether this estimate holds up between brothers and sisters. It is a population estimate used where it has not been tested."; such rows are not enabled by default, `brief:389`, `within_family_default`), "Under review" (`source_call_disputed`, `qc_review_required`).

**Today**: `result_rows` is empty (the registry is empty), so both layer tables render their captions, the embryo columns with QC footers, and one sentence in place of the rows: "Inherit has no calibrated model registered for embryos yet, so no condition row can be shown. What you see is the quality check for each file." The panel renders `embryo.tradeoffs.none-measurable`. No figure of kind `absolute`, `relative`, `percentile` renders anywhere on the route, proven by E2E.

**Below the table**: h2 "Quality check" — the fixed per-embryo QC table (`brief:1398`: call rate one decimal, score coverage, dropout estimate with interval and method, amplification method, source laboratory and assay, imputation, contamination; "Not stated by the source laboratory" for unknowns; imputed rows flagged and widened by the authority) — the QC metric detail is the one collapsible (`brief:414`); h2 "How sure we are" — the per-row entries above, `NO_RANGE_YET` once per layer table, the not_measured comparison sentence (`brief:1318`) only when a `not_measured` score ever renders (none does), the not-diagnostic line (`brief:1851`); h2 "Where this comes from" — `PROVENANCE_LINE`, the model versions and `computed_at` (`brief:1589`), the standing statement's review date only when a registered one exists (C5; none does).

#### 2.4.1 Condition-first pattern (below 1024px; "One condition at a time" toggle at any width, `brief:1021`)

A fixed header strip of embryo chips (horizontally scrollable — the one permitted horizontal scroller at 320px, `brief:1063`; roving tab stop `brief:1052`) with "Pin two to compare" (A/B mode); a vertical list of conditions, one `Surface` each, **in registry order** — the §3 §8.5 "sorted by the spread, largest first" and "Biggest difference between your embryos: about 3 in 1,000" (`brief:1024`) and the sub-threshold grouping (`brief:1025`) are superseded by X10.3 ("no … ordering by a computed quantity") and are not built; each condition expands (Enter/Space; Escape collapses) to the per-embryo cells in ordinal order (`brief:1026` "ordered by estimate" likewise superseded), each cell the §2.4 block; the trade-off panel and the standing statement persist above the list at every width (`brief:1027`). ADR 0019 records the elimination with X10.3 as the evidence.

### 2.5 Copilot for the cohort (Copilot workstream; this design fixes the interface)

Header "Asking about: your {n} embryos" / "your 1 embryo" (`brief:187,402`); scope segment `c-{cohortId}` (mirrors `s-{uuid}`; open decision 6); retrieval = `copilotCohortContext` (embryos, QC, findings, the parents' authorised carrier summaries — never raw variants); local-model only with the permanent line "For anyone’s genome but your own, Copilot only runs on a model you host yourself. Nothing leaves Inherit." (`brief:1924`; `canonical-artifacts.md:42`); the intent gate short-circuits before any model call and returns verbatim "Inherit does not recommend which embryo to choose. That decision belongs to you and your clinical team. I can explain what any number on this page means." (`brief:402`; X14.1 one refusal per class in `src/copy/copilot/refusals.ts`); the standing statement on the first turn, then pinned (`brief:1378`). Until the local transport exists the route renders `copilotCohortUnavailablePage` and the Overview box links to `/embryos`.

### 2.6 Dossiers

- **Embryo condition findings** — `data/embryo/allowed_conditions.json` stays empty. It becomes populated only when a `condition_registry` row, a `risk_models` row meeting every `model_constraints` (sex `combined`, `lifetime`, calibration cohort and `n`, strict baseline interval, a published within-family estimate with a citation and family count) and `data/citations.json` exist, and the G5.5(a) clinical-genetics and safeguarding/ethics review records sit under `docs/reviews/`. None of that is within this design's power (C5). The register row is therefore `not shipped` with the honest sentence of §2.3, not `withheld`: a withheld dossier needs three built designs (`brief:2680` item 4) and primary-source evidence the science dimension has not produced; the dossier request goes to that dimension.
- **Embryo sex disclosure** (`brief:396`) — deleted rather than gated (X10.2 `brief:2482`); the closed shapes cannot carry it. No dossier: it is a refusal (`/science/limits`, `brief:1488`), not a capability.
- **PDF storage** (`brief:379`, acceptance 27) — superseded by ADR 0016; the acceptance item is recorded as superseded in `docs/acceptance-matrix.md` with the canonical row as evidence.

## 3. Figures

Every quantity, as a spec (`src/lib/figures/spec.ts`):

| Quantity | Kind | Class | Basis | Provenance | Attribution |
| --- | --- | --- | --- | --- | --- |
| Positions read per embryo (call rate) | `coverage { read: sites_called, needed: sites_expected }` | `quality` | `observed` | `computed:embryos/qc` | `{ subjectId }` |
| Parent concordance, contamination, autosomal het rate | `natural-frequency { value }` ("about 97 in 100") | `quality` | `observed` | `computed:embryos/qc` | `{ subjectId }` |
| Allelic dropout estimate with interval | `interval` | `quality` | `observed` (laboratory-reported) or `modelled` (estimated) | `seed:embryo_qc/{embryo_id}` | `{ subjectId }` |
| Per-condition absolute risk (future) | `absolute` + `interval` + `natural-frequency` (matched baseline) | `estimate` | `modelled` | `seed:risk_models/{model_id}` + `citation:<id>` | `{ subjectId }` |
| Lead comparator (future) | `relative` inside `<RelativeFigure>` with two `absolute`, one `difference-pp`, one `natural-frequency` | `estimate` | `modelled` | `computed:embryos/compare` | `{ subjectId }` |
| Within-family accuracy (future, when `measured`) | `interval` | `estimate` | `modelled` | `citation:<id>` | `{ subjectId }` |
| Score coverage per finding | `coverage` | `quality` | `observed` | `computed:embryos/score` | `{ subjectId }` |
| Carrier state | `carrier-status` | `variant-call` | `observed` | `computed:embryos/carrier` | `{ subjectId }` |
| Context strip counts, "Step N of 5", dates | UI chrome (`inherit-figure-exempt`), never a figure | — | — | — | — |

### 3.1 What the contract cannot express (smallest change; requests to the Statistical-presentation stream)

1. **Figure size inside a table.** `<Figure>` renders `absolute` in Fraunces `text-2xl` (`figure.tsx:30,55`); `brief:620` forbids Fraunces in table cells. Smallest change: `FigureProps.size?: "figure" | "inline"` (default `figure`), `inline` = Inter, tabular, `text-sm`; `<ClaimBlock>` passes `figureSize` through. Without it the compare cells fail `gate:design`.
2. **`<RelativeFigure>` for embryo outputs** must carry the within-family sentence or its untested string in the same block (X4.3 `brief:2430`): a `children` slot after the four figures (verify against `src/components/figures/relative-figure.tsx`; add if absent).
3. **Multiple `natural-frequency` values in one QC block** already share one denominator (`claimBlock()`); a block mixing 0.97 and 0.03 selects 100 — no change.
4. No change is needed for `carrier-status`, `coverage`, `interval`, `data-compare-surface` (on the table, not the block) or `basis: "exact"` (Embryos renders no Mendelian fraction).

## 4. Copy

Mandated strings, quoted with lines, ship character-for-character with U+2019 and U+2014 (`brief:511`): the standing statement `brief:1376`; "Inherit does not rank embryos and does not recommend one." `brief:2632`; "The embryo with the lowest estimate for one condition often does not have the lowest estimate for another." `brief:1027`; "No embryo is first on every row." `brief:391`; "Embryo {a} has the lowest {condition_x} risk and the highest {condition_y} risk" `brief:1411`; "If you care about more than one condition, you cannot have the best of each. Choosing for one moves the others." `brief:1412`; "Not measurable", "Quality check not passed" `brief:392`; "Too close to tell apart" `brief:390`; "No one has measured whether this estimate holds up between brothers and sisters. It is a population estimate used where it has not been tested." `brief:389`; "Not stated by the source laboratory" `brief:1398`; "Not measured — the ranges below are wider because of this." `brief:1402`; "The data for this embryo is thinner than we would like." and "not measurable from this file" `brief:2229`; "About {NNS} couples would need to choose this way for one case to be avoided." and "(percentage points, not percent)" `brief:1388`; "…compared with picking one of these embryos at random.", "Other ways of comparing", "These four numbers describe the same embryo. They differ because they compare it with different things." `brief:1386`; "These embryos are not distinguishable for this trait using this data." `brief:2242`; the embryo skeleton substitutions "What this does and does not tell you", "There is nothing here that sets this embryo apart from the others.", "This is a limit of the file the laboratory sent, not a result about this embryo.", "About {b} in 100 people in the general population. Source: {source}." (rendered on the block denominator, X4.1) and "Inherit has no population figure to compare this against." `brief:400`; "No embryo files added yet." / "How to get your embryo files" `brief:372`; "No embryo files added." `brief:235`; the State E lines `brief:240`; the three upload questions and the `No` sentence `brief:375-377`; the four option labels and the secondary link `brief:377`; the request-data letter `brief:381`; the class attestations `brief:990-991`; the Tier-2 checkbox and sentence `brief:968`; the criminal-liability sentence `brief:1752`; the donor and disposition-authority screen sentences `brief:1731,1734`; the A.6 messages `brief:2200-2209`; the JobProgress steps `brief:1048`; the Copilot refusal `brief:402`; the local-only line `brief:1924`; `NOT_DIAGNOSTIC` `brief:1849`; "This is a model, not an observed outcome." `brief:2629`; the insurance item 6 `brief:1961`; "within 7 days" `brief:2414`.

Strings to write (grade ≤ 9, ≤ 25 words each, U+2019; drafted): landing tiles "Add the files an IVF laboratory returned to you." (kept from Overview), "Look at each file side by side, without ranking." (kept), "Ask about the files your laboratory sent." (kept); "Embryos added on {date}"; the eight status words of §2.1; "Waiting for {role} to turn on analysis"; "Compare these embryos"; "Copilot for embryos runs only on a model you host yourself. It is not connected on this site yet."; the two empty-state sentences of §2.1; the retention sentences of §1.6; "Stop and delete these files"; "Sharing ended on {date}. Every result built from these files was deleted."; "Add embryo files"; "Step {n} of 5"; "Inherit does not keep this name."; "Waiting for {role} to sign in their own account."; "The upload did not finish. No genetic data was kept, and every Record Key Card from this upload is now invalid."; "Files added"; "The email to send"; "Copy this email"; "Copy did not work. Select the text and copy it yourself."; "Inherit reads VCF files, VCF.GZ, gVCF and genotype tables. It cannot read a PDF."; "Show my embryos"; "The laboratory’s file for this embryo has not been added yet."; "Still checking the files."; "Waiting for {role} to turn on analysis before any result can show."; "Inherit has no calibrated model registered for embryos yet, so it shows no result for any condition. The quality check below is real; the results are not built."; "Inherit has no calibrated model registered for embryos yet, so no condition row can be shown. What you see is the quality check for each file."; "There is no trade-off to show: too little could be measured to set one row against another."; "This page shows {n} embryos because the laboratory sent {n} files. Inherit shows nothing about any embryo it has no file for."; "It does not say what will happen to any child."; "Quality check passed"; "Passed, with a thinner file"; "See the full quality table"; "Read {n} of {m} positions"; "Not read"; "No population figure"; "Not tested between siblings"; "Under review"; "Too little of this score is in the file the laboratory sent to give a number. {matched} of {n} positions were found."; "one copy found" / "no copy found at the positions covered" / "two copies found"; "Inherit did not produce this data. It came from a laboratory that Inherit has not audited." (open decision 5); "Inherit blocked this page because its data did not match the safety shape it must have. Nothing was shown. Try again later."; the twenty QC field labels in plain words ("Positions the file could read", "Agreement with each parent’s file", "Signs of mixed DNA", "Letters the test may have missed"…). All live in `src/copy/embryos/{index,upload,request-data,compare,detail,qc,tradeoffs}.ts` with `*_HEADING` / `*_LABEL` / `*_BUTTON` names (`src/copy/ancestry.ts` precedent); the A.6 codes in `src/copy/upload/errors.ts` (request: unlisted path); the copy ids the register names (`embryo.standing-statement`, `embryo.tradeoffs.*`, `embryo.qc.*`, `embryo.context.*`, `embryo.result.insufficient-coverage`, `embryo.within-family.not-tested`) are the export names. Words to add to `data/plain-vocabulary.json`: `added`, `checked`, `compared`, `passed`, `measured`, `measurable`, `quality`, `column`, `copy`, `each`, `every`, `same`, `different`, `apart`, `zip`, `pdf`, `spreadsheet`, `folder`, `text`, `four`, `five`, `too`, `which`, `letter`, `step`, `stop`, `read` — and exactly the further words `pnpm gate:readability` reports. No heading, button, `<th>`, label or status string contains `laboratory`, `cohort`, `condition`, `sample`, `estimate`, `evidence`, `model`, `carrier`, `variant`, `coverage`, `call rate` or `percentile` (X7.3; `FORBIDDEN_SHORT_TERMS`); "laboratory" in body copy is glossed on first use per page (`brief:2269`). The banned phrases `the best embryo`, `top embryo`, `winner`, `ranked` (`brief:894`) appear nowhere; "you cannot have the best of each" (`brief:1412`) is mandated and does not match the `\bthe best embryo\b` pattern.

## 5. Data and computation

- **From existing tables**: cohorts, participants, embryos, QC, findings, figures (§0); grants (`purpose_grants ⋈ directional_grants`, purpose `embryo.analysis`, `consent_purposes` seeds it); jurisdiction (`profiles.jurisdiction_code`, `data/jurisdictions.json`); `condition_registry`, `risk_models` (empty); `data/embryo/allowed_conditions.json`.
- **Computed in the worker** (A.7 `brief:2215`): `split_cohort_vcf` — sanitised fragments → per-ordinal canonical autosomal source (one object per embryo, never multisample; chrX/Y/M/MT/23–25/PAR/unplaced discarded without recording presence or count, `embryo-autosomal-only-v1.ingress`), `embryo_qc` per ordinal (`sites_expected` = union of loci of every eligible template and model with a declared position; `call_rate`; concordance against the parents' files when both are present and consented; `autosomal_het_rate` on 1–22 only; dropout and contamination when the source reports them), the X10.4 bands from `src/lib/embryos/qc-policy.ts` (call rate < 0.95 no figure, < 0.85 no results; concordance < 0.95 marginal, < 0.90 fail; score coverage < 0.80 no figure for that score; dropout > 0.10 not reportable, null → widen ×1.5 and say so; contamination > 0.05 no results — the only home of these numbers, `docs/canonical-artifacts.md:161`), the terminal whole-ordinal publication (§2.2), the notice outbox rows and Record Key date addenda. `score_embryo` — one `EmbryoFinding` per (embryo, registry condition) from the embryo's own called genotypes only (`brief:2241`; ADR 0007 by name: no parental substitution, no imputation), writing `embryo_scores` + `embryo_figures` with `source_binding_fingerprint`, `computation_revision`, `model_version`; the registry's `requestRule` denies any condition outside the exact file before enqueue. Neither job exists yet (E0/E3).
- **Computed on the server at request time** (serverless is permitted for "query-time report resolution", `brief:2215`): `src/lib/embryos/projection.ts` (the closed DTOs, recursively validated before crossing the RSC boundary — `serverComponentBoundary`; unknown key → the 500 contract, one pseudonymised `feature.blocked` audit event), `trade-offs.ts` (pure: over the full published finding matrix, `lowest_condition_id`/`highest_condition_id` per embryo, ties excluded; never persisted; never a lead count), `qc-policy.ts#contextStrip`, `access.ts`, `cohorts.ts`, `tier2.ts`.
- **Never computed or stored** (X10.2–X10.3; `brief:2087-2090,2096,2227,2242`; `filtersAndSorts`; `forbiddenShapeFields`): sex, karyotype or any proxy; a non-autosomal row; a laboratory sample, cycle, clinic or column label (transient in bounded memory only, `docs/canonical-artifacts.md:23`); a rank, composite, grade, star, lead count, ordering by a computed quantity, sort key or weighting; a parental-midpoint or imputed genotype; a demographic row for an embryo (trigger); a de-identified derivative (`brief:1791`); a display multiplier on any stored value (`brief:1355`, `displayed === stored` unit test); a relative figure outside `<RelativeFigure>`.
- **Purge and retention**: `cohort.restriction-whole` → `access.revocation-immediate`, `derived.revocation-60s`, `source.revocation-7d` over the registered target order (`…224119:169-265`: `embryo_scores` 60, `embryo_qc` 70, `embryo_figures` 72, projections 125, `embryo_variants`, ingest working state; Storage by database join, never prefix); `embryo.stored-or-unknown-24mo` (renewal by every `dispositionAuthority`), `embryo.donated-or-discarded-90d`, `embryo.transferred-claim-window`, `embryo.cohort-draft-30d`, `embryo.ingest-session-24h`, `embryo.disposition-proposal-7d`; `chat_messages` by `cohort_id`/`retrieved_subject_ids`. Every embryo table already sits in the sensitive-purge registry; account deletion purges every owned cohort regardless of another parent's consent (`docs/retention.md:45`).

## 6. Tests

### 6.1 Unit (vitest)
- `src/lib/embryos/qc-policy.test.ts` (`brief:2229`, X10.4): each gate fires at its exact threshold and one unit either side (0.95/0.85 call rate, 0.95/0.90 concordance, 0.80 coverage, 0.10 dropout, 0.05 contamination); null dropout widens every interval by exactly 1.5 and sets `embryo.qc.dropout-not-measured`; null contamination/dropout caps confidence and does not gate; `contextStrip` counts marginal-no-figure under `not_measurable`; an unknown reason maps to `qc_review_required`.
- `src/lib/embryos/policy.test.ts`: the closed-shape validator rejects any extra key (`sex`, `rank`, `lab_identifier`…) and each `forbiddenShapeFields` entry; `columnOrder` equals ascending `sample_ordinal` for a fixture whose values order differently; `resultRowOrder` equals registry `condition_id`; `tradeOffs` derivation over the full matrix names a real conflict, returns `none-measurable` with an empty array when no conflict exists, and exposes no per-embryo count; `displayed === stored` for every figure payload (`brief:1355,2242`); no output contains `"0%"`, "best", "rank".
- `src/lib/embryos/allowed-conditions.test.ts` (`brief:1417`): the committed file has zero conditions, the five allowed categories byte-equal to `condition_registry`'s check list, every forbidden class present; a fixture entry outside the categories, with a sex-stratified model, without a within-family citation, or absent from `condition_registry` fails; requesting an unregistered condition throws before enqueue.
- `src/lib/embryos/no-sex.test.ts` (`brief:2227`): a cohort VCF with chrX/chrY/chrM/MT/23/24/25/PAR rows yields zero such rows and no `parse_meta` count; `pgt-table.test.ts`, `multisample.test.ts` (`brief:2304`): the header rule by exact equality only, ≥ 3 of the six fields, sample count from `#CHROM`; `src/copy/upload/errors.test.ts` (`brief:2196`): every A.6 code has a message ≤ 240 characters with no allele, genotype or rsID.
- `src/lib/embryos/{cohorts,access,tier2}.test.ts`: co-parent listed, donor-only principal excluded, restricted cohort null, TEST-LOCAL only under the flag, a permitted actor with an unreviewed parent blocked (G5.1b).
- `src/components/embryo/*.test.ts` (`renderToStaticMarkup`): the compare table carries `data-compare-surface`, one attributed block per cell, no `aria-sort`, no `<th>` button, the G4.5 string, the panel outside any `details`, every column present for a `qc_fail` fixture with the chip and the reason; the chip renders no `bg-subject-*` class; the detail skeleton renders the two substituted strings and never "What you might do" / "a result about you"; `src/lib/figures/*.test.ts` (requested): `size: "inline"` renders no `font-display`.

### 6.2 E2E (production build, local stack, `INHERIT_TEST_JURISDICTION=1`, worker running before `pnpm e2e` (`brief:2285`), accounts A (uploader/parent) and B (co-parent) by `createConfirmedUser`)
- `e2e/embryo-ingest.spec.ts` (`brief:2308,2353`): A creates a draft (basis two evidenced parents), B accepts the co-parent invitation from the captured mail, both sign, A acknowledges disclosure and Charter, finalises and receives one Record Key Card per embryo in ordinal order with a provisional date; A uploads `e2e/fixtures/embryo/synthetic-cohort-8-grch38.vcf` through the browser sanitiser; the panel shows only stage names; after `worker_jobs` reaches `done`/`failed` (≤ 120 s): eight embryos, six `qc_pass`, one `qc_fail` with `embryo_call_rate` ("we could read only {pct} in 100 of the markers we need…", `brief:2207`), one with `contamination` (`brief:2209`), ordered by ordinal, labels "Embryo 1"–"Embryo 8", zero `chromosome > 22` rows in `embryo_variants` (service-role query), eight canonical objects each one sample, no object name containing a source label, the job `partial = true`, B's upload-time notice captured by template id; the sanitised fragments and handle map deleted after publication; A's export ZIP has no chrX/Y/M line in any embryo variant CSV (`brief:2227`); B's `/withdraw/[token]` `delete` → within 60 s a service-role count of every embryo-derived table for the cohort is 0 and `GET /embryos/compare` for A answers the zero-cohort state (acceptance 21 `brief:479`).
- `e2e/pdf-rejection.spec.ts` (`brief:2194`): a `%PDF-` fixture is refused in the browser before any request to `/api/embryo-ingest/*` (network log) with the exact `pdf_not_data` text; a bypassed PUT of PDF bytes to the chunk route answers 415 `pdf_not_data`, no fragment row or object exists, the session is `failed` and the due target persists until the unwind commits.
- `e2e/embryo-compare.spec.ts` (`brief:1123,970,2242`): h1 "Compare embryos"; ≤ 6 headings; the Tier-2 gate with zero `[data-figure-kind]` in `page.content()`, the exact checkbox text, then exactly one gate across compare → detail → compare (count of gate renders = 1); the standing statement verbatim, not inside `details`, present at 390×844 and 1280×800; `[data-trade-off-panel]` verbatim lines and the G4.5 string; `[data-compare-surface]` per layer; every column header in ordinal order for a fixture whose QC values order differently; no `[aria-sort]`, no `th button`; activating every interactive element leaves `th` order unchanged; the `qc_fail` column present with "Quality check not passed" and its footer reason with numbers; the context strip counts 8 / 6 / 2 (`brief:482`); zero `absolute|relative|percentile` figures today and the registry sentence once; ≤ 24 first-viewport interactives (acceptance 33); axe both themes (acceptance 34); no element's computed colour equals any `--dir-*`, `--state-*`, `--evidence-*` or `--subject-*` value (`brief:689`); zero matches for `/\b(sex|male|female|XX|XY|chrX|chrY|karyotype|rank|ranked|best embryo)\b/i` in `page.content()` (acceptance 23, 25 restated per X10.2); the detail page for `Embryo 3` with breadcrumbs `Embryos / Embryo 3`, chip "Embryo", the six h2 in embryo variant, the QC `coverage` figure attributed to that embryo's subject id, no "What you might do" / "a result about you" (acceptance 26).
- `e2e/embryo-accuracy.spec.ts` (`brief:1580`): every embryo RSC payload and `/api/embryo-*` response validates against the eight-key `EmbryoFinding` and the bound root shape; an injected response with an extra `sex` key and a numeric key is blocked with 500 `unsafe_response_blocked` and one `feature.blocked` audit row (L-23 `brief:1994`); no field forms a total order over embryos; column order equals ordinal order.
- `e2e/embryos.spec.ts`: `/embryos` empty state strings and one primary → `/embryos/request-data`; the letter verbatim and "Copy this email"; State E on `/overview` with the three counts and "Compare your embryos" for A **and** for B (through `listCohortsForAccount`); `/embryos/{uuid}` for a foreign or unpublished embryo → 404; every `jurisdiction-unavailable` state without the flag (a second Playwright project or `INHERIT_TEST_JURISDICTION` unset for one spec) renders the JSON copy and fetches no private row (route interception).
- Existing specs: `e2e/overview.spec.ts` unchanged (State E was never pinned); `e2e/network-audit.spec.ts` and `e2e/a11y.spec.ts` extended to the five routes (`brief:2274`); `e2e/rls.spec.ts` gains the G1.6 embryo assertions (user A cannot select any embryo table; anonymous denied; a restricted cohort returns zero rows to both parents).

### 6.3 Fixtures (synthetic, C7 `brief:2738`)
`e2e/fixtures/embryo/generate-cohort-vcf.ts` (the `generate-aims-vcf.ts` precedent; the brief's `scripts/generate-synthetic-cohort-vcf.ts` `brief:2310` sits under Platform's `scripts/` — request or keep under `e2e/fixtures/`): two synthetic parents drawn with seeded `mulberry32`, eight samples by simulated meiosis over the positions of the shipped templates plus deliberate chrX/chrY/chrM rows, sample 7 degraded to a 0.60 call rate, sample 8 contaminated; byte-identical across runs; a `PROVENANCE.md` row stating it describes no one; four real-shaped `pgt_table` fixtures and one ambiguous one (`brief:2190`); a `%PDF-` byte fixture; SHA-256s recorded. No real person's or embryo's data enters the tree.

## 7. Documentation and registers

- **ADRs** (`docs/adr/README.md` has 0001–0013 and 0016; 0014, 0015, 0017 are Family's, 0018 is pharmacogenomics'; A.12 names are retitled never renumbered, `decisions.md:112-121`): **0019 — Embryo comparison presentation model** (G7.1's named minimum: the matrix in ordinal order, no sort control, per-cell attributed blocks, the trade-off panel satisfied by statement with one real conflict and no lead count, colour suppression, sex filtered at ingest and absent from every shape, the §4 §6.1 standing statement as the one rendered version, the condition-first pattern without spread ordering; absorbing A.12's "embryo-comparison-not-ranking" and "no-cognitive-ability-or-embryo-sex-outputs"; alternatives rejected with evidence: sortable rows `brief:388` — X10.3; spread-ordered condition list and lead counts `brief:1024,1027,1411` — X10.3 and the register's `tradeOffs.forbidden`; a consented sex toggle `brief:396` — X10.2 and the discarded-at-ingest data; a stored PDF record `brief:379` — ADR 0016). **0020 — Embryo ingest and cohort lifecycle** (recommended: ordinal identity over laboratory labels, whole-ordinal publication after partial failure per A.10, no parental substitution or imputation (A.12's 0007), either-parent restriction of the whole cohort (A.12's 0009), the retention ceiling per disposition (A.12's 0015), the empty registry as a deliberate unavailable state; rejected: per-embryo visibility before terminal publication — `canonical-source-publication-v1`; a subject-style direct Storage upload — ADR 0016). The jurisdiction-gating ADR stays with the consent stream.
- **Capability register** (`docs/capability-register.md:43-45`), honest after each part: *Embryo ingest* → `shipped-degraded` once E0+E2 land ("every real jurisdiction is `unreviewed`, so the hosted service renders the jurisdiction copy; under TEST-LOCAL the sanitised ingest publishes one autosomal source per embryo with its quality row"), until then `not shipped` naming §2.2's honest state; *Embryo quality* → `shipped-degraded` with E2 ("the QC block and columns render observed figures; concordance needs both parents' consented files, otherwise 'not measurable from this file'"); *Embryo comparison* → `shipped-degraded` with E1 ("the matrix renders embryo columns, QC footers, the standing statement and the trade-off panel in its none-measurable form; no condition row renders because `data/embryo/allowed_conditions.json` is empty and the page says so"); new row *Embryo condition findings (single-locus, statistical, carrier)* → `not shipped` (§2.6). *Chat over embryos* stays with Copilot.
- **Acceptance matrix**: G2.2, G2.6, G4.5, G5.3, G5.4, G5.6, G7.1 rows updated with the spec paths above; acceptance 27 marked superseded by ADR 0016 / `docs/canonical-artifacts.md:22`; acceptance 25 restated under X10.2 (no sex row exists in any jurisdiction). `docs/test-diff-register.md` rows: `e2e/{embryo-ingest,pdf-rejection,embryo-compare,embryo-accuracy,embryos}.spec.ts` (new, with the assertion lists of §6.2), `e2e/{network-audit,a11y,rls}.spec.ts` (extended), `src/lib/embryos/*.test.ts`, `src/components/embryo/*.test.ts`, `src/copy/upload/errors.test.ts`, `src/lib/figures/*.test.ts` (extended: inline size).
- **Registers and ledgers**: `docs/schema-requirements.md` rows — the eight RPCs of E0 with signatures; `job_time_stats(kind)`; `subject_invitations` kinds for the co-parent and identified-donor invitations (verify against `…221908`); `docs/canonical-artifacts.md` rows for `src/copy/embryos/*.ts`, `src/lib/embryos/trade-offs.ts` (the one home of the conflict rule) and the worker entrypoint (`worker/src/index.ts` vs the registered `scripts/worker.ts`); `docs/route-register.json` requests — `api.results-acknowledge` (or the Family endpoint widened), the `c-{uuid}` scope segment form; `docs/protocol/decisions.md` (the decisions of §8 with reasons); `docs/protocol/approaches.md` (compare: ordinal matrix with statement-only trade-offs vs sortable/spread-ordered list, killed by X10.3; ingest: whole-ordinal publication vs per-embryo streaming, killed by the publication invariant); `e2e/README.md`, `e2e/fixtures/PROVENANCE.md`; `docs/anti-patterns.md` does not exist — AP-8 (population score between siblings) and the named "winner encoding", "parental substitution", "conclusion-laundering" ids the tests cite need the orchestrator to create it.

## 8. Open decisions (recommended default first)

1. **Which standing statement.** Default: §4 §6.1 (`brief:1376`) alone, because §4 governs what a surface may say (`brief:1145`) and the tie-break is fewer claims; §2's (`brief:394`) and §3's footer (`brief:1028`) are not rendered. Alternative: render all three — three versions of one claim on one page.
2. **Landing empty-state strings.** Default: §2 §6.0's (`brief:372`) inside §3 §6.5's four-part `EmptyState`; alternative: §3's own sentences (`brief:930`), which put "variant file" in the empty state.
3. **Conflict sentence wording.** Default: §4's "Embryo {a} has the lowest {condition_x} risk and the highest {condition_y} risk" (`brief:1411`); alternative: §2's "…the lowest estimate for {X} and the highest for {Y}." (`brief:391`) — "estimate" is jargon in a short role.
4. **The other three comparators.** Default: a labelled scroll-reachable block in the row's "How sure we are" entry (X13 forbids interaction-gated disclosure); alternative: the `Disclosure` §4 §6.3 literally names (`brief:1386`).
5. **Provenance line on embryo pages.** Default: the file-specific rewording ("…from a laboratory that Inherit has not audited.") because "a consumer testing company" is false for an embryo file; alternative: `PROVENANCE_LINE` verbatim (`brief:1645` mandates one sentence on every report of every class).
6. **Cohort scope segment.** Default: `c-{cohortId}` mirroring `s-{uuid}`; alternative: the bare UUID the Overview passes today (`overview/page.tsx:108`).
7. **Tier-2 cookie per domain.** Default: one cookie per domain so Family's acknowledgement never silently un-gates Embryos; alternative: one session cookie shared by both boundaries.
8. **Proving the finding cells without a populated registry.** Default: unit tests over synthetic `EmbryoFinding` fixtures only; alternative: an E2E under a test-only registry override, which needs a `docs/fixture-paths.md` row, the mock-token allowlist and an ADR (G8.2 b–c).
9. **Concordance rendering.** Default: `natural-frequency` at the block denominator ("about 97 in 100 positions agree with each parent"); alternative: a new `proportion` kind — a closed-vocabulary change (X4).
10. **Cohort card label.** Default: "Embryos added on {date}"; alternative: request a `label` column (A.3 `brief:2086`) — a user-typed label is a minimisation risk the canonical row 23 argues against.
11. **Fixture generator location.** Default: `e2e/fixtures/embryo/` (precedent); alternative: `scripts/` per `brief:2310`, which needs a Platform request.
12. **Overview State E `not_measurable`.** Default: adopt the register's definition (fail + marginal-no-figure) through `contextStrip()`; alternative: keep counting `fail` only, which contradicts the compare strip.

## 9. File map

New: `src/lib/embryos/{cohorts,access,tier2,projection,qc-policy,policy,allowed-conditions,trade-offs,ingest-client}.ts` + tests; `src/copy/embryos/{index,upload,request-data,compare,detail,qc,tradeoffs}.ts`; `src/components/embryo/{embryo-chip,cohort-card,cohort-list,empty-state,jurisdiction-line,retention-line,upload/{steps,questions,class-attestation,basis-select,evidence-pending,acknowledgements,record-key-cards,dropzone,progress},request-letter,compare/{standing-statement,context-strip,trade-off-panel,compare-table,compare-cell,column-header,column-footer,condition-first-list,qc-table},detail/{qc-block,findings-section}}.tsx` + `embryo.test.ts`; `e2e/{embryos,embryo-ingest,pdf-rejection,embryo-compare,embryo-accuracy}.spec.ts`; `e2e/fixtures/embryo/{generate-cohort-vcf.ts,synthetic-cohort-8-grch38.vcf,pgt-table-*.csv,not-data.pdf}`; `docs/adr/{0019,0020}-*.md`.

Changed: the five pages under `src/app/(app)/embryos/`; `src/copy/overview.ts` (`COPILOT_GROUP_SCOPES_AVAILABLE` stays; State E through `contextStrip`, request); requested — `src/lib/subject-colour.ts` and `src/components/subjects/subject-bar.tsx` (neutral embryo disc, `fileCount | null`), `src/lib/primary-routes.ts` (`embryos.request-data`, `embryos.detail`), `src/app/api/search/route.ts` (embryo href), `src/app/(app)/overview/page.tsx` (`listCohortsForAccount`), `src/components/figures/{figure,relative-figure}.tsx` + `src/lib/figures/spec.ts` (inline size, within-family slot), `src/components/family/result-gate.tsx` → `src/components/results/result-gate.tsx` + the acknowledge route, `src/copy/upload/errors.ts` (new, unlisted owner), `src/lib/genome/parsers/{sniff,pgt-table}.ts`, `src/lib/genome/build-inference.ts`, `data/ref/lab-tables/column-synonyms.json`, `data/ref/build-discriminating-sites.json`, `worker/src/{index,split-cohort,score-embryo}.ts`, `src/app/api/embryo-cohort-drafts/route.ts`, `src/app/api/embryo-cohorts/{route,[id]/withdraw/route,[id]/record-key-cards/route}.ts`, `src/app/api/embryo-ingest/[session]/{mapping,chunks/[sequence],complete}/route.ts`, `src/app/api/cohorts/[id]/restrict/route.ts`, `src/app/api/embryos/[id]/disposition/route.ts`, `src/emails/{embryo-upload-notice,co-parent-invitation,record-key-addendum}.tsx`, `supabase/migrations/` (Platform: the RPCs, `job_time_stats`, invitation kinds), `data/plain-vocabulary.json`, `e2e/{network-audit,a11y,rls}.spec.ts`, `e2e/README.md`, `e2e/fixtures/PROVENANCE.md`, `docs/route-register.json`, `docs/{capability-register,acceptance-matrix,test-diff-register,schema-requirements,canonical-artifacts}.md`, `docs/protocol/{decisions,approaches}.md`, `docs/adr/README.md`.

Untouched by design: `data/embryo/allowed_conditions.json` (stays empty), `data/jurisdictions.json` (no row invented), the embryo migrations, `src/app/globals.css`, `src/app/api/chat/route.ts`, `src/app/(marketing)/embryo-analysis/page.tsx` (its "If a child is born from this" panel is pinned by the register; its hard-coded jurisdiction sentence is the consent stream's).

## 10. Build split

**E0 — Platform, consent and worker prerequisites (not an Embryo agent; the interface Embryos builds against).** Migrations and RPCs with signatures: `create_embryo_cohort_draft_v1(p_account_id uuid, p_upload_situation text, p_basis text, p_donor_intent text, p_embryo_count smallint, p_contacts_ciphertext bytea[], p_contact_hmacs text[]) returns uuid`; `finalize_embryo_cohort_v1(p_account_id, p_draft_id, p_insurance_ack_id, p_charter_ack_id, p_nonce) returns table(cohort_id, embryo_ids uuid[], upload_session_id, cards jsonb)`; `open_embryo_ingest_session_v1(p_account_id, p_auth_session_id, p_cohort_id, p_declared_bytes bigint) returns table(session_id, handles text[], cookie_hash)`; `accept_embryo_ingest_chunk_v1(p_session_id, p_sequence int, p_object_id uuid, p_sha256 text, p_bytes int, p_lines int)`; `complete_embryo_ingest_v1(p_session_id, p_chunk_count int, p_nonce) returns table(job_id, status)`; `publish_embryo_ingest_v1(p_job_id, p_ordinal_results jsonb)` (the terminal whole-ordinal transaction, worker-only); `restrict_embryo_cohort_v1(p_principal_id, p_cohort_id, p_nonce)`; `record_embryo_disposition_v1(…)`; `job_time_stats(p_kind text) returns table(n_bucket text, p50_seconds, p95_seconds)`; `grant_purpose_v1` reuse for `embryo.analysis`. Routes: the nine `api.embryo-*`/`api.cohort-restrict` handlers; `src/copy/upload/errors.ts`; `sniffV2`, `pgt-table.ts`, build inference and the two `data/ref` files; the worker `split_cohort_vcf` handler (and the entrypoint name); the mail templates; `src/lib/legal/jurisdictions.ts` (shared with F0); the consent artifacts `consent.upload-embryo`, `attestation.embryo-parentage`, `attestation.embryo-disposition-rights`, `attestation.embryo-single-parent-basis`, `charter.future-person` with the §4 §6.1 sentence in the consent template (`brief:1378`).

**E1 — Graph, landing, request-data, compare and detail in their honest states (Embryo agent; needs only F0/E0's jurisdiction reader, else the shared Family reader).** `src/lib/embryos/{cohorts,access,tier2,projection,qc-policy,policy,allowed-conditions,trade-offs}.ts`, the copy files, `/embryos`, `/embryos/request-data`, `/embryos/compare`, `/embryos/[embryoId]` with the empty, consent-required, jurisdiction-unavailable and (over any published cohort) complete states, the requests to `subject-colour`, `subject-bar`, `primary-routes`, search, Overview, the figures stream and the Family gate; `e2e/embryos.spec.ts`, `e2e/embryo-compare.spec.ts` (the parts that need no ingest run against cohort rows the spec inserts with the admin client, labelled synthetic); ADR 0019 and the register rows. Interface out: `EmbryoCohortView`, `listCohortsForAccount`, `resolveCohortForAccount`, `resolveEmbryoForAccount`, `embryoCapability`, `contextStrip`, `qcBand`, `deriveTradeOffs`, `validateEmbryoDto`, `<EmbryoChip>`, `<CompareTable>`, `<TradeOffPanel>`, the copy ids.

**E2 — Upload and ingest UI (Embryo agent; depends on E0).** `/embryos/upload` (five steps), the browser sanitiser client, `JobProgress` with the embryo steps, the processing states on every route, the fixtures and generators, `e2e/embryo-ingest.spec.ts`, `e2e/pdf-rejection.spec.ts`, `e2e/embryo-accuracy.spec.ts`, ADR 0020. Until E0 lands, `/embryos/upload` renders steps 1–2 and, at the file step, `EMBRYO_INGEST_AVAILABLE = false` (the `COPILOT_GROUP_SCOPES_AVAILABLE` precedent) with "Inherit cannot take embryo files on this site yet." and the request-data link — never a dead control.

**E3 — Findings (blocked on the science dimension and G5.5 reviews; Embryo agent for the renderer only).** `score_embryo`, the populated registry with its citations, the finding cells, `<RelativeFigure>` with the comparator and NNS, the within-family figure, `e2e/embryo-accuracy.spec.ts`'s numeric assertions. Its pure parts (the cell renderer over synthetic `EmbryoFinding` fixtures, `trade-offs.ts`) are written and unit-tested in E1.

**Copilot cohort scope** stays with the Copilot workstream against §2.5's interface.

Sequencing: E0 ∥ E1 (E1's pure libraries and copy first) → E2 → E3; the Family F0 reader and the shared Tier-2 gate are prerequisites for E1's `consent-required`/`jurisdiction-unavailable` and gated states, and both can be stubbed behind the same interfaces while F0 is in flight.

### Critical Files for Implementation
- `/home/user/Inherit.bio/docs/route-register.json` — `policyContracts.embryo-autosomal-only-v1` (the closed shapes, `qcProjection`, `filtersAndSorts`, copy ids), `policyResolvers.embryo-ingest-session-v1`, `canonical-source-publication-v1`, `genetic-file-ingest-v1` and the five `embryos.*` routes; every DTO, state and prohibition in this design is bound to it.
- `/home/user/Inherit.bio/supabase/migrations/20260831223301_embryo_cohorts_and_authority.sql` — the tables the pages read (`embryos.display_label` from the ordinal, `embryo_qc`, `embryo_scores`, the participant sets); E0's RPCs extend it and E1's projections are typed against it.
- `/home/user/Inherit.bio/src/components/figures/claim-block.tsx` (with `src/lib/figures/{contract,spec}.ts` and `src/components/figures/figure.tsx`) — the container every compare cell, QC block and finding renders through; the inline figure size and the `<RelativeFigure>` within-family slot land here.
- `/home/user/Inherit.bio/src/components/subjects/subject-bar.tsx` (with `src/lib/subject-colour.ts` and `src/lib/subjects.ts`) — the embryo chip must lose its subject colour and file-count link, and the cohort graph must replace `listSubjectsForAccount` for co-parents.
- `/home/user/Inherit.bio/data/embryo/allowed_conditions.json` — the empty registry whose emptiness defines today's honest state on `/embryos/compare` and `/embryos/[embryoId]`, and whose entry shape fixes what a future finding row may be.